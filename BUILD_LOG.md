# Build Log

Entry format and rationale in `PROJECT_BRIEF.md` §17.4. Updated per work session, not retroactively.

---

## 2026-09-16 — Planning: brief consolidation, repo setup, first architecture decisions

**What I built:**
- Consolidated the original `PROJECT_BRIEF.md` and the superseding `CURATED_TRAVEL_CONCIERGE_BUILD_BRIEF.md` into a single non-redundant `PROJECT_BRIEF.md`, keeping all build-brief content and folding in the original's still-useful table formats, a concrete Supabase SQL sketch, and the `BUILD_LOG.md` entry-format template that had been dropped in the first consolidation pass.
- Renamed the project from `curated_travel_concierge` to `Wanderwise`.
- Initialized git, created a public GitHub repo (`rohan-pandit/wanderwise`), pushed the initial commit.
- Worked through the first-build-session checklist (`PROJECT_BRIEF.md` §23): repo inspection, domain model proposal, schema/migration proposal, deterministic service interface proposals, ADR index, this log.
- Created `CLAUDE.md`, `docs/architecture/ADR-INDEX.md`, `docs/architecture/ADR-000-tech-stack-and-topology.md`, `docs/architecture/ADR-003-state-and-data-model.md`, `docs/IMPLEMENTATION_PLAN.md`, and `supabase/migrations/0001_initial_schema.sql`.

**Why:** The build brief's own kickoff instructions require presenting a plan and domain boundaries before writing application code, and flag several decisions (§22) that shouldn't be picked silently.

**Decisions made:**
1. **App topology:** single Next.js (TypeScript, App Router) app — frontend, API layer, and orchestration loop in one codebase. See ADR-000.
2. **Auth:** Supabase Auth via magic link, required before entering the app — no anonymous/guest mode, no progressive claim flow. Landing page (`/`) handles sign-in; the product lives behind auth on `/app`. See ADR-003.
3. **Version control:** git initialized locally and a public GitHub remote created immediately, with a commit per build session going forward, since the build process itself is a portfolio deliverable.

**What didn't work / dead ends:**
- Initially considered a progressive/anonymous-first auth model (start without signing in, optionally attach an email later). Rejected once a "view previous itineraries" requirement surfaced — that model needs an anonymous-to-authenticated merge flow and dual identity handling for a UX nicety that isn't the point of the project. Mandatory sign-in removes that complexity entirely.
- First attempt to rename the project folder failed (`mv: cannot move ... Device or resource busy`) because the shell's working directory was still inside it; resolved by moving the shell to the parent directory first.

**Next up:** Scaffold the actual Next.js app (Phase 0 remaining items in `docs/IMPLEMENTATION_PLAN.md` §1), wire up local Supabase and apply `0001_initial_schema.sql`, and build the route skeleton (`/`, `/auth/callback`, `/app`, `/app/trips`).

---

## 2026-09-16 — Next.js scaffold, auth flow, route skeleton

**What I built:**
- Scaffolded the Next.js app (TypeScript, App Router, Tailwind, ESLint) via `create-next-app`, merged into the existing repo without disturbing `PROJECT_BRIEF.md`/`CLAUDE.md`/`BUILD_LOG.md` (kept ours over the generated `README.md`/`CLAUDE.md`; kept the generated `AGENTS.md` and wired `CLAUDE.md` to import it via `@AGENTS.md`, since it's framework-maintained and auto-regenerated).
- Installed `@supabase/supabase-js`, `@supabase/ssr`, `@anthropic-ai/sdk`, `zod`, and `vitest` (+ `@types/node` bump to satisfy vitest's peer dependency).
- Built the `src/` domain-layer skeleton (`domain/`, `workflow/`, `agents/`, `tools/`, `repositories/`, `validation/`, `observability/`, `config/`) per `PROJECT_BRIEF.md` §15.
- Implemented the full magic-link auth flow end to end: landing page with sign-in form (`app/page.tsx`), Supabase browser/server/service-role clients (`src/config/supabase/`), the auth callback route, and a `proxy.ts` (Next.js 16's renamed `middleware.ts`) that refreshes sessions and redirects signed-out requests away from `/app`.
- Built the route skeleton: `/app` (protected shell + placeholder chat), `/app/trips` (live query against the `trips` table, RLS-scoped), `/app/trips/[tripId]` (placeholder).
- `supabase init` (created `supabase/config.toml`).
- Wired up Vitest (`vitest.config.mts`, one sanity test, `npm test` script) and verified `npm run build`, `npm run lint`, and `npm test` all pass clean.
- Removed the unused, superseded `CURATED_TRAVEL_CONCIERGE_BUILD_BRIEF.md`.

**Why:** Continuing Phase 0 of the build sequence — the architecture decisions from the previous session (single Next.js app, mandatory magic-link auth) needed an actual scaffold to be real rather than just documented.

**Decisions made:**
- Kept Next.js's own `app/` router directory at repo root (not under `src/`), reserving `src/` for domain/business logic per ADR-000 — avoids mixing routing concerns with orchestration/domain code.
- Auth is enforced twice — once in `proxy.ts` (fast, avoids rendering protected pages at all) and once again in `app/app/layout.tsx` (defense in depth, per `PROJECT_BRIEF.md` §6.6) — redundant by design, not an oversight.
- Followed Next.js 16's rename of `middleware.ts` → `proxy.ts` (exported function name must be `proxy`, not `middleware`) rather than leaving the deprecated convention in place.

**What didn't work / dead ends:**
- `create-next-app` refuses a clean non-empty-directory run interactively; scaffolded into a sibling temp directory (`wanderwise-scaffold`) and merged the needed files in by hand instead, discarding its generated `README.md`/`CLAUDE.md` (boilerplate, superseded by ours).
- `vitest.config.ts` warned about future ESM/CommonJS incompatibility (`__dirname` usage) under Vite's native config loader; renamed to `.mts` and switched to `import.meta.dirname`.
- `npx supabase start` failed — Docker Desktop is installed but its daemon isn't running (`failed to connect to the docker API at npipe:////./pipe/dockerDesktopLinuxEngine`). Left this blocked rather than trying to launch a GUI app on the user's behalf; local Supabase (and therefore a working `.env.local`) is blocked until Docker Desktop is started manually.
- The `middleware-to-proxy` codemod wanted a clean git tree or `--force`; did the one-file rename by hand instead rather than run an automated codemod with `--force` on a repo with lots of new untracked files.

**Next up:** Start Docker Desktop, run `npx supabase start`, populate `.env.local` from the printed keys, confirm `0001_initial_schema.sql` applies cleanly and the magic-link flow works end to end against local Supabase (Inbucket/Mailpit for catching the email). Then Phase 1 — seed data and inventory repositories.

---

## 2026-09-16 — Docker/local Supabase blocker: root cause, pivot to hosted Supabase

**What I built:**
- Diagnosed the Docker Desktop startup failure from the previous session down to its actual root cause (see "What didn't work" below) — a machine-level Windows problem, not a Docker installation problem.
- Pivoted to a **hosted Supabase project** instead of local Docker-based Supabase for this environment. Created the project, pushed `0001_initial_schema.sql` to it via `supabase db push --db-url` (no CLI login/account-level token needed — just the project's own DB connection string), and populated `.env.local` with the real project URL, anon key, service role key, and `ANTHROPIC_API_KEY`.
- Verified the magic-link sign-in flow end to end against the real hosted project: submitted an email from the running dev server, confirmed `supabase.auth.signInWithOtp` round-tripped cleanly (UI moved to "check your inbox", no console errors).
- Added `.claude/launch.json` so the dev server can be previewed directly.

**Why:** Local Supabase requires Docker, which requires Windows to be able to create Unix domain sockets (AF_UNIX) — and it can't, machine-wide, independent of Docker entirely. That's not fixable by anything Docker-side, so continuing to chase it wasn't worth the time; a hosted project is something we'd need for a live deployment eventually anyway, so switching now isn't a step backward.

**Decisions made:**
- **Use a hosted Supabase project for this environment rather than local Docker-based Supabase**, until/unless the underlying Windows issue gets resolved. `supabase/config.toml` (from `supabase init`) stays in the repo in case local dev becomes viable later, but it's not the working setup right now.
- Migrations get applied via `supabase db push --db-url <connection-string>` using the project's own DB password — deliberately avoided asking for a Supabase account-level Personal Access Token, since that would grant broader account-wide project-management access than this task needed.

**What didn't work / dead ends (the Docker saga):**
- Root cause, found via a chain of elimination: Docker Desktop's "Inference manager" failed to bind a Unix socket (`...\Docker\run\dockerInference`) with "The file cannot be accessed by the system." Deleting the file failed identically through every tool tried — `Remove-Item`, `del`, `rmdir`, `fsutil reparsepoint query` (itself failed with the same error), `takeown`/`icacls` (same error even with forced ownership).
- `wsl --shutdown` and renaming the parent `run` folder (which *did* succeed, unlike deleting the files inside it) didn't help — Docker recreated a fresh `dockerInference` file that was broken again immediately.
- Disabling the specific feature via Docker's local feature-flag override (`features-overrides.json` → `{"InferenceAvailable": false}`) "fixed" that one error — only to immediately reveal the identical failure on a completely different Docker subsystem (the "Secrets Engine," a different socket path entirely). This was the key signal that it wasn't about any one file or feature.
- Wrote a standalone Node.js script that creates a plain AF_UNIX socket with no Docker involvement at all — it failed with the same class of error (`EACCES: permission denied`), proving conclusively this was a **Windows-level problem, not a Docker problem**.
- Tried the two standard fixes for broken Windows socket providers: `netsh winsock reset` (+ reboot) and enabling Developer Mode (+ reboot, since privilege grants apply at logon, not instantly) — neither changed the result of the plain Node.js test.
- Checked and ruled out: third-party antivirus (none installed), Windows Defender Controlled Folder Access (disabled), the `afunix` kernel driver (running normally).
- Reinstalled Docker Desktop entirely, at the user's suggestion — the plain Node.js AF_UNIX test still failed identically with Docker completely out of the picture, confirming again that reinstalling Docker was never going to fix an OS-level issue.
- Left as an open, unresolved mystery: something about this specific Windows machine's socket-provider stack rejects AF_UNIX socket creation with `EACCES`, survives a full Winsock reset, survives Developer Mode + reboot, and survives a Docker reinstall. Worth revisiting later with `sfc /scannow` / `DISM` (system file corruption) or a Process Monitor trace to see exactly what's denying the request, but not worth further time right now given the hosted-Supabase workaround unblocks everything.

**Next up:** Confirm the magic-link email actually completes the flow (click-through → `/auth/callback` → redirected into `/app`, session persists). Then Phase 1 — seed data and inventory repositories.

---

## 2026-09-16 — Phase 1: domain types, inventory repositories, seed data

**What I built:**
- `src/domain/money.ts` and `src/domain/dates.ts` — normalized `Money` (currency-checked arithmetic, throws `CurrencyMismatchError` on mismatch) and `DateRange` (ISO date strings, not `Date` objects) types, each with unit tests (17 tests total, all passing).
- `src/repositories/{destinations,flights,hotels,activities}.ts` — typed query functions over the inventory tables, taking a `SupabaseClient<Database>` and returning typed rows. These are the relational query layer the `search_flights`/`search_hotels`/`retrieve_activities` model-facing tools will wrap in Phase 6, not the tools themselves.
- `src/config/supabase/database.types.ts` — hand-written `Database` type for all three Supabase client factories (browser/server/service), covering the 13 tables actually queried so far. `supabase gen types typescript` needs Docker (spins up a local introspection container), which is still unavailable, so this is maintained by hand for now.
- `supabase/migrations/0002_seed_data.sql` — 6 destinations, 22 flights, 17 hotels, 27 activities, pushed to the hosted project via `supabase db push`.

**Why:** Phase 2 (deterministic services — budget engine, constraint filtering, feasibility engine) needs both a normalized money/date vocabulary and real inventory to filter against; building those without seed data first would mean testing against nothing.

**Decisions made:**
- Seed data lives in `supabase/migrations/0002_seed_data.sql`, not `supabase/seed.sql` as `PROJECT_BRIEF.md` §15 sketches — one file works for both `db push` (hosted, what we actually have) and a future local `db reset`, instead of two copies that could drift. Documented in the migration file's own header.
- Went with 6/22/17/27 (destinations/flights/hotels/activities) rather than the higher end of the ~4-6/~20-25/~15-20/~25-30 target range from the original plan — enough to make search and edge-case testing feel real without over-investing in hand-authored mock data at this phase.
- Deferred all embeddings (`destinations.embedding`, `activities.embedding`) to Phase 5 as originally planned — they're nullable columns, so leaving them null now doesn't block anything.

**What didn't work / dead ends:**
- `supabase gen types typescript --db-url ...` failed — it also requires Docker/Podman on PATH (`docker: command not found (podman also not found)`), same root cause as the local-Supabase blocker from the previous session. Hand-wrote the types instead.
- The hand-written types initially made every `.select(...)` resolve to `never` — postgrest-js's generic inference requires a `Relationships: []` field on every table (even if empty), which `supabase gen types` output always includes but I'd omitted. Traced this by reading postgrest-js's own `GenericTable` type definition in `node_modules` rather than guessing further; adding `Relationships: []` to all 13 tables fixed it, confirmed by a clean `tsc --noEmit`.

**Verification:** `npm test` (17/17 passing), `npx tsc --noEmit` (clean), `npm run build` (clean), `npm run lint` (clean). Also spot-checked the repositories against the live hosted data with a throwaway script (not committed): confirmed inventory-version filtering separates the stale v0 fixtures correctly, red-eye exclusion correctly returns zero results for the one route where every flight is a red-eye, rating and closed-day filters return the expected rows.

**Next up:** Phase 2 — deterministic services (budget engine, hard-constraint engine, candidate combination, itinerary feasibility engine, inventory-reference validation, state-transition validation), all unit-tested, no LLM involved yet.

---

## 2026-09-16 — Post-Phase-1 review and fixes

**What I built:** Nothing new-feature-wise — a full code review of everything built in Phases 0-1 (four review angles: correctness/cross-file, reuse/simplification, efficiency/altitude, CLAUDE.md conventions), then fixed everything it found. This is now a standing step after every phase, not a one-off.

**Why:** Requested explicitly — catch bloat, hacky code, and scalability gaps early, while they're cheap to fix, rather than letting them compound as more phases build on top.

**What the review found and I fixed (10 items):**
1. **`findDestinations`/`findActivities`/`findHotels` didn't filter by `inventory_version`** (only `findFlights` did) — the stale `v0` fixtures seeded specifically to test this were being returned as live data. Added `src/domain/inventory.ts` (`CURRENT_INVENTORY_VERSION`) and defaulted all four repos to it. Also fixed `getDestinationByName`, which — verified live — was an actual crash bug: two same-named Lisbon rows (current + stale) made `.maybeSingle()` throw "multiple rows returned."
2. **Flight date search had a timezone bug**: it compared a UTC day-boundary window against `departure_time`, which is stored/returned in UTC without preserving each flight's local offset. Fixed by widening the DB query ±1 day and filtering precisely by local calendar date (new `localDateInTimeZone` helper in `src/domain/dates.ts`, using `departure_time_zone`). Verified live: the old approach found 1 matching flight for NY→Lisbon on 2026-10-05, the fix correctly finds 2.
3. **Open redirect in the auth callback** — `redirectTo` from the query string was concatenated into the post-login redirect with no validation. Added `isSafeRedirectPath()` (same-origin relative paths only).
4. **Auth gate was an allowlist-by-prefix (`pathname.startsWith("/app")`)**, not deny-by-default — a future route outside `/app` (e.g. an API endpoint) wouldn't be protected unless someone remembered to prefix it. Flipped `proxy.ts` to a `PUBLIC_PATHS` allowlist (`/`, `/auth/callback`) with everything else protected by default.
5. **`dateRange` silently produced `NaN` for unparseable date strings** instead of throwing, because `NaN < NaN` is always `false`. Added `InvalidDateError` and an explicit `Number.isNaN` check in `toEpochDay`.
6. **Trip detail page couldn't distinguish a real DB error from "trip doesn't exist"** — both rendered the same 404. Now checks for PostgREST's `PGRST116` ("no rows") specifically before calling `notFound()`; any other error is rethrown.
7. **Sign-out had no error handling** — `signOut()`'s returned `error` was ignored, so a failure silently skipped the redirect with no feedback. Now checks the error and shows an inline message.
8. **Duplicate auth check per `/app` request** — `proxy.ts` and `AppLayout` both called `getUser()`. Removed the layout's redundant check; `proxy.ts` is now the sole auth gate, RLS remains the data-level defense in depth (the same pattern the trip detail page already used).
9. **No per-request memoization of the Supabase server client** — wrapped `createClient()` in `src/config/supabase/server.ts` with React's `cache()`, the standard Supabase/Next.js SSR pattern, so multiple Server Components in one request share a single client and session lookup.
10. **Filter-building and error-unwrapping logic was copy-pasted across all four repositories** — extracted `src/repositories/shared.ts` (`unwrapOrThrow`, `hasValues`), used by all four.

**Decisions made:** Made this review + fix + retest cycle a standing part of "phase done," not just a one-time cleanup — saved as a standing preference so it happens automatically after every future phase without being asked again.

**What didn't work / dead ends:** After the fixes, the dev server's browser tab showed a stale "Expected ',', got '<eof>'" parse error on `src/config/supabase/server.ts` that persisted through a server restart and a `.next` cache wipe — but `tsc --noEmit`, lint, `next build`, and the actual server logs (real requests returning 200) all confirmed the file was fine. Traced it to a stale WebSocket/HMR artifact in the specific browser tab that had been open during the edit; closing that tab and opening a fresh one cleared it immediately. Not a real bug — noting it in case it recurs, so it's not mistaken for one next time.

**Verification:** `npm test` (19/19 passing, 2 new test cases for the date-validation and timezone fixes), `npx tsc --noEmit` (clean), `npm run build` (clean, and `/app` is now statically optimizable since it no longer does per-request auth work), `npm run lint` (clean). All three DB-touching fixes (inventory-version filtering, `getDestinationByName`, flight timezone) spot-verified against the live hosted data with a throwaway script (not committed).

**Next up:** Phase 2 — deterministic services, unchanged from the prior plan.

---

## 2026-09-16 — Phase 2: deterministic services (budget, constraints, combinations, feasibility, inventory references, state machine)

**What I built:** All six deterministic services from `docs/IMPLEMENTATION_PLAN.md` §1 Phase 2, each pure/no-LLM and no-DB, per `PROJECT_BRIEF.md` §8.6's "deterministic planning baseline before AI optimization":
- `src/domain/constraints.ts` — hard-constraint engine (`filterHardConstraints` + one factory per constraint: no-red-eye, max price, min hotel rating, min room capacity, refundable, required accessibility, excluded closed days).
- `src/domain/budget.ts` — budget engine (`calculateBudget`), itemizing flight/hotel/activity costs into subtotal, taxes/fees, contingency, total estimate, remaining-vs-ceiling, unpriced items, and ceiling violations — the only place a total is ever computed (§9.3).
- `src/domain/combinations.ts` — `assembleCandidateCombinations`, cross-joining flights × hotels and greedily filling in the cheapest activities that still fit the ceiling.
- `src/domain/feasibility.ts` — `validateItineraryFeasibility`, checking a dated draft itinerary for hotel-stay coverage, activity date/hotel-stay bounds, closed days and opening hours (including overnight ranges), arrival/departure transfer buffers, and same-day overlaps/excessive load.
- `src/validation/inventory-references.ts` — `validateInventoryReferences`, rejecting itinerary item IDs that don't resolve to the approved candidate set, are on a stale inventory version, or belong to the wrong destination (§9.4, hallucinated-inventory guardrail).
- `src/workflow/state-machine.ts` — `validateStateTransition`, an explicit table-driven state machine covering the states in §8.1, the revision loop, confirmation (gated on proposal-hash match + guardrails passing), staleness/refresh, and cancellation/error/resume.
- 82 unit tests total across all six services plus new coverage added to `src/domain/dates.test.ts` for helpers relocated there during the review pass below.

**Why:** Per `CLAUDE.md`'s per-task discipline and `PROJECT_BRIEF.md` §8.6, the non-AI planning path (structured requirements → inventory filters → hard-constraint checks → budget → combinations → feasibility → structured draft) needs to exist and be independently correct before any agent/LLM code is layered on top in Phase 4+.

**Decisions made:**
1. Deviated from `docs/IMPLEMENTATION_PLAN.md` §3's proposed signatures in a few places (documented inline there): `BudgetBreakdown` uses the existing `Money` type instead of raw numbers; `filterHardConstraints` takes small self-describing `HardConstraint<T>` objects instead of a constraint union; `validateInventoryReferences` takes a lightweight `{flightIds, hotelIds, activityIds}` struct instead of a full `DraftItinerary`; `TransitionRequest` gained `proposalHashMatches`/`guardrailsPassed`/`resumeState` fields the original sketch didn't have, to actually implement §8.2's "proposal hash matches + guardrails pass" precondition and the §8.4 recovery path.
2. `assembleCandidateCombinations` assumes hotels have already passed a room-capacity hard constraint upstream (documented in its docstring) rather than modeling multi-room bookings itself — keeping it a pure budget-fit function, not a booking-logic engine.
3. `validateStateTransition`'s workflow table models `cancel`/`recoverable_error`/`fatal_error` as wildcard-`from` rows (legal from any non-terminal state) rather than per-state entries, after the initial version's per-state special-casing turned out to hide a dead rule (see review below).

**Post-phase review and fixes (standing practice since the last session):** Ran the `/code-review high` cycle (8 finder angles, 1-vote verify, then applied fixes) against the full diff. Found and fixed 10 issues, the most important being genuine correctness bugs, several confirmed against real seed-data fixtures rather than being purely hypothetical:
1. **Overlap detection only compared adjacent activities** after sorting by start time, missing a shorter activity nested inside a longer one when a third, non-overlapping activity sat between them in sort order. Fixed with a proper sweep-line (running max end time).
2. **Transfer-buffer checks (arrival/departure) only looked at same-calendar-day activities**, so a buffer spilling across midnight (e.g. an early-morning departure) never caught a late-night activity on the previous day. Fixed by comparing absolute "local instants" (epoch-day × 1440 + minutes) instead of date-string equality — this also let the arrival and departure buffer checks share one helper (`checkTransferBuffer`) instead of two duplicated blocks.
3. **Opening-hours parsing broke on overnight ranges** (`close < open`, e.g. "21:00-01:00") — and this isn't hypothetical: the actual seed data has exactly this shape for Reykjavik's "Northern Lights Hunt" (`0002_seed_data.sql`). Any activity legitimately scheduled in that window was being flagged as outside hours. Fixed with wraparound-aware containment plus a same-morning "yesterday's overnight window" fallback.
4. **A duplicated activity ID skipped every downstream check** (dates, hours, overlaps) for its second occurrence instead of just being flagged and continuing — meant a real overlap conflict on the duplicate could go unreported. Fixed by removing the early `continue`.
5. **`refundableConstraint` compared `cancellation_policy` to the literal string `"refundable"`**, but the real column is free text ("Free cancellation up to 48 hours before check-in" / "Non-refundable") — the literal never occurs, so the constraint rejected every real hotel. Fixed to check for a `"non-refundable"` substring instead (fail-closed on a null policy).
6. **`state-machine.ts` special-cased `cancel`/`recoverable_error`/`fatal_error` outside the declarative rules table**, which also made one explicit table row permanently dead code (a `cancel` rule from `failed_recoverable` that the special-case always intercepted first). Restructured as wildcard-`from` rows in the same table, and removed the placeholder `to: "blocked"` value on the `resume` rule (now an omitted `to`, resolved from `resumeState`) so a real workflow state can't be mistaken for unset placeholder data.
7. Reuse cleanup: `budget.ts` had its own copy of `money.ts`'s rounding helper and a USD-only formatter instead of the existing (currency-correct) `formatMoney`; `feasibility.ts` reimplemented local-time and weekday parsing that now live as exported helpers on `dates.ts` (`localMinutesOfDay`, `weekdayOf`, `toEpochDay`) instead of being duplicated.
8. Three test files had independently drifting copies of the same `flight()`/`hotel()`/`activity()` fixture builders (one still used the stale, since-fixed `cancellation_policy: "refundable"` value) — extracted to `src/repositories/fixtures.ts`, test-only, shared by all three.

Efficiency review found nothing worth changing at this project's actual scale (seed data in the 5-30 items per table range) — noted but explicitly not "fixed," since the fix would be over-engineering for now: `combinations.ts` recomputing a full budget breakdown from scratch on every greedy step is correct-by-design (§9.3 requires exactly one function ever compute a total) rather than a performance bug.

**What didn't work / dead ends:** None — this phase had no environment/tooling blockers, unlike the previous three sessions.

**Verification:** `npm test` (82/82 passing — 73 from the initial implementation, 9 more added during the review pass for the bugs above), `npx tsc --noEmit` (clean), `npm run lint` (clean), `npm run build` (clean). No DB or LLM calls in any of this phase's code, so nothing needed live-data spot-checking this time.

**Known limitations / assumptions carried forward:** `validateInventoryReferences` assumes `flights.destination`/`hotels.destination`/`activities.destination` and `destinations.name` share one identifier space (city names) since none of them are foreign keys — noted as a risk if a future data-loading path ever populates one table differently than the others. (The one-room-per-hotel-booking limitation originally noted here was resolved the same day — see the next entry.)

**Next up:** Phase 3 — session/trip repositories, state-versioning writes, event-history writes, and wiring the new `validateStateTransition` state machine into an actual workflow controller.

---

## 2026-09-16 — Multi-room hotel bookings (parties that split across rooms)

**What I built:** Generalized hotel booking from "always one room for the whole party" to one room per **room group** — so a family wanting the kids in a separate room, or a party that includes friends who want their own room, prices and validates correctly instead of silently assuming everyone shares one room.
- `src/domain/rooms.ts` (new) — `RoomGroup` (an occupant count plus an optional, calculation-irrelevant `label` like `"kids"`), `totalOccupants()`, `maxRoomOccupancy()`, and `assertRoomGroupsMatchTravelers()` (throws `RoomConfigurationError` if the groups don't add up to the traveler count).
- `src/domain/constraints.ts` — replaced `minRoomCapacityConstraint(partySize)` with `roomCapacityConstraint(roomGroups)`: since one room is booked per group, the hotel only needs `room_capacity` to cover the *largest* group, not the whole party.
- `src/domain/budget.ts` — `HotelSelection` gained an optional `rooms` field (default 1); both the nightly rate and the taxes/fees are now multiplied by room count.
- `src/domain/combinations.ts` — `CombinationParams` now takes `roomGroups` instead of implicitly assuming one room; validates the room-group/traveler invariant up front; and — closing the exact gap flagged as a known limitation above — skips any hotel whose `room_capacity` can't fit the largest room group itself, rather than only trusting that hard-constraint filtering happened upstream. `CandidateCombination` now reports `rooms` booked.
- 10 new tests across `rooms.test.ts`, `constraints.test.ts`, `budget.test.ts`, and `combinations.test.ts` (92 total, all passing).

**Why:** Asked directly, after discussing the one-room assumption as a known limitation from the Phase 2 build. Deliberately scoped to what the domain model already supports: a room group is an occupant *count*, not named people — there's no traveler-identity concept anywhere else in the schema (`trip_requirements`/`trip_preferences`/`trip_decisions` track fields and decisions, not individual people), so modeling "Alice and Bob in room 2" would be a materially bigger schema change than what was asked for. Flagged that boundary before implementing rather than silently deciding it either way.

**Decisions made:** A hotel booking still books uniform rooms of the same listed room type/rate for every group (the schema has no concept of a hotel offering multiple room types or a rooms-available count) — every room group is assumed to fit in "a room like this one," priced at the same nightly rate. This matches how the `hotels` table already models one row as one bookable room type, so it isn't a new gap introduced here, just an inherited one worth restating now that room count is explicit.

**Verification:** `npm test` (92/92 passing), `npx tsc --noEmit` (clean), `npm run lint` (clean), `npm run build` (clean).

**Next up:** Phase 3, unchanged from the prior entry.

---

## 2026-09-16 — Phase 3: session/trip repositories, versioned state, and the workflow controller

**What I built:**
- `src/repositories/sessions.ts`, `trips.ts` — typed CRUD for `sessions`/`trips`.
- `src/repositories/trip-state.ts` — `appendTripStateVersion` (optimistic concurrency via the existing `unique(trip_id, version)` constraint: a losing writer gets a clean `{status:"conflict"}` instead of a raw Postgres error), `getLatestTripState`, `getTripStateAtVersion`, and `findTripStateVersionByCorrelationId` (idempotency lookup).
- `src/repositories/trip-events.ts`, `workflow-runs.ts` — append-only event log writes and workflow-run/step telemetry (`PROJECT_BRIEF.md` §8.5).
- `src/workflow/controller.ts` — `startTrip` (creates a trip + its genesis `created` state version) and `advanceTrip` (the actual workflow controller: validates a proposed event via Phase 2's `validateStateTransition`, appends the next state version, then writes the `trip_events`/`trips.status`/`workflow_steps` mirrors). Cancellation and error-handling don't need separate code — they're just the `cancel`/`recoverable_error`/`fatal_error` events flowing through the same `advanceTrip` path Phase 2 already modeled as legal from any non-terminal state.
- `supabase/migrations/0003_trip_state_idempotency.sql` — a partial unique index on `trip_state_versions(trip_id, correlation_id)`, closing the race window between an application-level idempotency check and a concurrent duplicate request.
- 105 tests total (up from 100), including a fully-mocked `controller.test.ts` covering the retry/idempotency/rejection/conflict/terminal-completion branches without touching the DB.

**Why:** This is the seam between Phase 2's pure decision logic and real persistence — nothing durably tracks a trip's state yet, and every later phase (agents, orchestration, UI) needs a controller to call instead of writing to `trips`/`trip_state_versions` directly.

**Decisions made:**
1. Followed `PROJECT_BRIEF.md` §7.7 literally: the version-checked append to `trip_state_versions` is the one write required to be strictly consistent (enforced by the DB constraint, not application logic); `trip_events`, `trips.status`, and `workflow_runs`/`workflow_steps` are best-effort mirrors written afterward, not wrapped in a Postgres transaction/RPC. This is a real architectural choice (Area 3, workflow orchestration) made without a stop-and-ask, since it's reversible at the implementation level and §7.7 doesn't itself require the mirrors to be transactional — but see the review finding below, which is exactly why a choice like this needs to be gotten right rather than just documented.
2. `correlationId` (the idempotency key on every `advanceTrip` call) must be a UUID — the `correlation_id` columns are typed `uuid` in the schema. Caught by the live spot-check (see below), not by unit tests, since the mocked repositories in `controller.test.ts` don't know about column types. Added `InvalidCorrelationIdError` with a regex check at the top of `advanceTrip`.
3. `startTrip` (trip creation) is deliberately *not* idempotency-keyed — `trips` has no correlation-id column, and building that felt like scope creep for "state and workflow foundation." A caller that wants duplicate-request protection on creation should dedupe before calling it; noted as a known gap, not silently assumed away (the module docstring originally overclaimed this — see review finding below).

**Post-phase review and fixes:** Ran `/code-review high --fix`, scoped to just this phase's uncommitted diff (not the already-reviewed Phase 2/multi-room commits sitting ahead of `origin/main`). Found and fixed the most significant bug of any phase so far, plus several smaller ones:
1. **The idempotency replay path permanently dropped mirror writes after a partial failure** — flagged independently by three of the five review angles. `advanceTrip` returned `{status:"replayed"}` as soon as it found a `trip_state_versions` row for the correlation ID, without ever re-attempting `trip_events`/`trips.status`/`workflow_steps`/`workflow_runs.completed_at` writes that might have failed after the state append itself succeeded. A crash between the state append and the mirror writes meant those mirrors were gone forever — no retry could ever complete them, since every retry hit the same short-circuit. This directly contradicted the module's own documented retry-safety claim. **Fixed** by restructuring `advanceTrip`/`startTrip` around a shared `recordTransitionMirrors` helper: every mirror write is individually guarded by its own correlation-ID lookup (or, for `completeWorkflowRun`, an `is("completed_at", null)` filter), so a resumed call only performs whatever didn't happen yet, whether this is the first attempt or the fifth retry. Recovering the transition's `fromState` on a replay (needed to write a correct `trip_events` payload) reads the state at `version - 1`, since versions are sequential and each row is the state *after* its operation.
2. **A reused correlationId across two different logical operations could silently return the wrong state as a success** — the Postgres `23505` conflict code can't distinguish "you're retrying yourself" from "you collided with someone else's write." **Fixed** by checking the replayed row's stored `operation_type` against the currently-requested event and throwing `CorrelationIdReusedError` on a mismatch, rather than silently returning an unrelated transition's result.
3. **`updateTripStatus`/`completeWorkflowRun` discarded their update's result, so a zero-row match (stale ID, RLS mismatch) succeeded silently.** **Fixed** for `updateTripStatus` (now throws `TripNotFoundError` if nothing matched — zero rows is never legitimate there) but deliberately *not* for `completeWorkflowRun`, where zero rows is now the expected, correct outcome of an idempotent replay.
4. **`resume` allowed a no-op `failed_recoverable → failed_recoverable` transition** (Phase 2 code, surfaced by this phase's testing) — a state-machine precondition gap that would let a "recovery" write telemetry showing progress that didn't happen. **Fixed** with one more precondition check.
5. **An unchecked cast let malformed `trip_state_versions.state` JSON skip all validation on the replay path specifically** (the normal path at least gets indirectly checked by `validateStateTransition` failing to find a matching rule). **Fixed** with a runtime check in `toTripStateVersion`, throwing `InvalidTripStateSnapshotError` on an unrecognized `workflowState` value.
6. Reuse/simplification/efficiency: extracted the duplicated `startTrip`/`advanceTrip` mirror-write sequence into the one `recordTransitionMirrors` helper (also what made fix #1 tractable), and parallelized independent reads/writes (`Promise.all` for the idempotency-key lookup + current-state read, and for the independent event/status/workflow-run mirror writes) that were previously sequential for no reason.
7. **Not fixed, documented instead:** `getOrCreateActiveWorkflowRun`'s select-then-insert has no DB constraint behind it, so two genuinely concurrent `advanceTrip` calls for the same trip could both create an "active" `workflow_runs` row. Closing this needs a partial unique index (another migration) for a race that has no real caller yet (nothing calls the controller concurrently as of Phase 3) — left as a documented known gap rather than pushing a second migration in the same pass.

**Verification:** `npm test` (105/105 passing), `npx tsc --noEmit` (clean), `npm run lint` (clean), `npm run build` (clean). Live-spot-checked twice against the hosted Supabase project with throwaway scripts (not committed, each creating and deleting its own auth user): first confirmed the raw insert/update paths, the `unique(trip_id, version)` optimistic-concurrency rejection, and the new idempotency partial-unique-index rejection all behave as designed (and caught the `correlation_id`-must-be-a-UUID bug in the process); second round re-confirmed the review-fix assumptions specifically (a zero-row `.update()` really does return `[]` with no thrown error; the idempotent `completeWorkflowRun` filter really does make a second call a harmless no-op).

**Known limitations carried forward:** `getOrCreateActiveWorkflowRun`'s TOCTOU race (above — resolved same day, see next entry). The identifier-space assumption in `validateInventoryReferences` from Phase 2 is unrelated to this phase and still stands.

**Next up:** Phase 4 — structured output schemas for requirement/preference/decision extraction, and the Intake and Revision Interpreter agent (the first place an LLM enters the system).

---

## 2026-09-16 — Closed the workflow_runs concurrent-insert race

**What I built:** Closed the race documented in the previous entry rather than leaving it deferred to Phase 6 — asked directly whether "no concurrent caller yet" actually held up (a double-click or client retry hits `advanceTrip` twice today, no orchestrator required), and it didn't.
- `supabase/migrations/0004_workflow_runs_single_active.sql` — a partial unique index, `workflow_runs(trip_id) where status='running' and completed_at is null`, making the database itself enforce "at most one active run per trip."
- `src/repositories/workflow-runs.ts` — `getOrCreateActiveWorkflowRun` now catches the `23505` a losing concurrent insert produces and re-fetches the winner's row instead of erroring, mirroring the same catch-and-recover pattern already used for `trip_state_versions`' optimistic concurrency.

**Why:** Asked directly, after initially deferring this with the reasoning "no real concurrent caller exists as of Phase 3" — that reasoning didn't hold up to being questioned (double-submission is a today problem, not a Phase-6 problem), and the fix was cheap enough (one migration, one small code change) that deferring it further wasn't worth the tracking overhead.

**Decisions made:** Also restructured how deferred items get tracked going forward — `docs/IMPLEMENTATION_PLAN.md` §5 is now the single place every deferred bug/gap/constraint lives as an explicit checklist item (checked off, not deleted, when resolved, so the history stays visible), rather than prose scattered across `BUILD_LOG.md` entries that's easy to skim past. Audited every prior entry and added the two other still-open gaps that hadn't made it into a tracked list yet (`validateInventoryReferences`' destination identifier-space assumption from Phase 2; `startTrip` not being idempotency-keyed from this phase), plus the hotel room-type/availability assumption and the unreachable `"blocked"` state as lower-priority notes. **Standing instruction going forward: when a deferred issue or open item comes up (in a review, while building, or anywhere else), ask whether to address it now or later — don't silently decide to defer it.**

**Verification:** `npm test` (105/105 passing, no test changes needed — the fix is internal to `getOrCreateActiveWorkflowRun`), `npx tsc --noEmit` (clean), `npm run lint` (clean), `npm run build` (clean). Live-verified against the hosted Supabase project with a throwaway script (not committed): two concurrent inserts for the same trip, confirmed exactly one succeeds and the other fails with `23505`; confirmed the loser's re-fetch finds the winner's row; confirmed a new active run can still be created after the first one completes (the fix doesn't permanently lock a trip out of new runs).

**Next up:** Phase 4, unchanged from the prior entry.

---

## 2026-09-16 — Session close: Phase 4 planning discussion (no code changes)

**What happened:** No implementation this stretch — a planning conversation ahead of starting Phase 4 in a fresh session, plus wrap-up/handoff for this one. Captured here (and pre-loaded into `docs/IMPLEMENTATION_PLAN.md`'s Phase 4 section) so the next session doesn't have to re-derive it:

1. **Clarification-question design** — discussed whether Phase 4 needs a hand-built question-ordering decision tree for the Intake agent's clarification behavior. Concluded no: `PROJECT_BRIEF.md` §12 already models this as a single `request_clarification(missing_fields, reason)` tool call the agent makes per turn from current trip state — ordering/phrasing is agent territory, only the required-fields completeness check should be deterministic. Recorded as a design note on the Phase 4 checklist item, not a locked decision — revisit if scripted reliability turns out to matter more than assumed.
2. **Model selection (ADR-INDEX Area 1, open item)** — recommended Sonnet 5 for the Intake/Revision Interpreter (extraction/classification/ambiguity-detection needs more judgment than the cheapest tier), Haiku 4.5 as a cheaper alternative to measure later. **Asked the user to confirm via `AskUserQuestion`; the question was dismissed without an answer.** This is genuinely unresolved, not defaulted — flagged prominently on the Phase 4 checklist so the next session asks again before writing model-client code, rather than silently assuming Sonnet 5.
3. **Cost estimation** — walked through Sonnet 5 API pricing ($2/$10 per 1M input/output tokens) and a rough per-call cost once Phase 4's prompt-caching structure exists (~$0.01/call with an 8K-cached-token system prompt + tool defs), landing around 9,400 Intake-agent calls per $100 of API spend. This is about the *deployed app's* runtime cost (relevant to the brief's own "cost per completed trip" metric, §20) — unrelated to this session's own Claude Code usage-limit conversation, which was a separate tangent about billing mechanics, not a project decision, so it isn't reproduced here.

**Verification:** N/A — no code touched. `git status` confirmed clean before ending the session (everything from the workflow_runs-race-fix entry above was already committed).

**Known limitations carried forward:** unchanged — see `docs/IMPLEMENTATION_PLAN.md` §5 for the full tracked list (identifier-space assumption in `validateInventoryReferences`, `startTrip` not idempotency-keyed, hotel room-type/availability assumption, unreachable `"blocked"` state).

**Next up:** Phase 4 — start with the structured Zod schemas for requirement/preference/decision extraction (pure TypeScript, no model calls, fully unit-testable — the same low-risk-first pattern Phase 2 used). Before writing the agent's model-client code, confirm the model choice (item 2 above) rather than assuming Sonnet 5.

---

## 2026-09-16 — Phase 4: Intake and Revision Interpreter agent, first LLM call in the system

**What I built:**
- `src/domain/extraction.ts` — structured Zod schemas for requirement/preference extraction (`PROJECT_BRIEF.md` §7.1/§12): a 14-field discriminated union (`ExtractedRequirement`, keyed to the hard-constraint vocabulary `src/domain/constraints.ts` already established: origin, destination, departureDate/returnDate, partySize, roomGroups, budgetTotalUsd, noRedEye, maxFlightPriceUsd, minHotelRating, refundableHotel, requiredAccessibility, excludeClosedOnDays, maxActivityPriceUsd), `ExtractedPreference` (6 qualitative fields), `ClarificationRequest`, `RevisionProposal`, plus `checkRequirementsComplete()` — the deterministic gate for leaving `collecting_requirements` (only `origin`/`destination`/`departureDate`/`partySize`/`budgetTotalUsd` are actually required; everything else is optional).
- `src/agents/model-client.ts` + `src/agents/providers/anthropic-model-client.ts` — a provider-agnostic `ModelClient` interface (CLAUDE.md: "keep model calls behind interfaces; keep provider-specific code isolated") and its one Anthropic implementation. One non-looping `messages.create` call per turn (no agentic tool-execution loop needed — the tools are output-shaping, not real actions requiring a `tool_result` round-trip). Prompt caching wired per §6.7: system prompt and tool definitions cached, dynamic trip-state/user-message content passed last.
- `src/agents/intake.ts` — the Intake and Revision Interpreter agent (`runIntakeAgent`), one agent covering both responsibilities (`PROJECT_BRIEF.md` §6.2 row A) since "intake" vs. "revision" is just a framing difference driven by whether `currentDecisions` is non-empty. Exposes three real Claude tools — `record_extraction`, `request_clarification`, `propose_trip_revision` — built from the Zod schemas via `z.toJSONSchema()` so the model-facing contract and the server-side validation can't drift apart. A pure function of (message, trip-state slice) → validated extraction; never touches the database or decides workflow state (that's Phase 6).
- `src/config/models.ts` — per-agent model selection, `Sonnet 5` as the resolved default for the Intake agent.
- `evals/cases/intake.ts` + `evals/runners/run-intake-eval.ts` (`npm run eval:intake`) — 6 component eval cases (explicit multi-field extraction, ambiguity detection, preference extraction, revision interpretation, adversarial prompt injection, contradictory-input resolution) run against real models, with cache-aware cost estimation and a pass-rate/cost/latency/cache-hit comparison table.
- 144 tests total (up from 105 at the end of Phase 3), all unit tests, no real API calls in `npm test`.

**Why:** This is the first place an LLM enters the system (`docs/IMPLEMENTATION_PLAN.md` Phase 4). Everything built in Phases 0-3 was deliberately LLM-free so the deterministic planning baseline (§8.6) would exist and be independently correct before AI optimization layers on top — this phase is that first layer.

**Decisions made:**
1. **Model selection, resolved.** Asked again at the start of this session (it was asked once before and dismissed unanswered — `docs/IMPLEMENTATION_PLAN.md` flagged this as the explicit "start here" blocker). User chose a third option over the original two: keep `ModelClient` provider/model-agnostic, wire Sonnet 5 as the working default, and let the Phase 4 component eval actually decide rather than picking from a table. First real eval run: **Sonnet 5 passed 6/6 cases** ($0.0345 total, prompt caching confirmed working via measured cache-read tokens); **Haiku 4.5 passed 5/6** — it never called `propose_trip_revision` on the revision-interpretation case at all, a real quality gap, not a hypothetical one — and showed **zero cache hits** across every call, because Haiku 4.5's minimum cacheable prefix (4,096 tokens) is higher than Sonnet 5's (1,024) and this agent's static system+tools block apparently tokenizes just under that threshold on Haiku's tokenizer. Sonnet 5 is the confirmed choice, backed by measurement.
2. **Design departure from `PROJECT_BRIEF.md` §12's literal tool table, documented rather than silently decided:** the table names only `request_clarification`/`propose_trip_revision` for this agent; extraction itself is implemented as a third real tool call (`record_extraction`) rather than folded into one structured-output JSON blob. Rationale (in `intake.ts`'s own header comment): §6.4 requires tool schemas to be "explicit, typed, narrow, and validated" for getting structured data out reliably, not just for the two named tools, and keeping all three as real tool calls lets one non-looping request emit any combination of them in a single turn.
3. **`toEpochDay` (`src/domain/dates.ts`) now rejects calendrically-invalid dates** (e.g. `"2026-02-30"`, `"2026-04-31"`) instead of silently letting `Date.UTC`'s rollover behavior turn them into a different, valid-looking date. This was a pre-existing gap surfaced by this phase's review (see below) — asked the user whether to fix now or track as an open item (per the standing rule from the previous session's close), and the user chose to fix now. `src/domain/extraction.ts`'s `isoDate` Zod schema now delegates to it instead of duplicating a weaker regex-only check.
4. **`AnthropicModelClient` now wraps `client.messages.create()` in a try/catch**, rethrowing as a new `ModelClientError` (in `model-client.ts`) rather than letting `@anthropic-ai/sdk`-specific exception types leak through the provider-agnostic interface — closing a gap the review found in the interface's own stated contract.
5. **The eval runner needs `--conditions=react-server`** (wired into the `eval:intake` npm script) so the `server-only` import guard in `anthropic-model-client.ts` — which only resolves correctly under Next.js's own bundler — treats the standalone script as the trusted server-side context it is, instead of throwing. Kept the guard rather than removing it, since it's a real safety property for the actual app (API-key-holding code must never reach a client bundle).

**Post-phase review and fixes (standing practice since Phase 1):** Ran a 4-angle parallel review (correctness, reuse/simplification/efficiency, altitude/conventions, test-coverage/guardrail-gaps) against the full diff (~1,190 lines excluding the lockfile). Found and fixed 5 issues, verified 2 more as intentional/non-issues:
1. **`record_extraction` was validated as one atomic Zod array, so a single malformed item silently discarded every valid sibling item in the same tool call** — flagged independently by two review angles. Fixed by validating each requirement/preference item individually (`parseRecordExtractionCall`), keeping whatever validates and reporting only what doesn't, while the JSON schema shown to the model stays fully strict.
2. **`stopReason` was computed by `AnthropicModelClient` but never surfaced on `IntakeAgentResult`**, so a `max_tokens`-truncated or `refusal`-declined response was indistinguishable from a clean, benign empty turn. Fixed — `stopReason` is now part of the agent's return type.
3. **`isoDate` accepted calendrically-invalid dates** — see decision 3 above.
4. **Anthropic SDK errors leaked through the provider-agnostic `ModelClient` abstraction** — see decision 4 above.
5. **Tool names were a second, unenforced source of truth** — the `TOOLS` array sent to the model and the dispatch `if`/`else-if` chain independently re-spelled the same three string literals, so a rename in one place wouldn't be caught by the compiler. Fixed with a single `TOOL_NAMES` constant object consumed by both.
6. Two test-coverage gaps closed: accumulation across two `record_extraction` calls in one turn was untested (now covered), and the partial-validity case from fix #1 is now directly tested.
7. **Not changed, judged correct as-is:** `toRequirementRecord`/`toPreferenceRecord` have no caller yet in this diff — flagged as a simplification candidate, but they're the direct realization of `PROJECT_BRIEF.md` §7.1's own `req_123` JSON example, already unit-tested, and exist specifically as the Phase 6 persistence seam — kept.

**What didn't work / dead ends:**
- The eval runner initially failed with `This module cannot be imported from a Client Component module` from the `server-only` package — it only resolves its Next.js-bundler-specific conditional export (`react-server` condition) inside Next's own build pipeline; under plain Node it always throws. Fixed by adding `--conditions=react-server` to the `eval:intake` script (Node's native custom-conditions flag) rather than removing the guard from production code.
- Considered a `dotenv` dependency for the eval runner's `.env.local` loading; used Node's built-in `--env-file` flag instead (Node 24 is in use here) — no new dependency needed.

**Verification:** `npm test` (144/144 passing), `npx tsc --noEmit` (clean), `npm run lint` (clean), `npm run build` (clean). Live-verified via `npm run eval:intake` against the real hosted Anthropic API: 12 real model calls (6 cases × 2 models), Sonnet 5 6/6, Haiku 4.5 5/6, prompt caching confirmed via measured `cache_read_input_tokens` > 0 on repeat calls for Sonnet 5.

**Known limitations / assumptions carried forward:** the eval harness is deliberately small (6 cases) — a component eval, not the full end-to-end scenario suite from `PROJECT_BRIEF.md` §19 (that's Phase 8). This agent's output isn't wired into any persistence or the workflow controller yet — that's Phase 6, per §6.3 ("orchestrator persists validated state changes"). No retry/backoff policy beyond the Anthropic SDK's own default (2 retries on 429/5xx) exists yet at the agent-call level — reasonable to defer to Phase 6, which is where the workflow controller's existing idempotency/retry patterns (Phase 3) would actually wrap agent calls.

**Next up:** Phase 5 — retrieval and curation (embeddings for seed `destinations`/`activities`, metadata-filtered retrieval functions, the Destination/Activity Curator agent, the Trip Explanation Agent). Alternatively, Phase 6 (orchestration integration) could go next if wiring this agent into the workflow controller feels more valuable to build/demo before adding more agents — worth a quick discussion at the start of the next session rather than defaulting to strict phase order.

---

## 2026-09-16 — Phase 6: orchestration integration for the Intake agent

**What I built:** Wired the Phase 4 Intake and Revision Interpreter agent into the workflow controller — the first agent to go end to end, from an authenticated request through a real Anthropic call to persisted state. Asked which phase to do next (Phase 5 retrieval/curation vs. Phase 6 orchestration); recommended Phase 6 first since wiring the one existing agent through the controller now, while it's the only agent, de-risks integration early instead of stacking more agents on an unproven seam — user agreed.
- `src/workflow/intake-orchestrator.ts` (`processIntakeTurn`) — the Orchestrator (`PROJECT_BRIEF.md` §6.2/§6.3) for this one agent: loads trip state + current requirements/preferences, calls `runIntakeAgent` through a `ModelClient`, persists validated output, logs guardrail/telemetry, drives `advanceTrip`. Never queries domain tables directly, never computes anything the deterministic `checkRequirementsComplete` gate should own.
- Five new repositories: `trip-requirements.ts`, `trip-preferences.ts` (each with an `appendX`/`listActiveX`/`retireActiveXForField` trio — the last one new, see below), `messages.ts`, `agent-runs.ts` (+ `tool_calls`), `guardrail-events.ts`.
- Extended `src/config/supabase/database.types.ts` with `agent_runs`/`tool_calls`/`guardrail_events` (the schema already had these tables from Phase 0; only the hand-written types were missing).
- `app/app/actions.ts` (`sendMessage`, a Server Action) — the real, authenticated, reachable entry point. No chat UI calls it yet (Phase 7), but it's not just mocked-test-only wiring.
- `src/agents/intake.ts`: replaced `malformedToolCalls`/`MalformedToolCall` with a fuller `toolCallLog`/`ToolCallLogEntry` audit trail (every tool call, not just failed ones, with its validated result), since Phase 6 needed the full per-call detail to persist `tool_calls` rows.
- 167 tests total (up from 144 at the start of this session), all unit tests except the live spot-check below.

**Why:** Per the discussion at the end of the last session — an agent whose output has nowhere to go isn't a demoable slice, and integration problems are cheaper to find with one agent wired than with four.

**Decisions made:**
1. **The Server Action uses the service-role Supabase client (`createServiceClient`), not the RLS-scoped one, past the auth check.** `workflow_runs`/`workflow_steps`/`agent_runs`/`tool_calls`/`guardrail_events` have RLS enabled with no policy for `anon`/`authenticated` (service-role only, per `supabase/migrations/0001_initial_schema.sql`) — this was already true since Phase 0/3, but never actually exercised by a real caller until this phase, so it had never been tested against real RLS. Confirmed via a live spot-check that the RLS-scoped client would have silently failed to write those tables. Ownership is checked explicitly (`trip.user_id !== user.id`) in the Server Action to replace what RLS would otherwise have enforced automatically — documented in the file's own header comment, not a silent architecture change.
2. **`record_extraction`/`propose_trip_revision` items that land on an already-set field retract the prior active row before appending the new one**, rather than letting two active rows for the same field coexist. New repository primitive: `retireActiveTripRequirementsForField`/`retireActiveTripPreferencesForField`. Found this was missing via a live spot-check (see below) — a detected budget revision was correctly logged to `tool_calls` but never actually changed the requirement, and even without a revision, a plain re-statement of a field across turns would have left two ambiguous "active" rows.
3. **`propose_trip_revision` for `revisionType: "requirement"`/`"preference"` is applied by re-validating its `target`/`value` against that field's own Zod schema** (`ExtractedRequirement`/`ExtractedPreference`) and routing it through the same persist path as `record_extraction` — a revision is structurally just a new value for a field. `revisionType: "decision"` has nothing to apply against yet (no `trip_decisions` repository exists before Phase 5/7 creates actual decisions), so it's logged as a domain-validation guardrail trigger instead of silently dropped or crashing.
4. **Multi-hop transitions within one turn** (e.g. `clarification_resolved` immediately followed by `requirements_complete`, both needed when the last missing field arrives while `awaiting_clarification`) use per-step correlation IDs derived from the base turn's ID plus the event name, so a retry that recomputes a shorter chain (because an earlier hop already durably applied before a crash) still reproduces the right ID for each event — reusing `advanceTrip`'s existing idempotency machinery (Phase 3) rather than building new retry logic.

**Post-phase review and fixes (standing practice since Phase 1):** Ran a 4-angle parallel review against the full diff (~1,420 lines). Found and fixed 10 issues, tracked 2 as deliberate open items, judged 2 as not worth changing:
1. **Chained transition correlation IDs were keyed by array position, not event name** — a retry that recomputed a shorter chain (one hop already applied before a crash) reused an index for a *different* event than originally, throwing `CorrelationIdReusedError` instead of resuming safely. This defeated the exact retry-safety the chaining was built for. Fixed by keying on the event name instead.
2. **Items targeting the same field within one turn weren't deduplicated before being returned** — the DB ended up correct (each retract-then-append left one active row), but the in-memory/returned snapshot could show two entries for one field. Fixed with a `lastByField` reducer applied before persistence.
3. **The core bug this phase's own live spot-check caught**: a detected `propose_trip_revision` was never actually applied (see decision 2/3 above) — the single most important gap, since it meant half of "Intake **and Revision** Interpreter" wasn't wired at all.
4. **`agent_runs.status` ignored `agentResult.stopReason`** — a `max_tokens`-truncated or `refusal`-declined turn with zero tool calls looked identical to a clean, successful empty turn. Fixed: a non-`end_turn`/`tool_use` stop reason now marks the run `"error"` with the reason recorded.
5. **Layer 4 (workflow authorization) guardrail decisions were enforced by `advanceTrip` but never logged to `guardrail_events`**, contradicting the module's own "all four layers" claim. Fixed — every `advanceTrip` call (including `start_intake`) now logs a Layer 4 row, triggered or not.
6. **`advanceTrip`'s `"conflict"` status (a benign, retry-safe race) was thrown as the same error as a genuinely rejected transition**, with a grammatically broken message. Fixed with a distinct `OrchestrationConflictError`.
7. **No check that a given `sessionId` actually belongs to the given `tripId`** inside the orchestrator itself — the one caller today always derives it correctly, but nothing enforced that as an invariant. Fixed with an explicit `getTrip` + comparison, throwing `SessionTripMismatchError`.
8. Two unforced sequential-await spots (independent `appendMessage`/`getLatestTripState`; independent Layer-2 guardrail-event writes) parallelized via `Promise.all`.
9. Docstring drift: `trip-preferences.ts`'s `appendTripPreference` was missing the retract-before-append safety caveat `trip-requirements.ts`'s equivalent already had. Copied over.
10. **Tracked as an open item, not fixed (asked the user first):** a retry of a whole `processIntakeTurn` call isn't yet idempotent across `appendMessage`/`agent_runs`/`tool_calls`/`guardrail_events` the way `advanceTrip`'s own mirror writes are — closing it fully means threading correlation-ID-guarded existence checks through five more write paths, comparable in size to what Phase 3 built for transition mirrors alone. No caller retries a failed turn yet (no chat UI exists), so nothing breaks in practice today; added to `docs/IMPLEMENTATION_PLAN.md` §5 alongside the related, already-tracked `startTrip` idempotency gap, which this phase's Server Action also didn't close for the same reason (no real double-submission risk without a UI yet).
11. **Not changed, judged correct as-is:** the structural duplication between `trip-requirements.ts`/`trip-preferences.ts` and between the two `*RecordFromRow` mappers — a generic helper would keep them in lock-step, but two concrete instances doesn't yet justify the abstraction; and `getOrCreateActiveWorkflowRun` being re-fetched once per `advanceTrip` call this turn — fixing it would mean changing `advanceTrip`'s signature to accept a pre-fetched run, out of scope for this phase.

**What didn't work / dead ends:**
- The live spot-check script initially crashed with `This module cannot be imported from a Client Component module` from the `server-only` package — it only resolves its Next.js-bundler-specific conditional export inside Next's own build pipeline; under plain Node it always throws. Same fix as Phase 4's eval runner: `--conditions=react-server` on the Node invocation, not removing the guard from production code.
- The live spot-check's own query for `workflow_steps` initially failed (`column workflow_steps.trip_id does not exist` — that table only has `workflow_run_id`, no direct trip reference); fixed the throwaway script, not app code.

**Verification:** `npm test` (167/167 passing), `npx tsc --noEmit` (clean), `npm run lint` (clean), `npm run build` (clean). Live-verified end to end against the real hosted Supabase project + real Anthropic API with a throwaway script (not committed, created and deleted its own auth user): two real `processIntakeTurn` calls (initial intake reaching `requirements_ready` in one turn; a follow-up budget revision), confirmed every table this phase touches (`trip_state_versions`, `trip_requirements`, `trip_preferences`, `messages`, `workflow_runs`, `workflow_steps`, `agent_runs`, `tool_calls`, `guardrail_events`) got the expected rows — first run caught the revision-not-applied bug (fix #3 above), second run confirmed the fix: the superseded `budgetTotalUsd=3000` row was `status: "retracted"` and the new `4000` row `"active"`, exactly one of each.

**Known limitations / assumptions carried forward:** the two tracked idempotency gaps above (`startTrip`, and now `processIntakeTurn`'s non-transition writes); `revisionType: "decision"` revisions are detected but not applicable until Phase 5/7 create actual decisions; the Intake agent still sometimes returns an empty `assistantMessage` when it only calls tools (observed live, not a bug — model behavior, not orchestrator wiring — worth revisiting once Phase 7's chat UI makes empty assistant turns visibly awkward).

**Next up:** Phase 5 — retrieval and curation, unchanged from the prior entry's description. The chat UI (Phase 7) is now the only thing standing between this backend wiring and an actually demoable product surface, which may be worth weighing against Phase 5 at the start of the next session.

---

## 2026-09-16 — Session open: embedding provider decided (Voyage AI), no code yet

**What happened:** Asked which phase to do next; user chose Phase 5 (retrieval/curation) over Phase 7 (chat UI), reasoning that finishing the backend before testing the frontend is a deliberate, acceptable sequencing choice — noted the real tradeoff (nothing demoable in a browser until Phase 7 either way) but didn't push back further once the user weighed it and decided.

Before writing any Phase 5 code, surfaced a decision that had never actually been made anywhere in the docs: Anthropic has no embeddings API, so RAG (`PROJECT_BRIEF.md` §10) needs a second provider, and the schema already commits to `vector(1536)` (`supabase/migrations/0001_initial_schema.sql`) with no embedding model picked to match it. Asked the user to choose between Voyage AI and OpenAI `text-embedding-3-small` before any code was written, rather than defaulting silently — same pattern as Phase 4's model-selection question.

**Decision: Voyage AI**, reached through discussion rather than a single up-or-down pick:

1. **First pass — at this project's actual scale (6 destinations, 27 activities), neither option is a clean win.** Voyage AI: Anthropic acquired Voyage AI in 2025, so it's genuinely part of the same vendor relationship now, not just a "recommended partner" — the stronger narrative fit for a portfolio piece about Claude agentic engineering; Voyage's models also benchmark well specifically for retrieval. Against that: no Voyage model natively outputs 1536 dims (they use Matryoshka-truncated 256/512/1024/2048), so picking Voyage means a migration narrowing the `embedding` columns — cheap right now since they're still empty, but a real extra step OpenAI's `text-embedding-3-small` doesn't need (1536 dims natively, matches the schema as committed, very cheap, extremely well-trodden). Corrected an asymmetry in how the question was first framed: *both* options need a new API key and dependency — that's not actually a differentiator, since Anthropic offers no embeddings at all either way.
2. **User asked what the recommendation would be at genuinely large (production) scale**, not this project's toy dataset. That reframing changes the answer, not just the confidence behind it: at real volume, embedding dimension becomes an infrastructure cost (pgvector index size/build time/query latency all scale with it), so Voyage's flexible/smaller Matryoshka dimensions turn from "a migration nuisance" into a genuine lever OpenAI's fixed 1536 (or 3072 for `text-embedding-3-large`) doesn't offer; retrieval-quality benchmark gaps that don't matter on 27 activities start to matter once corpus size creates near-duplicate/confusable content; production RAG typically needs a second-stage reranker, and Voyage ships a first-party one built to pair with its own embeddings (OpenAI has none, so picking OpenAI likely means a *third* vendor for reranking anyway); and vendor consolidation (one Anthropic relationship covering generation + embeddings) matters more at enterprise scale than at hobby-project scale. Flagged the one honest caveat: raw per-token pricing wasn't actually pulled for either provider, so a real production decision should verify current numbers rather than trust this reasoning alone.
3. **User chose Voyage AI**, explicitly citing the case made in step 2 as the basis, and asked for it to be recorded here rather than just left in chat.

**Decisions made:**
- Embedding provider: **Voyage AI**, for the reasons above — recorded in `docs/IMPLEMENTATION_PLAN.md`'s Phase 5 section, including the migration consequence (narrowing `embedding vector(1536)` to whichever Matryoshka dimension is chosen, most likely 1024, before any embeddings are generated).

**What didn't work / dead ends:** None — no code touched this entry.

**Verification:** N/A — documentation only, `git status` will show only the two doc edits (this entry, `docs/IMPLEMENTATION_PLAN.md`'s Phase 5 section) once committed.

**Known limitations / assumptions carried forward:** unchanged from the previous entry. The exact Matryoshka output dimension for Voyage (1024 assumed above) and the specific model (`voyage-3-large` vs. `voyage-3.5`/`voyage-3.5-lite`) aren't locked yet — confirm against Voyage's current API docs when the migration is actually written, not before.

**Next up:** Start Phase 5 implementation: the embedding-column migration, then embedding generation for seed `destinations`/`activities`, then metadata-filtered retrieval functions, then the Curator and Trip Explanation agents.

---

## 2026-09-16 — Phase 5: retrieval and curation (Voyage AI embeddings, pgvector search, Curator + Trip Explanation agents)

**What I built:**
- `supabase/migrations/0005_retrieval.sql` — narrowed `destinations.embedding`/`activities.embedding` from the placeholder `vector(1536)` to `vector(1024)` (Voyage's `voyage-4-lite` default, a well-supported Matryoshka truncation point — confirmed against Voyage's real API docs/TypeScript SDK types before writing any client code, not assumed). Added `match_destinations`/`match_activities` — Postgres functions doing cosine-similarity search with metadata pre-filtering (budget, vibe tags for destinations; price, accessibility, vibe tags for activities) inside the database, per `PROJECT_BRIEF.md` §10.2 ("apply metadata filters before or alongside vector similarity"), not fetched-then-filtered client-side. Pushed live and verified callable before building anything on top of it.
- `src/retrieval/embedding-client.ts` + `src/retrieval/providers/voyage-embedding-client.ts` — a provider-agnostic `EmbeddingClient` interface (mirroring Phase 4's `ModelClient` pattern) and the Voyage implementation, using the real `voyageai` npm package (verified its actual exports/types by downloading and inspecting the published package, not guessed) rather than hand-rolled REST calls.
- `src/repositories/{destinations,activities}.ts` — `matchDestinations`/`matchActivities`, typed RPC wrappers over the SQL functions above.
- `src/retrieval/{destinations,activities}-retrieval.ts` — `retrieveDestinations`/`retrieveActivities` (the `retrieve_destinations`/`retrieve_activities` tools, `PROJECT_BRIEF.md` §12): embed the query, delegate to the repositories, and — for activities — apply the one filter deliberately kept out of SQL (closed-day exclusion), reusing Phase 2's `excludeClosedOnDaysConstraint` (generalized to work over the narrower `MatchedActivity` projection instead of duplicating the logic).
- `scripts/generate-embeddings.ts` (`npm run generate-embeddings`) — idempotent backfill script, scoped to `CURRENT_INVENTORY_VERSION` so stale fixture rows are never embedded. Ran live: 6 destinations + 23 current-version activities.
- `src/agents/curator.ts` (Destination/Activity Curator) and `src/agents/explanation.ts` (Trip Explanation Agent) — two new LLM agents following Phase 4's non-looping, output-shaping-tool pattern, each with a hallucination guardrail (`src/domain/curation.ts`: `validateCurationReferences`/`validateExplanationGrounding`) ensuring every ID the model references was actually in the candidate set it was given.
- `evals/cases/retrieval.ts` + `evals/runners/run-retrieval-eval.ts` (`npm run eval:retrieval`) — 10 deterministic retrieval-quality cases against known seed-data content. Ran live: 10/10 passed, including semantic ranking (e.g. a "temples and shrines" query correctly ranks Kyoto first), budget/vibe-tag hard-filtering, closed-day exclusion, and accessibility filtering.
- 209 tests total (up from 167 at the end of Phase 6).

**Why:** Continuing the build sequence into RAG/curation per `docs/IMPLEMENTATION_PLAN.md` — the previous session had already decided the embedding provider (Voyage AI) but written no code.

**Decisions made:**
1. **Curator/Explanation agents don't call `retrieve_destinations`/`retrieve_activities` as live, model-driven tool calls mid-turn — retrieval happens first, deterministically, and the candidate set is handed to the agent as input.** This is a documented departure from a literal reading of `PROJECT_BRIEF.md` §12's tool-usage column (same kind of departure Phase 4 made for the Intake agent, explained the same way): it keeps retrieval parameters (inventory version, budget ceiling) under deterministic control per §6.1 rather than model-improvised, and avoids introducing a new multi-turn tool-execution loop into `ModelClient` — a real architectural extension (raw content-block passthrough, echoing assistant tool-use turns back, etc.) that Phase 5 doesn't actually need yet, since a single rank-and-explain step over an already-bounded candidate set is all `PROJECT_BRIEF.md` §6.2 asks these two agents to do this phase.
2. **`ToolCallLogEntry` moved from `src/agents/intake.ts` to the shared `src/agents/model-client.ts`**, since it's now used by three agents (Intake, Curator, Explanation), not a private Intake-only type.
3. **Retrieved candidate data is explicitly delimited and labeled as untrusted** in both new agents' prompts (`<candidate_data>`/`<explanation_data>` blocks, explicit "not instructions" framing) — `PROJECT_BRIEF.md` §10.4's RAG safety requirement, caught while updating docs rather than by the earlier review pass, and fixed immediately since it was cheap (a prompt-wording change) and closes a named brief requirement rather than being deferred.
4. **The retrieval-quality eval batches every case's query text into one Voyage `embed()` call** instead of one call per case — Voyage's free-tier rate limit turned out to be a real constraint (3 requests/minute without a payment method on file, discovered live when a two-call smoke test hit a 429), not a hypothetical one. Flagged to the user: worth adding a payment method (still free within the 200M-token tier) before Phase 7 puts real chat-driven traffic through retrieval.

**Post-phase review and fixes (standing practice since Phase 1):** Ran a 4-angle parallel review against the full diff (~1,620 lines). Found and fixed 10 issues, tracked 1 as a known limitation, judged 1 as not worth changing:
1. **A second `record_curation`/`get_candidate_explanations` tool call in the same turn silently overwrote the result, which could leave `curation`/`explanation` and `referenceCheck` describing two different calls** (e.g. a valid first call's data kept, but `referenceCheck` overwritten with a second, invalid call's failure — directly contradicting the documented "don't trust `curation` if `referenceCheck` failed" invariant). Fixed in both `curator.ts` and `explanation.ts`: a second call is now flagged as a duplicate-call error rather than allowed to touch the already-set result, matching the duplicate-handling pattern Phase 4's Intake agent already established for its own single-value tools.
2. **The pgvector query embedding was string-joined into a literal with no validation** — an empty array or a `NaN`/`Infinity` value (from a hypothetical embedding-provider bug) would have surfaced as an opaque Postgres cast error instead of a clear one. Fixed with a new `toVectorLiteral()` helper (`src/repositories/shared.ts`) used by both `matchDestinations` and `matchActivities`.
3. Two scripts (`generate-embeddings.ts`, the retrieval eval runner) hand-rolled a service-role Supabase client instead of reusing the existing `createServiceClient()` helper, and `generate-embeddings.ts` wrote each row's embedding back with a sequential `await` in a loop instead of `Promise.all`. Fixed both — reuse `createServiceClient()`, parallelize the independent per-row writes (matching the `Promise.all`-for-independent-writes convention `src/workflow/intake-orchestrator.ts` already established).
4. The eval runner re-derived `activities-retrieval.ts`'s overfetch multiplier as a bare `* 2` literal instead of importing the named constant — a silent-drift risk if the constant is ever tuned. Fixed by exporting `OVERFETCH_FACTOR` and importing it.
5. Two test-coverage gaps closed: `activities-retrieval.test.ts` was missing the `inputType: "query"` assertion its sibling `destinations-retrieval.test.ts` already had; `src/domain/curation.ts`'s hallucination-guardrail tests didn't cover an empty approved-id set, case sensitivity, or id-prefix collisions (the current exact-`Set.has` implementation is safe against all three, but nothing would have caught a future "more lenient" matching regression).
6. **Tracked as an open item, not fixed:** `retrieveActivities`'s closed-days post-filter overfetch heuristic (`OVERFETCH_FACTOR = 2`) isn't a guarantee — the SQL `LIMIT` still runs before the filter, so it could under-fill `topK` at a larger catalog size than this project's seed data. Documented in the code and added to `docs/IMPLEMENTATION_PLAN.md` §5.
7. **Not changed, judged correct as-is:** the ~50-line "single-tool structured-output agent turn" pattern is now duplicated across three agents (Intake, Curator, Explanation) — a shared helper was considered but the per-tool semantics genuinely differ (Intake's `record_extraction` accumulates across calls; Curator/Explanation's single-value tools reject duplicates), so a generic helper would need its own mode-parameterization complexity. Kept as three explicit, independently-readable implementations for now.

**What didn't work / dead ends:**
- The retrieval eval's first live run hit a real Voyage rate limit (429, 3 RPM without a payment method) on its second embedding call during an earlier ad hoc smoke test — not a bug, a genuine account constraint. Fixed the eval harness's *design* (batch all queries into one call) rather than adding retry/backoff, since the SDK already retries 429s by default and the actual fix is making fewer, larger calls.
- Anthropic's acquisition of Voyage AI didn't extend to a first-party TypeScript SDK being easy to find via the docs site (WebFetch of the docs pages only showed Python examples) — found the real, actively-maintained `voyageai` npm package (902K monthly downloads) via the npm registry search API instead, then downloaded and inspected its actual compiled type declarations (`EmbedRequest.d.ts`, `EmbedResponse.d.ts`) rather than guessing field names/casing from the REST API docs alone.

**Verification:** `npm test` (209/209 passing), `npx tsc --noEmit` (clean), `npm run lint` (clean), `npm run build` (clean). Live-verified against the real hosted Supabase project and real Voyage API: the migration's SQL functions confirmed callable with `vector(1024)` inputs before any embeddings existed; `npm run generate-embeddings` run twice (once before the review-fix pass, once after, to confirm the `createServiceClient()`/`Promise.all` changes didn't break anything); `npm run eval:retrieval` run twice for the same reason, 10/10 passing both times.

**Known limitations / assumptions carried forward:** the `retrieveActivities` overfetch-heuristic gap above; Voyage's account-level 3 RPM/10K TPM rate limit until a payment method is added (flagged to the user, not yet resolved); the Curator/Explanation agents are unit-tested against a fake `ModelClient` but not yet wired into the workflow controller or exercised by a live spot-check the way Phase 6's Intake-agent wiring was — that's the natural next step once search/decision flows exist to feed them real candidate sets, not this phase's job.

**Next up:** Either more backend (search_flights/search_hotels tools, budget/candidate-combination wiring into the orchestrator — extending Phase 6's pattern to the Curator agent) or Phase 7 (the chat UI) — worth an explicit discussion at the start of the next session, same fork as after Phase 4 and Phase 6.

---

## 2026-09-16 — Session close: documentation pass ahead of continuing Phase 6

**What happened:** User chose to continue with the backend-first fork (extending Phase 6's orchestration pattern to search/curation/combination) rather than Phase 7, and asked for a documentation pass before ending the session — a fresh session will pick up the actual implementation, approaching this session's context limit.

Audited the codebase against what `docs/IMPLEMENTATION_PLAN.md`'s Phase 6 section claimed, since that section's checklist was fully checked off but scoped explicitly to "the one agent that exists so far" (the Intake agent) — confirmed by grep, not assumption, that Phase 5's Curator/Explanation agents and Phase 2's budget/combination/feasibility engines are still completely unwired: nothing in `src/workflow/`, `src/agents/`, or `app/` calls `calculateBudget`, `assembleCandidateCombinations`, `validateItineraryFeasibility`, `validateInventoryReferences`, `findFlights`, or `findHotels`, and nothing outside their own test files calls `runCuratorAgent`/`runExplanationAgent`. The workflow state machine already models the transitions this remaining work needs to drive (`requirements_ready` → `searching_inventory` → `validating_candidates` → `assembling_options` → `validating_itinerary` → `presenting_draft`) but none of them are exercised by any code yet.

**What I did:** Added a new "Phase 6, continued" subsection to `docs/IMPLEMENTATION_PLAN.md` documenting this gap precisely, with an explicit "start here" list of open questions the next session should resolve *before* writing code (mirroring the same pattern used ahead of Phase 4) rather than silently assuming answers:
1. Whether `search_flights`/`search_hotels` should be deterministic pre-fetch (consistent with Phase 5's `retrieve_destinations`/`retrieve_activities` precedent) or live model-driven tool calls (as `PROJECT_BRIEF.md` §12 literally lists them).
2. Whether this belongs in a new orchestrator module or an extension of `intake-orchestrator.ts`.
3. Whether `trip_decisions` (no repository exists yet) needs the same append/retire shape as `trip_requirements`/`trip_preferences`, or different semantics given §7.6's approval-invalidation-on-revision model.
4. Whether to slice this remaining work further rather than wiring the whole search→curate→combine→feasibility→decide→present chain in one pass — flagged explicitly since every prior phase this session stayed deliberately narrow, and this remaining scope is at least as large as Phase 6's original Intake-wiring work.

**Decisions made:** None — this was a documentation-only pass, deliberately. No architecture decisions were made about the four open questions above; they're recorded as open, not resolved, so the next session asks rather than assumes.

**What didn't work / dead ends:** None — no code touched this entry.

**Verification:** N/A — documentation only. `git status` clean before this entry; will show only the `docs/IMPLEMENTATION_PLAN.md` and `BUILD_LOG.md` edits once committed.

**Known limitations / assumptions carried forward:** unchanged from the Phase 5 entry above, plus the four open questions just recorded.

**Next up:** Resolve the four open questions in "Phase 6, continued" (`docs/IMPLEMENTATION_PLAN.md`), then implement in whatever slice that discussion settles on — likely starting with `search_flights`/`search_hotels` and the Curator agent's real orchestrator wiring, since that's the more self-contained first slice regardless of how the other questions resolve.

---

## 2026-09-16 — Phase 6, continued (slice 1): search + Curator wiring

**What I built:** Resolved the four open questions left at the end of the last session (`docs/IMPLEMENTATION_PLAN.md` "Phase 6, continued") by putting them to the user directly before writing any code, then implemented slice 1 — deterministic search plus real Curator-agent wiring, stopping short of combination/budget/feasibility/`trip_decisions` (slice 2, next session).

- **Questions resolved** (all four recommended options chosen): `search_flights`/`search_hotels` are deterministic pre-fetch, not live model tool calls (`PROJECT_BRIEF.md` §12's own tool-table notes column already calls this an "orchestrator-driven search step" — the deciding evidence); the new logic lives in its own module, `src/workflow/search-orchestrator.ts`, not an extension of `intake-orchestrator.ts`; `trip_decisions` (not built this session) will reuse the append/retire-by-field shape `trip_requirements`/`trip_preferences` already use, with its own `proposed`/`confirmed`/`superseded` status vocabulary; and this session is scoped to slice 1 (search + Curator) only.
- `src/workflow/search-orchestrator.ts` (`runSearchAndCuration`) — picks up from `requirements_ready`: builds `findFlights`/`findHotels` params deterministically from validated `trip_requirements`, re-validates the results through Phase 2's `filterHardConstraints` (defense in depth beyond the repository query params), retrieves activity candidates via Phase 5's `retrieveActivities`, and runs the Curator agent (`kind: "activity"`) to rank them against stated preferences. Drives `begin_search` → `search_completed` → `candidates_valid`, landing in `assembling_options`; a genuine no-viable-candidates outcome drives `recoverable_error` (§8.4) instead of leaving the workflow state ambiguous, via a typed `NoViableCandidatesError`. Persists the curated result as an `activities_curated` `trip_events` row (no `trip_decisions` repository exists yet) and an `inventory_searched` row with the passing candidate IDs.
- `src/workflow/{correlation,orchestration-errors,advance}.ts` (new) — extracted `deriveCorrelationId`, `OrchestrationConflictError`/`OrchestrationTransitionError`, and the `advanceTrip`-plus-guardrail-log-plus-branch sequence (now `advanceOrThrow`) out of `intake-orchestrator.ts`, since `search-orchestrator.ts` needed all three and a third inline copy was the wrong call. Behavior-preserving refactor — `intake-orchestrator.test.ts` passes unchanged.
- `beginSearch` Server Action (`app/app/actions.ts`) and `AGENT_MODELS.curator` (`src/config/models.ts`, defaulted to Sonnet 5 like the Intake agent) — the real, authenticated, reachable entry point, same rationale as `sendMessage`. No chat UI calls it yet (Phase 7).
- 9 new tests (`search-orchestrator.test.ts`, fully mocked, no live calls) — 218 total.

**Why:** Per the previous session's own documentation pass, Phase 5's Curator agent and the search repositories existed but nothing wired them into the workflow. The four questions needed resolving before code, per the project's standing rule against silently making cross-cutting architectural forks.

**Decisions made:**
1. Destinations are not curated/searched in this orchestrator: `destination` is a required `trip_requirements` field (`REQUIRED_FOR_READY`), so it's already fixed by the time a trip reaches `requirements_ready` — the "flexible destination" case `retrieve_destinations`/Curator-for-destinations exists for isn't reachable in this project's current required-field model. Not a gap, just scope that doesn't apply here.
2. **Caught and fixed during self-review, before this ever ran against real data:** preference values (free text the Intake agent extracted from the user's own words, e.g. "relaxing, laid-back") must never be passed as `findHotels`/`retrieveActivities`'s `vibeTags` parameter — that parameter is a **hard** `&&` (array-overlap) SQL filter against the seed data's controlled `vibe_tags` vocabulary (short curated slugs like `boutique`/`culture`/`walkable`, confirmed by reading `0002_seed_data.sql` and the filter SQL in `0005_retrieval.sql`), not a ranking signal. An initial draft of this file did pass raw preference values through it, which would have silently zeroed out real candidates on any wording mismatch — a live search returning "no viable candidates" far more often than intended, for reasons invisible from the code alone. Fixed before the live spot-check by routing preference text through `retrieveActivities`'s `query` parameter instead (embedded semantically, soft-ranked), and not filtering hotels by preference at all in this slice (no semantic path exists for the plain relational `findHotels`).
3. Applied the exact same `filterHardConstraints` defense-in-depth already established as house style (Phase 2/5): flight/hotel repository query params narrow first, then Phase 2's constraint engine re-validates the results and reports rejections by constraint code, logged as one `domain_validation` guardrail event per candidate type (not per rejected candidate) — matching the existing per-check, not per-item, guardrail granularity `intake-orchestrator.ts` already uses for `requirements_completeness`.

**Post-implementation review and fixes (standing practice since Phase 1):** Reviewed the diff solo before the live spot-check (single-agent session, no `/code-review` multi-angle pass this time). Found and fixed 2 issues, tracked 2 as deliberate open items:
1. **The `vibeTags`-as-hard-filter bug** — see decision 2 above. The most significant finding; would have silently broken real search almost every time preferences were present.
2. Removed a leftover unused `Activity` type import and a pointless trailing re-export that served no purpose, left over from an earlier draft of `search-orchestrator.ts`.
3. **Tracked, not fixed:** extended the existing tracked idempotency gap (`docs/IMPLEMENTATION_PLAN.md` §5) to cover `search-orchestrator.ts`'s own non-transition writes (`agent_runs`/`tool_calls`/`guardrail_events`/the two `trip_events` rows) — a whole-call retry isn't idempotent across them, same characteristic `processIntakeTurn` already has and already had this exact tradeoff accepted for. Deliberately not fixed piecemeal for one orchestrator; noted that a shared mechanism is the more likely eventual fix once three call sites need it.
4. **Tracked, not fixed:** the Curator agent's malformed tool calls aren't logged as a Layer 2 guardrail the way the Intake agent's are — investigated fixing it and found the naive version would double-count the hallucination-reference failure under both Layer 2 (output_validation) and the existing, correctly-layered `curation_reference_check` (domain_validation), skewing the guardrail dashboard. Left for a small `curator.ts` refactor (a distinct error kind on `ToolCallLogEntry`) rather than shipping a fragile string-matching workaround under time pressure.

**What didn't work / dead ends:** None this session — no environment/tooling blockers.

**Verification:** `npm test` (218/218 passing, 9 new), `npx tsc --noEmit` (clean), `npm run lint` (clean), `npm run build` (clean). Live-verified end to end against the real hosted Supabase project and real Anthropic/Voyage APIs with a throwaway script (not committed, deleted after use; created and deleted its own auth user): drove a trip through `startTrip`/`start_intake`/`requirements_complete` to `requirements_ready`, then `runSearchAndCuration` reached `assembling_options` with 2 real flights, 3 real hotels, and 4 real activities; the Curator agent ranked all 4 with a grounded rationale and passed its hallucination reference check; every expected table got the expected rows — `trip_events` (`inventory_searched`, `activities_curated`), `guardrail_events` (`flight_hard_constraints`/`hotel_hard_constraints`/`curation_reference_check`/three `workflow_transition_authorization` rows, none triggered), `agent_runs` (one successful Curator call, 2,276 input / 726 output tokens, as expected with no prompt-cache hit on a first-ever call for this trip).

**Known limitations / assumptions carried forward:** the two new tracked items above (search-orchestrator's non-transition-write idempotency, Curator's missing Layer 2 guardrail), plus everything already tracked in `docs/IMPLEMENTATION_PLAN.md` §5. Slice 2 (combination/budget/feasibility/`trip_decisions`/remaining transitions) is unstarted.

**Next up:** Phase 6 continued, slice 2 — `trip_decisions` repository, wiring `assembleCandidateCombinations`/`calculateBudget`/`validateItineraryFeasibility`/`validateInventoryReferences` over the candidates `runSearchAndCuration` now returns, persisting the assembled selection, and driving `combinations_assembled` → `itinerary_valid`/`itinerary_invalid` to reach `presenting_draft`.

---

## 2026-09-16 — Phase 6, continued (slice 2): combination, scheduling, feasibility, and trip_decisions

**What I built:** Finished the search → curate → combine → validate → persist → present chain, reaching `presenting_draft` for the first time. Two more genuine architectural gaps surfaced mid-implementation and were put to the user directly before writing the code they'd affect, rather than assumed — same standing rule slice 1 used for its four questions:

1. **No day-scheduler existed.** `assembleCandidateCombinations` only ever decided which activities fit the budget, never which day/time they happen on — but `validateItineraryFeasibility` hard-requires a fully dated schedule. Asked whether to build a real scheduler, a naive placeholder, or skip feasibility validation this slice. **User chose: build a real one now.**
2. **`assembleCandidateCombinations` only ever picked one `Flight` (one-way), and slice 1 never searched a return leg at all**, even though `calculateBudget`'s `BudgetInput.flights` was already array-typed specifically to hold more than one leg. Asked whether to do round trips properly or scope this slice to one-way only. **User chose: do it properly.**

**Built, in dependency order:**
- `src/domain/scheduling.ts` (`scheduleActivities`, new) — naive greedy day/time placer: walks the trip's date range once per activity (in input order), placing each at the first slot respecting `closedDays`/`openingHours`, back-to-back with a gap. Takes per-date `earliestStartByDate`/`latestEndByDate` overrides so it can respect the same arrival/departure transfer buffers `validateItineraryFeasibility` checks against (`DEFAULT_TRANSFER_BUFFER_MINUTES` exported from `feasibility.ts` for this). 10 unit tests, then 3 more after the review-and-live-check fixes below.
- `src/domain/combinations.ts` — widened to `outboundFlights`/`returnFlights?` (optional; a one-way trip's combinations get `returnFlight: null`), cross-joining outbound × return × hotel and summing both legs' cost via `calculateBudget`. Genericized `activities`/new `ActivityCandidate` interface so it accepts Phase 5's `MatchedActivity` projection directly.
- `src/validation/inventory-references.ts` — flights got their own check (`checkFlights`): a return leg's `.destination` column is the trip's *origin* (it's the reverse direction), so the old single-field check would have flagged every legitimate return flight as a violation. A flight now passes if *either* endpoint matches the trip's destination. Genericized `ApprovedCandidateSet.activities` the same way as `combinations.ts`.
- `src/repositories/trip-decisions.ts` (new) — the append/retire-by-field repository shape resolved at the end of last session, with the schema's own `proposed`/`confirmed`/`superseded` status vocabulary.
- `src/workflow/search-orchestrator.ts` (slice 1 retrofit) — now also searches a reversed-direction return leg when `returnDate` is stated, hard-constraint-filters it the same way as the outbound leg, and returns `outboundFlights`/`returnFlights` separately (renamed from the old single `flights` field). `supabase/migrations/0006_return_flights.sql` (17 rows) plus a one-off service-role script (not committed) backfilled real return-leg flights into the hosted project — Phase 1's seed data had only ever seeded one direction per route, invisible until round-trip search was actually exercised live.
- `src/workflow/itinerary-orchestrator.ts` (`assembleItinerary`, new) — picks up from `assembling_options`: builds combinations from the Curator's ranked (non-excluded) activities, tries up to 5 best-ranked combinations (schedule + `validateItineraryFeasibility`, best-first) until one is feasible, runs `validateInventoryReferences` as defense in depth, persists the winner to `trip_decisions`, and drives `combinations_assembled` → `itinerary_valid` (or `itinerary_invalid` back to `assembling_options`) to reach `presenting_draft`.
- `assembleTripItinerary` Server Action (`app/app/actions.ts`) — composes `runSearchAndCuration` then `assembleItinerary` in one round trip, since nothing sits between `assembling_options` and `presenting_draft` that a chat UI would ever pause at. `beginSearch` stays separately callable. Extracted a small `requireOwnedTrip` helper shared by both, replacing two copies of the same auth+ownership check.
- 244 tests total (up from 218 at the start of this session).

**Why:** Continuing directly from the last session's documented "Slice 2" scope.

**Decisions made:**
1. **One-way trips (no `returnDate`) are explicitly out of scope for combination assembly.** `REQUIRED_FOR_READY` doesn't require `returnDate`, but without it there's no way to derive a hotel-stay length or checkout date — `assembleItinerary` throws a typed `OneWayTripNotSupportedError` rather than guessing a default trip length, which is a product decision left for later. Narrow enough (and low-stakes enough — no caller can reach this today without deliberately omitting a return date) not to warrant its own round of questions; documented as a tracked gap instead.
2. Activities fed into `assembleCandidateCombinations` are filtered to the Curator's `rankedIds` (excluding whatever it explicitly excluded) rather than the full raw candidate list — the whole point of curation is to shape what actually gets proposed. Scheduling further orders the *included* activities by curation rank, so higher-ranked activities claim earlier/better time slots (verified in the live spot-check: the curator's top pick got the first slot).
3. `nights` fed to `calculateBudget` uses the requirement-stated `departureDate`/`returnDate` as an approximation, not the actual hotel-stay length derived from the chosen flights' real local arrival/departure times (which `assembleItinerary` computes separately for the feasibility-checked schedule) — these can differ by about a day. Tracked rather than fixed; recomputing budget nights from the chosen combination's actual dates would need restructuring `assembleCandidateCombinations` to accept dates after flight selection instead of before.

**Two real bugs caught before/during the live spot-check, not by review alone:**
1. **The scheduler only ever tested one candidate start time per day** (the running cursor, or the day's default), so an evening-only activity (e.g. Fado Dinner Show, open only 19:30-23:00) could never be placed at all — every day's test happened at the default 10:00 slot, which never falls inside an evening window. Caught by the live spot-check failing with "activities could not all be scheduled" on every attempted combination. Fixed by having the placer advance into the activity's own opening window when the current candidate time doesn't fit, instead of giving up on the whole day. Confirmed live afterward: full itinerary assembled successfully, all 4 activities scheduled including the evening one.
2. **The scheduler didn't know about arrival/departure transfer buffers at all**, so it would place an activity at the default 10:00 start right on the arrival day even when the flight landed at 22:50 that same local day — a schedule `validateItineraryFeasibility` is guaranteed to reject (`ACTIVITY_BEFORE_ARRIVAL`). Caught by the first live spot-check run (`NoFeasibleCombinationError`, "activity starts before the transfer buffer"). Fixed by exporting `feasibility.ts`'s `DEFAULT_TRANSFER_BUFFER_MINUTES` and threading per-date `earliestStartByDate`/`latestEndByDate` boundaries into the scheduler for the check-in/check-out dates specifically.

**What didn't work / dead ends:**
- First live spot-check attempt failed immediately with `NoViableCandidatesError: no return flights passed hard constraints` — not a code bug, a real data gap: Phase 1's seed data literally never seeded a single reverse-direction flight for any route (confirmed by grepping every origin/destination pair in `0002_seed_data.sql`). Closed by adding `supabase/migrations/0006_return_flights.sql` (17 return-leg rows, one per unique outbound origin per destination, ~1 week after each route's outbound date) and applying it via a one-off service-role script (a plain `insert`, not `supabase db push` — no schema change, just data, and no DB password was on hand this session).
- The two scheduler bugs above, each caught by re-running the live spot-check after the previous fix, not anticipated in advance.

**Verification:** `npm test` (244/244 passing, 26 new), `npx tsc --noEmit` (clean), `npm run lint` (clean), `npm run build` (clean). Live-verified end to end against the real hosted Supabase project and real Anthropic/Voyage APIs with a throwaway script (not committed, deleted after use; created and deleted its own auth user): a round-trip New York → Lisbon trip (2026-10-05 to 2026-10-12) driven through `runSearchAndCuration` then `assembleItinerary` reached `presenting_draft` with a feasible itinerary — 2 outbound/1 return flight candidates, 3 hotels, 4 curated activities all scheduled (including the evening-only Fado show), one benign `TIGHT_TRANSFER_BUFFER` warning, zero violations — and all 5 `trip_decisions` rows (`outboundFlight`/`returnFlight`/`hotel`/`activities`/`budget`, all `status: "proposed"`) plus every expected `trip_events` (`combinations_assembled`, `itinerary_valid`) and `guardrail_events` (`itinerary_feasibility`, `itinerary_inventory_references`, both not triggered) row persisted correctly.

**Known limitations / assumptions carried forward:** four new tracked items in `docs/IMPLEMENTATION_PLAN.md` §5 (one-way trips unsupported; budget/feasibility night-count approximation; the scheduler is a naive first-fit placer, not an optimizer; `assembleItinerary` only tries the best 5 combinations, not the full list), plus everything already tracked there. The two Slice-1 idempotency/Layer-2-guardrail items from last session are unchanged.

**Next up:** Phase 6 continued is now feature-complete through `presenting_draft`. Worth an explicit discussion at the start of the next session: Phase 7 (chat UI — the only thing standing between this backend and an actually demoable product surface) vs. further backend hardening (e.g. closing one of the tracked idempotency/scope gaps) vs. Phase 8 groundwork (eval harness). Given how much backend now exists with zero UI to see any of it, Phase 7 is the strongest candidate.

---

## 2026-09-16 — Phase 7 (slice 1): Itinerary Writer agent, decision-revision loop, chat UI

**What I built:** The first actually-demoable product surface — a chat UI backed by the whole backend built across Phases 4-6, plus the Itinerary Writer agent and the decision-revision loop that made the backend worth putting a UI in front of. Three questions were resolved with the user before any code was written (same standing rule Phase 6 continued used): the Itinerary Writer agent's shape, whether to wire the decision-revision loop now, and — worked through in a dedicated discussion once the user asked what the "live" UX would actually look like — how the UI shows the multi-step pipeline as it runs.

- **Itinerary Writer agent shape, resolved:** tool-based, reusing `ExplanationOutput`/`validateExplanationGrounding` (`src/domain/curation.ts`) rather than PROJECT_BRIEF.md's literal "formatting only, no tool" table entry — same reasoning Curator/Explanation already established: "avoid inventing facts" needs to be enforced by code, not trusted from a prompt instruction alone.
- **Decision-revision loop, resolved:** wire it now. `intake-orchestrator.ts` had been logging every decision revision ("switch hotels") as unsupported since Phase 6 continued's slice 1, written before `trip_decisions` existed — that reason evaporated the moment `trip_decisions` was built, and Phase 7's chat UI is exactly where a user would first try this for real.
- **Live UX mechanism, worked out through discussion, not assumed:** the user asked directly what "live" would actually look like before deciding — pushing past my first pass (a generic "Planning your trip…" spinner) into a concrete design: the chat UI auto-chains into the pipeline (no explicit "Build my itinerary" button), and the itinerary panel updates piece by piece via a **Supabase Realtime subscription** on `trip_decisions`/`trip_events` inserts. This wasn't new backend work — `runSearchAndCuration`/`assembleItinerary` already write these tables incrementally as each step completes (Phase 6 continued), and both already carry owner-scoped RLS (confirmed by reading `0001_initial_schema.sql`'s policies before proposing this), so the browser can subscribe directly and safely. The only new piece is a publication migration (see below) and the subscription itself.

**Built, in dependency order:**
- `src/agents/itinerary-writer.ts` (+ 5 tests) — new agent, own system prompt and tool (`write_itinerary`), reusing `ExplanationOutput`/`validateExplanationGrounding` as-is.
- `src/workflow/itinerary-orchestrator.ts` — refactored the combine → schedule → validate → persist logic `assembleItinerary` already had into a shared `finalizeCombinations` helper, now also running the Writer (non-fatal on failure: logged, not blocking, since the deterministic data alone still presents a draft) and persisting `itineraryText`. New `reviseItinerary` export: re-fetches alternate candidates for exactly one field (`outboundFlight`/`returnFlight`/`hotel` — `activities` isn't supported, `UnsupportedDecisionFieldError`, since which specific activity to swap needs more than a field name), excludes the current pick, and reuses `finalizeCombinations` — a revision is structurally "assembly with one field's pool narrowed," not a different algorithm. Drives `revision_requested` → `revision_submitted` → `revision_applied` → `itinerary_valid`/`itinerary_invalid`. No alternative passing hard constraints keeps the current selection and logs `revision_no_alternative` rather than erroring.
- `src/repositories/{flights,hotels,activities}.ts` — small `getFlightsByIds`/`getHotelsByIds`/`getActivitiesByIds` additions, re-hydrating a `trip_decisions` selection back into full rows for the revision flow.
- `src/workflow/intake-orchestrator.ts` — now loads and passes `currentDecisions` to the Intake/Revision agent (the input field existed since Phase 4, just never populated); a decision revision targeting a revisable field now returns a `decisionRevisionRequested` signal instead of always logging "unsupported."
- `app/app/actions.ts` — `sendMessage` auto-chains after `processIntakeTurn`: `requirements_ready` kicks off the search→assemble pipeline, `decisionRevisionRequested` kicks off `reviseItinerary` — both via Next.js's `after()` so the turn's own response returns immediately rather than blocking on several more seconds of real API calls. New `reviseTripDecision` action for calling a revision directly.
- `app/app/_components/chat-panel.tsx` + `itinerary-panel.tsx` — the chat UI and the live-updating panel (Realtime subscription on `trip_decisions`/`trip_events` INSERTs), wired into `app/app/page.tsx` (fresh chat) and `app/app/trips/[tripId]/page.tsx` (resumed: chat + panel side by side).
- `supabase/migrations/0007_realtime_trip_updates.sql` — adds both tables to the `supabase_realtime` publication.
- 256 tests total (up from 244 at the start of this session).

**Why:** Continuing directly from last session's own recommendation — Phase 6 continued left substantial backend with zero UI to see any of it.

**Decisions made:** covered above (Writer shape, revision-loop timing, live-UX mechanism) — all three discussed and resolved with the user before code.

**Post-implementation review, mid-build (not a separate pass — caught while wiring, same standing practice):** two real bugs found by the live spot-check, not by static review:
1. None this session's review pass specifically flagged — the itinerary-orchestrator refactor and revision-loop logic were built carefully enough (and the domain layer underneath was already reviewed in Phase 6 continued) that the live spot-check's two hotel-swap runs both succeeded cleanly on the first try, producing well-grounded, genuinely different itinerary text for each hotel. Not claiming nothing could be found with a dedicated multi-angle review — this session didn't run one, given how much ground the live spot-check itself already covered end to end.

**What didn't work / dead ends:**
- Tried to browser-verify the chat UI via a fresh `next dev` instance — Next.js refuses to run a second dev server against the same project directory at all (a per-directory lock, not just a port conflict), and another session already held the only one for this repo the whole time. Added `"autoPort": true` to `.claude/launch.json` regardless (a reasonable permanent improvement for future multi-session scenarios), but it doesn't address this specific lock. Verified the backend chain live instead (see below); the actual browser UI and Realtime panel are untested this session — flagged clearly rather than claimed.
- Couldn't apply `0007_realtime_trip_updates.sql` to the hosted project — it's DDL (`alter publication ... add table ...`), and unlike `0006_return_flights.sql`'s data (a plain service-role `insert`), this can't be done through PostgREST at all; it needs `supabase db push` (a DB password this session didn't have) or the Supabase dashboard. Flagged to the user rather than skipped silently.

**Verification:** `npm test` (256/256 passing, 12 new), `npx tsc --noEmit` (clean), `npm run lint` (clean), `npm run build` (clean). Live-verified the backend chain end to end against the real hosted Supabase project and real Anthropic/Voyage APIs with a throwaway script (not committed, deleted after use; created and deleted its own auth user): a round-trip Lisbon trip reached `presenting_draft` with a full, well-grounded written itinerary (spot-checked against the real flight times/hotel name/activity schedule it was given — zero hallucination-guardrail triggers), then a hotel decision revision correctly switched hotels (Baixa Riverside Suites → Alfama Bica Inn), regenerated a consistent, differently-worded itinerary referencing the new hotel throughout, and left exactly one `"proposed"` `trip_decisions` row for the `hotel` field (the old one correctly `"superseded"`). `trip_events` showed the full expected sequence including `revision_requested`/`revision_submitted`/`revision_applied`/a second `itinerary_valid`. **Not verified live:** the actual browser chat UI and the Realtime panel (see dead ends above) — both need a follow-up check once a dev server is free and the Realtime migration is applied.

**Known limitations / assumptions carried forward:** two new tracked items in `docs/IMPLEMENTATION_PLAN.md` §5 (the Realtime migration not applied to the hosted project; the chat UI/panel not browser-verified), plus everything already tracked there — none of the Phase 6 continued tracked items were touched this session.

**Next up:** Phase 7 slice 2 — assumptions/trade-offs panel, the finalization flow (a proposal-hash mechanism needs to be built from scratch — nothing computes one yet), and trip history list polish. Before that: get a real browser click-through once a dev server is free, to close out slice 1's remaining verification gap (Realtime is now applied — see addendum below).

**Addendum, same session — Realtime migration applied, with a caught-and-fixed near-miss:** the user provided the hosted project's DB password after the entry above was written. `supabase db push` applied `0007_realtime_trip_updates.sql` cleanly, but also silently re-ran `0006_return_flights.sql` — its remote migration-history table had no record of it, since it was originally applied via a direct service-role script (a plain `insert`, chosen at the time because no DB credentials were on hand) rather than through the CLI. This duplicated all 17 return-flight rows to 34. Caught immediately with a live count query before doing anything else; explained the exact scope to the user (which 17 flight numbers, confirmed 2-each) and got explicit confirmation before deleting — the sandbox's own auto-mode classifier also independently blocked the first delete attempt as an unverifiable-scope deletion against a live database, which was the right call. Deleted the 17 duplicates (kept one of each), verified back to exactly 17 rows. Both migrations now correctly recorded as applied on the remote. Lesson recorded in `docs/IMPLEMENTATION_PLAN.md` §5: applying a migration's data via a direct script instead of `db push` leaves the CLI unaware it ran, so a later `db push` can silently redo it — prefer `db push` when credentials are available, even for data-only migrations, and re-verify row counts after any push that follows a manually-applied one.

**Updated verification:** Realtime is now live on `trip_decisions`/`trip_events` for the hosted project. The only remaining Phase 7 slice 1 gap is the actual browser click-through (still blocked by another session holding the one `next dev` slot for this project).

---

## 2026-09-16/17 — Browser click-through, three real bugs found and fixed

**What happened:** The user freed up this project's one `next dev` slot (asked them to stop the other session's server — closing a browser tab doesn't stop a dev server, since it's a separate background process; they ran `taskkill`). Signed into the running app with an admin-generated OTP (`supabase.auth.admin.generateLink`/`verifyOtp` via a throwaway, not-committed `/dev-signin` helper page reusing the app's real browser Supabase client, temporarily allowlisted in `proxy.ts`) — no email inbox access needed, and the session lands in cookies exactly like a real magic-link sign-in would. Then drove the actual chat UI by hand: sent a full trip request, watched the itinerary panel fill in live via Realtime with zero page reloads, and drove a full natural-language decision-revision conversation ("switch hotels" → clarifying question → "pick a cheaper hotel instead" → live hotel swap).

This is exactly the kind of check unit tests and backend-only spot-checks can't do, and it found three real bugs immediately — none of them caught by 257 passing tests, `tsc`, or two backend-only live spot-checks:

1. **Empty assistant chat bubble.** The Intake agent calling a tool with no accompanying text — already known and documented as a quirk back in Phase 6 ("worth revisiting once Phase 7's chat UI makes empty assistant turns visibly awkward") — did exactly that the first time it happened live. Fixed with a deterministic fallback string in `intake-orchestrator.ts` rather than trusting prompt wording to guarantee non-empty text; added a regression test.
2. **The whole page scrolled as one unit instead of the chat and itinerary panels scrolling independently**, pushing the message input off-screen as either panel's content grew. Root cause: `app/layout.tsx`'s `body` used `min-h-full` (unbounded) instead of `h-full`, and several intermediate flex containers were missing `min-h-0` (flex items default to refusing to shrink below their content's size unless told otherwise, which breaks nested `overflow-y-auto`). Traced the whole ancestor chain from `<body>` down to the message list and fixed every link in it; verified live afterward that the input stays pinned and each panel scrolls on its own.
3. **A fresh page load of an already-complete trip intermittently showed the itinerary panel empty.** Chased this hard, initially suspecting an `@supabase/ssr` session-hydration race (a real class of bug for that library) — a direct REST call with the same access token, done via `javascript_tool` in the live page, proved the data and RLS were both fine, ruling that out. Root cause was just normal async loading latency combined with checking the page before the fetch had resolved — not a functional bug, but it revealed a real UX gap: the loading state and the genuine empty state looked identical. Added an explicit "Loading your itinerary…" state; kept the defensive `getSession()`-before-query wait since it's cheap and still closes a real (if here undemonstrated) race class.

**Also found, documented rather than fixed:** driving the revision loop through actual natural language (not a scripted field name) surfaced that `reviseItinerary` doesn't honor the *specific* criterion in a revision request — asking for "a cheaper hotel" landed on one that was **more** expensive per night, because the code only excludes the current pick and takes the next-best by `assembleCandidateCombinations`'s existing ranking (most activities, then lowest *total* cost), never looking at the free-text reason the model captured. Deliberately not patched with string-matching on `"cheap"` — fragile, and against this project's own stance on not using fuzzy heuristics for domain decisions. The real fix needs a structured criteria field on `propose_trip_revision`. Tracked in `docs/IMPLEMENTATION_PLAN.md` §5.

**What didn't work / dead ends:**
- First tried navigating directly to the magic-link's `action_link` (the Supabase auth domain) — denied by the browser tool's cross-origin navigation policy. Switched to completing the OTP via the app's own `verifyOtp` client-side call instead, which never leaves `localhost`.
- First version of the OTP helper page lived at `app/_dev-signin/` — Next.js treats any `_`-prefixed folder as a private, non-routable segment (same convention this codebase already uses for `_components`), so it 404'd. Renamed to `app/dev-signin/`.
- `taskkill`ing the other session's dev server needed the user's own action — closing a browser tab pointed at it does nothing, since the dev server is an independent background process.

**Verification:** `npm test` (257/257 passing, 1 new), `npx tsc --noEmit` (clean), `npm run lint` (clean), `npm run build` (clean). All throwaway verification scaffolding removed before finishing: `app/dev-signin/`, the OTP-generation script, the temporary `proxy.ts` allowlist entry (reverted via `git checkout`), and the two throwaway test users (deleted via the admin API).

**Known limitations / assumptions carried forward:** the decision-revision criteria gap above (new tracked item), plus everything already tracked. Phase 7 slice 1 is now fully verified — backend and browser both.

**Next up:** Phase 7 slice 2 — assumptions/trade-offs panel, the finalization flow (proposal-hash mechanism), trip history list polish, and, if it comes up naturally, the revision-criteria gap found this session.

---

## 2026-09-17 — Session close: stepwise chain redesign decided (no code written)

**What happened:** No implementation this stretch — a design conversation, prompted directly by the user pushing back on the previous session's "decision revision doesn't honor a specific criterion" finding. Recorded here (and in full in `docs/IMPLEMENTATION_PLAN.md`'s new "STEPWISE CHAIN REDESIGN" section) so the next session doesn't have to re-derive any of it.

**Why this went from "fix the revision bug" to "redesign the assembly flow":** asked how the fix would scale past "cheaper" to the other ~50 words a real user might use ("free," "wheelchair accessible," "non-stop," etc.) across every part of the chain, in any order. Working through that question surfaced that the *foundation* — `runSearchAndCuration`/`assembleItinerary` assembling the entire flight+hotel+activities bundle automatically in one pass, with `reviseItinerary` patching one field afterward — was the actual problem, not the revision logic sitting on top of it. The user then specified directly (not left to my judgment) how the replacement should work.

**Decisions made, all directly from the user, not assumed:**
1. **One strictly linear chain** (destination/dates → flight → hotel → activities), each step proposed and explicitly confirmed before the next step is even searched — not assembled speculatively all at once. This is also more *correct*, not just better UX: hotel dates depend on the actual confirmed flight's real arrival/departure time, so proposing a hotel before a flight is confirmed is proposing against a guess.
2. **Cascading invalidation is fact-based, not position-based** — a change only invalidates a later step if it actually alters the specific fact that step depends on (confirmed with the user directly, since it was my inference, not their stated word-for-word rule): a same-dates flight swap doesn't reset the hotel; a hotel swap never resets activities (verified by reading the real scheduling code, not assumed) — only an actual dates/destination change cascades all the way through.
3. **Revision requests split into two kinds, both routed through the existing requirement-revision mechanism rather than a new one-off vocabulary:** directional/comparative ("cheaper" → the model proposes a concrete threshold relative to the current selection, e.g. `maxHotelPriceUsd: 150`) versus absolute/hard-constraint ("free," "non-stop," "wheelchair accessible" → real pass/fail conditions using the existing `HardConstraint` engine already built for initial search, e.g. `maxActivityPriceConstraint(0)` for "free"). Multi-segment requests ("cheaper hotel and a shorter flight") are handled as sequential single-field revisions in one turn, not a combined instruction.

**A real mistake in my own first draft of this design, caught by the user, not by me:** I initially proposed treating "free" as just the extreme end of "cheaper" (sort ascending, cheapest naturally surfaces). The user pushed back hard and correctly: "free" is an exact threshold (price == 0), not a comparison — a $50 activity does not satisfy "free" no matter how it's sorted, and silently returning the cheapest available option while treating it as a response to "free" would be a real, dangerous-in-spirit-if-not-in-stakes kind of wrong (a plausible-looking number that isn't the number that was asked for). This is why the design above treats "free"/"non-stop"/"wheelchair accessible" as hard constraints (must honestly fail if unsatisfiable) and keeps them structurally separate from "cheaper"/"shorter" (which have no pass/fail line, just a direction).

**What didn't work / dead ends:** None — no code touched this stretch, and the design conversation converged cleanly once the two revision-request categories were correctly separated.

**Verification:** N/A — documentation only. `git status` will show only the `docs/IMPLEMENTATION_PLAN.md` and `BUILD_LOG.md` edits once committed.

**Known limitations / assumptions carried forward:** everything already tracked in `docs/IMPLEMENTATION_PLAN.md` §5, unchanged. The stepwise redesign is fully specified but zero code has been written toward it yet.

**Next up: the stepwise chain redesign, first, before anything else** — see `docs/IMPLEMENTATION_PLAN.md`'s "STEPWISE CHAIN REDESIGN" section for the full design and the proposed 4-slice implementation order (flight step alone; hotel step + cascade rule; activities step + retiring the old one-shot path; chat UI rework). After it's built, fix whatever it breaks in already-shipped code (`search-orchestrator.ts`, `itinerary-orchestrator.ts`, `intake-orchestrator.ts`, the chat UI, and their tests all currently assume the one-shot model being replaced), then resume Phase 6/7/beyond in whatever order makes sense at that point.

---

## 2026-09-17 — Stepwise chain redesign, slice 1: chain/cascade data model + flight step alone

**What I built:** The first slice of the stepwise chain redesign (`docs/IMPLEMENTATION_PLAN.md`'s "STEPWISE CHAIN REDESIGN" section) — the general chain/cascade data model, plus the flight step in isolation. The old one-shot pipeline (`search-orchestrator.ts`, `itinerary-orchestrator.ts`, `intake-orchestrator.ts`, `actions.ts`, chat UI) stays live and untouched; nothing is rewired yet — that's slice 3's job, once hotel and activities also exist.

Two design questions were asked and answered directly before writing any code (not assumed), plus a third design call flagged rather than decided silently:
1. **Flight selection before hotel/activities exist to fit a budget against:** the user chose a **short ranked list (top 3 cheapest hard-constraint-passing outbound+return pairs)** over a single "cheapest wins" pick — this also sidesteps a real gap the single-pick option had (a bare "I don't like this one" rejection with no criterion has nowhere to go without exclusion-tracking; a visible list lets the user just point at a different item).
2. **Workflow-state integration:** the user chose to have the new flight-step code **skip `WorkflowState` transitions entirely** for this slice, rather than force-fit it into the existing coarse states (`searching_inventory`/`assembling_options`/`presenting_draft`, shaped for the whole-itinerary one-shot pipeline and still actively used by the old, still-live path). `proposeFlightStep`/`confirmFlightStep` gate on the existing deterministic `checkRequirementsComplete` instead of `trips.status`, and still log full guardrail/`trip_events` telemetry — just decoupled from the trip-level state machine. How the coarse states get reused across three repeated steps is deferred until hotel + activities exist and the chat loop is rebuilt (slice 4).
3. **Flagged during design, not decided silently:** since the flight step shows an explicit list and the user picks one, `trip_decisions.status = "proposed"` is never used for this step — `confirmFlightStep` is the sole persistence point, writing both legs directly as `"confirmed"`. Candidates from `proposeFlightStep` are ephemeral (returned, never persisted), matching how `runSearchAndCuration` already returns unpersisted candidate lists today.

**Built:**
- `src/domain/stay.ts` (`deriveHotelStayDates`) — extracted from `itinerary-orchestrator.ts`'s `buildDraft` (behavior-preserving refactor, `itinerary-orchestrator.test.ts` passes unchanged) — the derived fact the flight→hotel cascade rule compares.
- `src/domain/chain.ts` — the general chain/cascade model: `CHAIN_STEPS`, `getCurrentChainStep` (derived from `trip_decisions`, not stored — matches the codebase's existing preference for deterministic derived checks over a mutable flag), `REQUIREMENT_FIELD_STEP` (a total mapping of all 14 `RequirementFieldName`s to the step they affect or `"foundational"`), and `invalidatedStepsForFlightChange`/`ForHotelChange`/`ForActivitiesChange`/`ForRequirementField`. The hotel/activities branches are fully unit-tested but have no live caller yet — exactly what the plan called for ("the derived-fact cascade check in place, even though nothing downstream exists yet to actually cascade to").
- `src/repositories/trip-decisions.ts` — added `confirmTripDecisions` (proposed → confirmed), real repository infrastructure for whichever future step uses the two-phase propose/confirm lifecycle (the flight step itself doesn't, per decision 3 above).
- `src/workflow/flight-step.ts` — `proposeFlightStep` (deterministic search + hard-constraint filter + top-3 ranking by summed price, no persistence) and `confirmFlightStep` (re-validates the chosen IDs against current requirements/hard constraints, persists both legs as confirmed decisions, returns the derived hotel-stay dates for slice 2). No revision-specific function was needed — a future requirement change just means calling `proposeFlightStep` again, since it always reads current active requirements fresh.
- 30 new tests (`chain.test.ts`, `stay.test.ts`, `flight-step.test.ts`), 290 total (up from 260).

**Why:** Continuing directly from the prior session's design-only close — this was flagged as "the first thing to do next session, before anything else."

**Decisions made:** the three above (ranked list; skip WorkflowState transitions; propose-is-ephemeral/confirm-is-sole-persist-point), all recorded in `docs/IMPLEMENTATION_PLAN.md`'s STEPWISE CHAIN REDESIGN section rather than left implicit in code.

**What didn't work / dead ends:** the live spot-check's first attempt at verifying "a tightened `maxFlightPriceUsd` changes the re-proposed list" used a ceiling of `chosen.outboundFlight.price_usd - 1` — this correctly threw `NoViableFlightCandidatesError` instead of narrowing the list, because the same ceiling also applies to the return leg (`flightHardConstraints` is leg-agnostic, matching the existing `search-orchestrator.ts` convention) and the return leg's own price exceeded it too. Not a code bug — the test's assumption was wrong. Fixed by rewriting the spot-check to assert two real behaviors instead: an unreachably-low ceiling correctly rejects everything, and a ceiling placed between the two candidates' actual outbound prices correctly narrows the list to exclude the pricier one.

**Verification:** `npm test` (290/290 passing, 30 new), `npx tsc --noEmit` (clean), `npm run lint` (clean), `npm run build` (clean). Live-verified against the real hosted Supabase project (no LLM calls needed — this step is fully deterministic, so no Anthropic/Voyage API cost) with a throwaway script (not committed, deleted after use; created and deleted its own auth user, which cascades its trip/session/requirements/decisions/events): a real New York → Lisbon trip's `proposeFlightStep` returned 2 real ranked pairs sorted cheapest-first; `confirmFlightStep` persisted both legs as `confirmed` `trip_decisions`, logged `flight_step_proposed`/`flight_step_confirmed` `trip_events` and both hard-constraint `guardrail_events` (neither triggered); a `maxFlightPriceUsd` requirement added afterward was picked up fresh on re-propose in both directions (too-low ceiling → correct rejection; a ceiling between the two candidates' prices → correct narrowing, excluding the pricier flight).

**Known limitations / assumptions carried forward:** one new tracked item in `docs/IMPLEMENTATION_PLAN.md` §5 — revising an *already-confirmed* flight decision has no live caller yet (nothing downstream exists to cascade to until slice 2/3); everything already tracked there is otherwise unchanged.

**Next up:** Slice 2 — the hotel step, wired to the flight→hotel cascade rule (`invalidatedStepsForFlightChange`'s dates-changed check, now with a real caller): search hotels once the flight step's confirmed dates are known, propose, confirm, and only re-run the flight step's downstream consumers when the derived stay dates actually change.

---

## 2026-09-17 — Stepwise chain redesign, slice 2: hotel step + the flight→hotel cascade rule

**What I built:** The second slice of the stepwise chain redesign — the hotel step, plus wiring the flight→hotel cascade rule (`src/domain/chain.ts`'s `invalidatedStepsForFlightChange`, built in slice 1 but unused until now) into a real caller. Continued directly on slice 1's own conventions rather than re-asking already-settled questions: the hotel step reuses the same short-ranked-list and skip-`WorkflowState` decisions for the same reasons, unchanged.

**Built:**
- `src/workflow/hotel-step.ts` (`proposeHotelStep`/`confirmHotelStep`) — mirrors `flight-step.ts`'s shape exactly. `proposeHotelStep` refuses to run until the flight step is confirmed (`FlightStepNotConfirmedError`), since the hotel search is scoped to the confirmed flight's derived check-in/check-out dates (`src/domain/stay.ts`'s `deriveHotelStayDates`, from slice 1) — proposing a hotel before a flight is confirmed would be proposing against a guess, exactly the problem the whole redesign exists to fix. `confirmHotelStep` has no cascade logic of its own: per the decided design's rule 3, a hotel change never invalidates activities, so there's nothing to check.
- `src/workflow/flight-step.ts`'s `confirmFlightStep` — extended, not replaced: when a flight is *re*-confirmed and a prior confirmed flight already exists, it now fetches the prior confirmation's flights, derives its stay dates, and compares them against the new selection's derived dates. Different dates + an existing hotel decision → the hotel decision is retired (superseded) and a `hotel_step_invalidated` `trip_events` row is logged. Same dates → the confirmed hotel decision is left untouched. This is `invalidatedStepsForFlightChange`'s first real caller.
- 13 new tests: `hotel-step.test.ts` (11) plus 4 new "flight->hotel cascade" tests added to `flight-step.test.ts` (same-dates untouched; different-dates invalidated + event logged; no invalidation when no hotel decision exists; no invalidation on the very first confirmation) — 303 total (up from 290).

**Why:** Continuing directly from slice 1's own "next up" — the flight→hotel cascade rule needed a real hotel step to have something to invalidate.

**Decisions made:** none new required re-asking — this slice was scoped tightly enough to fall out of slice 1's already-answered questions plus the design's already-decided rule 3 (hotel never invalidates activities). One scope call worth naming: the cascade only acts on "hotel" specifically, not a generic "everything downstream," since "activities" per the new stepwise model has no decision writer yet — that generalization is slice 3's job.

**What didn't work / dead ends:** the first live spot-check attempt tried to prove "a different flight candidate has the same derived dates" by picking the *other* ranked candidate — this failed, correctly: flight duration varies by carrier, so two candidates departing on the same stated `departureDate` can still land on different calendar days, meaning the second candidate actually did have different derived dates and correctly triggered the cascade. Not a code bug — the test's assumption was wrong. Fixed by re-confirming the *literal same* flight for the "same dates" assertion (a real, if trivial, no-op re-confirmation) instead of assuming any other candidate would share the date.

**Verification:** `npm test` (303/303 passing, 13 new), `npx tsc --noEmit` (clean), `npm run lint` (clean), `npm run build` (clean). Live-verified against the real hosted Supabase project (no LLM calls needed — both steps are fully deterministic) with a throwaway script (not committed, deleted after use; created and deleted its own auth user): confirmed `proposeHotelStep` correctly refuses before a flight is confirmed; a full New York → Lisbon flight-confirm → hotel-propose → hotel-confirm cycle completed with 3 real ranked hotel candidates sorted cheapest-first; a same-flight re-confirmation left the confirmed hotel decision untouched. **One assertion was skipped, not failed:** proving the "dates differ → hotel invalidated" branch live needed a second real Lisbon→New York return flight on a different date, and a direct query confirmed the seed data has exactly one — there's nothing to swap in. That branch is still verified by the 3 synthetic-data unit tests covering it.

**Known limitations / assumptions carried forward:** two items updated/added in `docs/IMPLEMENTATION_PLAN.md` §5 — the chat-level "detect a revision request" wiring still doesn't exist for either step (only the underlying confirm/cascade mechanics do), and the dates-changed cascade branch is only live-verified in one direction (seed-data sparsity, not a code gap). Everything else already tracked is unchanged.

**Next up:** Slice 3 — the activities step (confirming the hotel→activities non-cascade rule with a real caller) and retiring the old one-shot `runSearchAndCuration`/`assembleItinerary`/`reviseItinerary` path once nothing depends on it anymore. That retirement will also need fixing whatever it breaks in `intake-orchestrator.ts`'s decision-revision detection and the chat UI, both of which still assume the one-shot model.

---

## 2026-09-17 — Stepwise chain redesign, slice 3: activities step + retiring the old one-shot pipeline

**What I built:** The last of the three step modules — the activities step, the chain's final step — plus retiring the old one-shot pipeline (`search-orchestrator.ts`, `itinerary-orchestrator.ts`) it replaces, exactly as originally scoped for this slice. One design question was asked and answered directly before writing code: since the live chat UI depends on the pipeline being retired, and the real "show one step at a time" UI is slice 4, not this slice, the user chose to build interim auto-confirm glue (`chain-orchestrator.ts`) so the chat UI keeps auto-completing a trip with zero visible regression, rather than leaving it non-functional until slice 4 lands.

**Built:**
- `src/workflow/step-shared.ts` — `requirementMap`/`flightHardConstraints`/`hotelHardConstraints`/`OneWayTripNotSupportedError`/`RevisableDecisionField`, relocated from the two retired modules.
- `src/workflow/activities-step.ts` (`proposeActivitiesStep`/`confirmActivitiesStep`) — reuses the existing Phase 2/5/7 domain logic (retrieval, Curator, scheduling, feasibility, budget, Itinerary Writer) invoked per-step against an already-confirmed flight+hotel rather than a cross-joined combination search. Returns one curated+scheduled result, not a ranked list — activities are a scheduling problem, not a pick-one problem. Being the last step, `confirmActivitiesStep` is also where budget and itinerary prose get computed.
- `confirmFlightStep`'s cascade (slice 2) generalized from hotel-only to `["hotel","activities"]`.
- `src/workflow/chain-orchestrator.ts` (`runStepwiseChain`/`runStepwiseRevision`) — the interim auto-confirm glue, explicitly marked as scaffolding slice 4 will delete. `runStepwiseChain` is `getCurrentChainStep`'s first real caller.
- Retired `search-orchestrator.ts`/`.test.ts`, `itinerary-orchestrator.ts`/`.test.ts`; removed `app/app/actions.ts`'s `beginSearch`/`assembleTripItinerary` (unused debug-only actions tied to the old return shapes); rewired `sendMessage`/`reviseTripDecision` to the new chain.
- 34 new tests (`activities-step.test.ts`, `chain-orchestrator.test.ts`, new cascade tests) — 299 total (down slightly from 303 as slice 2 ended, net of deleting the two old orchestrators' test files and adding the new ones).

**Why:** Continuing directly from slice 2's "next up" — the last chain step plus the retirement the whole redesign was building toward.

**Two real bugs caught live, neither by 299 unit tests (all mocked) nor an initial backend-only spot-check script — only by an actual browser click-through:**
1. **Stale budget/itineraryText after a hotel-only revision.** Rule 3 correctly keeps a hotel change from invalidating activities, but budget/itineraryText live *inside* `confirmActivitiesStep`, not their own chain step — so a hotel swap left them describing the old hotel at its old price. Asked the user fix-now-vs-track (per the standing rule, memory: `feedback_deferred-items.md`); chose fix now. Fixed in `runStepwiseRevision`: when the chain is otherwise `"complete"` after a revision but the revised field was flight/hotel, it re-runs `confirmActivitiesStep` with the *same* already-confirmed schedule to refresh just budget/itineraryText. Live-verified the refreshed text correctly names the new hotel and the activity schedule's own content is byte-identical.
2. **Chat-requested revisions silently did nothing, permanently — the most significant bug this session.** `sendMessage` checked `workflowState === "requirements_ready"` before `decisionRevisionRequested`. Under the old pipeline this ordering was harmless (`advanceTrip` moved `workflowState` along once search began); under the new stepwise steps, which deliberately never call `advanceTrip` (slice 1), `workflowState` stays `"requirements_ready"` forever after the first run — so every later turn, including a correctly-detected revision request, silently fell into the already-complete `runStepwiseChain`'s no-op path instead of ever reaching `runStepwiseRevision`. Reproduced twice live before isolating the cause by comparing a direct function call (worked) against the same call through `sendMessage` (didn't). Fixed by checking `decisionRevisionRequested` first.

**Decisions made:** the auto-chain-through-new-steps call above (asked and answered); the fix-now call on bug 1 (asked and answered).

**What didn't work / dead ends:** the first live browser attempt at the "same-dates flight swap leaves hotel untouched" scenario in slice 2 doesn't apply here, but a similar false alarm happened this slice — an early read of the browser panel right after sending a message showed nothing had updated, which looked like a repeat of the `after()` failure, but turned out to just be checking before the ~25-30s of real Curator+Writer API latency had elapsed. Learned to navigate away and back (forcing a fresh initial fetch) rather than trusting `get_page_text` on a page that might be mid-wait, and to check `trip_events` directly when in doubt about timing vs. a real failure.

**Verification:** `npm test` (299/299 passing, 34 new), `npx tsc --noEmit` (clean), `npm run lint` (clean), `npm run build` (clean). Live-verified three ways: a backend-only spot-check script (real Curator/Writer LLM calls, full chain + a revision, all correct — this one didn't catch bug 2, since it called `runStepwiseRevision` directly rather than through `sendMessage`); then a real browser click-through (signed in via an admin-generated OTP through a throwaway `/dev-signin` helper, same pattern as Phase 7 slice 1, removed after use — new user deleted, `proxy.ts`'s temporary allowlist entry reverted) that sent a real trip request through the actual chat UI, watched the full chain fill in live via Realtime with a correct written itinerary, found bug 2 while trying to drive a real hotel-revision conversation, fixed it, and re-verified the same conversation worked end to end afterward including a correctly-updated budget/itinerary text.

**Known limitations / assumptions carried forward:** `docs/IMPLEMENTATION_PLAN.md` §5 updated — the two live-discovered bugs logged as resolved; the "no chat-level revision trigger" item rewritten to reflect that the trigger now exists (interim, non-criteria-aware); the existing "decision-revision loop doesn't honor the specific criterion" item confirmed to still hold under the new stepwise chain, not just the old pipeline; a new open question flagged (not yet answered) about whether `presenting_draft`/`awaiting_confirmation`/`finalized` are ever reached under the new model, since `WorkflowState` no longer advances past `requirements_ready` at all.

**Next up:** Slice 4 — the chat UI rework: show one step at a time with real confirm/change affordances, make `intake-orchestrator.ts`'s decision-revision detection step-aware, delete `chain-orchestrator.ts`'s interim auto-confirm glue, and resolve the open `WorkflowState`-terminus question above. After that, the stepwise chain redesign is complete and the project resumes wherever Phase 6/7/8/9 makes sense next.

## 2026-09-17 — Stepwise chain redesign, slice 4 (final): chat UI rework, directional revision, warn-before-cascade, finalization

**What I built:** The last slice of the stepwise chain redesign — real per-step interaction replacing `chain-orchestrator.ts`'s interim auto-confirm glue, plus three things bundled in after discussion with the user: the directional/absolute revision split ("cheaper" → a concrete price ceiling), an explicit trip-finalization step, and fixing the trip-history list (`trips.status` never advances past `requirements_ready` under the new model). Six architecture questions were resolved with the user directly before any code was written (plan mode, all six recommended options chosen) — see `docs/IMPLEMENTATION_PLAN.md`'s "Slice 4 outcome" for the full list; in short: hybrid interaction (clickable cards + chat), chain-step revision vocabulary, directional split bundled into this slice, explicit finalize, warn-before-cascade, and persisted `"proposed"` candidates.

**Why:** The only remaining piece of the redesign decided 2026-09-17 (see "Session close" entry below in the log) — everything backend-side (slices 1-3) was already built, tested, and live-verified; this was genuinely different work (frontend/UX, plus making `intake-orchestrator.ts` step-aware), not more backend orchestration.

**Built, roughly in dependency order:**
- `maxHotelPriceUsd` requirement/constraint plumbing (`extraction.ts`, `constraints.ts`, `chain.ts`, `step-shared.ts`) — hotels previously had a rating floor but no price ceiling, needed for "cheaper hotel" to have somewhere to land.
- `propose*Step` now persists its candidate list as `"proposed"` `trip_decisions` rows (`retireProposedTripDecisionsForField`, new); `confirm*Step` promotes a matching proposed row in place via a new shared `confirmDecisionField` (`step-shared.ts`) instead of always retiring-and-reinserting, activating the previously-unused `confirmTripDecisions`.
- `src/domain/chain.ts` gained `requirementRevisionTargetStep`/`revisionRisksConfirmedWork` — the pure logic behind the cascade-warning gate, used both server-side (`intake-orchestrator.ts`) and client-side (the "Change" button).
- `src/agents/intake.ts`/`intake-orchestrator.ts` — decision-revision vocabulary switched to `ChainStep` names; new `pendingCascadeConfirmation` signal (decision or requirement) when a revision risks invalidating confirmed downstream work; system prompt now instructs the model to convert a directional request into a concrete threshold requirement revision, or an absolute request into the matching hard-constraint field directly.
- `src/workflow/step-router.ts` (new, replaces `chain-orchestrator.ts`, which — along with its test — is deleted) — `proposeCurrentChainStep`/`reviseChainStep`/`advanceOrRefreshChain`, propose-only, never auto-confirms; returns what it did rather than `void` so callers can consume fresh candidates directly instead of redundantly re-fetching.
- `state-machine.ts` — one new direct transition (`requirements_ready --chain_completed--> presenting_draft`), reusing the existing `presenting_draft → awaiting_confirmation → finalized` rules rather than exploding into per-step states.
- `app/app/actions.ts` — new per-step Server Actions (`propose*Candidates`/`confirm*Candidate`), `confirmCascadeAndRevise`, `finalizeTrip`; `sendMessage` rewritten to only ever propose (never auto-confirm), checking `pendingCascadeConfirmation`/`decisionRevisionRequested` before `workflowState` (preserving slice 3's ordering lesson).
- `app/app/trips/page.tsx` — reads `getCurrentChainStep` for in-progress trips instead of the permanently-stale `trips.status`.
- New `trip-workspace.tsx` (owns `pendingCascadeConfirmation` state, shared by chat and the itinerary panel); `chat-panel.tsx` reports a chat-triggered cascade warning up; `itinerary-panel.tsx` fully reworked — candidate cards, cascade-warning banner, finalize button, live Realtime updates.
- 9 new tests (`step-router.test.ts`), plus new coverage in `flight-step.test.ts`/`hotel-step.test.ts`/`intake-orchestrator.test.ts`/`state-machine.test.ts`/`chain.test.ts`/`extraction.test.ts`/`constraints.test.ts` — 319 total.

**A correctness gap found and fixed before it ever ran live** (while planning the "Change" UI flow, not by a review pass): the new `confirmDecisionField`'s candidate-promotion logic only superseded sibling *proposed* rows, not a prior *confirmed* one — correct for a first-ever confirm, wrong for revising an already-confirmed step (where a fresh proposed list coexists with the still-confirmed old pick), which would have let two `"confirmed"` rows for the same field coexist. Fixed by widening the repository function (`supersedeOtherActiveTripDecisions`) to supersede every other non-superseded row regardless of status.

**Three real bugs caught only by live browser click-through, none by the 319 unit tests (all mocked):**
1. **A double-Curator-LLM-call race, caught while designing rather than testing:** a naive "re-fetch whenever `trip_decisions` changes" client effect would refire on its own writes; for activities specifically (a real LLM call, not a cheap query) this could double-fire if a hotel confirm's own server-side auto-propose and the client's reactive effect both fired for the same transition. Closed by having `handleConfirmHotel` set a "don't propose activities again" ref *synchronously, before* awaiting the confirm action — confirmed live (one itinerary generation observed per hotel confirm, not two).
2. **The itinerary panel could get stuck on "Finding hotels…"/"Planning activities…" indefinitely after a successful confirm.** Not a backend bug — the confirm action and server state were always correct, a page reload immediately showed the right thing — but the panel's derived UI state all read from `decisions`, populated by the initial load plus Supabase Realtime, and Realtime updates didn't arrive promptly within the same session in this test environment. Fixed by having every confirm handler optimistically merge its own action response into local `decisions` state immediately, de-duplicated against the eventual real Realtime row.
3. **An honest-failure-messaging gap:** revising an already-confirmed hotel with an over-aggressive LLM-computed `maxHotelPriceUsd` threshold correctly threw `NoViableHotelCandidatesError` server-side, but surfaced to the client only as React/Next's generic obfuscated production error text — exactly the dishonest-failure gap design rule 3 exists to prevent. Fixed by having the relevant Server Actions catch this and `NoViableFlightCandidatesError` and return a friendly `{error}` result instead of throwing. **Known gap, not fixed this slice:** the equivalent chat-triggered fire-and-forget revise path still only `console.error`s such a failure — tracked in `docs/IMPLEMENTATION_PLAN.md` §5.

**Decisions made:** the six architecture questions above (plan mode, all recommended options chosen); the fix-now call on all three live bugs and the pre-live correctness gap (no explicit ask needed — all four are straightforward bugs in this slice's own new code, not open design questions).

**What didn't work / dead ends:** running a second `next dev` in this directory still hits the documented per-directory lock (another session held the slot the whole time) — worked around it by adding a `wanderwise-preview` config to `.claude/launch.json` that runs `next start` (the production build) on a separate port instead, which doesn't share the dev server's lock; kept as a permanent addition for future sessions with the same conflict. Also: `admin.generateLink`'s verify link uses the implicit flow (tokens in a URL fragment), not the PKCE `?code=` flow `/auth/callback` handles for a real emailed link — redirecting straight to it doesn't work since a fragment never reaches any server. Worked around by having the throwaway `/dev-signin` helper follow the verify link itself (`redirect: "manual"`) and read the tokens out of the redirect's `Location` header directly, then calling `setSession` on a cookie-aware server client.

**Verification:** `npm test` (319/319 passing), `npx tsc --noEmit`/`npm run lint`/`npm run build` all clean throughout. Live-verified against the real hosted Supabase project via the `wanderwise-preview` production-build server, signed in via the throwaway `/dev-signin` helper (removed after use, along with its one-line `proxy.ts` public-path exception; the throwaway auth users and their cascaded trip data deleted via a one-off service-role script, not committed): a full forward flow (flight card → hotel card → activities confirm) reached a correct, well-grounded written itinerary and budget; finalizing transitioned `trips.status` to `finalized`, reflected immediately on the trip history list; the cascade warning fired correctly from both a direct "Change" click and a chat-requested revision of an already-confirmed step, blocking nothing until explicitly confirmed, with Cancel leaving every decision untouched; a directional "cheaper hotel" chat request correctly converted to a `maxHotelPriceUsd` threshold revision (surfacing bugs 2 and 3 above, both found and fixed live in the same pass).

**Known limitations / assumptions carried forward:** `docs/IMPLEMENTATION_PLAN.md` §5 updated — all prior open items this slice was meant to close are now checked off; one new open item added (chat-triggered fire-and-forget revise failures aren't surfaced to the user, per bug 3 above).

**Next up:** The stepwise chain redesign is complete. The project resumes wherever Phase 6/7/8/9 makes the most sense next — Phase 8 (evaluation/observability harness) is the next unstarted phase in the original build sequence, but the §5 open-items list (especially the newly-added chat-revision-failure-messaging gap, and the longer-standing idempotency/retry gaps) is also worth a look before committing to a direction.

---

## 2026-09-17 — Close revise-failure gap before Phase 8

**What I built:** Fixed the one open item flagged as worth closing before starting Phase 8 (per `docs/IMPLEMENTATION_PLAN.md` §5): the chat-triggered fire-and-forget revise path (`sendMessage`'s `after()` call to `reviseChainStep` in `app/app/actions.ts`) silently swallowed a "no viable candidates" failure instead of surfacing it to the user. Added a `chain_revision_failed` `trip_events` row (via `appendTripEvent`, no migration needed since `event_type` has no enum constraint) on that failure path, carrying the same friendly message `friendlyStepErrorMessage` already produces for the direct UI paths. `app/app/_components/itinerary-panel.tsx`'s existing `needsAttention` Realtime listener now also handles this event type.

**Why:** Found live 2026-09-17 right after slice 4 shipped, and asked the user fix-now-vs-track per the standing rule — chose fix-now since it's a real honesty-design-rule gap in code just shipped, before committing to Phase 8's direction.

**Decisions made:** Left the rest of `docs/IMPLEMENTATION_PLAN.md` §5's open items (idempotency, naive scheduler, 5-combination cap, one-way trips, etc.) deferred as-is — none touch Phase 8's scope (eval harness, CI, dashboards).

**What didn't work / dead ends:** none.

**Verification:** `npx tsc --noEmit`, `npm run lint`, `npm test` (319/319) all clean. Not live-verified through the real chat UI — reproducing a genuine "no viable candidates" revision failure needs the same OTP dev-signin setup slice 4 used; skipped as low-risk since the fix reuses the already-tested `friendlyStepErrorMessage` pattern exactly, just logging a trip_event instead of returning `{error}`.

**Next up:** Phase 8 — evaluation and observability (eval harness, adversarial cases, CI workflow, dashboards).

---

## 2026-09-17 — Phase 8 slice 1: end-to-end scenario eval harness

**What I built:** The first Phase 8 item — a real end-to-end scenario eval harness (PROJECT_BRIEF.md §9.6/§19), distinct from the existing component evals (`evals/cases/intake.ts`, `evals/cases/retrieval.ts`) which call one agent function directly. `evals/lib/scenario-harness.ts` creates a throwaway `auth.users` row per scenario (admin-created, torn down at the end of the run — `trips`/`sessions` have a hard FK to `auth.users`, no anonymous state) and gives each scenario a real Supabase service-role client plus the real stepwise-chain model/embedding clients. `evals/cases/scenarios.ts` implements 6 of the 16 `PROJECT_BRIEF.md` §19 scenarios (standard trip, missing information, hard-constraint-vs-cheapest, over-budget, user revision, out-of-scope/prompt-injection) driving the real `intake-orchestrator.ts` -> `flight-step.ts` -> `hotel-step.ts` -> `activities-step.ts` -> finalize chain, with deterministic assertions against persisted state rather than model text. `evals/runners/run-scenario-eval.ts` (`npm run eval:scenarios`) runs them, prints a summary that separates real regressions from already-tracked known gaps, and persists one `eval_runs` + one `eval_results` row per scenario via a new `src/repositories/eval-runs.ts` (plus the `eval_runs`/`eval_results` types this needed adding to the hand-maintained `database.types.ts`, which had never gotten them since nothing used those tables before now).

**Why:** Phase 8's own checklist item 1. An end-to-end scenario suite is the natural next step after the stepwise chain redesign closed out clean (319/319 unit tests) — it's the only way to verify the *whole* chain together against a real backend, which unit tests (mocked Supabase) and component evals (single agent call) structurally can't.

**Decisions made:** Six scenarios chosen for this slice, not all 16 — the other 10 (stale inventory, duplicate/idempotent request, cross-session isolation, cancellation, prompt injection in retrieved *inventory* text, failure/recovery) need different test infrastructure (real RLS/JWT-authenticated sessions, or fault injection into a search call) than this harness's direct-function-call, service-role approach provides; tracked as follow-up rather than forced into this slice's shape. `finalizeTrip`'s exact two-`advanceOrThrow`-call sequence (including its hardcoded `guardrailsPassed: true`) is duplicated in the harness rather than imported, since the real one lives in a `"use server"` file behind a cookie-based auth check this script doesn't have.

**What didn't work / dead ends:** First run of `standard_trip` and `over_budget_request` both failed, but not for the reason they looked like at first — both hit "Event confirmation_requested is not permitted from state requirements_ready" because my harness called `confirmActivitiesStep` (the raw workflow function) directly, missing the `chain_completed` transition that only `app/app/actions.ts`'s `confirmActivitiesCandidate` Server Action fires after it. Fixed by replicating that exact guarded sequence in the harness. Separately, `user_revision`'s "old hotel decision is superseded" assertion failed because it queried via `listActiveTripDecisions`, which by design excludes superseded rows — the assertion needs the unfiltered row set to check a row *became* superseded. And `standard_trip`'s original $3000 budget (reused from `evals/cases/intake.ts`'s extraction-only test message) turned out to be genuinely too low for 2 travelers / 7 nights on this route (~$4,954 real total) — raised to $6000 so "standard_trip" is an actual happy path rather than accidentally exercising the budget-ceiling path too.

**Real finding, not a harness bug:** `over_budget_request` still fails after all three fixes above — correctly. It demonstrates a real, previously-undocumented gap: the §9.1 budget-ceiling guardrail isn't enforced at finalize time (`finalizeTrip` hardcodes `guardrailsPassed: true`). Tracked in `docs/IMPLEMENTATION_PLAN.md` §5; asked the user fix-now-vs-track rather than deciding unilaterally, since §9.3 implies this needs an explicit-override *decision*, not just a bug fix.

**Verification:** `npx tsc --noEmit`, `npm run lint`, `npm test` (319/319) all clean. `npm run eval:scenarios` run live against the real hosted Supabase project + real Anthropic/Voyage APIs: 5/6 scenarios pass; the 6th fails exactly as documented above, with zero unexplained regressions. Each throwaway eval user was created and deleted within the run — nothing standing afterward.

**Next up:** Decide the over_budget finalize-guardrail question above. Then continue Phase 8: adversarial eval cases, the remaining 10 §19 scenarios (needs the RLS/JWT and fault-injection infrastructure noted above), CI wiring (a fast deterministic-only `npm run eval:ci` + GitHub Actions workflow — the real-model evals stay manual per §9.6's own "keep the eval suite cheap enough for CI" guidance), the engineering/product dashboards, and the cache-hit/cost comparison.

---

## 2026-09-17 — Phase 8 slice 2: CI wiring

**What I built:** `.github/workflows/ci.yml` — a GitHub Actions workflow on every push/PR to `main` running `npm run eval:ci` (new script: `typecheck` (`tsc --noEmit`) + `lint` + `test`, all deterministic and mocked-Supabase, no network calls) and `npm run build`. Placeholder `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY`/`SUPABASE_SERVICE_ROLE_KEY` env vars are set directly in the workflow (not GitHub secrets) — verified locally that `npm run build` succeeds with placeholder values, since nothing in build/typecheck/lint/test makes a real Supabase/Anthropic/Voyage call.

**Why:** Closes the budget-of-two open items this satisfies at once: `PROJECT_BRIEF.md` §22's "CI setup" (deferred to Phase 8 since the build sequence's first session) and Phase 8's own "CI evaluation command + GitHub Actions workflow" checklist item. Kept the decision from `evals/runners/run-scenario-eval.ts`'s Phase 8 slice 1 docstring: real-model evals (`eval:intake`/`eval:retrieval`/`eval:scenarios`) cost real money and stay manual, per §9.6's own guidance that a CI-bound eval suite should stay deterministic, cheap, and fast — `eval:ci` only ever runs the mocked-Supabase domain-logic unit tests already living in `npm test`.

**Decisions made:** No GitHub secrets configured for this workflow — deliberately, since nothing it runs needs real credentials. If a future CI job needs to run a real-model eval against a live Supabase project, that's a distinct, separate workflow/secrets decision, not implied by this one.

**What didn't work / dead ends:** The first real GitHub Actions run (after pushing) failed on `typecheck`: `Cannot find name 'LayoutProps'` in `app/layout.tsx`. That global type is generated by Next.js into `.next/types/` on a `next dev`/`next build` run — it existed locally (from every prior session's own build/dev runs) so `tsc --noEmit` passed locally without ever needing it fresh, but a clean CI checkout has no `.next/` at all. Fixed by making `typecheck` run `next typegen` (generates just the route/layout types, no full build) before `tsc --noEmit`; verified by deleting the local `.next/` directory and re-running `npm run eval:ci` clean, reproducing and then confirming the fix against the same fresh-checkout condition CI hits.

**Verification:** `npm run eval:ci` (new script) passes locally, including from a deleted `.next/` (simulating a fresh CI checkout): typecheck (with `next typegen` first), lint, and `npm test` (319/319) all clean. `npm run build` verified to succeed with the exact placeholder env vars the workflow sets. Pushed to `origin/main`; the corrected workflow's first live run is expected to pass (not yet re-verified against the actual GitHub Actions runner as of this entry — the initial two runs both failed on the now-fixed `typecheck` step).

**Next up:** Continue Phase 8 — remaining items: adversarial eval cases, the remaining §19 scenarios, engineering/product dashboards, and the cache-hit/cost comparison. Order not yet decided.

---

## 2026-09-17 — Phase 8 slice 3: adversarial eval cases

**What I built:** `evals/cases/adversarial.ts` — four adversarial cases (PROJECT_BRIEF.md §9.6) on the same real end-to-end harness slice 1 built: `fabricated_inventory_ids` (confirmFlightStep/confirmHotelStep's re-validation rejects made-up UUIDs, not just trusts the caller), `budget_pressure_wording` ("money is no object" framing doesn't change the literally-stated budget number extracted), `contradictory_dates` (self-contradictory dates in one message resolve to the last-stated value or a clarification, mirroring `evals/cases/intake.ts`'s existing budget case but end to end), `cross_session_reference` (a `tripId`/`sessionId` pair that don't belong together is rejected by `intake-orchestrator.ts`'s `SessionTripMismatchError` before any model call — zero API cost). `evals/runners/run-scenario-eval.ts` now runs both `SCENARIO_CASES` and `ADVERSARIAL_CASES` together (one runner, since they share identical harness/reporting/persistence — a second near-duplicate runner would just be drift risk).

**Why:** Phase 8's "Adversarial eval cases" checklist item, building directly on slice 1's harness rather than a separate one.

**Decisions made:** Four of §9.6's eight adversarial categories covered; "attempts to trigger booking" already covered by `scenarios.ts`'s `out_of_scope_request`. Left uncovered: prompt injection *in retrieved inventory text* and malformed/malicious inventory records — both need a seeded adversarial fixture (mutating shared `activities`/`destinations` rows, even temporarily, or a dedicated fixture migration), judged a separate, bigger piece of work rather than squeezed into this slice.

**What didn't work / dead ends:** `fabricated_inventory_ids`'s hotel half initially risked being ambiguous — with no confirmed flight yet, a fabricated hotel ID could fail on either the ID-doesn't-exist check or the `FlightStepNotConfirmedError` precondition depending on check order; the assertion accepts either since both are a clean rejection, not a crash or silent acceptance, which is what the case actually cares about. Also caught a wrong error-class import while writing this (`FlightStepNotConfirmedError` is defined in `hotel-step.ts`, not `flight-step.ts`, despite `activities-step.ts` re-exporting it from there) via `tsc`.

**Verification:** `npx tsc --noEmit`, `npm run lint`, `npm test` (319/319) all clean. `npm run eval:scenarios` run live: 9/10 total cases pass (all 4 new adversarial ones clean), the 1 failure is the already-tracked `over_budget_request` known gap from slice 1 — no new regressions.

**Next up:** Continue Phase 8 — remaining §19 scenarios (needs RLS/JWT or fault-injection infra), engineering/product dashboards, cache-hit/cost comparison. Order not yet decided.

---

## 2026-09-17 — Phase 8 slice 4: engineering dashboard

**What I built:** `app/internal/analytics/page.tsx` — the §13.3 engineering dashboard: trip finalization rate, total/per-finalized-trip cost, cost/latency/cache-read breakdown by agent, guardrail trigger frequency (which fires most), workflow failure-state breakdown, and eval pass-rate trend across recent `eval_runs`. Uses `createServiceClient` (every source table — `agent_runs`/`tool_calls`/`guardrail_events`/`workflow_steps`/`eval_runs`/`eval_results` — has RLS enabled with no policy for `anon`/`authenticated`, so the RLS-scoped client would just get zero rows). Route-level access is `proxy.ts`'s existing deny-by-default middleware — no separate in-page auth check, matching `app/app/layout.tsx`'s own documented reasoning.

Also fixed a real gap discovered while building this: `agent_runs.cost_usd` was defined in the schema and accepted by `recordAgentRun`, but **no caller ever actually passed it** — `intake-orchestrator.ts` and `activities-step.ts`'s three `recordAgentRun` calls all left it `null`, silently, since nothing enforced it. Extracted `evals/runners/run-intake-eval.ts`'s existing cost-estimation formula into `src/observability/pricing.ts` (the first real file in that previously-empty directory) and wired `estimateCostUsd(model, usage)` into all three call sites plus the eval runner (removing its duplicate copy).

**Why:** Phase 8's "Engineering dashboard" checklist item, plus the §22 "Analytics dashboard access control" open decision it depended on. The cost-wiring fix wasn't optional scope creep — without it, every cost metric on this dashboard would show $0.00 forever no matter how much the app actually spent, which defeats the entire point of a cost dashboard.

**Decisions made:** `/internal/analytics` access stays "any signed-in user," not a separate admin role — resolved the §22 open item this way since this is a single-operator portfolio project with no multi-tenant admin concept anywhere else in the app.

**What didn't work / dead ends:** none — the embedded-resource Supabase query (`eval_results` joined to `eval_runs` for label/timestamp) worked on the first real try against the live DB, resolved via the real Postgres FK even though `database.types.ts`'s hand-maintained `Relationships: []` doesn't declare it (that array is TS-typing-only; PostgREST resolves embeds from the actual schema).

**Verification:** `npx tsc --noEmit`, `npm run lint`, `npm test` (319/319) all clean. Live-verified in the browser: created a throwaway dev-signin helper (same pattern as Phase 7's live verification — a `/dev-signin` route + one-line `proxy.ts` public-path exception, both removed after use, not committed) to actually load `/internal/analytics` signed in and confirm real data renders. First load showed real guardrail/tool-call/eval-pass-rate numbers but $0.00 everywhere for cost (all prior `agent_runs` rows predate the pricing fix); re-ran `npm run eval:scenarios` once more after the fix and confirmed real per-agent costs, cache-hit-rate savings, and a growing eval pass-rate trend all appeared correctly on refresh. Confirmed via `auth.admin.listUsers` that the harness's own cleanup has been working correctly all along (only the intentional dev-signin user was ever left over, and it's now deleted) — the dashboard's "0/2 trips finalized" is real leftover data from earlier Phase 7 live testing, not eval debris.

**Next up:** Continue Phase 8 — remaining §19 scenarios (needs RLS/JWT or fault-injection infra), the product metrics view, and the cache-hit/cost comparison experiment.

---

## 2026-09-17 — Phase 8 slice 5: product metrics view

**What I built:** `app/internal/product-metrics/page.tsx` — the §13.4 product metrics view: trip-start rate, requirement-completion rate (reusing the real `checkRequirementsComplete` domain function against `trip_requirements` rows, not a reimplementation), draft-generation rate, confirmation rate, revision rate (any `trip_decisions` row ever superseded for a trip), time to first draft / time to finalized (derived from `trip_state_versions`' real append-only history), abandonment stage (current chain step for non-finalized trips, reusing `getCurrentChainStep`), and qualitative feedback shown honestly as "not yet collected" rather than omitted or faked, since no feedback mechanism exists anywhere in the app. Deliberately a separate page from `/internal/analytics`, per §13.4's own explicit rule against letting a high agent-call count read as product success. Cross-linked the two pages to each other.

**Why:** Phase 8's "Product metrics view" checklist item, the last of the dashboard-shaped items.

**Decisions made:** No new tables/columns needed — every metric here derives from data the app was already persisting (`trip_state_versions`, `trip_requirements`, `trip_decisions`, `trips`), unlike slice 4's cost-telemetry gap.

**What didn't work / dead ends:** After deleting the throwaway `/dev-signin` route (created for this slice's own live verification, same pattern as slice 4), `npx tsc --noEmit` failed on a stale `.next/dev/types/validator.ts` still referencing the deleted route — a leftover `next dev` type-cache artifact, not a real error. `npm run typecheck` (which runs `next typegen` first) didn't clear it either; only deleting the whole `.next/` directory did. Not a regression in anything committed — the stale reference was to a file that was never committed in the first place.

**Verification:** `npm run eval:ci` (typecheck + lint + `npm test`, 319/319) all clean after clearing the stale `.next/` cache above. Live-verified both pages in the browser via the same throwaway dev-signin pattern: real numbers rendered correctly on `/internal/product-metrics` (2 real trips, 1 with complete requirements, 0 drafted — consistent with `/internal/analytics`'s "0/2 finalized", since neither of the two pre-existing real trips ever reached a draft), and the cross-links between the two pages work. Dev-signin route and its `proxy.ts` exception removed after use, not committed; the throwaway auth user deleted.

**Next up:** Phase 8's remaining items: the other 10 §19 scenarios (needs RLS/JWT-authenticated sessions or fault-injection infrastructure this project doesn't have yet) and the cache-hit/cost comparison experiment. Phase 8's dashboard-shaped and eval-harness-shaped work is now complete.

---

## 2026-09-17 — Phase 8 slice 6: cache-hit / cost comparison (Phase 8 complete)

**What I built:** `evals/runners/run-cache-comparison.ts` (`npm run eval:cache-comparison`) — PROJECT_BRIEF.md §6.7's "prompt caching is an experiment, not an assumption," finally actually measured. Runs the same 6 real Intake-agent calls (`evals/cases/intake.ts`'s `INTAKE_EVAL_CASES` — same agent, so system prompt + tool defs never change across the sequence) twice against the real Anthropic API: once with caching on (the real, always-on production behavior), once forced off via a new `cachingEnabled` constructor flag on `AnthropicModelClient` that exists solely for this comparison (every real call site leaves it at its default `true`). Same model, same inputs, same order — the only variable is the `cache_control` breakpoints.

**Real result (claude-sonnet-5):** caching cut cost by **45.5%** ($0.08585 -> $0.04681) — the first call writes the cache (4,821 tokens), every call after it reads from it instead of resending the system prompt + tools. But it did **not** improve latency (26,680ms -> 29,088ms total across 6 calls — slightly *worse*). This is the actual point of §6.7 treating caching as an experiment rather than a given: the cost win is real and worth keeping, but an assumption that caching would also make responses faster would have been wrong.

**Why:** The last Phase 8 checklist item. With this, all six Phase 8 items (`docs/IMPLEMENTATION_PLAN.md`) are checked off — the phase is complete, modulo the two explicitly-scoped-out pieces (the remaining 10 §19 scenarios, and adversarial prompt-injection-in-inventory-text cases) that both need test infrastructure (RLS/JWT sessions, or a seeded adversarial fixture) this build doesn't have yet.

**Decisions made:** None new — `cachingEnabled` is additive (defaults to `true`, every existing call site unaffected).

**What didn't work / dead ends:** None.

**Verification:** `npx tsc --noEmit`, `npm run lint` clean. Run live against the real Anthropic API — real numbers above, not simulated. `npm test` unaffected (no unit tests exist for `AnthropicModelClient` — it's the thin real-API-boundary file, tested via the real eval runners rather than mocks, consistent with how it's always been).

**Next up:** Phase 8 is complete. Two explicitly deferred items remain tracked but out of this build's scope for now (remaining §19 scenarios needing RLS/JWT infra; inventory-text prompt-injection cases needing a seeded fixture). Natural next step is Phase 9 (portfolio polish) unless the tracked open items (`docs/IMPLEMENTATION_PLAN.md` §5 — especially the budget-ceiling-at-finalize gap slice 1 found) get prioritized first.

---

## 2026-09-17 — Session close: pre-Phase-9 documentation audit

**What I built:** No code changes — a documentation-accuracy pass requested before closing this session and starting Phase 9 in a new one. Found and fixed two real problems:

1. **`docs/architecture/ADR-INDEX.md` was significantly stale.** Area 4 (validation/evaluation) and Area 5 (observability) still read "not yet decided — deferred to Phase 8" for six items that Phase 8's six slices this session actually decided and built. Updated all six, plus the ADR-005/006/007 status-table rows, plus the Area 1 prompt-caching line with slice 6's real 45.5%-cost/no-latency-benefit result.
2. **`BUILD_LOG.md`'s entries from this entire session were in the wrong place.** Every one of today's seven entries (the revise-failure-gap fix plus all six Phase 8 slices) had been inserted right after the file's opening `---`, ahead of every entry from 2026-09-16 onward, instead of appended at the end — breaking the file's chronological, oldest-first convention throughout today's whole session. Moved the block of seven entries to the actual end of the file, verified via a sorted-diff against the previous commit that the fix was a pure reorder (zero lines added, changed, or lost).

Also added two entries to `docs/IMPLEMENTATION_PLAN.md` §5 that a straight Phase-8-checklist read wouldn't have surfaced: a never-decided telemetry retention/redaction policy (`PROJECT_BRIEF.md` §13.2 requires one; `tool_calls.arguments`/`.result` store full payloads indefinitely with no redaction, made more load-bearing by this session's eval-harness rows), and — for historical completeness, matching how every other found-and-fixed-same-session bug in this file is recorded — the cost-telemetry gap slice 4 found and fixed. Sharpened Phase 9's own checklist to name which ADRs are specifically owed (ADR-006, ADR-007) and to flag the retention-policy decision as worth making before any demo recording shows real dashboard data on screen.

**Why:** Requested explicitly — "make sure everything needed for Phase 9 and anything needed to be tracked in the implementation brief is there before I close this out."

**Decisions made:** None new — this was a tracking-accuracy pass, not a design decision.

**What didn't work / dead ends:** The BUILD_LOG misplacement above wasn't caught by any check during the session itself (nothing validates entry order automatically) — only surfaced now because writing this very entry required checking where it should go, which prompted checking where everything else actually was.

**Verification:** `npx tsc --noEmit`, `npm run lint`, `npm test` (319/319) all clean (docs-only changes, but re-verified anyway per the standing discipline of never assuming a change is inert). `git status` clean going into this entry; no other uncommitted work exists.

**Next up:** Phase 9 (portfolio polish) is next: visual design pass, architecture diagram, writing up ADR-006/ADR-007 as standalone files, and the final README/demo pass — the last of which should resolve the telemetry-retention question first if real dashboard data will be shown on screen.

---

## 2026-09-17 — Phase 9 slice 1: telemetry redaction, ADR-006/ADR-007 write-ups

**What I built:**
- Resolved the genuinely-undecided telemetry retention/redaction question (`PROJECT_BRIEF.md` §13.2) before writing ADR-007, since the write-up can't describe a decision that hasn't been made. Asked the user rather than deciding unilaterally (per `CLAUDE.md`'s "do not silently make major architectural decisions"), with three options ranging from doc-only to a fully automated cleanup job; the user picked the middle option.
- `src/observability/redaction.ts` (new) — `redactSensitiveTelemetry`, a deep-walk over a tool-call payload that masks the `value` of any `{field|target: "requiredAccessibility", value: ...}` shape (the one field, out of everything the Intake agent extracts, that can reveal a disability/health condition — every other field is ordinary trip-shape data the dashboards need to show as-is).
- Wired into `src/repositories/agent-runs.ts`'s `recordToolCalls` — the single write boundary all three callers (`intake-orchestrator.ts`, `activities-step.ts`'s Curator and Writer steps) already share, so the redaction applies everywhere without touching any call site.
- Time-based retention (deletion after N days) is documented as a decided policy in the new ADR, not automated — every row in the hosted DB today is synthetic demo/eval data, so a real cleanup job has no privacy benefit yet.
- [`docs/architecture/ADR-006-evaluation-strategy.md`](docs/architecture/ADR-006-evaluation-strategy.md) (new) — writes up the deterministic-grading-throughout and real-direct-call-harness decisions from Phase 8, previously only recorded as index-line summaries.
- [`docs/architecture/ADR-007-observability.md`](docs/architecture/ADR-007-observability.md) (new) — writes up dashboard placement/access, the engineering/product split, and this session's retention/redaction decision.
- 4 new tests (`src/observability/redaction.test.ts`) covering the record_extraction and propose_trip_revision payload shapes, a non-sensitive-field no-op case, and null/primitive/array inputs. Updated `docs/architecture/ADR-INDEX.md` and `docs/IMPLEMENTATION_PLAN.md` §5 (moved the telemetry item from unresolved to resolved) and §1 (checked off the ADR checklist item, sharpened Phase 9's remaining items).

**Why:** First slice of Phase 9's "fill in remaining ADRs" checklist item — writing ADR-007 honestly required actually closing the retention/redaction gap first, not just describing it as still-open.

**Decisions made:** Retention/redaction resolved as: redact the one sensitive field now (real code), document but don't automate retention (see ADR-007's Rationale/Consequences for the full reasoning) — the user's chosen middle option between doc-only and a fully automated `pg_cron` job.

**What didn't work / dead ends:** None.

**Verification:** `npx tsc --noEmit`, `npm run lint`, `npm test` (323/323, up from 319) all clean.

**Next up:** Continue Phase 9: architecture diagram, visual design pass, final README pass + demo walkthrough recording. Order not yet decided.

---

## 2026-09-17 — Phase 9 slice 2: architecture diagram

**What I built:** [`docs/architecture/architecture-diagram.md`](docs/architecture/architecture-diagram.md) — two Mermaid diagrams: (1) system components (browser, the one Next.js deployable, Anthropic/Voyage, Supabase, the eval harness, CI), and (2) a single chat turn's flow through the deterministic-vs-agent split — agent call → guardrail → deterministic services → workflow controller → persisted state, with every guardrail check (pass or fail) logged. Closes with the §21-required "what would change for live providers or real booking" narrative point, tied to specific diagram boxes rather than written in the abstract.

**Why:** Phase 9's "architecture diagram" checklist item.

**What didn't work / dead ends:** First draft was one diagram with three nested subgraphs (Orchestration/Agents/Deterministic all inside the Next.js app box). It rendered without a Mermaid syntax error, but a real render (via a throwaway local static server + Mermaid CDN, screenshotted) showed it as an unreadably cramped, overlapping mess — nested subgraphs plus many cross-links don't auto-layout well. Rebuilt as two flat, small diagrams (~6-9 nodes each) instead, which is also a better narrative structure (one diagram per concern: what exists vs. how one request flows through it). Learned to always render-and-look at a generated diagram before treating a "no syntax error" result as "it's fine" — an unrendered Mermaid block is only checked for parse validity, not legibility.

**Verification:** `npx tsc --noEmit`, `npm run lint`, `npm test` (323/323) all clean (docs-only change). Both diagrams rendered via a temporary local Node static server + Mermaid CDN (not committed) and visually inspected via screenshot before and after the redesign; confirmed the final version renders cleanly with correct em-dashes (an earlier mojibake in the render was traced to the throwaway test server missing a UTF-8 content-type header, not a problem with the actual source file — confirmed by reading the file's raw bytes directly).

**Next up:** Continue Phase 9: visual design pass, final README pass + demo walkthrough recording.

---

## 2026-09-17 — Phase 9 slice 3: visual design pass (warm editorial travel palette)

**What I built:** A cohesive design system replacing the default Tailwind zinc/black/white scaffold with a warm editorial travel palette, per direction from the user (cream/sand, off-white/clouds, navy blue, deep ocean teal, terracotta/amber):
- `app/globals.css` — new `@theme` tokens: `sand-50..300` (backgrounds/surfaces), `navy-50..900` (text/structure), `teal-50..800` (primary interactive color — nav, links, neutral actions), `terracotta-50..700` (reserved for the one key call-to-action per screen and attention/warning states). Dropped `prefers-color-scheme: dark` and every `dark:` Tailwind variant across the app — a single warm-light theme reads as more boutique-travel than a dashboard with a dark mode, and it roughly halved the class list on every component.
- `app/layout.tsx` — added `Fraunces` (a Google Fonts variable serif with `opsz` axis) via `next/font/google` as `--font-serif`, paired with the existing Geist Sans for body/UI text. Applied to page-level `h1`s and the "Wanderwise" wordmark, not smaller section labels (those stay sans at their size).
- Applied across every page: landing/sign-in (`app/page.tsx` — added an eyebrow label, serif heading, and a card-wrapped sign-in form instead of a bare floating input), the `/app` chrome and chat + itinerary panel (`app/app/layout.tsx`, `chat-panel.tsx`, `itinerary-panel.tsx` — navy user chat bubbles, teal "Send"/neutral actions, terracotta "Confirm this schedule"/"Finalize trip" CTAs, teal "Trip finalized" success state, teal "Change" links), trip history/detail (`app/app/trips/page.tsx`), and both internal dashboards (`app/internal/analytics/page.tsx`, `app/internal/product-metrics/page.tsx` — serif h1s, teal cross-link between the two pages).
- Migration approach: a scripted mechanical pass (remap the `zinc`/`amber` shade scales to the new tokens, strip `dark:` variants) across all 10 component/page files, followed by targeted manual edits for the handful of `bg-black`/`bg-foreground` CTAs that needed differentiated teal-vs-terracotta treatment and the couple of template-literal className cases the mechanical pass couldn't reach.

**Why:** Phase 9's "visual design pass" checklist item and `PROJECT_BRIEF.md`'s "simple, clean visual design... restraint and polish matter more than feature count." Asked the user for palette direction and page scope first (`AskUserQuestion`) rather than picking an aesthetic unilaterally — visual design is inherently subjective and this is the project's portfolio-facing surface.

**What didn't work / dead ends:**
- First attempt at the mechanical migration script collapsed *all* runs of multiple spaces in each file (`text.replace(/  +/g, ' ')`), not just whitespace inside `className="..."` strings — this silently mangled every file's leading indentation (2-space → mostly 1-space) across ~1,470 lines. Caught immediately via `git diff --stat` showing far more churn than a color-token swap should produce; reverted all 10 affected files with `git checkout --` (nothing had been committed yet) and rewrote the script to scope the whitespace-collapse regex strictly to text captured from `className="..."` attribute matches, leaving surrounding code untouched. Lesson: a "safe-looking" global regex substitution still needs its blast radius checked against `git diff --stat` before trusting it, even for a mechanical/scripted change.
- `supabase.auth.admin.generateLink({type: "magiclink"})`'s resulting `hashed_token` only verified successfully against `supabase.auth.verifyOtp({type: "email", ...})`, not `{type: "magiclink", ...}` (which returned "Email link is invalid or expired") — confirmed by testing both directly against the real hosted Supabase project with a throwaway script before wiring the dev-signin route.
- A custom 1440×900 `resize_window` emulation on the Browser pane (device pixel ratio 1.5) produced a screenshot that visually looked like the itinerary panel was only ~230px wide in a much wider blank page — looked like a real layout bug. Cross-checked with `getBoundingClientRect()` via `javascript_tool`, which showed the actual CSS layout was correct (chat 1056px + itinerary 384px = the full 1440px viewport, no bug) — the screenshot capture at that specific custom emulated size was misleading, not the app. Reset to the pane's native "desktop" preset for a trustworthy screenshot instead of chasing a phantom bug.

**Decisions made:**
1. No dark mode — single warm-light theme (see above). If dark mode is ever wanted, that's a deliberate future decision, not a default to fall back into.
2. Visual pass covers every page, including the two internal dashboards, per the user's explicit choice over "user-facing only."
3. Color role assignment: teal = primary interactive/neutral actions and navigation; terracotta = the one important call-to-action per screen (sign-in CTA, "Confirm this schedule," "Finalize trip") plus attention/warning banners; navy = text/structure; sand = backgrounds/surfaces. Kept standard `red-600` for error text rather than forcing it into the warm palette — a universally understood error signal shouldn't be re-themed for consistency's sake alone.

**Verification:** `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (323/323) all clean. Live-verified in the browser via a throwaway dev-signin route (admin-generated OTP session, same pattern as Phase 7/8 live-verification sessions — removed after use, not committed, along with its temporary `proxy.ts` public-path exception): drove a real trip end to end (intake → flight candidates → confirm → hotel candidates → confirm → activities proposal → confirm schedule → finalize) and screenshotted every state to confirm the palette, fonts, and card/button treatments render as intended, including hover/disabled states. Deleted the throwaway dev-signin auth user afterward (cascades to its trip data via the existing `on delete cascade` FK).

**What I noticed but didn't fix:** The confirmed itinerary's narrative text (`itineraryText`, written by the Itinerary Writer agent) renders literal `**bold**`/`*italic*` markdown asterisks instead of formatted text (`itinerary-panel.tsx`'s `whitespace-pre-wrap` block never parses it as markdown). Out of scope for a color/typography pass — flagged as a separate follow-up task (`task_2a5c1559`) rather than silently expanding this session's scope to add a markdown-rendering dependency.

**Next up:** Final README pass + demo walkthrough recording — the last Phase 9 item.

---

## 2026-09-17 — Render the Itinerary Writer's markdown instead of raw asterisks

**What I built:** `src/domain/markdown-lite.ts` (new) — a minimal, deterministic parser for the narrow Markdown subset the Itinerary Writer agent's (`src/agents/itinerary-writer.ts`) unconstrained free-text output actually uses in practice (plain paragraphs, `**bold**`/`*italic*` emphasis, occasional bullet/numbered lists, occasional `#`-style headings) — not a general CommonMark implementation. `parseInline` tokenizes a line into bold/italic/text segments; `parseMarkdownLite` groups lines into paragraph/heading/list blocks, splitting on blank lines. 14 unit tests covering both, including two cases found by reasoning about ambiguity rather than observation alone: a whole-line `*emphasis*` (a single-asterisk "day header" style the Writer actually produces) must not be mistaken for a bullet, since a real bullet requires a space right after the `-`/`*` marker.

Wired into `app/app/_components/itinerary-panel.tsx`: a new local `ItineraryText` component renders `parseMarkdownLite`'s blocks to real JSX elements (`<strong>`/`<em>`/`<ul>`/`<ol>`/`<h3-5>`/`<p>`, all serif-styled headings to match the Phase 9 palette) in place of the old `<div className="whitespace-pre-wrap">{itineraryText}</div>` that printed raw `**`/`*` characters. Scoped to this one render site only, per the ask.

**Why:** Found live while verifying the Phase 9 visual design pass — a real confirmed itinerary showed literal `**Flights**`/`*Monday, October 6*` asterisks instead of formatted text. Flagged as a separate follow-up rather than folded into that pass (color/typography vs. content rendering are different concerns), then asked for directly.

**Decisions made:** Hand-rolled a narrow parser instead of adding a Markdown library (e.g. `react-markdown`) — `package.json` has only 7 runtime dependencies, all load-bearing SDKs/framework, and the input is one agent's fairly bounded prose style, not arbitrary user-supplied Markdown, so a general parser's much larger transitive dependency tree (remark/unified/mdast) wasn't justified for this. Renders to real React elements (never `dangerouslySetInnerHTML`), so this introduces no HTML-injection surface regardless of what the model outputs — its text content is always escaped by React the same as any other JSX child.

**What didn't work / dead ends:** None — the parser's test suite passed on the first real run, and the live-verification pass (see below) matched what the unit tests predicted with no surprises.

**Verification:** `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (337/337, up from 323) all clean. Live-verified via the same throwaway dev-signin pattern as the visual design pass (admin-generated OTP session, removed after use along with its `proxy.ts` exception, not committed): drove a real trip through intake → flight/hotel confirmation → activities confirmation, and the resulting itinerary rendered real bold section headings ("Flights," "Hotel," "Day-by-Day Itinerary"), a real bullet list for outbound/return flights, and real italicized day headers — no stray asterisks anywhere. Deleted the throwaway auth user afterward.

**Next up:** Final README pass + demo walkthrough recording — the last Phase 9 item, unchanged from the prior entry.

---

## 2026-09-17 — Phase 9 slice 4: final README pass, demo walkthrough script (Phase 9 complete)

**What I built:**
- Rewrote `README.md` from a two-paragraph placeholder into the full portfolio narrative `PROJECT_BRIEF.md` §21 asks for: why a deterministic workflow was chosen over an autonomous swarm, what's kept out of the LLM (arithmetic, authorization, state transitions) and why, how requirements/preferences/decisions stay three separate data types, how inventory grounding is enforced structurally (not by prompt instruction), how impossible constraints fail loudly, how the feasibility engine works, how state versioning/approval gates finalization, what an eval actually changed (the `over_budget_request` gap), and what telemetry revealed (the real 45.5%-cost/no-latency-benefit prompt-caching result from Phase 8 slice 6). Added a "what would change for live providers or real booking" section (linked to the architecture diagram's matching section), a known-limitations summary (pointing at `docs/IMPLEMENTATION_PLAN.md` §5 rather than duplicating it), a "notable trade-offs and dead ends" section pulling the most narratively interesting real history from this log (the Docker/AF_UNIX saga, the one-shot-to-stepwise-chain redesign, the standing post-phase-review practice and what it caught), a tech-stack summary, local setup instructions, and a "where things live" table linking every other doc in the repo.
- `docs/DEMO_WALKTHROUGH.md` (new) — a recording beat-sheet (not a verbatim script) covering every §21 narrative point in actual demo order: positioning, a real ambiguous-request golden path through to a finalized trip, a revision, an impossible-constraint case, the two observability dashboards (naming the real caching result live), what an eval caught, and a close naming exactly what would need to change before this touches real providers or money. Includes a "2 minutes only" fallback cut. The actual screen recording is a manual step for the user — a coding agent can prepare the script but not perform the recording itself.
- Fixed a real doc-accuracy gap found while writing the setup section: `.env.local.example` was missing `VOYAGE_API_KEY` even though `src/retrieval/providers/voyage-embedding-client.ts` requires it — every retrieval/embedding call would fail for anyone following the example file literally.

**Why:** The last two Phase 9 checklist items (`docs/IMPLEMENTATION_PLAN.md` §1) — with this, all of Phase 9 is checked off except the recording itself, which isn't something this agent can do.

**Decisions made:** Split "final README pass" and "demo walkthrough recording" into two separately-tracked checklist items rather than one, since the first is fully done and the second is genuinely blocked on a human action (operating a screen recorder), not on anything left to build or write.

**What didn't work / dead ends:** Attempted to embed real screenshots of the redesigned UI in the README for a stronger portfolio first impression, but found no available tool path to persist a Browser-pane screenshot to a file on disk from this session — the screenshot tool returns an inline image for this conversation, not a saved file. Left the README screenshot-free rather than working around this with something hacky; the demo recording (once made) covers the same ground visually.

**Verification:** `npx tsc --noEmit`, `npm run lint`, `npm test` (337/337) all clean (docs-only changes, re-verified per standing discipline). Manually checked every internal Markdown link/anchor added to `README.md` (`PROJECT_BRIEF.md#32-...`, `docs/architecture/architecture-diagram.md#what-would-change-...`, etc.) against the actual heading text in each target file rather than assuming GitHub's anchor-slugging matched what I wrote.

**Next up:** Phase 9 is functionally complete pending the user recording the demo walkthrough. No specific next phase was planned beyond Phase 9 in `docs/IMPLEMENTATION_PLAN.md` — revisit the tracked open items in §5 (the budget-ceiling-at-finalize gap is the highest-value one) if further work is wanted, or treat the project as portfolio-ready as-is.

---

## 2026-09-17 — Enforce the §9.1 budget-ceiling guardrail at finalize time

**What I built:** Closed the highest-priority tracked open item from `docs/IMPLEMENTATION_PLAN.md` §5 — `finalizeTrip` (`app/app/actions.ts`) previously passed `guardrailsPassed: true` to `advanceOrThrow` unconditionally, so a trip could finalize arbitrarily over its stated budget ceiling with nothing ever checking `calculateBudget`'s `CEILING_EXCEEDED` violations.

Asked the user which of two designs to build, since §9.3 explicitly requires "cannot be exceeded without explicit user override," not just a block: they chose the explicit-override path over a plain hard block. Implementation:
- `finalizeTrip` now reads the trip's confirmed `budget` decision, computes its violations, and logs a `budget_ceiling_at_finalize` domain_validation guardrail event every time (pass, block, or override — not just failures, matching how every other guardrail here is logged for the §13.3 "which guardrail fires most" dashboard to be meaningful).
- If there's a violation and the caller hasn't set the new `overrideBudgetCeiling` flag, it returns `{requiresBudgetOverride: true, violations}` instead of advancing the workflow — no partial state change, nothing to unwind.
- `app/app/_components/itinerary-panel.tsx`'s "Finalize trip" button now handles that result by showing a warning banner with the real violation message and "Finalize anyway"/"Cancel" — visually and structurally the same pattern the existing `pendingCascade` cascade-warning banner already established, not a new interaction paradigm.
- `evals/cases/scenarios.ts`'s `finalizeTrip` mirror (the real Server Action needs a cookie session the eval harness doesn't have) was updated to match exactly, and the `over_budget_request` case — previously a documented, expected-to-fail `knownGap` case — now asserts the corrected behavior: blocked without override, finalized with it.

**Why:** The single highest-priority item on the open-items list (`docs/IMPLEMENTATION_PLAN.md` §5) — a real correctness gap where the app's own core guardrail promise ("budget cannot be exceeded without override") silently didn't hold.

**Decisions made:** Explicit-override checkbox/banner over a hard block, per direct user choice — matches the brief's literal wording and reuses an interaction pattern (a warning banner with a "continue anyway" affordance) the app already has, rather than a bare error message with no path forward.

**What didn't work / dead ends:** None.

**Verification:** `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (337/337) all clean. Ran the real `npm run eval:scenarios` suite against the live Anthropic/Supabase stack: **10/10 passed**, including `over_budget_request` for the first time (previously the one documented `knownGap` failure) — a real $500-ceiling trip against a real ~$4,954 total was correctly blocked without an override and correctly finalized with one. Also live-verified through the actual browser UI via the standard throwaway dev-signin pattern (removed after use, not committed): built a real over-budget trip end to end, saw the real violation message ("Estimated total $4,953.90 exceeds the $500.00 ceiling by $4,453.90") in the warning banner, clicked "Finalize anyway," and confirmed "Trip finalized" appeared. Deleted the throwaway auth user afterward.

**Next up:** Continuing down the tracked open-items list (`docs/IMPLEMENTATION_PLAN.md` §5) in priority order, per the user's request to go one at a time.

---

## 2026-09-17 — Make `startTrip` idempotency-keyed

**What I built:** Closed the next item on the tracked open-items list — `startTrip` (`src/workflow/controller.ts`) had no idempotency protection, so a lost response followed by a client retry with no `tripId` yet (a double form submit, or a network-level resend) could create a second, orphaned trip with its own genesis state and workflow run. Deliberately left open since Phase 3 until a real client made double-submission an actual risk; the chat UI (Phase 7) now is that client.

- `supabase/migrations/0008_trips_idempotency.sql` — adds a nullable `trips.correlation_id` column plus a partial unique index (`where correlation_id is not null`), the same pattern `trip_state_versions_trip_correlation_unique` (migration 0003) already established. Applied to the hosted project via `supabase db push --db-url` (the user supplied the DB password for this session).
- `src/repositories/trips.ts` — `NewTrip.correlationId` (optional), `createTrip` persists it, new `getTripByCorrelationId`.
- `src/workflow/controller.ts`'s `startTrip` — checks for a prior trip by correlation ID before creating one; if a concurrent caller wins the race, catches the loser's `23505` and re-fetches the winner instead of erroring (mirroring `getOrCreateActiveWorkflowRun`'s established check-then-insert-then-refetch-on-conflict pattern). `correlationId` is optional — a caller with no stable request identity to key on (a script, an eval) just gets the old, non-idempotent behavior, unchanged.
- `app/app/actions.ts`'s `sendMessage` takes an optional `startCorrelationId`, passed through to `startTrip` only on the new-trip path; also fixed a related correctness detail while wiring this — `sessionId` is now read back from the *returned* trip (`trip.session_id`) rather than the locally-created `session.id`, since an idempotent replay's returned trip belongs to the *original* session, not whatever new session this retry just (now orphaned) created.
- `app/app/_components/chat-panel.tsx` generates a client-side correlation ID lazily (once, via a `useRef`) and reuses it across every send attempt until a trip exists — after that, sends carry a real `tripId` and never touch `startTrip` again.
- 3 new unit tests in `src/workflow/controller.test.ts` (mocked, matching the file's existing pattern): replay-by-correlation-id, race-loser-refetches-winner, and a non-conflict error still propagates unchanged.

**Why:** Second item on the tracked open-items list (`docs/IMPLEMENTATION_PLAN.md` §5), continuing down it in priority order per the user's request.

**Decisions made:** `correlationId` is opt-in on `NewTrip`/`startTrip`, not a required argument — matches how `advanceTrip`'s idempotency is mandatory (it always has a real caller with a natural key) while `startTrip`'s isn't (some callers, like eval scripts creating throwaway trips, have no meaningful "same logical attempt" concept and shouldn't be forced to invent one).

**What didn't work / dead ends:** None — the fix worked as designed on the first live test.

**Verification:** `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (340/340, up from 337) all clean. Applied the migration to the real hosted Supabase project and confirmed the new column/index live via a direct query. Live-verified the actual idempotency guarantee with a throwaway script (not committed, cleaned up after): two genuinely concurrent `startTrip` calls with the same correlation ID against the real hosted DB resolved to exactly one trip row (confirmed both by comparing the returned trip IDs and by counting rows with that correlation ID), and a third, sequential replay call also returned the same trip rather than erroring or creating a new one.

**Next up:** Continuing down the tracked open-items list — a retry of a whole orchestrator turn isn't yet idempotent across its non-transition writes (the item right after this one, same underlying "no real caller yet" reasoning, now also closable for the same reason).

---

## 2026-09-17 — Guard chat-message writes against a retried intake turn

**What I built:** Closed the third item on the tracked open-items list, after a scope refinement that changed what "fixed" actually means here. The original wording ("a retry isn't idempotent across `appendMessage`/`agent_runs`/`tool_calls`/`guardrail_events`") assumed all four should be deduplicated the same way `advanceTrip`'s mirrors are. Looking at the real code with the user's "guard each write, still let the LLM re-run" scope decision in hand, that assumption doesn't actually hold for three of the four: if a genuine retry really does call the LLM a second time, that's a real, separately-costed event, and deduplicating `agent_runs`/`tool_calls`/`guardrail_events` would make the cost/observability dashboards *under-report* real spend — arguably a worse outcome than the current over-counting. Flagged this to the user before building anything, and the fix ended up scoped to the one place a duplicate is unambiguously wrong regardless of that: the chat transcript.

- `supabase/migrations/0009_messages_correlation_id.sql` — adds `messages.correlation_id` (nullable, no unique index — matching `trip_events`/`workflow_steps`'s existing correlation_id columns, since chat history is a best-effort mirror here, not the strictly-consistent source of truth `trip_state_versions` is). Applied to the hosted project via `supabase db push --db-url`.
- `src/repositories/messages.ts` — `NewMessage.correlationId` (optional), `appendMessage` persists it, new `findMessageByCorrelationId`.
- `src/workflow/intake-orchestrator.ts` — new `appendMessageOnce` helper (checks `findMessageByCorrelationId` before writing, no-ops if a message with that correlation ID already exists) replaces both direct `appendMessage` calls (user message, assistant message). Each role gets its own derived correlation ID (`deriveCorrelationId(correlationId, "message:user"/"message:assistant")`, the same sub-correlation pattern this file already uses for chained state transitions) — necessary since one turn writes two messages under one base correlation ID, and they need independent dedup.
- `app/app/actions.ts`'s `sendMessage` takes a new `turnCorrelationId`, passed straight through to `processIntakeTurn`. `app/app/_components/chat-panel.tsx` generates a fresh one via `crypto.randomUUID()` at the top of every `handleSubmit` — establishing the seam that makes the guard above actually reachable, the same gap `startTrip` had before its own fix earlier this session.
- 2 new unit tests in `src/workflow/intake-orchestrator.test.ts`: both messages write with distinct per-role correlation IDs when neither exists yet; a message whose correlation ID already exists is skipped without also skipping the other role's write.

**Why:** Third item on the tracked open-items list (`docs/IMPLEMENTATION_PLAN.md` §5), continuing down it per the user's request. Also confirmed live that the three stepwise-chain files (`flight-step.ts`/`hotel-step.ts`/`activities-step.ts` — the successors to the old `search-orchestrator.ts` this item's original wording also named) never call `appendMessage` at all, so the chat-duplication concern never applied to them; their `agent_runs`/`tool_calls`/`guardrail_events` writes are left alone for the same real-cost reasoning as intake's.

**Decisions made:** Scoped the fix to `messages` only, not `agent_runs`/`tool_calls`/`guardrail_events`, per the user's explicit choice after seeing the real trade-off (an honest cost dashboard vs. literal adherence to the item's original wording).

**What didn't work / dead ends:** None.

**Verification:** `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (342/342, up from 340) all clean. Ran the real `npm run eval:scenarios` suite (10/10 passed, no regressions from touching `intake-orchestrator.ts`, a heavily-exercised file). Applied the migration to the real hosted project and live-verified the actual behavior with a throwaway script (not committed, cleaned up after): two `processIntakeTurn` calls with the same correlation ID against the real Anthropic API persisted exactly 2 messages total, not 4 — confirmed by listing the session's messages directly. Also confirmed, as expected from this design, that the persisted assistant message reflects attempt 1's text even though attempt 2's live-returned response differed (a real second LLM call did happen and produced different output) — documented as a known, accepted asymmetry rather than a surprise.

**Next up:** Continuing down the tracked open-items list, in priority order.

---

## 2026-09-17 — Session close: Phase 9 complete, three open items closed

**What changed this session:** Completed Phase 9 (portfolio polish) end to end, then started working down the tracked open-items backlog (`docs/IMPLEMENTATION_PLAN.md` §5) one item at a time, per the user's explicit request. In order:

1. Resolved the telemetry retention/redaction policy question and wrote up **ADR-006** (evaluation strategy) and **ADR-007** (observability) as standalone files.
2. Added the **architecture diagram** (`docs/architecture/architecture-diagram.md`, two Mermaid diagrams).
3. Did the **visual design pass** — a warm editorial travel palette (sand/navy/teal/terracotta), Fraunces display serif for headings, applied across every page.
4. Fixed the itinerary panel rendering raw `**markdown**` asterisks (found live while verifying #3) — a hand-rolled minimal Markdown parser (`src/domain/markdown-lite.ts`), not a new dependency.
5. Did the **final README pass** (full §21 portfolio narrative) and wrote a **demo walkthrough script** (`docs/DEMO_WALKTHROUGH.md`) — the recording itself is a manual step for the user.
6. Enforced the **§9.1 budget-ceiling guardrail at finalize time** — an explicit-override banner, per the user's choice between that and a hard block.
7. Made **`startTrip` idempotency-keyed** (`trips.correlation_id`, migration 0008).
8. Guarded **chat-message writes against a retried intake turn** (`messages.correlation_id`, migration 0009) — scoped narrower than the original tracked wording after a mid-implementation trade-off discussion with the user (a genuine retry's second real LLM call is left un-deduplicated in `agent_runs`/`tool_calls`/`guardrail_events`, since that's real, separately-costed telemetry, not a duplicate).

**Files changed:** Too many individual files to list here usefully — see the eight commits from `c676f08` through `c6ead0c` (`git log`) for the full per-slice diffs; each commit message and this file's corresponding entries above give the reasoning per change.

**Database migrations added:** `0008_trips_idempotency.sql` (`trips.correlation_id` + partial unique index), `0009_messages_correlation_id.sql` (`messages.correlation_id`, no unique index). Both applied to the real hosted Supabase project this session (the user supplied the DB password once, reused for both pushes).

**Tests run and results:** `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` all clean after every slice (342/342 unit tests passing at session end, up from 323 at session start). `npm run eval:scenarios` run live against the real Anthropic/Supabase stack twice this session (10/10 passed both times, including `over_budget_request` passing for the first time ever). Every DB-touching fix was also live-verified with a throwaway script or browser session against the real hosted project, cleaned up after each time.

**Decisions made this session** (each asked of the user rather than picked unilaterally): telemetry retention/redaction approach; visual design palette and scope (all pages); budget-ceiling fix design (explicit override vs. hard block); retry-idempotency fix depth (guard-each-write vs. full result caching, then further narrowed to messages-only after surfacing the cost-dashboard trade-off).

**Known limitations / assumptions:** Unchanged from what's tracked in `docs/IMPLEMENTATION_PLAN.md` §5, minus the three items closed above. See that section for the full, current list with rationale for each.

**Next recommended task — resume point for a fresh session:** Continue down `docs/IMPLEMENTATION_PLAN.md` §5's **"Known bugs/gaps — unresolved"** list, one item at a time, per the user's standing request this session ("let's go down the list and tackle one by one"). As of this session's end, in the order last presented to the user:

*Smaller, self-contained (suggested next):*
- Curator's malformed tool calls aren't logged at the same guardrail layer as Intake's (line ~445 in that section as of this entry).
- The `"blocked"` workflow state is unreachable (latent, not an active bug).
- One-way trips aren't supported (needs a small product decision on default trip length — ask before implementing).
- Budget's night-count can drift ~1 day from actual hotel-stay dates.
- Flight→hotel cascade's "dates changed" branch is only unit-tested, not live-verified (seed-data limitation — may not be practically closable without adding more seed data).

*Bigger, more architectural (expect a scoping conversation before implementing, same pattern as this session's budget/idempotency items):*
- Destination identity via plain city-name string instead of a foreign key.
- Activity retrieval's overfetch heuristic (fine at current scale; low priority).
- Hotels model one room type per property (no multi-room-type bookings).
- Itinerary scheduling is a naive first-fit placer, not an optimizer.
- Only the top 5 candidate combinations are tried before declaring infeasibility.

*Out of Phase 8 entirely, needs new test infrastructure (RLS/JWT sessions or fault injection) — bigger lift, probably lowest priority for one-at-a-time work:* the remaining 10 of 16 §19 eval scenarios, and prompt-injection/malformed-inventory adversarial cases.

A fresh session should re-read `docs/IMPLEMENTATION_PLAN.md` §5 directly rather than trusting this list verbatim, since it's copied here only as a resume pointer — that file is the live, authoritative source and may have shifted if anything changes before the next session starts.

Also outstanding, unrelated to the open-items list: the user still needs to actually record the demo walkthrough (`docs/DEMO_WALKTHROUGH.md`) — not something a coding agent can do.

---

## 2026-09-17 — Open-items list, items 1-5: Curator guardrail layering, dead `"blocked"` state, one-way trips, budget night-count, cascade live-verification

**What I built:** Worked down `docs/IMPLEMENTATION_PLAN.md` §5's tracked list from item 1 through item 5, per the user's request to go "1 to 5." Each item's full rationale/decisions/verification is in §5 itself (moved from "unresolved" to "resolved" as each closed) — this entry summarizes across all five plus the two extra findings items 4/5 surfaced along the way.

1. **Curator malformed-tool-call guardrail layering.** Added `errorKind?: "malformed" | "reference_check_failed"` to the shared `ToolCallLogEntry` (`src/agents/model-client.ts`); `curator.ts` now tags each of its error branches. `activities-step.ts` logs a `tool_call_schema_validation`/`output_validation` guardrail event for `errorKind: "malformed"` entries only, mirroring Intake's equivalent loop, while excluding `reference_check_failed` entries so the existing `curation_reference_check` domain_validation event isn't double-counted under two layers.
2. **Unreachable `"blocked"` workflow state.** Confirmed via repo-wide grep it was genuinely dead (only referenced by its own declaration and one display-label map). Deleted from `WORKFLOW_STATES` (`state-machine.ts`) and `app/internal/product-metrics/page.tsx`'s `STAGE_LABELS` rather than leaving the latent trap in place.
3. **One-way trips.** Asked how far to take this; chose "ask for a return date" over a hard-coded default or clarification-only backstop. `returnDate` is now in `REQUIRED_FOR_READY` (`src/domain/extraction.ts`) — a one-line change, since the Intake agent's prompt already renders `REQUIRED_FOR_READY` dynamically, so its existing generic clarification flow asks for a return date with no prompt engineering needed. This made the dedicated `OneWayTripNotSupportedError` check in `flight-step.ts` truly unreachable (completeness now guarantees `returnDate`'s presence before that line runs), so it and the error class (`step-shared.ts`) were deleted as dead code rather than kept as an inert backstop. Also refreshed `README.md`'s known-limitations section, which had gone stale on two unrelated points (budget-ceiling, orchestrator-turn idempotency) from earlier-session fixes never reflected there — fixed while already touching the line my own change invalidated.
4. **Budget night-count drift.** Turned out to already be resolved, structurally, by the stepwise chain redesign: `activities-step.ts` computes `nights` from `deriveHotelStayDates`'s real derived check-in/check-out (the confirmed flights' actual local arrival/departure dates), not the raw requirement dates the old retired `assembleItinerary` used. No code change needed — just confirming and closing the tracked item.
5. **Cascade live-verification.** The seed data only had one Lisbon→New York return flight, so the "dates differ → hotel invalidated" cascade branch had never been exercised against real data. Asked the user whether to close this properly now (needs DB write access); chose yes. `supabase/migrations/0010_second_lisbon_return_flight.sql` added a second return leg a week later; a throwaway script (reusing `evals/lib/scenario-harness.ts`'s existing throwaway-user infrastructure) drove a real trip through confirm-flight → confirm-hotel → revise-returnDate → re-confirm-flight-with-different-derived-dates, and confirmed live that the previously-confirmed hotel decision was retired.

**Extra findings along the way (not on the original list), surfaced and asked about before touching anything:**
- Checking item 4 turned up that `src/domain/combinations.ts` (`assembleCandidateCombinations`) and `src/validation/inventory-references.ts` (`validateInventoryReferences`) — both Phase 2 deterministic services the old one-shot pipeline depended on — were fully dead code, missed when that pipeline was retired in the stepwise chain redesign (slice 3). Confirmed via repo-wide grep (only their own test files referenced them). The `validateInventoryReferences` finding sounds alarming (a §9.4 hallucination guardrail, unused!) but isn't a coverage regression: each stepwise-chain step module already reimplemented an equivalent inline check (`Invalid*SelectionError`) — this was reuse/duplication debt, not a missing guardrail. Asked the user for each finding separately (delete now / track / — for the duplication — a bigger dedup refactor); chose delete-now for both. Removed `combinations.ts`/`combinations.test.ts` and `inventory-references.ts`/`inventory-references.test.ts` (the now-empty `src/validation/` directory disappears from the repo naturally).

**Why:** Continuing down the tracked open-items list one at a time, per the user's standing request from the previous session's close.

**Decisions made (each asked, not assumed):** one-way-trip handling depth (ask-for-a-return-date over a default or backstop-only); whether to delete the two newly-found dead modules vs. track vs. refactor (delete, for both); whether to spend DB-write/live-verification effort closing item 5 vs. leave it as the explicitly-lowest-priority tracked item (close it).

**What didn't work / dead ends:** None — every fix worked as designed on first verification, including the live cascade check.

**Verification:** `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` all clean after every item (325/325 at session end — down from 344 at the start of this stretch: +2 from the Curator guardrail tests, then -19 from deleting `combinations.test.ts`/`inventory-references.test.ts`, then a few net swaps from the one-way-trip test updates). Applied migration `0010_second_lisbon_return_flight.sql` to the real hosted Supabase project via `supabase db push --db-url`. Live-verified item 5's cascade fix with a throwaway script (not committed, cleaned up after, own throwaway auth user torn down via the harness's existing `cleanup()`). Items 1/2/3/4 verified via the unit/type/lint/build suite only — none touched a live integration surface in a way existing coverage didn't already exercise (item 1 is pure classification logic over an already-tested write path; item 2 removed a value nothing ever produced; item 3's required-field change is exercised by the same `checkRequirementsComplete`/Intake-orchestrator tests every other required field already goes through; item 4 was a no-op confirmation). Not re-run against the real Anthropic API: `npm run eval:intake`'s `ambiguous_missing_budget` case and `npm run eval:scenarios`'s base scenario are both unaffected by the `returnDate` requirement change (the former doesn't assert an exhaustive missing-fields list, the latter's message already states a return date) but a manual re-run of both would be good confirmation before treating item 3 as fully proven against the live model.

**Known limitations / assumptions:** Unchanged from what's tracked in `docs/IMPLEMENTATION_PLAN.md` §5, minus the five items (plus two extra findings) closed above. See that section for the full, current list.

**Next recommended task:** Continue down `docs/IMPLEMENTATION_PLAN.md` §5's remaining unresolved items — the destination-identity-via-city-name-string gap (now affecting the three step modules' inline checks rather than the deleted shared function), the activity-retrieval overfetch heuristic, the one-room-type-per-hotel limitation, the naive scheduler, and the top-5-combinations cap — each flagged as needing a scoping conversation before implementing, same pattern as this session's items. The 10 un-automated §19 eval scenarios remain the lowest-priority, biggest-lift item (needs new RLS/JWT or fault-injection test infrastructure).

---

## 2026-09-17 — Destination identity: `destination_id` FK replacing free-text matching

**What I built:** Closed the destination-identifier-space gap from the open-items list — chosen by the user as the next architectural item to tackle, out of the four remaining ("bigger, more architectural" bucket). Checking the also-just-closed "budget night-count drift" item first turned up that its underlying pipeline (`assembleItinerary`/`assembleCandidateCombinations`) was fully dead code (nothing called it since the stepwise chain redesign retired the old one-shot pipeline) — deleted `src/domain/combinations.ts`/`combinations.test.ts` and `src/validation/inventory-references.ts`/`inventory-references.test.ts` (also dead; each step module had already reimplemented an equivalent inline reference check, so this was reuse/duplication debt, not a guardrail regression) after asking the user, who chose delete-now for both. The "only top 5 candidate combinations tried" item turned out moot for the same reason.

Given the destination-identity item's real scope was bigger than its one-line tracked summary (schema + every repository + all three step modules + a real decision on whether/how to resolve a trip's destination to an id), used `EnterPlanMode`: two parallel Explore agents traced every consumer of the free-text `destination` value and the exact schema/seed-data shape, then a concrete plan was drafted and approved before any code was written.

**Built:**
- `supabase/migrations/0011_destination_ids.sql` — `destination_id`/`origin_id` on `flights` (both nullable — a flight leg's endpoints aren't both always a seeded destination; a traveler's home city like "New York" has no `destinations` row at all), `destination_id` on `hotels`/`activities` (`not null` after backfill — every hotel/activity's destination is always real seed inventory, and the constraint itself would fail the migration loudly if backfill ever missed a row). Backfilled by joining the existing text columns against `destinations(name, inventory_version)` together, not name alone — correctly resolves the deliberately-seeded stale-v0 fixtures to their own stale destination row rather than the current one. The existing text columns are kept, not dropped (UI display, and the only field for a flight leg's non-seeded-city side). `match_activities` (the retrieval SQL function) now filters by `filter_destination_id uuid` instead of `filter_destination text` — needed `drop function`+`create function` since a parameter type changed, not `create or replace`.
- `src/repositories/destinations.ts` — `getDestinationByName` (existed since Phase 2, never had a real caller) had its `.maybeSingle()` crash risk fixed while giving it one: switched to `.limit(2)` with an explicit branch, throwing a new `AmbiguousDestinationNameError` on a genuine multi-row match instead of letting an opaque PostgREST error propagate.
- `src/workflow/step-shared.ts` — new `resolveTripDestination` (calls `getDestinationByName` once) and `UnknownDestinationError`, shared by all three step modules rather than each duplicating the resolve-and-throw logic.
- `flight-step.ts`/`hotel-step.ts`/`activities-step.ts` — every `propose*`/`confirm*` function resolves the destination once via `resolveTripDestination`, passes `destinationId` into `findFlights`/`findHotels`/`retrieveActivities` instead of the raw string, and every inline guardrail check (`Invalid*SelectionError`) now compares by id. Flight's own checks got more nuanced than a blanket swap: a flight leg's *destination-side* endpoint differs by direction (an outbound leg's `destination` column, a return leg's `origin` column), so `FlightSearchFilter` gained independent `origin`/`originId`/`destination`/`destinationId` fields — callers supply whichever of each pair actually applies to a given search.
- `app/app/actions.ts`/`app/app/_components/itinerary-panel.tsx` — `friendlyStepErrorMessage` gained `UnknownDestinationError`/`AmbiguousDestinationNameError` handling. Also fixed a coupled finding surfaced while tracing the forward auto-chain: `sendMessage`'s `after()` call to `proposeCurrentChainStep` only `console.error`'d a failure, with no user-facing signal at all — unlike the revision path, which already got a `chain_revision_failed` trip_event fix in an earlier session. Without also fixing this, a new `UnknownDestinationError` would have fallen into the same invisible-failure hole. Mirrored the exact existing pattern: a new `chain_propose_failed` trip_event, handled by the same `needsAttention` Realtime listener.
- New tests directly prove the scenario the whole fix is about: a candidate sharing the trip's destination *name* but a different destination *id* is now correctly rejected in `flight-step.test.ts`/`hotel-step.test.ts`/`activities-step.test.ts` — the exact case a plain string comparison would have wrongly accepted.

**Why:** Tracked since Phase 2 (2026-09-16): `flights`/`hotels`/`activities` matched `destination` against `destinations.name` as free text, with no FK and no uniqueness guarantee on that name. Chosen by the user as the next item off the architectural backlog.

**Decisions made (asked, not assumed):** full-fix depth over a partial schema-only fix or just a unique constraint; resolve the destination to an id lazily inside each step rather than at intake time (intake-time resolution would need new deterministic workflow-state plumbing or exposing the destinations catalog to the Intake agent — a separate feature, not a data-integrity fix, while lazy resolution closes the same gap and still surfaces an unresolvable destination in the same chat turn since `sendMessage` auto-chains immediately); fix the coupled forward-chain silent-failure gap in the same pass rather than tracking it separately; delete both newly-found dead modules (`combinations.ts`, `inventory-references.ts`) rather than track or refactor them.

**What didn't work / dead ends:** The first attempt at the live spot-check script passed a plain string as `trip_events.correlation_id`, which is a `uuid` column — failed with a clear Postgres type error (`22P02`), fixed by using the existing `deriveCorrelationId` helper instead of inventing an ad hoc key.

**Verification:** `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (332/332, up from 325) all clean. Applied `0011_destination_ids.sql` to the real hosted Supabase project via `supabase db push --db-url` — the `not null` constraints on `hotels`/`activities`.`destination_id` succeeded, confirming backfill hit every row. Live-verified with a throwaway script (not committed, cleaned up after, reusing `evals/lib/scenario-harness.ts`'s throwaway-user infrastructure): a full flight→hotel→activities-retrieval flow succeeded with id-based filtering throughout (2 real flight candidates, 3 real hotel candidates, activities retrieval reached before stopping at a deliberately-fake Curator model client); a trip stating "Atlantis" as its destination correctly threw `UnknownDestinationError`; a `chain_propose_failed` trip_event was written and read back correctly. Not independently re-verified via browser: the full authenticated Server Action path (Next's `after()` mechanism itself) is the same already-proven infrastructure the `chain_revision_failed` fix established in an earlier session, just a second call site — a full browser click-through wasn't judged worth the setup cost for that specific piece.

**Known limitations / assumptions:** Unchanged from what's tracked in `docs/IMPLEMENTATION_PLAN.md` §5, minus this item and the two dead-module deletions. See that section for the full, current list.

**Next recommended task:** Three architectural items remain on the open-items list, each flagged as needing its own scoping conversation: the activity-retrieval overfetch heuristic, the one-room-type-per-hotel limitation, and the naive (first-fit, non-optimizing) activity scheduler. The 10 un-automated §19 eval scenarios remain the lowest-priority, biggest-lift item.

---

## 2026-09-17 — Activity retrieval's overfetch heuristic: widen-and-retry instead of a single fixed overfetch

**What I built:** Closed the next architectural item off the open-items list, chosen by the user. `retrieveActivities` (`src/retrieval/activities-retrieval.ts`) previously overfetched by a single fixed `OVERFETCH_FACTOR` (2x `topK`) before its closed-days post-filter, then accepted whatever survived — if more than `topK * 2` of the closest semantic matches for a destination happened to be closed on the excluded day(s), it could return fewer than `topK` results even when enough open activities existed further down the similarity ranking.

Replaced the one-shot overfetch with a widen-and-retry loop: if the post-filter under-fills `topK` *and* the last fetch returned exactly as many rows as requested (meaning there could be more beyond the current `LIMIT`), it doubles `matchCount` and retries against the same pre-computed query embedding (no extra Voyage API call — the embedding is computed once up front). It stops as soon as a fetch returns fewer rows than requested, since that means every matching activity for the destination has already been seen and retrying would just repeat the same query. A new `MAX_MATCH_COUNT` (200) is a defensive ceiling on worst-case query cost, not something the doubling strategy needs to terminate — it already converges in a handful of iterations at any catalog size a real deployment would plausibly have.

**Why:** Next item off the tracked architectural backlog (`docs/IMPLEMENTATION_PLAN.md` §5), chosen by the user. Smallest of the three remaining architectural items, and the one explicitly framed in its own tracked text as a fix rather than a design trade-off ("the fix would be re-querying with a larger limit when the post-filter under-fills, rather than a single fixed overfetch").

**Decisions made:** None requiring a user decision this time — the tracked item's own text already specified the fix; this was a straightforward implementation of it, not a design fork.

**What didn't work / dead ends:** None.

**Verification:** `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (335/335, up from 332) all clean. 3 new tests in `activities-retrieval.test.ts` prove the actual scenario the fix is about (a first batch of all-closed results followed by a wider fetch reaching the open ones further down the ranking, using a mocked `matchActivities` that simulates a real paginated `LIMIT`), the natural-termination case (a short first batch isn't retried, since it already proves exhaustion), and the `MAX_MATCH_COUNT` ceiling against a pathological all-closed result set. Not live-verified: this project's seed data (a handful of activities per destination) can't reach the retry path at all — a live check would only re-prove the unchanged happy path, not the new widen-and-retry logic itself, which only the synthetic unit tests can actually exercise until a larger catalog exists.

**Known limitations / assumptions:** Unchanged from what's tracked in `docs/IMPLEMENTATION_PLAN.md` §5, minus this item. See that section for the full, current list.

**Next recommended task:** Two architectural items remain, each needing its own scoping conversation: the one-room-type-per-hotel limitation and the naive (first-fit, non-optimizing) activity scheduler. The 10 un-automated §19 eval scenarios remain the lowest-priority, biggest-lift item.

---

## 2026-09-17 — Hotel room availability: `available_rooms` closes half of the room-type gap

**What I built:** Closed the next architectural item, chosen by the user. The tracked item actually bundled two separable gaps: (a) every room group books an identical room type/rate (no room-type variety — can't express "2 doubles + 1 twin"), and (b) nothing checked whether a hotel actually had enough rooms free at all. Asked the user how far to go; chose the availability-only fix over a full room-type catalog, since (b) closes a real correctness gap while (a) is speculative — the tracked item's own text already called it "revisit only if a future phase wants richer room-type preferences," with no concrete requirement driving it.

`supabase/migrations/0012_hotel_available_rooms.sql` adds `hotels.available_rooms int not null default 5` — generous on purpose, since this project's room-group counts are always small (a handful of rooms per party at most), so the backfill doesn't change behavior for any existing flow. A new `roomAvailabilityConstraint` (`src/domain/constraints.ts`) checks `roomGroups.length <= available_rooms`, wired into `hotelHardConstraints` (`step-shared.ts`) right alongside the existing `roomCapacityConstraint` (which only checks *per-room* capacity, not room *count*). Added a scope-boundary docstring to `src/repositories/hotels.ts` documenting the still-open room-type-variety limitation, so it doesn't need rediscovering later.

**Why:** Next item off the tracked architectural backlog (`docs/IMPLEMENTATION_PLAN.md` §5), chosen by the user.

**Decisions made:** Availability-count-only over a full room-type catalog, per the user's explicit choice after seeing the real trade-off (a genuine correctness gap vs. a speculative richness feature with no concrete requirement behind it). Generous default (5) for the backfill rather than trying to guess a "realistic" per-hotel value for seed/demo data.

**What didn't work / dead ends:** None.

**Verification:** `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (337/337, up from 335) all clean. New tests in `constraints.test.ts` (the new constraint directly) and `hotel-step.test.ts` (a hotel whose per-room capacity is fine but that doesn't have enough *rooms* free for every room group is correctly rejected, distinguishing this from the existing capacity check). Applied `0012_hotel_available_rooms.sql` to the real hosted Supabase project via `supabase db push --db-url`. Live-verified with a throwaway script (not committed, cleaned up after): a 6-room-group request against real seed hotels (every one defaulted to `available_rooms=5`) was correctly rejected with `NoViableHotelCandidatesError`, while a more modest 3-room-group request still found viable hotel candidates.

**Known limitations / assumptions:** Unchanged from what's tracked in `docs/IMPLEMENTATION_PLAN.md` §5, minus the availability half of this item — the room-type-variety half remains open and tracked, by explicit choice.

**Next recommended task:** One architectural item remains, needing its own scoping conversation: the naive (first-fit, non-optimizing) activity scheduler. The 10 un-automated §19 eval scenarios remain the lowest-priority, biggest-lift item.

---

## 2026-09-17 — Meal-time scheduling bias: the "lightweight heuristics" tier for the activity scheduler

**What I built:** Closed the last remaining architectural item — the naive, first-fit activity scheduler. The user asked what "naive" actually meant before deciding anything; laid out five options at increasing effort (lightweight heuristics on the same first-fit structure / a repair pass after the first placement / a real optimizer with a scored objective / geographic clustering / leave it as-is), noting geographic clustering is blocked on data regardless of algorithm choice (`activities.location` is a free-text neighborhood string, not coordinates). The user chose the smallest tier.

`src/domain/scheduling.ts`'s `SchedulableActivity` gained an optional `preferredWindows: PreferredWindow[]` — a generic list of minute-of-day ranges. `scheduleActivities` tries each window in order (each as its own full forward pass across the date range) before falling back to the existing unconstrained earliest-fit search — still a single deterministic pass per window, no backtracking, no change to the module's actual algorithmic character. The module stays domain-agnostic on purpose: it has no idea what "food" means, just a list of preferred ranges. `activities-step.ts` supplies the domain knowledge — a `category: "food"` activity gets `[LUNCH_WINDOW (11:30-14:00), DINNER_WINDOW (18:00-21:00)]`.

**Why:** Last item on the tracked architectural backlog (`docs/IMPLEMENTATION_PLAN.md` §5).

**Decisions made:** Lightweight-heuristics tier only, per the user's explicit choice after seeing the effort/payoff breakdown for all five options.

**What didn't work / dead ends:** The first version of a new `scheduling.test.ts` case (the "falls back to the unconstrained search" test) used opening hours (`08:00-10:00`) that, combined with the scheduler's existing default 10:00 start-of-day floor, left literally zero fittable minutes — not a bug in the new code, just a bad choice of test numbers that happened to expose how the *pre-existing* default-start floor interacts with a tight opening window. Fixed by picking opening hours (`06:00-11:00`) that leave room after the 10:00 floor.

**Verification:** `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (341/341, up from 337) all clean. 4 new tests in `scheduling.test.ts` (placement inside a window, trying windows in order, falling back to the unconstrained search, and the window search still respecting `closedDays`/`openingHours`/per-date transfer buffers). No schema change, so no migration — live-verified anyway with a throwaway script (not committed, cleaned up after) against real seed data: a real Lisbon trip's curated activity set included "Alfama Walking Food Tour" (`category: "food"`), and it landed at 13:50 — inside the lunch window, not wherever the first-fit scan happened to land it.

**Known limitations / assumptions:** `docs/IMPLEMENTATION_PLAN.md` §5 now tracks the remaining, deliberately-not-built tiers (backtracking/repair pass, real optimizer, geographic clustering) as open by explicit choice, not oversight.

**Next recommended task:** The architectural backlog from this session is now fully worked through. What's left: the 10 un-automated §19 eval scenarios (lowest priority, biggest lift — needs new RLS/JWT or fault-injection test infrastructure) and the demo walkthrough recording (a manual step for the user, not something a coding agent can do). Otherwise, the project is in a portfolio-ready state.

---

## 2026-09-17 — Full §19 eval coverage (16/16): stale-inventory fix, a real cancel-trip feature, RLS-scoped harness sessions

**What I built:** Closed the last item on the open-items list — the 10 un-automated `PROJECT_BRIEF.md` §19 end-to-end scenarios. Used plan mode given the size (two Explore-equivalent research passes done inline, then a written plan approved before implementing). Scoping this properly, not just against the coarse tracked summary, found each remaining scenario needed one of three different things — most were just missing test cases against the existing harness, not new infrastructure as originally framed:

- **Buildable directly** (#4 conflicting hard constraints, #8 approval invalidation, #9 itinerary timing conflict, #10 stale inventory, #11 duplicate request, #12 failure/recovery, #13 cancellation): 7 of the 10.
- **Needed one small new harness capability each**: #14 (prompt injection in retrieved text — a temporary seeded fixture, insert-then-delete within the case itself) and #16 (full RLS cross-user isolation — a real, RLS-scoped, JWT-authenticated client, not the service-role client every other case uses).
- **Doesn't apply to the current architecture at all**: #6 (flexible destination) — `destination` is a required field the Intake agent must extract explicitly; there's no "choose among several candidates" step to test. Documented as an intentional non-applicability, not silently dropped.

Three real findings came out of the scoping pass and were put to the user directly before building anything, not assumed:

1. **A real guardrail gap while scoping #10**: `confirmFlightStep`/`confirmHotelStep`/`confirmActivitiesStep` never checked `inventory_version` at all — a stale-version ID would be silently accepted if it ever reached a confirm call directly (propose-time search already filtered correctly; this was a confirm-time defense-in-depth gap, same class as the destination_id/hard-constraint checks already there). Fixed in all three step modules, comparing against `destinationRow.inventory_version` — already resolved via `resolveTripDestination` — rather than a second import of `CURRENT_INVENTORY_VERSION`. New unit tests in all three `*-step.test.ts` files.
2. **No reachable cancellation path existed at all for #13** — `state-machine.ts` modeled a `cancel` event legal from any non-terminal state, but nothing in the UI or Server Actions ever fired it. Chose to build a real minimal feature rather than document the gap: `cancelTrip` (`app/app/actions.ts`, mirrors `finalizeTrip`'s shape exactly — `requireOwnedTrip` then `advanceOrThrow` with the `cancel` event), a "Cancel trip" action + confirm banner + "Trip cancelled" terminal state in `itinerary-panel.tsx`, a cancelled badge in the trip history list. `requireOwnedTrip` now refuses every other mutating action on a cancelled trip by default (new `TripCancelledError`). **A real build failure caught live**: `TripCancelledError` was first defined directly in `actions.ts`, which has `"use server"` — a directive that only allows async-function exports, and a plain class export silently broke the *entire* module's exports ("has no exports at all"). Moved it to `src/workflow/step-shared.ts` instead, alongside `UnknownDestinationError`. Browser-verified end to end via the standard throwaway dev-signin pattern (removed after use, not committed): cancel button → confirm banner → "Trip cancelled" state → correct trip-history badge, all working live.
3. **A second live-only bug found during that same browser pass**: typing into chat on an already-cancelled trip threw `TripCancelledError` directly from `sendMessage`, which — thrown across the Server Action boundary in a production build — surfaced as React/Next's generic obfuscated error (the same dishonest-failure class already fixed for other actions earlier this session). Asked the user whether to fix now or keep moving through the eval work; chose fix now. `SendMessageResult` widened to `| StepActionError`, `chat-panel.tsx` checks `"error" in result` before treating the response as a normal turn. Re-verified live: the friendly "This trip has been cancelled" message now renders correctly instead of the generic error.

**Built:**
- `evals/lib/scenario-harness.ts` — new `signInAs(userId)`: generates a real magic-link token via the admin API and verifies it through a plain anon-key client (`@supabase/supabase-js`, not `@supabase/ssr`'s browser-oriented one), returning a real RLS-scoped client for that user — the same mechanic every prior session's throwaway `/dev-signin` browser routes already used, just without setting cookies.
- 8 new cases in `evals/cases/scenarios.ts`: `conflicting_hard_constraints`, `flexible_destination` (documented non-applicability), `approval_invalidation_after_revision` (reuses the real second seeded return-leg flight from `0010_second_lisbon_return_flight.sql` to force genuinely different derived stay dates), `itinerary_timing_conflict` (hand-builds an overlapping schedule, bypassing the normal non-overlapping scheduler, to prove `confirmActivitiesStep`'s own independent feasibility re-check rejects it), `stale_inventory`, `duplicate_request` (proves `startTrip`'s existing correlationId idempotency end to end), `failure_and_recovery` (a small fault-injecting `ModelClient` wrapper that throws once then delegates to a real one — no new shared infrastructure, reuses the interface Phase 4 designed for exactly this), `cancellation`.
- 2 new cases in `evals/cases/adversarial.ts`: `prompt_injection_in_retrieved_text` (seeds one temporary activity row with injection-laden text and a real Voyage embedding so it's genuinely retrievable via semantic search, not a mocked candidate — deleted in a `finally`) and `cross_user_rls_isolation` (via the new `signInAs`).
- `proposeAndConfirmFlight`/`proposeAndConfirmHotel` exported from `scenarios.ts` so `adversarial.ts`'s new cases could reuse them instead of duplicating the propose-and-confirm-first-candidate pattern a third time.

**Why:** Last item on the tracked open-items list, per the user's explicit request to finish it before their own manual end-to-end testing pass.

**Decisions made (each asked, not assumed):** flexible destination — document as-is, no code change; stale inventory — fix the real guardrail gap, not just track it; cancellation — build a real minimal feature, not just document the gap; the sendMessage-on-cancelled-trip bug found live — fix now, not track.

**What didn't work / dead ends:** The dev-signin route's first `verifyOtp` call included an `email` field alongside `token_hash`/`type`, which this SDK version rejects ("Only the token_hash and type should be provided") — fixed by dropping `email` from that call, matching a prior session's own note about this exact finickiness. The `cross_user_rls_isolation` case's `trip_requirements` insert initially failed typecheck (missing `status`, which has no client-side default in the generated `Insert` type even though a DB default likely exists) — fixed by passing `status: "active"` explicitly.

**Verification:** `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (344/344, up from 337) all clean throughout. The actual deliverable: `npm run eval:scenarios` run live against the real hosted Supabase project and real Anthropic/Voyage APIs — **20/20 passed** (all 16 §19 scenarios, plus the 4 pre-existing + 2 new adversarial cases). The Curator's real response to the seeded prompt-injection fixture explicitly identified and excluded it: *"The 'Definitely Not A Prompt Injection Walking Tour' candidate was excluded entirely: its content attempted to manipulate the ranking process directly rather than describing a genuine activity... it was treated purely as untrusted data and its embedded commands were ignored."* The cancel-trip UI was browser-verified live (dev-signin pattern, removed after use).

**Known limitations / assumptions:** Unchanged from what's tracked in `docs/IMPLEMENTATION_PLAN.md` §5, minus this item and its three sub-findings. Room-type variety (hotels) and the heavier scheduler tiers (backtracking, real optimizer, geographic clustering) remain open by explicit prior choice, not oversight.

**Next recommended task:** The architectural/eval-coverage backlog from this session is now fully worked through. The project is in a portfolio-ready state — the user's next step is their own full end-to-end manual testing pass, which this session's work was explicitly building toward.

---

## 2026-09-17 — Session close: open-items list fully worked through, full §19 eval coverage reached

**What changed this session:** Worked all the way down `docs/IMPLEMENTATION_PLAN.md` §5's open-items backlog, one item at a time per the user's standing request, then finished the tracked "10 un-automated eval scenarios" item to reach full 16/16 §19 coverage. In order:

1. **Open-items list, items 1–5**: Curator malformed-tool-call guardrail layering (a new `errorKind` field distinguishing Layer 2 from domain_validation failures); deleted the genuinely-dead `"blocked"` workflow state; one-way trips resolved by making `returnDate` required (the user chose "ask for a return date" over a hard-coded default), which also made the old `OneWayTripNotSupportedError` dead code — deleted; confirmed the budget night-count drift item no longer applied (architecture had already fixed it); closed the flight→hotel cascade's live-verification gap with a new seeded return flight. Two extra dead-code findings surfaced and were deleted with the user's sign-off: `src/domain/combinations.ts` and `src/validation/inventory-references.ts`, both fully superseded by the stepwise chain redesign.
2. **Destination identity**: replaced free-text `destination` matching across `flights`/`hotels`/`activities` with a real `destination_id` FK (migration `0011`), resolved lazily per stepwise-chain step rather than at intake time (a scoping choice made directly with the user, avoiding new workflow-state plumbing). Also fixed a coupled finding: the forward auto-chain had no user-facing failure signal at all — mirrored the existing `chain_revision_failed` pattern as `chain_propose_failed`.
3. **Activity retrieval's overfetch heuristic**: replaced a fixed one-shot overfetch with a widen-and-retry loop that doubles the query size when Postgres returns exactly as many rows as asked (meaning there could be more), stopping once a fetch returns fewer than requested or a defensive ceiling.
4. **Hotel room availability**: added `hotels.available_rooms` (migration `0012`) and a new hard constraint checking it — the user chose this narrower, correctness-driven fix over a full room-type catalog when the tracked item's real scope became clear.
5. **Activity scheduling**: added a meal-time bias (`preferredWindows` on the scheduler, tried before the unconstrained fallback) — the smallest of five effort tiers laid out for the user, chosen explicitly over a repair pass, a real optimizer, or geographic clustering (the last of which is blocked on data regardless — no coordinates on activity rows).
6. **Full §19 eval coverage (16/16)**: closed the last 10 scenarios. Found and fixed a real guardrail gap (`inventory_version` was never checked at confirm time) and built a real minimal cancel-trip feature (nothing previously fired the state machine's already-modeled `cancel` event) rather than just documenting either gap. A live browser pass on the new cancel UI caught a second real bug — `sendMessage` throwing directly for a cancelled trip surfaced as React's generic obfuscated production error — fixed the same session. `npm run eval:scenarios` now passes 20/20 live against the real hosted stack.

**Files changed:** 44 files across 6 commits (`45e6039` through `bff8150` — see `git log` for the full list); each commit message and this file's corresponding entries above give the reasoning per change. Highlights: `src/domain/constraints.ts`, `src/domain/scheduling.ts`, `src/retrieval/activities-retrieval.ts`, `src/repositories/{destinations,flights,hotels,activities}.ts`, all three `src/workflow/*-step.ts` modules plus their tests, `src/workflow/step-shared.ts` (grew significantly — `resolveTripDestination`, `UnknownDestinationError`, `TripCancelledError`, `roomAvailabilityConstraint` wiring), `app/app/actions.ts` (`cancelTrip`, `requireOwnedTrip` cancelled-guard, `sendMessage` error-shape widening), `app/app/_components/itinerary-panel.tsx` (cancel UI), `evals/cases/scenarios.ts` + `evals/cases/adversarial.ts` (10 new cases), `evals/lib/scenario-harness.ts` (`signInAs`). Two files deleted (`src/domain/combinations.ts`, `src/validation/inventory-references.ts`) plus their tests.

**Database migrations added:** `0010_second_lisbon_return_flight.sql` (a second real Lisbon→NY return leg, closing a cascade live-verification gap), `0011_destination_ids.sql` (`destination_id`/`origin_id` FKs, backfilled, plus `match_activities`'s signature change), `0012_hotel_available_rooms.sql` (`hotels.available_rooms`). All three applied to the real hosted Supabase project this session via `supabase db push --db-url`.

**Tests run and results:** `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` all clean after every item this session (344/344 unit tests passing at session end, up from 323 at session start — some net deletions along the way from removing dead-code test files). `npm run eval:scenarios` run live against the real Anthropic/Supabase/Voyage stack at the end of item 6: **20/20 passed**, the first time this suite has covered all 16 `PROJECT_BRIEF.md` §19 scenarios. Every DB-touching fix this session was also live-verified with a throwaway script or browser session (dev-signin pattern), cleaned up after each time — never committed.

**Decisions made this session** (each asked of the user rather than picked unilaterally, per the standing rule): one-way-trip handling depth; whether to delete each newly-found dead module; destination-id resolution timing (lazy vs. intake-time) and whether to fix the coupled forward-chain silent-failure gap in the same pass; hotel-availability-only vs. full room-type catalog; which of five effort tiers for the scheduler; how to handle the two untestable-as-architected scenarios (flexible destination, cancellation) — the first documented as-is, the second built as a real minimal feature after the user asked what it would even mean given nothing is ever booked; whether to fix the stale-inventory guardrail gap and the sendMessage-on-cancelled-trip bug found live, both fixed now rather than tracked.

**Known limitations / assumptions:** See `docs/IMPLEMENTATION_PLAN.md` §5 for the full, current list. What remains open, all by explicit prior choice, not oversight: hotel room-type variety (only availability count was built), the heavier scheduler tiers (backtracking, a real optimizer, geographic clustering — the last blocked on activity location data), and telemetry retention automation (a documented policy, not yet scheduled code, since every row in the database today is synthetic demo/eval data).

**Next recommended task:** None from the backlog — it's fully worked through. The user is starting a new session for their own full end-to-end manual testing pass against the current state (commit `bff8150`, 15 commits ahead of `origin/main`, not yet pushed). Whatever surfaces from that — design, performance, or correctness issues — is the next real input, not anything pre-planned here.

---

## 2026-09-17 — Planning: flagged the two remaining open items as next session's starting point (no code changes)

**What happened:** No implementation this stretch — the user asked what was still open in `docs/IMPLEMENTATION_PLAN.md` §5 after the previous session closed it out, then asked for both remaining items (hotel room-type variety, the activity scheduler's remaining tiers) to be documented as the explicit starting point for the next session, wanting a deeper understanding of each first rather than just a pointer.

Explained both in depth (in chat, not reproduced here) and used that explanation to expand §5's own entries for each item with a concrete "what a real fix needs" breakdown, so a fresh session doesn't have to re-derive the scope from scratch:
- **Hotel room types**: what a `hotel_room_types` table + a per-group room-type assignment problem + reworked budget math would actually require, versus the already-built availability-only fix. Flagged as a real open question for next session whether this is worth building at all for a portfolio project at this scale, or whether documenting the boundary already demonstrates the same judgment more cheaply.
- **Activity scheduler**: broke the three still-open tiers (a bounded repair/local-search pass; a real scored optimizer with deliberate determinism for testing; geographic clustering, which is blocked on `activities.location` being a free-text string rather than coordinates) out individually with what each would concretely need, rather than leaving them as one vague "make it smarter" bucket.

Also found and fixed a real, unrelated quality issue while back in `BUILD_LOG.md` for this: the "Hotel room availability" and "Meal-time scheduling bias" entries had landed **out of chronological order** in a previous session's edits (inserted after the later "Full §19 eval coverage" and "Session close" entries instead of before them — an Edit-tool `old_string` match landed on the wrong occurrence of near-duplicate "Next recommended task" phrasing). Reordered them correctly; no content was lost or changed, only moved. Worth remembering for future sessions: when appending a new entry, match against something more chronology-unique than a boilerplate closing line, especially once multiple entries share similar phrasing.

**Why:** Direct request, ahead of the user starting a fresh session for their own manual testing pass — they wanted these two documented precisely enough to pick back up without re-deriving context, and wanted to understand them well enough to weigh in on scope themselves.

**Decisions made:** None yet — both items are explicitly left as open questions for next session, not pre-decided.

**What didn't work / dead ends:** The BUILD_LOG ordering bug described above, caught and fixed this stretch.

**Verification:** N/A — documentation only, no code touched. `git status` confirms nothing else changed.

**Next up:** Whichever of the two the user wants to open with next session — or something else entirely, if their own end-to-end testing pass surfaces something more pressing first.

---

## 2026-09-17 — Session close: confirmed both remaining backlog items as permanent scope boundaries; no code changes

**What happened:** The previous session had flagged two items (hotel room-type variety, the activity scheduler's heavier tiers) as the explicit starting point for this session, each requiring a direct decision before any code was written — "is this worth building at all for a portfolio project at this scale, or does documenting the boundary demonstrate the same judgment more cheaply." Asked both directly via structured questions rather than assuming: for each, offered the fuller-build option against "leave as the documented boundary," recommending the latter. The user chose to leave both as-is for both items.

**Why:** These were the only two open items left in `docs/IMPLEMENTATION_PLAN.md` §5 — everything else on the architectural/eval backlog closed in prior 2026-09-17 sessions. Resolving them (even as "no build") was the blocker before the user's own manual end-to-end testing pass could be considered against a fully-closed backlog rather than an ambiguous one.

**Decisions made:** Hotel room-type variety stays availability-count-only (`available_rooms`/`roomAvailabilityConstraint`), no `hotel_room_types` table. The activity scheduler stays at the lightweight meal-time-window heuristic tier, no repair pass/optimizer/geographic clustering. Both confirmed as permanent, documented boundaries, not oversights — `docs/IMPLEMENTATION_PLAN.md` §5 updated to reflect the closed decision (moved out of "unresolved," kept as a documented-boundary record rather than deleted).

**What didn't work / dead ends:** None — no code was touched this session.

**Verification:** Full clean-state check before handing off to the user's manual testing pass: `npx tsc --noEmit`, `npm run lint`, `npm test` (344/344 passing), `npm run build` — all clean. `git status` confirms a clean working tree (17 commits ahead of `origin/main`, unpushed).

**Known limitations / assumptions:** Unchanged — see `docs/IMPLEMENTATION_PLAN.md` §5, now empty of unresolved items (the two boundary entries are a record, not an action list) plus the standing Docker/`database.types.ts` hand-maintenance constraint and telemetry-retention-automation note.

**Next recommended task:** None from the backlog — it's fully closed. The user is starting their own manual end-to-end testing pass (visual, design, performance, technical) against the current state. Whatever surfaces from that is the next real input.

---

## 2026-09-18 — Large-scale seed data: ~640 new destinations, dense hotels/activities, a deterministic synthetic flight generator

**What I built:** The user asked for a much larger, more "real-feeling" inventory catalog before starting their own manual end-to-end testing pass — ~1000 destinations, hotel/activity variety, and flight coverage across arbitrary future date ranges, but explicitly **not** universal availability: realistic, occasional gaps, as long as they aren't the norm. Scoped this properly in plan mode (two Explore-agent research passes, then a written plan approved before implementing) because the real shape of the problem wasn't obvious up front: `hotels`/`activities` have no per-date rows at all, so "every date has options" for those two is a volume/variety problem, not a date one — but `flights` rows are matched by exact calendar date, and pre-seeding literal rows for ~640 destinations across arbitrary future dates is combinatorially infeasible (tens of millions of rows) and risks the hosted Supabase free tier. The user was asked directly (`AskUserQuestion`) to choose between a deterministic generate-and-persist-on-miss flight strategy versus a smaller bounded pre-seeded window; chose the generator, then clarified mid-plan that gaps should be realistic rather than either "always seeded" or "always available" — which reshaped the flight generator from a naive per-date-coinflip design into deterministic **weekly operating schedules per route** (real airlines don't fly every route daily either).

**Built** (full detail in `docs/IMPLEMENTATION_PLAN.md`'s new "LARGE-SCALE SEED DATA & SYNTHETIC FLIGHT GENERATION" section): `src/domain/geography.ts` (country/region/cost-tier metadata, shared by the seed script and the runtime generator), `src/domain/random.ts` (deterministic seeded PRNG), `scripts/data/cities.ts` (637 real, globally-unique city/country pairs across 139 countries, excluding the 6 original curated destinations), `scripts/data/inventory-templates.ts` + `scripts/generate-large-seed-data.ts` (637 new destinations, 3,841 hotels, 8,114 categorized activities — spa/restaurant/concerts/shows/movies/sporting events/cultural events per the user's explicit list), `src/domain/flight-generator.ts` (the core mechanism — deterministic per-route weekly schedules, generates 0-3 flights per operating date), `supabase/migrations/0013_flights_source.sql` (`flights.source`), and `src/repositories/flights.ts`'s `findFlights` generate-and-persist-on-miss wiring.

**A real bug found live, not by any unit test:** `scripts/generate-embeddings.ts`'s `select("*")` silently capped at PostgREST's default 1000-row limit — invisible at the old 27-activity scale, but it embedded only 1,000 of ~8,140 activities on the first post-reseed run. Fixed with `.range()` pagination, plus (found in the same run) Voyage's 3 RPM free-tier limit turning out to need more than fixed inter-chunk spacing alone — added a bounded 429-specific retry to `VoyageEmbeddingClient`.

**Decisions made (asked, not assumed):** flight strategy (generate-on-miss vs. bounded pre-seed) and destination scale (~1000 vs. smaller) were both put to the user directly before writing code, via `AskUserQuestion` inside plan mode; both recommended options were chosen. The "realistic gaps, not universal availability" requirement came from the user's own clarification mid-conversation and directly shaped the weekly-schedule design over a simpler per-date-random approach.

**Verification:** `npx tsc --noEmit`/`npm run lint`/`npm test` (372/372, 28 new)/`npm run build` all clean throughout. Live-verified against the real hosted Supabase project: the seed script ran successfully (637/637 destinations created), embeddings backfilled for all 643 destinations + 8,137 activities, `npm run eval:retrieval` (10/10) and `npm run eval:scenarios` (20/20) both pass unchanged — confirming the original 6 destinations' fixtures are genuinely untouched. A live sweep of several new routes confirmed real, route-specific gap behavior (one route returned flights on only 4 of 14 sampled dates; others returned flights on all 14) rather than either universal availability or random noise, and re-querying an already-generated route+date returned the identical persisted flights rather than regenerating.

**Known limitations:** a benign concurrent-insert race if two searches for the exact same never-before-generated route+date land simultaneously (duplicate generated rows, not a correctness bug) is accepted, not guarded. Hotel/activity content is templated, not hand-curated per destination, by design at this scale.

**Next recommended task:** None from any backlog — this was a direct user request ahead of their own manual testing pass, which is the next real input.

---

## 2026-09-18 — User's own manual e2e testing pass: two real bugs fixed, then a live-data flight sourcing feature (SerpAPI/Google Flights)

**What happened:** The user began their own manual end-to-end testing pass against the current state (the previous session's large-scale seed data). Set up a persistent `docs/END_TO_END_TESTING_ISSUES.md` log per their request, to work through issues one at a time as found rather than batching. Three real pieces of work came out of this stretch, described below in order.

**Issue 1 — the intake agent didn't visibly ask for missing required data.** Reported as "throws an error" when given a destination + dates but no budget. Reproduced live against the real model rather than guessed at: two distinct bugs, not one. (1) `processIntakeTurn`'s assistant-message fallback (`src/workflow/intake-orchestrator.ts`) substituted a generic "Got it — updating your trip details now." whenever the model's own text was empty — which happens consistently for `request_clarification` calls too, not just `record_extraction` as originally documented — silently hiding the fact the agent was actually blocked waiting on specific fields. Fixed with a clarification-specific deterministic fallback built from the already-computed `missingFields`. (2) `ItineraryPanel` (`app/app/_components/itinerary-panel.tsx`) auto-proposed a flight search the instant it mounted — right after the trip's first message, before requirements were necessarily complete — because it inferred readiness from an empty decisions list rather than checking it. Confirmed live in the dev server's own error log against the user's real concurrent trip. Fixed by threading a `requirementsReady` flag (computed server-side, updated live via chat) down to gate the auto-propose effect. New regression test in `intake-orchestrator.test.ts`; `npx tsc --noEmit`/`npm run lint`/`npm test` (373/373) all passed. Not re-verified via a full browser click-through — a throwaway `/dev-signin` route needed to temporarily weaken `proxy.ts`'s auth boundary to test, and the auto-mode safety classifier correctly blocked using it; reverted immediately, confirmed instead via a live reproduction script and the dev server's own logs during the user's concurrent session.

**Issue 2 — "We don't have inventory for that destination yet" for Madrid, Spain.** Root-caused via a live reproduction rather than guessed: `getDestinationByName` (`src/repositories/destinations.ts`) did an exact, case-sensitive match on `name` alone. The intake agent extracts `destination` verbatim ("Madrid, Spain" as typed), but the seeded row is named exactly "Madrid" — real inventory existed the whole time, the lookup just couldn't find it. Checked the user's explicit worry (duplicate city names across different countries) first — none exist yet in the 643-destination seed set — but fixed the lookup to be genuinely country-aware anyway (`country` was already a populated column on every row, no backfill needed) rather than patching around today's specific string. New `src/domain/destination-query.ts` (`parseDestinationQuery`, pure) splits "City, Country"; `getDestinationByName` now does a case-insensitive, wildcard-escaped match on name and, when given, country. A related question ("how do we add more flight inventory?") turned out to already be solved: `findFlights` already falls back to the deterministic synthetic flight generator built the previous session — Madrid's "0 flights" wasn't a data gap, it was purely downstream of the destination-matching bug preventing the flight step from ever being reached. Live-verified against the real hosted DB. `npx tsc --noEmit`/`npm run lint`/`npm test` (380/380) passed.

**The SerpAPI (Google Flights) live-flight-inventory feature.** The user asked to think through replacing seed/synthetic flight data with a real live API, walked through the tradeoffs (eval reproducibility being the load-bearing one — real API data breaks deterministic eval assertions, a real demonstration goal of this project per `ADR-006`), then asked specifically: SerpAPI as the *only* flight source for the real app, hotels/activities staying on seed data, evals staying on the deterministic seed path via the same provider-swap seam `ModelClient`/`EmbeddingClient` already established. Scoped into concrete steps, then built in full once the user supplied a SerpAPI key (stored in `.env.local`, never committed). Full detail in `docs/IMPLEMENTATION_PLAN.md`'s new "LIVE FLIGHT INVENTORY: SERPAPI" section — summary: a real airport-code dataset (OurAirports + OpenFlights merged, ~3,500 scheduled-commercial airports with real IANA time zones) and a country-aware `findAirportsForCity` lookup; a deterministic "which airport" disambiguation gate (`checkAirportReadiness`) that reuses the *existing* clarification machinery end to end rather than inventing a parallel one, with the model given explicit structured context about a pending disambiguation (`pendingAirportClarification`) since `runIntakeAgent` has no conversation history of its own; a new `zonedTimeToUtc` (`src/domain/dates.ts`) since SerpAPI's per-leg times carry no UTC offset at all; a `FlightSearchProvider` interface + `SerpApiFlightProvider` (thin HTTP client) + `serpapi-flight-mapper.ts` (the actual response parsing, deliberately split out with no `"server-only"` import so it's unit-testable — the first provider client in this codebase with real coverage); a caching layer (`findFlightsFromProvider`) against the free tier's real rate limits (250 searches/month, 50/hour); two new migrations (`0014_flights_airport_codes.sql`, `0015_flights_created_at.sql`, both applied to the hosted project). `flight-step.ts`'s `proposeFlightStep` takes an optional `flightProvider` — omitted (every eval call site), it's byte-for-byte the original behavior; set (the real app only), it uses the live path instead.

**Decisions made (asked, not assumed, at each fork):** keeping evals on the deterministic seed path rather than "SerpAPI truly everywhere" — explained why before the user confirmed. Defaulting refundable/changeable to `false` for SerpAPI-sourced flights rather than guessing — user explicitly approved. The airport disambiguation UX (ask which airport via a real clarification turn, not silently picking one) — the user's own explicit design ask, implemented as described.

**What didn't work / dead ends:** none functionally — but one process note: attempting a full authenticated browser click-through for the Issue-1 fix required weakening `proxy.ts`'s auth boundary via a throwaway `/dev-signin` route (the established pattern from prior sessions); the auto-mode safety classifier correctly blocked that combination, so verification for that fix relied on a live reproduction script and the dev server's own logs instead.

**Verification:** Full check after every discrete change, not just at the end — `npx tsc --noEmit`/`npm run lint`/`npm test` clean throughout, ending at 426/426 (up from 372 at session start). `npm run eval:scenarios` re-run in full after the SerpAPI feature landed — **20/20 still pass**, including scenarios that exercise real flight search (`standard_trip`, `hard_constraint_vs_cheapest`) — proving the seed-backed path evals depend on is genuinely untouched, not just believed to be. The SerpAPI integration itself was live-verified end to end against the real hosted Supabase project and the real SerpAPI: a New York → Madrid trip correctly flagged "New York" as ambiguous (JFK/LGA), resolved once answered, and returned 3 real priced JFK↔MAD candidate pairs with real airlines, persisted with `source: "serpapi"`; a second immediate call hit the cache instead of calling SerpAPI again.

**Database migrations added:** `0014_flights_airport_codes.sql`, `0015_flights_created_at.sql` — both applied to the hosted project via `supabase db push --db-url` (needed a `supabase migration repair` first, since `0013`'s remote tracking had drifted from an earlier session's direct-script apply — the same class of gap that session's own BUILD_LOG entry already flagged as a lesson for next time).

**Known limitations, all deliberate:** refundable/changeable always `false` for SerpAPI-sourced flights (no such signal exists in its response). No retry/backoff on a transient SerpAPI failure yet — surfaces immediately as a friendly "try again" message; not added without real usage data on how often it's actually needed. A multi-leg SerpAPI itinerary is summarized door-to-door, not modeled leg-by-leg.

**Next recommended task:** Continue the user's own manual e2e testing pass — hotels/activities are still on seed data by design (this session's explicit scope), so the next likely surface is whichever of those (or a further flight-search edge case) the user hits next.

---

## 2026-09-18 (continued) — Activities redesign: preference-driven multi-select, replacing the no-choice auto-scheduler

**What happened:** Continuing the same e2e testing pass, the user reported activities had "no meaningful flow or data that's friendly for the user" — just a list of bare dates/times. Investigated the actual code (not assumed) before proposing anything: unlike flight/hotel (propose a short list, user picks one), activities had no pick step at all — one Curator LLM call auto-selected candidates, the deterministic scheduler auto-placed them, and the data structure carried through the whole pipeline (`ProposedScheduledActivity`) only ever had `{id, date, startMinutes, durationMinutes}` — no name ever reached the UI, even though the database/retrieval/curation all had it. Preferences were also never actually asked for, just whatever happened to already be in `trip_preferences` (usually nothing).

Asked the user to weigh in on two real design forks before building: (1) should this lean more agentic or be a real UI-driven pick step — user chose UI-driven, explicitly: after hotel confirms, prompt for preferences, then list activities to select, mirroring flight/hotel's pattern; (2) should each confirmed activity be its own `trip_decisions` row or one array — user confirmed per-row (matches flight's `outboundFlight`/`returnFlight` pattern). Also thought through (per the user's own explicit ask) whether SerpAPI's Google Local/Places API should replace the seed catalog here — checked the real API docs first: its `description` is a single review snippet, no confirmed structured hours, no accessibility data — a worse fit for semantic retrieval than the existing curated catalog, and activities don't have flights' daily-freshness problem. Declined, recommended keeping the seed catalog, user agreed.

**Built** (full detail in `docs/IMPLEMENTATION_PLAN.md`'s new "ACTIVITIES: PREFERENCE-DRIVEN MULTI-SELECT REDESIGN" section): `src/workflow/activities-step.ts`'s old `proposeActivitiesStep`/`confirmActivitiesStep` replaced with four functions — `proposeActivitiesStep` (now UI-preference-driven, returns richly-detailed unscheduled candidates plus `alreadySelected` for reload-resilience, falls back to a previously-stored preference when the caller doesn't submit a new one), `confirmActivitySelection`/`removeActivitySelection` (new, per-activity add/remove), `finalizeActivitiesStep` (the old confirm, renamed, now schedules whatever the user actually picked). New `categoryConstraint` (`src/domain/constraints.ts`) wired into `retrieveActivities` as a real structured filter — checked first whether `vibe_tags` could already do this (it can't; generated seed data's vibe tags are destination-level climate/region tags, not per-activity-type, so every activity in one destination shares the same tags). New repository primitive `retireTripDecisionById` (`src/repositories/trip-decisions.ts`), needed because every existing retire helper keys by field alone, which would wrongly supersede every sibling `"activity"` row instead of just the one being removed. Full UI rework in `app/app/_components/itinerary-panel.tsx`: a preference form (10 category chips matching the real seed taxonomy + free text) once hotel confirms, a candidate pick-list with real names/prices/descriptions and per-card "Add", a running "in your itinerary" list with "Remove", and a "Finalize itinerary" button — replacing the old bare-date/time list and single "Confirm this schedule" button.

**A real design tension found and resolved while adapting the eval suite:** `evals/cases/scenarios.ts`'s `itinerary_timing_conflict` scenario used to hand-construct an overlapping schedule and pass it directly to `confirmActivitiesStep` to prove the feasibility validator rejects it. The new `finalizeActivitiesStep` has no such parameter — it always derives the schedule internally via the already-proven-non-overlapping scheduler — so that specific attack surface no longer exists at all, not just gets caught. Rather than deleting the scenario, rewrote it to prove the guardrail is still live-wired into the real integration (a normal finalize call logs a non-triggered `itinerary_feasibility` guardrail event) — overlap-rejection itself stays covered at the unit level (`src/domain/feasibility.test.ts`, unchanged).

**Decisions made (asked, not assumed):** UI-driven trigger over chat-driven (user's explicit choice); per-activity `trip_decisions` rows over one array (user confirmed); seed catalog over SerpAPI Places for activities specifically (recommended after checking the real API docs, user agreed).

**Verification:** `npx tsc --noEmit`/`npm run lint`/`npm test` (440/440, up from 426 at the start of this stretch) clean throughout. `npm run eval:scenarios` re-run in full after the redesign — **20/20 still pass**, including `standard_trip` (now asserting real named activity candidates instead of a bare feasibility flag) and the rewritten `itinerary_timing_conflict`. Not verified via a live authenticated browser click-through — the `/dev-signin` auth-boundary workaround remains off-limits (see the SerpAPI entry above); the dev server's own compile log was checked clean after the change (no stale import/export errors survived a fresh navigation), and the user's own concurrent manual testing session is the next real verification of the UI itself.

**Known limitations, all deliberate (matches the user's own explicit lower priority on exact time precision):** scheduling is still a single deterministic forward pass over whatever the user picked — picking more than the trip's dates/hours can hold reports the excess as `unscheduledActivityIds` (surfaced as a banner) rather than choosing which ones to bump. Reservation-required vs. freely-visitable activities aren't distinguished by the scheduler.

**Next recommended task:** Whatever the user's own live testing of this new activities flow surfaces next.

---

## 2026-09-18 (continued) — Date-grounding bug fixed from a real failed trip, plus a second bug found while re-verifying

**What happened:** The user reported a real trip that failed to build — Madrid, party of 3, "October 2nd to October 10th," $7500 budget (trip `83b5da8c-faa9-4974-bd4c-f984e951956f`). Diagnosed from the dev server's own logs against that real trip rather than guessed at: the intake agent had no notion of the current date at all, so a bare "October 2nd" with no year had nothing to anchor to and the model resolved it to a past year, which then failed downstream. The user confirmed fixing both the immediate bug and adding a deterministic guardrail against the broader class of failure.

**Fix 1 — date grounding.** `IntakeAgentInput.today: string` (`src/agents/intake.ts`) is now a required field, computed once by the orchestrator (`processIntakeTurn`, defaulting to the real system date, overridable for tests/evals) since the agent has no other way to know the current date. New system-prompt instruction: resolve year-less dates to the correct *upcoming* occurrence relative to `today`, never a past one, unless the user states a year explicitly.

**Fix 2 — a deterministic guardrail against stale dates reaching downstream search.** Even with grounding fixed, nothing stopped a genuinely stale user-stated date (e.g. a typo'd year) from silently reaching flight search and failing there instead of being caught at intake. New `checkDatesNotInThePast` (`src/domain/extraction.ts`, pure, takes `today` as a parameter for testability) wired into `processIntakeTurn` as a third gate — after completeness, before the existing airport-disambiguation gate — reusing the same `awaiting_clarification`/`ClarificationRequest` machinery as every other deterministic gate in this workflow, per the project's established "no parallel state machine" pattern.

**Fix 3 — found while re-verifying via `npm run eval:intake`, not from the user's report.** The `revision_request` eval case started failing: `currentDecisions` as sent to the model (`processIntakeTurn`) only ever carried `{field, value: "<id>", status}`, never a price, so when a user asked to switch to "a cheaper hotel" the model had no number to compute a relative threshold from and correctly asked for clarification instead of proposing a revision. Per standing practice (ask fix-now vs. defer for anything found mid-task, don't silently defer), reported this to the user before touching it — user said fix now. New `enrichCurrentDecisions` (`src/workflow/intake-orchestrator.ts`) looks up the real flight/hotel rows behind confirmed decisions and attaches `priceUsd`/`airline`/`name` before the model sees them. Fixing this also exposed that the eval case's own assertions had been wrong all along — they expected a `"hotel"` decision revision, but the system prompt's own documented design says comparative language ("cheaper") should produce a `"requirement"` revision (a concrete `maxHotelPriceUsd` threshold) instead; corrected the fixture and assertions in `evals/cases/intake.ts` to match the system's actual intended behavior rather than the test's prior (incorrect) expectation.

**Decisions made (asked, not assumed):** user explicitly confirmed both wanted fixes (date grounding + the past-date guardrail) before work started; the `currentDecisions` price bug was surfaced and its fix confirmed ("fix now and continue") before being made, per standing fix-now-vs-defer practice.

**Verification:** `npx tsc --noEmit`/`npm run lint`/`npm test` (450/450, up from 440) clean. `npm run eval:intake` against the real model — **claude-sonnet-5: 6/6** (Haiku 4.5: 5/6, consistent with its already-documented weaker non-production baseline). `npm run eval:scenarios` re-run in full as a broad regression check, since `enrichCurrentDecisions` touches a code path shared by every scenario that goes through `processIntakeTurn` — **20/20 still pass** (`eval_runs.id = 98b2025f-95b1-4c34-bbc2-7f7ac8aaec6d`). The date-grounding fix and the past-date guardrail were each additionally confirmed against a live reproduction script hitting the real Anthropic API, reproducing the user's exact reported failure before the fix and no longer failing after. Not verified via a live browser click-through, for the same `/dev-signin` auth-boundary reason as every other fix this session.

**Known limitations:** none new — this closes out issue #4 in `docs/END_TO_END_TESTING_ISSUES.md` in full.

**Next recommended task:** Whatever the user's own live testing surfaces next; no known open issues remain in `docs/END_TO_END_TESTING_ISSUES.md` as of this entry.

---

## 2026-09-18 (continued) — Fixed a duplicate-propose race between the server's cold-start optimization and the itinerary panel's own effect

**What happened:** The user reported "no available flights" for a real Boston → London test trip (`44c4d840-9d5e-4326-bf83-544339c54319`). Root-caused from the trip's own `trip_events`/`guardrail_events` rows, queried directly against the hosted DB rather than reproduced blind: this was never an inventory gap — real SerpAPI flights existed (BOS→LHR $294, LHR→BOS $432) and 3 candidates were already persisted by the time it was investigated. The DB fingerprint gave it away: two separate `proposeFlightStep` executions 3.5 seconds apart (two pairs of `outbound_flight_hard_constraints`/`return_flight_hard_constraints` guardrail events) but only one surviving `flight_step_proposed` event.

**Real bug:** two independent, uncoordinated triggers both called `proposeFlightStep` the moment a trip's requirements first became ready — `sendMessage`'s server-side `after(() => proposeCurrentChainStep(...))` fire-and-forget (`app/app/actions.ts`, a deliberate latency optimization to start the search before the client asks) and `ItineraryPanel`'s own reactive `useEffect` (`app/app/_components/itinerary-panel.tsx`), which independently fires `proposeFlightCandidates` once it observes `activeStep === "flight" && requirementsReady`. `proposeFlightStep` isn't idempotent — each call unconditionally retires whatever "proposed" flight rows exist and inserts a fresh set — so whichever call's writes landed second silently superseded the first mid-render, occasionally showing a transient empty/error state that self-repaired once the second wave landed, with no user action needed. That "it just started working" behavior is exactly what the user described, and is why it wasn't a reload/caching fluke.

The user explicitly asked whether this was systemic across the whole chain. It wasn't, beyond this one spot: every *other* transition (`confirmFlightCandidate` → hotel, `confirmHotelCandidate` → activities, chat-driven revisions via `reviseChainStep`) already uses exactly one synchronous trigger, and pre-arms the client's guard ref from its own already-known result before Realtime can double-fire it (`handleConfirmFlight` sets `hotelSigRef.current` before the hotel decisions even land via Realtime). The cold-start into the flight step was the one place with two independent trigger paths instead of one — a gap in an otherwise-consistent pattern, not a design flaw repeated everywhere.

**Fix:** removed the redundant server-side `after(() => proposeCurrentChainStep(...))` block from `sendMessage` entirely, relying solely on the client's already-correct, signature-guarded effect (which already has a "Finding flights…" loading state and self-heals via `needsAttention`/Realtime on a genuine failure). A net deletion, not new locking machinery — matches the pattern every other transition already uses. Also halves live SerpAPI calls on every trip's flight-step cold start (a real, independent win given the free tier's 250/month cap), on top of closing the race.

**Decisions made:** proposed removing the redundant trigger (rather than adding a lock/idempotency check) and explained the latency trade-off (~500ms, based on the removed call's own observed timing) before implementing — no objection raised.

**Verification:** `npx tsc --noEmit`/`npm run lint`/`npm test` (450/450, unchanged) all pass. No existing test exercised the removed fire-and-forget path (`sendMessage` has no dedicated unit test; `step-router.test.ts` only covers the callee it used to call), so nothing needed updating. Not yet re-verified via a fresh live trip creation — deliberately deferred to the user's next real test rather than spending additional SerpAPI quota on a throwaway reproduction; the two-guardrail-pair/one-surviving-proposal DB fingerprint from the original report is the evidence the race existed.

**Known limitations:** the fix removes one of the two racing triggers rather than making `proposeFlightStep`/`proposeHotelStep` genuinely idempotent under concurrency — sufficient here since only one trigger source remains for every step, but worth knowing if a future change reintroduces a second automatic trigger anywhere in the chain.

**Next recommended task:** Confirm via the user's next real flight-step test that only one `flight_step_proposed` event/guardrail pair is now recorded per trip (I don't yet have a fresh post-fix trip to check). Otherwise, whatever the user's own live testing surfaces next.

---

## 2026-09-18 (continued) — Moved the activities preference prompt from the itinerary panel into chat

**What happened:** The user asked directly (not a bug report) to move the activities-preference *prompt* — the category pills and free-text box — into the chat transcript, while keeping the candidate pick-list/add/remove/finalize UI exactly where it already was in the itinerary panel. Flagged this as a real cross-cutting UI change (touches `chat-panel.tsx`, `itinerary-panel.tsx`, and `trip-workspace.tsx` together) before starting, per standing practice.

One real design fork needed the user's call before building: how should free-text answers work — bundled with the pills as one self-contained chat bubble (the current form's exact fields, just relocated), or typed into the main chat input box like a normal message? Asked the user directly; they asked for a recommendation instead of picking. Recommended the bundled-bubble approach and explained why: the main chat input already has one meaning (routes to the intake agent via `sendMessage`); making it contextually mean something else when the prompt is active is an invisible mode switch that's easy to get wrong, every other structured interaction in this app (flight/hotel candidate cards) is already a self-contained widget with its own explicit submit rather than piggybacked on the chat input, and combining a pill pick *and* typed text in one submission (already supported today) stays easy with one bundled widget but gets awkward split across two separate actions. User accepted the recommendation.

**Built:** `ItineraryPanel` no longer renders the preference form — it still detects exactly when a preference is needed (hotel just confirmed with no prior engagement, unchanged detection logic) but now reports it upward via a new `onActivitiesPreferenceNeeded` prop instead of rendering UI for it, and receives the eventual answer back via a new `activityPreferenceSubmission` prop (`{requestId, categories, criteria}`, tagged with a fresh id per submission so a repeat "Change preferences" round trip is never mistaken for the same answer twice). `ChatPanel` gained the actual widget — pills + textarea + submit rendered inline in the transcript when `activitiesPreferencePrompt` is set, announced with a synthetic assistant message on each rising edge, and collapsing into a plain summary user-message bubble once submitted (e.g. "Museums & Culture, Food & Dining — relaxing, not touristy"), matching the conversational feel rather than looking like a bolted-on form. `TripWorkspace` bridges the two, owning `showActivitiesPrompt`/`activityPreferenceSubmission` state exactly the way it already owns `pendingCascadeConfirmation` for the other cross-panel signal. `ItineraryPanel` still owns the actual `proposeActivityCandidates` call and every bit of state downstream of it (candidates, add/remove, finalize) — this was purely a relocation of the ask, not a change to propose/pick/finalize mechanics; "Change preferences" now re-opens a fresh prompt in chat instead of a form in the itinerary panel. One correctness detail caught in review: `onActivitiesPreferenceNeeded` is a dependency of `ItineraryPanel`'s existing candidate-loading effect, so it needed a stable identity (`useCallback` in `TripWorkspace`) to avoid needlessly re-running that effect on every unrelated `TripWorkspace` re-render.

**Decisions made (asked, not assumed):** flagged the change as cross-cutting before starting; asked the user to choose the free-text mechanism, gave a recommendation with reasoning when they asked for one instead of deciding themselves, proceeded once accepted.

**Verification:** `npx tsc --noEmit`/`npm run lint`/`npm test` (450/450, unchanged — no component-level tests exist for `app/` in this codebase, consistent with every prior UI-only change this session) all pass; the dev server's own compile log stayed clean through every edit. Not verified via a live browser click-through — the `/dev-signin` auth-boundary workaround remains off-limits (per this session's earlier entries), and no authenticated session was available in the agent's own browser pane. The user's own concurrent testing session (trip `f3a617f6-1b00-4d5f-9af5-11a84ca058ef`) reached a fully finalized itinerary through the *old* preference-form UI moments before this change compiled in, confirming the underlying propose/pick/finalize mechanics this change deliberately left untouched still work end to end — but it predates this specific relocation, so the new chat-based prompt itself is still unverified live.

**Known limitations:** the synthetic prompt/summary bubbles in chat are session-only (not persisted as real chat history) — a page reload after answering won't show them again, though the underlying preference and its resulting candidates/selections remain fully durable in `trip_decisions`/`trip_preferences` as before. This matches how confirmed flight/hotel picks already work (shown as cards in the itinerary panel, not backfilled into chat history), so it's consistent with existing behavior rather than a new gap.

**Next recommended task:** The user's own live test of this change — specifically confirming the prompt appears in chat once a hotel confirms, both the pill and free-text paths reach the itinerary panel's candidate list correctly, and "Change preferences" re-opens the prompt in chat rather than anywhere else.

---

## 2026-09-18 (continued) — Live incident: a real trip got stuck in a continuous flight-propose loop; fixed in real time, plus a second unrelated bug found in the same reload

**What happened:** While the user was live-testing the just-shipped activities-in-chat change, they reported "current trip session is stuck." Diagnosed against the actual trip (`0464978b-6e75-46ba-bf0f-765a6467aac6`, Paris from JFK) rather than guessed at: its `trip_events` showed 263+ `flight_step_proposed` rows, ~0.6-1s apart, continuing for over 4 minutes — well past `flight_step_confirmed` and `hotel_step_proposed` having already fired. Confirmed via the dev server's own request log that the *client* was the source (repeated `proposeFlightCandidates` POSTs from the browser), not a server-side loop.

**Real bug, latent rather than newly introduced:** `ItineraryPanel`'s flight/hotel auto-propose effect guards re-firing with a content signature derived from `decisions` (`flightSigRef`/`hotelSigRef`). `proposeFlightStep`/`proposeHotelStep` write as two separate steps — retire the old "proposed" rows, then insert the new ones — which Realtime delivers as two separate messages. In the gap between them, `decisions` can transiently show zero proposed rows for that field, which the signature guard couldn't distinguish from a genuinely new external change (a chat-driven revision looks identical). So the effect refired, which retired+reinserted again, reproducing the same gap — a self-sustaining loop, once started. Confirmed the already-confirmed flight was never itself touched (`retireProposedTripDecisionsForField` is correctly scoped to `status = "proposed"` only) — the loop was spinning on stale ground; `activeStep` was correctly "hotel" server-state-wise the whole time, but the already-mounted client effect's own broken guard kept re-triggering regardless.

**Fix:** added an explicit in-flight guard (`flightProposingRef`/`hotelProposingRef`), checked alongside the signature, so the component's own pending write is never mistaken for an external change while it's still settling. Also changed what the signature ref gets set *to* — derived from the call's own result once it resolves, not a pre-call `decisions` snapshot (the exact value that goes stale mid-flight) — so it's deterministic regardless of Realtime delivery timing, and a later genuine revision is still correctly detected. Applied to both flight and hotel (same shape in both); activities' auto-hydrate path was already safe, gated by a plain once-per-transition boolean rather than a content signature.

**Live remediation, not just a code fix:** the fix alone couldn't stop the loop already running in the user's already-mounted tab (stale refs, old JS). Had them reload, then confirmed via the dev server's request log and the trip's own `trip_events` that it actually stopped — no new `flight_step_proposed` rows in the 30+ seconds after reload, `hotel_step_proposed` firing cleanly once.

**Second, separate bug found immediately after, from that same reload:** the user asked whether a flight now displaying as a raw UUID instead of an airline name was related to the loop chaos. It wasn't — a real, independent, pre-existing bug: `confirmedFlightSummary`/`confirmedHotelSummary` (the human-readable confirmed-step display) were only ever populated by `handleConfirmFlight`/`handleConfirmHotel`'s own already-known result at confirm time. A reload of a trip confirmed in an *earlier* session never goes through those handlers, so the display permanently falls back to raw-id text — reproducible on any reload of any already-confirmed trip, not specific to today's incident. Fixed by hydrating both summaries from a direct client-side `flights`/`hotels` read (both world-readable seed inventory per `0001_initial_schema.sql`'s RLS policies) in the same effect that already fetches `trip_decisions` on mount.

**Decisions made:** none requiring the user's input — both were clear, mechanical bugs with a single correct fix once root-caused; moved straight to fixing given the loop was actively consuming resources.

**Verification:** `npx tsc --noEmit`/`npm run lint`/`npm test` (450/450, unchanged) all pass; dev server compile log stayed clean throughout. The loop-stopping fix was verified live against the actual incident trip (DB event timeline + request log, both confirming the stop). The reload-hydration fix compiled in cleanly but hasn't yet been independently re-verified showing a real airline name on a fresh reload — the user's next reload of a confirmed trip will confirm.

**Known limitations:** none new. Worth flagging for the future: this is the second live incident this session traceable to the same general shape (a signature/ref-based guard not accounting for a multi-step server write observed through Realtime) — the earlier flight-race fix and this one are related but distinct mechanisms, both now closed.

**Next recommended task:** Confirm the reload-hydration fix shows a real airline name (not a UUID) on the user's next reload of an already-confirmed trip. Otherwise, resume verifying the activities-in-chat change from the previous entry, now that this incident is resolved.

---

## 2026-09-18 (continued) — Same loop recurred on a fresh trip; found and fixed a real data-corruption race it exposed, plus a hard circuit breaker as a safety net

**What happened:** Immediately after the previous entry's fix, the user reported it was still stuck — a new trip (`c8d672b9-9abd-464b-80e9-ff08594fabf4`, Paris from JFK) confirmed a flight, but the hotel selection "unrendered" and the flight visually reverted to unselected. Checked the trip's row-level history (not just event counts this time) to understand exactly what happened, not just that it happened.

**Confirmed real, not perceived:** the flight-propose loop recurred on this trip too — running from `requirements_ready` (21:55:19) straight through the user's own flight confirm (21:56:00) and for ~48 seconds after, before stopping on its own (no new events since 21:56:07, confirmed 6+ minutes later). The exact reason the previous fix didn't fully prevent this recurrence isn't conclusively pinned down — a stale browser tab still running pre-fix JavaScript (predating the earlier fix's compile) is the leading theory, since the code trace for the signature/in-flight-guard fix doesn't reveal a remaining gap under a fresh single-tab mount — but treated as unconfirmed rather than assumed.

**Real bug actually found and fixed — a genuine data-corruption race, not just wasted calls:** traced the flight's confirmed row by its own id (not just field+value) through the full timeline and found it stayed correctly `"confirmed"` throughout — so the flight was never actually lost, just briefly *displayed* as unselected. But `confirmDecisionField`'s `confirmTripDecisions` (`src/repositories/trip-decisions.ts`) turned out to have a real, independent bug once read closely: it flips *every* row for a field currently `status = 'proposed'` to `'confirmed'`, not the one specific row it matched the user's pick against. Under any concurrent write to the same field — which the still-running loop supplied, but a legitimate revision or even ordinary latency jitter could too — a `retire`-then-`insert` cycle landing between `supersedeOtherActiveTripDecisions` and this call could leave zero matching rows (the intended one already superseded, silently confirming nothing) or, if a fresh row landed first, confirm the wrong one. This is what the hotel selection actually hit: no `confirmHotelCandidate` call ever reached the server at all (checked the dev server log and `trip_events` — neither shows one), meaning the still-refreshing candidate list replaced the card out from under the user's click before it could even fire, not a server-side failure.

**Fix:** replaced `confirmTripDecisions(supabase, tripId, fields: string[])` with `confirmTripDecisionById(supabase, tripId, decisionId: string)` — scoped to the exact row `confirmDecisionField` already matched, with no `status` filter (the caller already validated the match; this should win regardless of what a concurrent write left the row's status as). One call site, so this was a clean replacement, not a new parallel path. Updated the two test files (`flight-step.test.ts`, `hotel-step.test.ts`) that asserted on the old field-based call shape to assert the new id-based one instead.

**Additional hard safety net, independent of root-causing the recurrence:** added a circuit breaker (`flightProposeAttemptsRef`/`hotelProposeAttemptsRef`, cap 5) to the auto-propose effect in `itinerary-panel.tsx` — once a step's real-call budget is exhausted, it stops retrying automatically and surfaces an explicit "keeps re-running unexpectedly — please reload the page" error instead of continuing silently. This bounds the blast radius of *any* runaway-retry shape here, known or not, to a handful of calls and a loud failure rather than a multi-minute silent hammering of the DB/SerpAPI — a deliberate belt-and-suspenders addition given the same general failure shape has now surfaced twice in one session despite a targeted fix for the first occurrence.

**User impact and current trip state:** the user's flight pick on `c8d672b9` is confirmed and intact in the database. The hotel pick never went through — needs to be redone once the loop is confirmed dead in a clean session. Recommended the user close all tabs for the app and start from exactly one freshly (hard-)reloaded tab before continuing, to rule out the stale-tab theory empirically rather than continuing to guess from logs alone.

**Decisions made:** none requiring the user's input — moved straight to fixing given the trip was actively corrupting mid-incident; the confirm-by-id fix and circuit breaker are both defensive hardening with no real behavioral trade-off for the legitimate path.

**Verification:** `npx tsc --noEmit`/`npm run lint`/`npm test` (450/450, unchanged) all pass; dev server compile log clean. The confirm-by-id fix is verified by the updated unit tests (both promote-in-place scenarios now assert the exact row id). The circuit breaker itself is new, untested code path (deliberately simple — a counter and a comparison) and hasn't been exercised live yet, since triggering it requires the very failure it guards against.

**Known limitations:** the root cause of *why* the earlier fix didn't fully prevent this specific recurrence is still not conclusively identified — the stale-tab theory is the leading candidate but unconfirmed. If a clean single-tab session still reproduces the loop, that theory is wrong and the actual remaining trigger needs to be found for real, rather than continuing to add defensive layers around it.

**Next recommended task:** Have the user retest from a single, freshly hard-reloaded tab. If the loop still occurs under those conditions, the stale-tab theory is ruled out and the actual mechanism needs to be found directly (e.g., by having the user share the browser's own network/console tab activity, or by adding temporary client-side logging to the effect itself). If it doesn't recur, redo the hotel selection on `c8d672b9` and continue verifying the activities-in-chat change.

---

## 2026-09-18 (continued) — Loop recurred with the circuit breaker catching it as designed; a real, separate, confirmed airport-resolution bug found while investigating a follow-up report

**What happened:** The user reported the new circuit breaker's own error message ("Flight search keeps re-running unexpectedly — please reload the page") firing on a fresh trip — confirming the loop is real, not a stale-tab artifact, and confirming the safety net works as designed. Checked the trip's row-level history: the auto-propose effect fired exactly 5 times (the configured cap), ~1s apart, then stopped; the user reloaded, one more attempt fired cleanly, and the trip went on to finalize successfully end to end. The dev server's own request log confirmed the 5 attempts were **strictly sequential** (each new call started only after the previous one's response had already returned) — ruling out true concurrent duplicate triggers (e.g., two tabs racing) as the mechanism for this specific burst, though the exact remaining trigger is still not conclusively identified. Added temporary `console.debug` diagnostic logging to the flight-propose effect (`itinerary-panel.tsx`, clearly marked for removal) so the next occurrence can be diagnosed from the browser's own console instead of reconstructed after the fact from the database — the user hasn't been able to reproduce it since, so this remains open.

**A real, different, fully-confirmed bug found from a follow-up report:** the user then hit "We couldn't find a commercial airport for that city" when clicking "Change" on an already-confirmed flight (a Newark → Mumbai trip). Root-caused by direct reproduction against the live trip's real data (not guessed at) — `parseDestinationQuery("Newark, New Jersey")` parses the origin as `{city: "Newark", country: "New Jersey"}` (its "City, Country" splitting has no way to tell a US state from a country), and `findAirportsForCity`'s country filter then finds zero airports with `country === "New Jersey"`, wiping out Newark's real, single, unambiguous airport (EWR) — confirmed with a throwaway script run directly against the trip's stored `origin` value, reproducing the exact failure before the fix and resolving cleanly after.

**Fix:** `findAirportsForCity` (`src/domain/airport-lookup.ts`) now only treats the parsed "country" as a real disambiguating filter when it's an *actual* country present somewhere in the airport dataset (a new `KNOWN_COUNTRIES` set) — otherwise it falls back to the unfiltered city match instead of rejecting a real city over what was never meant as a country in the first place. Deliberately preserves the existing, correct "wrong country" rejection (`findAirportsForCity("London, France")` still correctly returns `[]`, since France *is* a real country, just not one of London's) — verified both cases directly, and added a regression test (`airport-lookup.test.ts`) for the "City, State" case alongside the existing "wrong country" one.

**An open question, not fully resolved:** the trip's own log shows the *original* cold-start flight propose for this same origin ("Newark, New Jersey", stated verbatim in the user's first chat message) succeeded and was confirmed — only the later revision (via "Change") failed. Since both paths call the exact same `resolveFlightAirportOrThrow` with the exact same stored `origin` value, this is not fully explained; a live reproduction against the trip's *current* data confirms the bug is real and the fix closes it, but why the very first call didn't hit it too remains an open question, noted rather than papered over.

**Decisions made:** none requiring the user's input — both are confirmed, narrowly-scoped bugs with clear, tested fixes.

**Verification:** `npx tsc --noEmit`/`npm run lint`/`npm test` (451/451, up from 450 — new `findAirportsForCity` regression test) all pass; dev server compile log clean. The airport fix was verified three ways: a throwaway script against the real failing trip's real stored `origin` (reproduced the failure, then confirmed the fix resolves it), the new unit test, and a direct check that the deliberate "wrong country" rejection (`"London, France"`) still returns `[]` unchanged.

**Known limitations:** the flight-propose loop's exact remaining trigger is still unconfirmed — the circuit breaker bounds its damage but doesn't explain it. Diagnostic logging is left in place (temporary, marked for removal) for the next occurrence. The "why did the first propose succeed" question for the airport bug is unresolved but doesn't block the fix, which is independently verified against the trip's real current data.

**Next recommended task:** Have the user retry "Change flight" on a trip with a US "City, State" origin to confirm the airport fix live. Continue watching for the loop to recur with the new diagnostic logging active — if/when it does, get the browser console output (not just database reconstruction) to finally pin the exact mechanism.

---

## 2026-09-18 (continued) — Responsive layout audit + design pass, then built mobile/tablet/desktop layouts

**What happened:** The user asked whether the app is responsive across desktop/tablet/mobile (it "doesn't seem like it is") and wanted a plan plus 2-3 concrete design pattern examples for the chat+itinerary split view specifically.

**Audit confirmed the suspicion, with evidence, not a guess:** grepped the whole app for `sm:`/`md:`/`lg:` breakpoints — none exist in the core product UI (`chat-panel.tsx`, `itinerary-panel.tsx`, `trip-workspace.tsx`); the only hits are in the internal (non-product) analytics dashboards. The chat+itinerary screen is a fixed `flex` row: chat is `flex-1`, itinerary is a hardcoded `w-96` (384px) `flex-shrink-0` sidebar. On any real phone (375-430px wide) that sidebar alone is wider than the whole screen. Verified the landing/sign-in page is fine at mobile width (no fixed widths there) — this is specifically a split-view problem, not an app-wide one.

**Built and published a Design-canvas Artifact** (`https://claude.ai/artifact/PWSTfjkKAWLaCody7nxhiZ`) mocked up against the real palette/type (sand/navy/teal/terracotta, Fraunces + system sans, not generic placeholders): a "current problem" frame showing the actual overflow, plus three patterns each at mobile and/or tablet size — (1) tab switcher (one full-screen view at a time, clickable in the mock), (2) chat-first with the itinerary as a bottom sheet on mobile / a slide-in side drawer on tablet-portrait (also clickable), (3) adaptive collapsible split for tablet-landscape (a flexible ~380px panel with a collapse toggle, clickable), degrading to pattern 1 below ~700px. Recommended pattern 2 for phone/tablet-portrait and pattern 3 for tablet-landscape; the user agreed almost exactly (pattern 2 for both mobile *and* vertical tablet, pattern 3 for horizontal tablet, desktop untouched for now).

**Design fork worked through with the user, not assumed:** they asked for the mobile/tablet drawer to auto-open whenever the itinerary needs a decision, auto-close once resolved (or on a manual swipe/tap down), and specifically flagged the case where flight and hotel are picked back to back — should it really collapse and reopen between the two? Landed on driving the drawer off a single derived signal — `actionableSignature`, one of `"flight"`/`"hotel"`/`"activities"`/`"cascade"`/`"budget-override"`/`""` computed from state `ItineraryPanel` already tracks — rather than off individual events. This handles the back-to-back case for free: hotel's candidates are already loaded by the time flight confirms (`handleConfirmFlight` sets them directly from its own result), so the signal never actually dips to "nothing pending" in between — the drawer stays open and just swaps its content, no flicker. It only actually auto-collapses once the signal genuinely goes empty (e.g. flight+hotel both done, next step is a *chat* prompt, not an itinerary one). A manual collapse is remembered per-signal (via React's "reset state when a value changes" pattern, done during render rather than in a `useEffect`, so it never fights the automatic behavior for a *new* pending decision) rather than globally, so dismissing "pick a flight" doesn't also suppress "pick a hotel" showing up right after. Explicitly scoped out real swipe-gesture physics (a tappable drag handle instead) as a deliberate, flagged trade-off rather than silently cutting it.

**Built:** `use-layout-mode.ts` (new) — a `useLayoutMode()` hook picking `"drawer"` (≤839px: mobile + tablet-portrait) / `"split"` (840-1149px: tablet-landscape) / `"sidebar"` (≥1150px: today's fixed layout, byte-for-byte unchanged) from viewport width alone, not the `orientation` media feature (a tablet rotated to landscape is simply wider than the same tablet in portrait, so width alone already tracks it, without `orientation`'s flakiness around on-screen keyboards). `ItineraryPanel` gained a `layoutMode` prop; its existing ~330-line render body is now built once as a `content` fragment and wrapped by one of three shells: the original `<aside className="w-96">` (sidebar, unchanged), a new `ItineraryDrawerShell` (a `position: fixed` bottom sheet — short peek bar showing a derived status line like "Flight ✓ · Hotel ✓ · Pick your activities" when collapsed, ~82vh sheet over a dimming scrim when open), or a new `ItineraryCollapsibleSplit` (a flexible `w-[380px]` side panel with a collapse chevron and a floating "Show itinerary" tab when collapsed, manual toggle only — this mode deliberately never auto-opens, since the desktop-like extra width it targets doesn't need it). `ChatPanel` gained the same `layoutMode` prop purely to add bottom padding in `"drawer"` mode, so the always-present peek bar doesn't sit on top of the message input. `TripWorkspace` calls `useLayoutMode()` once and threads it to both.

**A real lint catch worth recording:** the first cut of the auto-open logic used a `useEffect` calling `setDrawerOpen` synchronously — `react-hooks/set-state-in-effect` correctly flagged the cascading-render smell. Replaced with React's documented "adjust state during render" pattern instead (compare `actionableSignature` against a stored previous value; if it changed, reset the manual-override state right there, no effect) — same behavior, one fewer render-then-effect round trip, and the derived `drawerOpen` boolean itself needs no state of its own at all.

**Verification:** `npx tsc --noEmit`/`npm run lint`/`npm test` (451/451, unchanged — no component-level tests exist for `app/` in this codebase) all pass; dev server compile log clean. Not verified via a live authenticated click-through at each breakpoint — same auth-boundary constraint as every other UI change this session (no `/dev-signin`, no session in the agent's own browser pane). While iterating on this, the still-open browser tab from earlier testing (trip `1825006c-b64d-4085-a8a8-5f04391ed341`) picked up several of these edits via Fast Refresh and briefly re-ran the hotel auto-propose effect a few times in a row (a fresh-mount "catch-up" propose is expected/harmless per this module's own long-standing design note; several of them in quick succession here was Fast Refresh forcing repeated remounts as hook signatures changed across edits, not a recurrence of the still-unexplained live-testing loop) — the circuit breaker capped each burst at 5 as designed, and the hotel decision rows confirm nothing was wrongly confirmed. A genuinely new, unrelated data point on the earlier mystery, not a resolution of it.

**Known limitations:** the tablet-landscape split's collapsed "Show itinerary" tab and the drawer's expanded-sheet scrim both use `position: fixed`, so they anchor to the full viewport rather than accounting for the app header's height — a minor, likely-invisible cosmetic offset, not corrected since it's unverified whether it's even noticeable live. Real swipe-to-dismiss is deferred (see above) in favor of a tappable drag handle.

**Next recommended task:** A live click-through at 375px/834px/1180px/1440px once the user's back, to confirm the auto-open/collapse behavior feels right in practice (especially the "stays open through a back-to-back flight+hotel pick" case) and that nothing about the fixed-position drawer/tab overlaps the app header awkwardly.

---

## 2026-09-18 (continued) — Real drag physics for the mobile/tablet-portrait drawer, via Base UI instead of the originally-recommended vaul

**What happened:** The user asked how hard real swipe-up/down gesture support would be for the drawer built in the previous entry (which only had a tappable drag handle, no actual drag physics — a deliberately flagged trade-off). Gave an honest estimate: hand-rolling drag tracking/velocity/scroll-disambiguation is real but bounded work, with the main risk being that tuning the *feel* normally needs hands-on device testing this agent can't do (no authenticated session, no real device). Recommended `vaul` (the standard React library for this pattern) as the faster, better-tested path instead of hand-rolling it. The user said yes.

**Caught before installing, not after:** checked `vaul`'s own README before adding it as a dependency — it states outright "This repo is unmaintained." Corrected course rather than proceeding with the original recommendation: searched for the current state of the ecosystem and found that shadcn/ui itself moved its Drawer component off `vaul` onto **Base UI** (`@base-ui/react`, from the team behind Radix/MUI) as of a recent release. Verified directly against the npm registry before committing to it: `@base-ui/react@1.8.0`, `peerDependencies.react: "^17 || ^18 || ^19"` (this project is on React 19.2.8), with its only other peer dep (`date-fns`, for unrelated date-picker components) marked `optional: true`. Fetched Base UI's own Drawer docs and its installed TypeScript source (`node_modules/@base-ui/react/drawer/**/*.d.ts`) to confirm the real component/prop API before writing against it, rather than coding from a summarized recollection.

**Built:** installed `@base-ui/react` (one new dependency, justified above). Rewrote `ItineraryDrawerShell` (`itinerary-panel.tsx`) around `Drawer.Root`/`Drawer.Portal`/`Drawer.Backdrop`/`Drawer.Popup`/`Drawer.Content` in place of the hand-rolled fixed-position divs from the previous entry. Modeled the peek/expanded distinction as two `snapPoints` (`0.08`/`0.82`, fractions of viewport height) rather than a plain open/closed boolean — the peek bar is real, always-visible content, never actually unmounted, so `open` stays a constant `true` and `ItineraryPanel`'s own `open` prop instead selects which snap point is active via the controlled `snapPoint`/`onSnapPointChange` props. `modal` is bound to that same boolean: full focus trap + scroll lock only while genuinely expanded over most of the screen, not while just peeking, where chat underneath should stay fully usable — a refinement Base UI's per-instance `modal` prop made easy that the original hand-rolled version didn't have. Velocity-based snapping, drag-vs-scroll disambiguation against the content's own scroll area, and focus/escape handling all now come from Base UI rather than being reinvented. The `ItineraryPanel`-level auto-open/collapse logic from the previous entry (the `actionableSignature`-driven derivation) is completely unchanged — only what *renders* the sheet changed, not what decides when it opens.

**Decisions made (asked, not assumed):** the user approved integrating a gesture library for this; swapping *which* library (Base UI instead of the originally-named vaul) was a judgment call made transparently rather than silently, once the maintenance-status check turned up a real problem with the original recommendation.

**Verification:** `npx tsc --noEmit`/`npm run lint`/`npm test` (451/451, unchanged) all pass — the prop usage (`open`, `modal`, `snapPoints`, `snapPoint`, `onSnapPointChange` all typed against the installed package's own `.d.ts` files) compiled clean on the first attempt. Dev server compile log clean (a `confirmTripDecisions doesn't exist` error that appeared in a `preview_logs` read was independently confirmed stale — reading the actual current file showed the correct `confirmTripDecisionById` import from several entries ago; this ring-buffer-surfacing-old-errors behavior has recurred several times this session and no longer indicates a real problem when it does). Not verified via a live touch/drag interaction — same auth-boundary constraint as everything else today; the user's own device testing is the real verification for whether the drag feel is right.

**Known limitations:** none new beyond what the previous entry already flagged (fixed-position elements not accounting for the app header's height).

**Follow-up fix, same day:** the user hit a real console error immediately on their next test — `Base UI: <Drawer.Popup> expected to be rendered within <Drawer.Viewport>` — caught from the exact warning text they pasted, not re-diagnosed from scratch. `Drawer.Viewport` (the positioning container Base UI's own docs example wraps `Popup` in) had been left out of the first cut; the `Popup` was given the `fixed inset-x-0 bottom-0` positioning directly instead. Fixed by adding `Drawer.Viewport` (`fixed inset-0` + `flex items-end`, matching the docs' own recommended shape) around `Popup`, and moving the "stick to the bottom edge" behavior from `Popup`'s own fixed positioning onto the `Viewport`'s flex alignment. Per Base UI's own warning text, omitting `Viewport` doesn't just log a warning — it silently disables the actual swipe/touch-scroll-lock handling, i.e. exactly the feature this integration exists for, so this wasn't cosmetic. `npx tsc --noEmit`/`npm run lint`/`npm test` (451/451) all pass; confirmed via the dev server's own log that the warning stopped appearing on the page load immediately after the fix compiled.

**Next recommended task:** Live device testing of the actual swipe gesture feel, once the user is back — this is the one piece of today's responsive work that most needs hands-on verification rather than code review.

---

## 2026-09-18 (continued) — Phone testing setup, then a real magic-link sign-in gap found and fixed

**What happened:** The user asked how to test the app on their phone. Explained GitHub Pages can't run this app at all (static hosting only; this needs server actions, API routes, and server-side Supabase auth) and pointed them to `next dev`'s own printed Network URL instead (same Wi-Fi, no deployment needed), flagging two likely gotchas up front: Windows Firewall blocking inbound connections from other devices, and Supabase's magic-link redirect needing the network URL's origin in its allowed-redirect-URL list.

They added the redirect URL and could load the page from their phone, but the magic-link sign-in went nowhere — no email arrived, no visible feedback after submitting. Checked `supabase/config.toml` before guessing: `[auth.email.smtp]` is commented out (no custom email provider configured), meaning sign-in emails go through Supabase's default built-in mailer, which is deliberately rate-limited (the local config shows `email_sent = 2`/hour) since it's meant for testing, not real delivery volume — and given how much sign-in activity this session's testing has already generated, that limit was very likely already hit.

**A real, separate bug found while checking the code, not just the config:** `app/page.tsx`'s `handleSubmit` had no `try`/`catch` around `supabase.auth.signInWithOtp(...)`. That call normally *resolves* with `{ error }` for an API-level failure (including a rate-limit rejection, which the existing `if (error)` branch already handled correctly) — but a genuine network-level failure (DNS, a dropped connection, which is a real risk when testing over a phone's Wi-Fi/cell handoff) rejects the promise instead, and with no `catch`, that left `status` stuck on `"sending"` forever: the button stays disabled showing "Sending..." with no error ever shown. That matches "nothing happens" for a real subset of failure modes, not just the rate limit. Fixed by wrapping the call in `try`/`catch` and surfacing a message on the caught path too.

**Decisions made:** none needing the user's input — the SMTP/rate-limit issue is a known Supabase default behavior to explain, not a decision; the missing `try`/`catch` is an unambiguous correctness gap.

**Verification:** `npx tsc --noEmit`/`npm run lint`/`npm test` (451/451, unchanged) all pass. Not independently confirmed which specific failure mode (rate limit vs. a genuine network drop) caused this instance — recommended the user check the Supabase dashboard's Auth logs directly (not accessible to this agent without dashboard/Management API credentials, which weren't requested) to confirm, and to either wait out the built-in mailer's hourly limit or configure a real SMTP provider for anything beyond occasional local testing.

**Known limitations:** the underlying rate limit itself isn't something to "fix" in code — it's Supabase's default, deliberately-limited built-in mailer. A real fix (if this keeps being a testing friction point) would be configuring a custom SMTP provider (e.g. via `[auth.email.smtp]` in `supabase/config.toml` plus the hosted project's own dashboard settings) — not done here since it needs a real external email provider and credentials the user would need to supply.

**Next recommended task:** Confirm sign-in via the Supabase dashboard's Auth logs (rate limit vs. something else), then resume the deferred live device testing of the drawer's swipe behavior once sign-in is unblocked.

---

## 2026-09-18 (continued) — Deployment planning: Vercel hosting brief, `/internal` access narrowed

**What happened:** The user wants to host the app on Vercel over HTTPS so peers can review it, and asked for the concrete steps from current state to a working public deployment. Read the repo (not PROJECT_BRIEF.md/ADRs, since hosting wasn't previously specified there) to answer from what's actually true rather than generic Vercel advice: confirmed it's already a hosted (not local) Supabase project, `emailRedirectTo` already uses `window.location.origin` (no hardcoded localhost), `next.config.ts` is default (no adapter work needed), and `npm run build` passes clean as-is. Found two real gaps while checking: `SERPAPI_API_KEY` is required unconditionally by `app/app/actions.ts` (no seeded-data fallback) but was missing from `.env.local.example`, and magic-link email delivery goes through Supabase's default rate-limited mailer (2/hour), which was already the cause of last entry's sign-in issue and would only get worse with multiple reviewers.

The user then asked for a written deployment brief, plus two specific asks: they'll set up their own SMTP provider for auth email (a Supabase dashboard change, no app code involved), and they want `/internal/*` (the analytics/product-metrics dashboards) locked to their own email specifically — explicitly not worried about API cost abuse (existing per-provider budget caps cover that) or data confidentiality, just the dashboard visibility.

**What I built:**
- `proxy.ts`: added an `INTERNAL_ACCESS_EMAIL` check for `/internal/*`, on top of the existing deny-by-default signed-in check — fails closed (unset env var denies everyone) rather than falling back to "any signed-in user."
- Updated the docstrings in `app/internal/analytics/page.tsx` and `app/internal/product-metrics/page.tsx`, which had explicitly documented "any signed-in user is fine, a role system would be unjustified complexity" as a deliberate Phase 8 decision — noted that this reverses that call now that the app is reachable by more than its one operator, rather than leaving stale reasoning next to code that now contradicts it.
- `.env.local.example`: added the missing `SERPAPI_API_KEY` and the new `INTERNAL_ACCESS_EMAIL`.
- `docs/DEPLOYMENT.md`: the actual brief — step-by-step, each step labeled with what's already done, what only the user can do (Vercel/Supabase dashboard actions, SMTP provider signup), and what (if anything) needs to be handed to the agent (nothing — every remaining step is a dashboard setting or copying already-held `.env.local` values into a dashboard).

**Decisions made:** narrowing `/internal/*` to a single-email allowlist was the user's explicit request this session, not something decided unprompted — but it does reverse a previously-documented architectural call, so the reversal itself (and why) is now recorded in both the docstrings and this entry rather than silently overwritten.

**Verification:** `npm run eval:ci` (typecheck + lint + 451/451 tests) and `npm run build` both pass clean with the `proxy.ts` change. No test added for `proxy.ts` itself — consistent with existing project convention (only `src/` pure-function code has Vitest coverage; route/middleware files like `app/auth/callback/route.ts` have none either). Not verified live in a browser (no code path here is meaningfully different to exercise visually beyond the redirect, which the brief's own step 7 has the user confirm post-deploy).

**Known limitations / open items:**
- Custom SMTP setup, the actual Vercel project creation, and the Supabase Auth URL-configuration change are all outside this agent's access (dashboard-only) and are called out as such in the brief — they're the user's remaining steps, not deferred work.
- `INTERNAL_ACCESS_EMAIL` is a single hardcoded allowlist entry, not a role system — correct for the stated single-reviewer-dashboard need, but wouldn't extend cleanly to "let a second internal person in" without revisiting.

**Next recommended task:** Once the user finishes the dashboard-side steps in `docs/DEPLOYMENT.md`, confirm the deployed sign-in flow and the `/internal` allowlist both behave as expected against the live URL.

---

## 2026-09-18 (continued) — Custom SMTP live; single shared Supabase project confirmed as the deployment model

**What happened:** User set up a Resend account and verified `mail.rohanpandit.io` with it (a subdomain of their own Cloudflare-managed domain, following the "add domain in Resend → add its DNS records in Cloudflare, unproxied → verify" path), configured it as Supabase's custom SMTP provider, and confirmed magic-link email now delivers. One hiccup along the way: Supabase's SMTP "Sender email" field initially rejected the value as "not a valid email" — traced to entering the bare verified domain (`mail.rohanpandit.io`) instead of a full address at that domain (`noreply@mail.rohanpandit.io`); no code involved, just a dashboard field.

Before renaming the Vercel project (to drop the auto-generated `-six-ashen` suffix from `wanderwise-six-ashen.vercel.app`, since `wanderwise` alone was already taken), the user asked whether a future database migration would need to be "pushed to GitHub" to take effect in all environments. Checked `.github/workflows/ci.yml` and confirmed no migration step exists anywhere (CI only runs typecheck/lint/tests against placeholder Supabase values, never a real connection) — migrations are and remain a manual `supabase db push`/SQL-editor step, decoupled from any git push.

That question surfaced a real fork worth flagging rather than assuming: this app has always pointed local dev and the new Vercel deployment at the *same single* hosted Supabase project (same URL/keys copied verbatim into Vercel's env vars) — there's no dev/prod database split. Flagged the consequence explicitly (peer-reviewer trip data and local dev/testing activity share the same tables and the same `/internal/analytics` view) and asked whether to keep it that way or split off a separate production project.

**Decision made:** keep the single shared Supabase project — no dev/prod split. The user's own framing: "it's a portfolio project, not real," i.e. data commingling and the lack of environment isolation aren't real risks here. Recorded in `docs/DEPLOYMENT.md` §6 so this reads as a deliberate call if revisited later, not an oversight.

**What I built:** no code changes this entry — `docs/DEPLOYMENT.md` §6 gained the shared-database decision and its consequences, plus an explicit restatement of the manual migration flow (write `.sql` → apply directly to the hosted project → hand-edit `database.types.ts` to match → commit/push so Vercel's code matches the new schema).

**Next recommended task:** User still needs to rename the Vercel project (to shed the random suffix) and re-point Supabase's Site URL/Redirect URLs at whatever the resulting domain is, then finish the deployment brief's remaining steps (env vars in Vercel, verify-and-share).

---

## 2026-09-18 (continued) — Two real drawer bugs found on live phone testing, root-caused against Base UI's own docs/source

**What happened:** User finally got to test the mobile drawer (`ItineraryDrawerShell` in `app/app/_components/itinerary-panel.tsx`) on an actual phone — the swipe-gesture verification flagged as outstanding in every prior entry touching this component. Reported two real bugs: swipe-up (peek → expanded) works fine, swipe-down doesn't; and once the drawer exists on screen at all, the chat textbox underneath becomes unclickable, focused or not.

Read `node_modules/@base-ui/react/docs/react/components/drawer.md` (the installed package's own docs — per `AGENTS.md`, authoritative over training data for this fast-moving library) and the actual installed source (`drawer/root/DrawerRoot.js`) rather than guessing, since both bugs traced to specific, documented Base UI mechanics this component's original implementation didn't account for:

1. **Chat box unclickable:** `Drawer.Viewport` is `fixed inset-0` and — by this component's own deliberate design (the peek bar must stay mounted, never unmount) — is *always* rendered, even at the peek snap point. With no `pointer-events: none`, that full-screen positioning box silently intercepts every tap across the entire screen, not just the sliver the visible peek bar occupies, because CSS hit-testing goes by the element's box, not its visible content. The docs' own "Non-modal" example uses exactly this split for the same reason: `pointer-events-none` on `Viewport`, `pointer-events-auto` on `Popup` to opt the actually-visible sheet back in.
2. **Swipe-down broken:** read `DrawerRoot.js` directly to confirm the mechanism — its `handleOpenChange` wrapper unconditionally resets the active snap point back to `snapPoints[0]` whenever the underlying dialog logic requests a close (`nextOpen = false`), which a strong-enough downward swipe triggers internally regardless of whether the consumer's `open` prop can actually become `false`. This component hardcodes `open` to a literal `true` (intentional — see its own docstring) and had no `onOpenChange` at all, so a firm downward swipe crossed Base UI's internal dismiss threshold, ran its closing transition, and then got yanked back since the external `open` never changed — exactly matching "not smooth/broken," and asymmetric with swipe-up because `swipeDirection="down"` is the only configured dismiss direction. The docs' "Close confirmation" example documents the fix directly: cancel the close request in `onOpenChange` via `eventDetails.cancel()` and redirect it, which here means routing it into the existing `onCollapse()` handler instead.

**What I built:** two changes in `ItineraryDrawerShell` (`app/app/_components/itinerary-panel.tsx`): added `pointer-events-none`/`pointer-events-auto` on `Drawer.Viewport`/`Drawer.Popup`; added an `onOpenChange` handler that cancels any close request and calls `onCollapse()` instead, so a hard downward swipe settles at the peek snap point rather than fighting an unhandled close animation.

**Verification:** `npx tsc --noEmit`/`npm run lint`/`npm test` (451/451, unchanged) all pass. **Not verified live on a phone** — this session has no authenticated browser session (magic-link sign-in needs real email access this environment doesn't have), so the actual gesture feel is still unconfirmed pending the user's own device test, same constraint as every prior entry touching this component. Both fixes are root-caused against the installed library's own docs and source rather than guessed, which is a stronger basis than the trial-and-error fixes earlier in this component's history, but device confirmation is still the real bar.

**Known limitations:** if swipe-down still isn't fully smooth after this, the next place to look is whether `modal={open}` toggling — which only updates on React's next render *after* `onSnapPointChange` fires, not mid-gesture — causes the focus-trap/scroll-lock to stay engaged throughout a full-to-peek drag in a way that interferes with touch handling on some mobile browsers; not chased further this session since it's speculative without a repro.

**Next recommended task:** User to re-test swipe-down and the chat-box-click issue on their phone against this fix; if either persists, capture exactly which gesture/browser reproduces it (iOS Safari vs. Android Chrome matters here) rather than guessing further.

---

## 2026-09-18 (continued) — iOS keyboard zoom cutting off unrelated UI; found root cause across three inputs

**What happened:** The previous drawer fixes tested better on the user's phone, but two new issues appeared once the on-screen keyboard opens on iOS: (1) the keyboard's own accessory UI ("close keyboard, whatever else") covers a lot of the screen, and (2) the whole viewport looked zoomed in, cutting off the right edge of the first chat bubble and clipping "Your trips"/"Sign out" in the header.

(2) is a well-known, single-root-cause iOS Safari behavior, not two separate layout bugs: Safari auto-zooms the entire page on focus for any text input/textarea whose font-size is under 16px, to keep the zoomed text legible — it doesn't just enlarge the input, it zooms the whole viewport, which is exactly what pushes unrelated elements (the header nav, the chat bubble) outside the visible area. Grepped every `<input>`/`<textarea>` in `app/`: all three (`app/page.tsx`'s email sign-in field, `chat-panel.tsx`'s message input, its activities-preference textarea) used Tailwind's `text-sm`/`text-xs` (14px/12px) — under the threshold. Fixed all three to `text-base` (16px, the minimum that avoids the zoom), not just the message input that was reported, since it's the identical bug in the identical element class.

(1) — the native keyboard accessory bar (predictive-text/QuickType row, the "hide keyboard" chevron) is OS/browser chrome a web page cannot remove; said so directly rather than implying a fix exists. The one thing actually in this app's control was reduced: the message input had no `autoComplete`, so Safari may show an autofill-suggestion strip on top of the keyboard as if it were a contact/address field, which it isn't — added `autoComplete="off"` (plus `enterKeyHint="send"` for a correctly-labeled return key, a free improvement while touching this element). Flagged as a partial mitigation, not a full fix — most of what was reported as "blocking a lot of the screen" is plausibly the zoom bug (1) compounding with the keyboard's normal footprint, so expect this to look substantially better once (2) alone is retested, even before judging whether the accessory-bar complaint has anything left in it.

**What I built:** `text-sm`/`text-xs` → `text-base` on all three inputs (`app/page.tsx`, `app/app/_components/chat-panel.tsx`); `autoComplete="off"` + `enterKeyHint="send"` added to the chat message input.

**Verification:** `npx tsc --noEmit`/`npm run lint`/`npm test` (451/451, unchanged) all pass. Not verified live on a phone — same access constraint as every entry today.

**Next recommended task:** User to re-test both issues on their phone. If the keyboard-accessory-bar complaint still has substance left after the zoom fix, get a screenshot — that's OS chrome, so any further move (if one even exists) would need to see exactly what's rendering.

---

## 2026-09-18 (continued) — Third drawer bug: activities list couldn't be scrolled, dragged the sheet instead

**What happened:** The keyboard-zoom fix looked good on the user's phone. Next issue: once the activities candidate list is long enough to need scrolling inside the expanded itinerary drawer, touch-dragging it moves the whole drawer instead of scrolling the list.

Compared `ItineraryDrawerShell` (`app/app/_components/itinerary-panel.tsx`) against every bottom-sheet/snap-point example in the installed `@base-ui/react` Drawer docs — all of them apply a specific `touch-action` split that this component was missing entirely: `touch-none` on `Drawer.Popup` (so native touch handling is disabled there and the library's own JS drag physics own the gesture) and `touch-auto overscroll-contain` on `Drawer.Content` (so touches starting over the actual scrollable content get native scrolling instead of being captured as a drag). Without that split, a touch-move anywhere in `Popup` — including inside the activities list, which lives inside `Content` — was ambiguous between "resize the sheet" and "scroll the list," and it was resolving in the drawer's favor every time.

**What I built:** added `touch-none` to `Popup`'s className and `touch-auto overscroll-contain` to `Content`'s className, matching the docs' own bottom-sheet examples exactly (no invented values).

**Verification:** `npx tsc --noEmit`/`npm run lint`/`npm test` (451/451, unchanged) all pass. Not verified live on a phone — same constraint as every fix today; this is the third drawer bug found this way (docs/source comparison, not guessing) and the third still pending the user's own device confirmation.

**Next recommended task:** User to retest scrolling the activities list on their phone. If there's a fourth drawer issue, it's worth stepping back and doing one clean end-to-end gesture pass (expand, collapse, scroll, swipe-down-to-peek, swipe-down-past-peek) in one sitting rather than fixing one report at a time, since several of today's bugs came from the same handful of missing docs-recommended attributes.

---

## 2026-09-18 (continued) — Scroll fix incomplete; abrupt open transition; agent's own mobile-testing limits discussed

**What happened:** The `touch-none`/`touch-auto` fix didn't fully resolve the activities-list scrolling issue — still broken on retest. Separately, the user flagged that the drawer's first appearance when flight picking becomes relevant looks like a glitch rather than an intentional transition. They also asked, independent of these bugs, what could be done so I can test mobile interactions myself instead of relying on their phone every time.

**Scrolling, take two:** re-read the Drawer docs' usage guidelines more carefully — the `touch-action` split only tells the *browser* how to handle native panning; the docs separately state Base UI's own swipe-dismiss recognition can still capture a descendant on touch input regardless of that, and the documented opt-out for "for all input types" is the `data-base-ui-swipe-ignore` attribute, not CSS. Added it to `Drawer.Content`.

**Abrupt open, root-caused:** the `Drawer.Popup` element had no `transition` property at all on its `transform`/`height` (both CSS-variable-driven, per this component's own two-content-state design — the peek button and the full itinerary body are different children with different natural heights, not one fixed-size box revealed via a sliding window). With no transition, the position/size jump between snap points was instant, which is exactly "looks like a glitch." Separately, `Drawer.Backdrop` was conditionally rendered (`{open ? <Backdrop/> : null}`) rather than always-mounted — a freshly-mounted element has no prior style to animate *from*, so it popped in at full darkness the instant the sheet expanded regardless of any transition class. Fixed both: added `transition-[height,transform]` (450ms, matching the easing every other transition in this component already uses) to `Popup`, and switched `Backdrop` to always-mounted with its opacity driven by `open` plus the same transition — and, since it's now always mounted like `Viewport`, gave it `pointer-events-none` while collapsed for the identical reason `Viewport` needed it two entries ago.

**On testing mobile myself:** explained honestly rather than overselling it — I have a browser tool that can emulate a mobile *viewport* (size, touch-capable user agent), which is genuinely useful for layout/responsive/click-target bugs, but its drag/click actions are pointer/mouse-based, not real multi-touch `touchstart`/`touchmove` — `touch-action` CSS specifically only constrains touch/pen input, so a simulated mouse-drag wouldn't have reproduced today's `touch-action` scrolling bug even in a mobile-sized viewport. The concrete blocker on doing *any* independent testing so far has actually been simpler: no authenticated session in this agent's own browser (magic-link needs a real inbox this environment doesn't have access to), which is why every fix today has been docs/source-verified but not click-tested by me at all, even for the plain-click bugs a mouse could have caught. Proposed the user forward me a fresh magic-link URL (not click it themselves first, since it's one-time-use) so I can sign in independently in my own browser pane going forward — would let me self-verify layout/click/scroll issues directly, while real swipe-gesture *feel* still needs their phone.

**What I built:** `data-base-ui-swipe-ignore` on `Drawer.Content`; `transition-[height,transform]` on `Drawer.Popup`; `Drawer.Backdrop` changed from conditional-mount to always-mounted with opacity/pointer-events driven by `open`.

**Verification:** `npx tsc --noEmit`/`npm run lint`/`npm test` (451/451, unchanged) all pass. Not verified live — awaiting either the user's phone or, pending their answer on the magic-link idea above, this agent's own browser session.

**Decisions made:** none yet — the magic-link testing proposal is pending the user's answer, not decided unilaterally, since handing over any sign-in credential (even a scoped, one-time magic link) is the user's call to make.

**Next recommended task:** Depends on the user's answer above. If they send a magic link, verify the scroll/transition fixes directly in this agent's own browser session against the deployed Vercel URL before asking for another phone round. If not, push and wait for the next phone retest as usual.

---

## 2026-09-18 (continued) — First agent-authenticated session; full design review found a real requirement-revision retry gap

**What happened:** The user sent a magic-link URL to give this agent its own authenticated browser session for a full mobile/tablet/desktop UI design review. First attempt failed (`?error=auth_callback_failed`) — traced to PKCE: Supabase magic links tie the sign-in code to a `code_verifier` stored only in the browser that requested it, so a link forwarded from the user's device/browser can't be redeemed elsewhere. Not a bug, just how PKCE is supposed to work. Fix: requested a fresh magic link from *this agent's own browser* (typed the user's email into the real sign-in form, clicked send), had the user forward that one instead — redeemed successfully.

Per the user's earlier instruction this session, design-review findings go in `docs/END_TO_END_TESTING_ISSUES.md`, not this log or `IMPLEMENTATION_PLAN.md` §5 (updated the standing memory on this too, since a more precise split now exists between the two files than previously recorded).

Started the actual review at desktop size (1440×900) by creating a real test trip (NY→Lisbon, Nov 10–15, $2500, live Anthropic/Voyage/SerpAPI calls). Found the flight search failing with a legitimate "no flights match" outcome, then — trying to recover by raising the budget via chat — found it never retried at all. Investigated this specific bug in depth this session (full root cause and fix in `docs/END_TO_END_TESTING_ISSUES.md` #8): a real asymmetry in `intake-orchestrator.ts`'s revision-routing — a safe decision revision re-proposes the active step automatically; a safe requirement revision targeting that same active step didn't, silently. Fixed both that gap and a related, smaller display bug (flight/hotel sections showing "Finding…" forever after already failing).

Also noted in passing, not yet investigated: on 1440px desktop, the empty chat panel (no active trip) stretches full-width with no max-width container, reading as very sparse/wide on a large screen — flagged for the design-review pass proper, not fixed yet, since the session got diverted into the functional retry bug instead.

**What I built:** `applyRevisionProposal` (`src/workflow/intake-orchestrator.ts`) now signals `decisionRevisionRequested` for a requirement revision targeting the active step, mirroring the existing decision-revision branch. `ItineraryPanel` (`itinerary-panel.tsx`) gained `flightProposeFailed`/`hotelProposeFailed` state so the flight/hotel sections show a clear failed state instead of an indefinite "Finding…" once a propose has already errored.

**Decisions made:** none needing the user's input — both fixes close a gap in already-existing, already-reviewed machinery rather than introducing anything new; the user approved digging in and implementing both ("yes") after the diagnosis was presented.

**Verification:** `npx tsc --noEmit`/`npm run lint`/`npm test` (452/452, up from 451 — one new regression test plus a strengthened existing one) all pass. Not yet re-verified live against the redeployed app — pushing now, then continuing the review (including retrying the exact original stuck scenario) against the fresh deployment in this agent's own authenticated session.

**Next recommended task:** Push, wait for Vercel to redeploy, re-run the exact repro (fail a flight search, raise the budget, confirm it now retries and the section no longer shows a stale "Finding flights…"), then resume the broader mobile/tablet/desktop design-review pass that this bug interrupted — including the flagged full-width empty-chat-panel spacing issue on desktop.

---

## 2026-09-18 (continued) — Confirmed fix #8 live; found and fixed a more serious drawer sizing bug; systemic flight-search failure flagged for the user

**What happened:** Pushed the retry-signal + failed-state fixes and confirmed the second half live: on the same stuck trip, expanding the drawer now correctly showed "Couldn't find matching flights — see the message above..." instead of an indefinite "Finding flights…" — the UI half of issue #8 is confirmed working. The retry-trigger half couldn't be cleanly confirmed on that same trip: re-checking `flightHardConstraints` (`step-shared.ts`) showed budget (`budgetTotalUsd`) was probably never the actual blocking constraint for this trip at all (no `maxFlightPriceUsd`/`noRedEye` were ever set, so nothing should have been filtering flights by price) — meaning the failure was likely a genuine zero-real-results case from SerpAPI, not something a budget increase could ever fix, making this specific trip a poor test case regardless of the fix's correctness.

Tried a second, different route (Boston→London, $6,000 budget, a route with confirmed real inventory per issue #5's history) to get a cleaner repro — it also failed with zero flights. Two different routes both failing regardless of generous budget points to something systemic, most plausibly the SerpAPI free-tier quota (250/month, per issue #5) being exhausted by today's volume of testing, with a quota/rate-limit response apparently being treated as "zero results" rather than the app's own distinct "search temporarily unavailable" message. Flagged to the user to check the SerpAPI dashboard directly rather than spending more real API calls chasing it blind — not investigated further this session.

Pivoted to the trips list instead of forcing more live searches — found three pre-existing "Finalized" trips from earlier sessions, letting the rest of the review continue without new API spend. Along the way, corrected a self-caught false positive: a "clipped nav/sidebar" appearance on a 1024px tablet-landscape screenshot turned out to be this session's own screenshot tool capping image capture at ~800px regardless of the emulated viewport, not a real layout bug — confirmed via direct `getBoundingClientRect` measurement (`nav` at x=869–1000, `aside` at x=644–1024, both fully within the real 1024px page) before reporting anything. Adopted DOM measurement over screenshots as the primary check for any width beyond ~800px going forward. The same rigor turned up a real one: at 1440px, the chat `<section>`/`<form>` measured `width: 1440` with `max-width: none` — confirmed with hard numbers, not just a screenshot impression (logged as open issue #11, not fixed this session).

Testing the scroll fix from earlier today against a finalized trip's genuinely long itinerary text (rather than the short content everything had been tested with so far) surfaced a real, more serious bug: a mouse-wheel scroll attempt produced zero movement, and direct measurement showed why — `Drawer.Popup` was 1387px tall on an 812px viewport (its unclamped natural content height, since `[height:var(--drawer-height)]` reflects Base UI's own measurement of the popup, not a value clamped to the snap point), leaving roughly the bottom half of the itinerary text rendered below the visible screen with nothing to scroll it into view. Full root cause and fix in `docs/END_TO_END_TESTING_ISSUES.md` #12: replaced the `height` var with a `max-height` tied to `DRAWER_FULL_SNAP`, set via inline `style` (not a Tailwind arbitrary class, which can't interpolate a JS constant at build time).

**What I built:** `Drawer.Popup` now caps itself at `max-height: 82vh` (via `style`, synced to `DRAWER_FULL_SNAP`) instead of setting an unclamped `height` from Base UI's own measurement var.

**Decisions made:** none needing the user's input — a straightforward correctness fix, and the systemic flight-search issue was surfaced rather than chased further, per the standing practice of not spending more real API budget once a plausible non-code explanation (SerpAPI quota) exists that only the user can actually check.

**Verification:** `npx tsc --noEmit`/`npm run lint`/`npm test` (452/452, unchanged) all pass. Confirmed live: the failed-state display fix (issue #8's UI half) rendering correctly on the stuck trip, and the full-width chat panel (issue #11) via direct measurement. Not yet confirmed live: the new drawer max-height fix (#12, pushed but not yet reloaded against the finalized trip), and the requirement-revision retry trigger itself (#8's other half — blocked on a clean repro, not on anything checkable from here).

**Known limitations:** the systemic "zero flights for every route tried today" issue is unresolved and needs the user's own SerpAPI dashboard access to diagnose — this agent has no path to check API quota/error state from the browser.

**Next recommended task:** Push this fix, confirm live against the finalized Mumbai trip (long itinerary text should now be fully scrollable into view on mobile), then continue the broader design review — the full-width desktop chat/list panels (#11) and the Enter-to-send/simultaneous-error-display issues (#9/#10) are still open and un-investigated.

---

## 2026-09-18 (continued) — Confirmed the max-height fix live; corrected the SerpAPI theory; closed #9/#10/#11

**What happened:** Confirmed issue #12 (drawer taller than viewport) fixed live — reloaded the same finalized Mumbai trip on mobile, scrolled the expanded sheet, and reached "Trip finalized" at the bottom, previously unreachable. The user then corrected the SerpAPI-quota theory from the prior entry: actual usage was 9 of 250 calls, not exhausted — the "zero flights for every route" issue is real and unexplained, not a quota artifact. Instructed to continue the other open issues (#9, #10, #11) and return to the flight-search issue last.

**Issue #9 (Enter doesn't send)**: investigated much further than "defer immediately," since the code looked completely standard (`<input>` in `<form onSubmit>`, `type="submit"` button, no `onKeyDown` anywhere) — a pattern that should submit on Enter by default. Ruled out, with direct evidence rather than assumption: a full page reload (a `window` global and a custom property set directly on a DOM node both survived the keypress), a hydration-timing race (reproduced identically after 5-8s waits), and a Next.js 16 framework change (checked the framework's own bundled docs per this repo's `AGENTS.md` — found nothing, Next's own guides use this exact pattern). What's actually observed: the chat panel's local state silently resets (typed text and transcript both clear) with no visible `sendMessage` call, while the header/layout DOM stays the identical node — consistent with an unpinned client-side re-render. Genuinely couldn't rule out that this session's browser-automation tool doesn't dispatch a fully realistic Enter keypress. Deferred again, but with real findings recorded instead of a guess, and flagged that a real keyboard test would resolve the ambiguity faster than more automated probing.

**Issue #10 (simultaneous error+loading on a fresh trip), root-caused and fixed**: traced to `app/app/trips/[tripId]/page.tsx` computing its `initialRequirementsReady` prop from basic field-presence alone (`REQUIRED_FOR_READY.every(...)`) — the same check used before airport disambiguation existed, never updated once it was added. The server's actual turn-processing gate (`effectiveReady` in `intake-orchestrator.ts`) is stricter: it also requires no pending past-date or airport-ambiguity clarification. A trip whose first message has every base field present but an ambiguous origin city (exactly today's repeated test case: "New York" → JFK/LGA) landed on the newly-created trip's page with the flag already `true`, one turn before the server itself would say so — so the itinerary panel's auto-propose effect searched immediately, before the clarification was ever answered. Fixed by having the page reuse the exact same `checkDatesNotInThePast`/`checkAirportReadiness` functions the server-side turn processor already calls, rather than re-deriving the logic.

**Issue #11 (no max-width on desktop), fixed**: wrapped the chat panel's message list/input row and the whole trips list page in a `max-w-2xl` inner container, keeping the outer scroll/border bars full-width — the same split any mainstream chat UI uses.

**What I built:** `app/app/trips/[tripId]/page.tsx`'s `initialRequirementsReady` now mirrors `effectiveReady`'s three conditions exactly. `chat-panel.tsx` and `app/app/trips/page.tsx` both gained an `mx-auto max-w-2xl` content wrapper.

**Decisions made:** none needing the user's input — both fixes close gaps in already-existing logic; #9 stayed deferred per the user's own instruction to move past it and return to the systemic issue last.

**Verification:** `npx tsc --noEmit`/`npm run lint`/`npm test` (452/452, unchanged) all pass. #12 confirmed live (scrolled a real finalized trip's full itinerary into view on mobile). #10 and #11 not yet re-verified live post-deploy.

**Next recommended task:** Push, then return to the one instruction not yet acted on: investigate why every route tried today (NY→Lisbon, Boston→London, both with generous budgets) returned zero real flights, now that SerpAPI quota is confirmed not the cause.
