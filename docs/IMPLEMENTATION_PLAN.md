# Implementation Plan

Milestones, domain model, schema, and service-interface proposals from the first build session (2026-09-16). This is a living document — update it as milestones complete or scope shifts, per `PROJECT_BRIEF.md` §17.1.

Source of truth for *why* is `PROJECT_BRIEF.md`. This doc is the *how* and *in what order*.

---

## 1. Milestones (mapped to `PROJECT_BRIEF.md` §16 build sequence)

### Phase 0 — Architecture and project setup
- [x] Consolidate and finalize `PROJECT_BRIEF.md`
- [x] Decide app topology (single Next.js/TS app) — ADR-000
- [x] Decide identity/auth strategy (mandatory magic-link) — ADR-003
- [x] Initialize git, create public GitHub repo (`rohan-pandit/wanderwise`)
- [x] Create `CLAUDE.md`
- [x] Create ADR index and the two decided ADRs
- [x] Propose initial domain model and schema (this doc + migration)
- [x] Create `BUILD_LOG.md`
- [x] Scaffold the actual Next.js app (`create-next-app`, TypeScript, App Router, Tailwind)
- [x] Configure environment variables — `.env.local` populated with a **hosted** Supabase project's URL/anon/service-role keys, plus `ANTHROPIC_API_KEY` (see below for why hosted instead of local)
- [x] Supabase workflow established — **against a hosted project, not local Docker** (see below). Migration `0001_initial_schema.sql` pushed via `supabase db push --db-url`. Local Supabase (`supabase start`) remains blocked by a machine-level Windows issue (see `BUILD_LOG.md`, 2026-09-16 "Docker/local Supabase blocker" entry) — `supabase/config.toml` from `supabase init` is still in the repo in case local dev becomes viable again later, but the working setup for now is hosted-only.
- [x] Set up route skeleton: `/` (landing + magic-link sign-in), `/auth/callback`, `/app` (protected — chat + itinerary placeholder), `/app/trips` (history list, live query), `/app/trips/[tripId]` (placeholder)
- [x] Set up test runner (Vitest) with a trivial passing test, wired into `npm test`

### Phase 1 — Domain model and seeded data
- [x] Finalize normalized money/date types — `src/domain/money.ts` (`Money`, currency-checked arithmetic) and `src/domain/dates.ts` (`DateRange`, ISO-date-string based, no `Date` objects in domain code), both unit-tested
- [x] Implement inventory repositories — `src/repositories/{destinations,flights,hotels,activities}.ts`, typed query functions over a `SupabaseClient<Database>`, backing the future `search_flights`/`search_hotels`/`retrieve_activities` tools
- [x] Hand-wrote `src/config/supabase/database.types.ts` (the `Database` type for all Supabase clients) since `supabase gen types` requires Docker, which is still unavailable — keep in sync by hand until Docker works, then regenerate and diff
- [x] Write seed data — 6 destinations, 22 flights, 17 hotels, 27 activities in `supabase/migrations/0002_seed_data.sql` (lives in `migrations/`, not `seed.sql`, so one copy of the data works for both `db push` against hosted and a future local `db reset` — see the file's header comment), pushed to the hosted project and spot-verified against live queries
- [x] Edge-case fixtures — deliberately included: red-eye flights (including the cheapest option on a route, for eval scenario 5), a stale `inventory_version = 0` row in each of the four inventory tables (eval scenario 10), a Monday-closed activity, two activities in Barcelona with overlapping opening hours (for later itinerary-feasibility-engine testing), a full-day (480 min) activity, null `description`/`rating` fields, a non-refundable hotel, a 2-person-capacity hotel (occupancy constraint), and two near-duplicate-looking Lisbon hotels

### Phase 2 — Deterministic services (no LLM)
- [x] `calculateBudget()` — `src/domain/budget.ts`
- [x] `filterHardConstraints()` — `src/domain/constraints.ts`
- [x] `assembleCandidateCombinations()` — `src/domain/combinations.ts`
- [x] `validateItineraryFeasibility()` — `src/domain/feasibility.ts`
- [x] `validateInventoryReferences()` — `src/validation/inventory-references.ts`
- [x] `validateStateTransition()` — `src/workflow/state-machine.ts`
- [x] Unit tests for all of the above, independent of any LLM call (82 total, all passing)

### Phase 3 — State and workflow foundation
- [x] Session/trip repositories (typed CRUD) — `src/repositories/{sessions,trips}.ts`
- [x] State versioning writes, optimistic concurrency — `src/repositories/trip-state.ts` (`unique(trip_id, version)`, plus a partial unique index on `(trip_id, correlation_id)` — `supabase/migrations/0003_trip_state_idempotency.sql`)
- [x] Event history writes — `src/repositories/trip-events.ts`
- [x] Explicit workflow state machine — built in Phase 2 (`src/workflow/state-machine.ts`); Phase 3 wires it into an actual controller
- [x] Retry/idempotency/cancellation handling — `src/workflow/controller.ts` (`startTrip`/`advanceTrip`); cancellation is just the `cancel` event through the same controller (Phase 2's wildcard-from rule)
- [x] Workflow telemetry (`workflow_runs`/`workflow_steps` writes) — `src/repositories/workflow-runs.ts`

### Phase 4 — Intake and revision interpretation

- [x] Structured output schemas (Zod) for requirement/preference/decision extraction — `src/domain/extraction.ts` (`ExtractedRequirement` discriminated union over 14 fields, `ExtractedPreference`, `ClarificationRequest`, `RevisionProposal`, `checkRequirementsComplete()`), pure TypeScript, no model calls, unit-tested (28 tests including calendar-validity edge cases).
- [x] Intake and Revision Interpreter agent — `src/agents/intake.ts` (`runIntakeAgent`), one agent covering both responsibilities per `PROJECT_BRIEF.md` §6.2 row A (intake vs. revision is a framing difference driven by whether `currentDecisions` is non-empty, not a separate code path). Calls a `ModelClient` it doesn't own (`src/agents/model-client.ts`), so its parsing/aggregation logic is unit-tested (14 tests) with a fake client — no real API calls in `npm test`.
- [x] Clarification behavior — built as designed: a single `request_clarification(missing_fields, reason)` tool call the agent makes each turn; the agent decides what to ask and how to phrase it, only the `REQUIRED_FOR_READY` completeness check is deterministic code.
- [x] Revision interpretation — `propose_trip_revision` tool, same agent as intake.
- [x] Component evals for extraction/classification accuracy — `evals/cases/intake.ts` (6 cases: explicit multi-field, ambiguity detection, preference extraction, revision interpretation, adversarial prompt injection, contradictory-input resolution) + `evals/runners/run-intake-eval.ts` (`npm run eval:intake`, real API calls against both candidate models).
- [x] **Model selection — resolved 2026-09-16.** User declined both the bare Sonnet-5 recommendation and Haiku-4.5-only; chose to keep the model-client interface (`ModelClient`) provider/model-agnostic, wire **Sonnet 5** as the working default (`src/config/models.ts`), and let the component eval decide. First real eval run: **Sonnet 5 passed 6/6 cases** ($0.0345 total, ~4.6s avg latency, prompt caching confirmed working — 4,216 cache-read tokens/call from the 2nd call onward); **Haiku 4.5 passed 5/6** ($0.0303 total, ~2.4s avg latency, faster but failed the `revision_request` case — never called `propose_trip_revision` at all) and showed **zero cache hits** across all 6 calls, because Haiku 4.5's minimum cacheable prefix is 4,096 tokens (vs. 1,024 for Sonnet 5 — see the `claude-api` skill's caching doc) and this agent's static system+tools block apparently tokenizes just under that threshold on Haiku's tokenizer. **Sonnet 5 is the confirmed choice** — a real quality gap on revision interpretation, not just a hypothetical judgment concern. Re-run `npm run eval:intake` if the prompt/tools change materially enough to revisit this.
- [x] Set up prompt caching structure — `src/agents/providers/anthropic-model-client.ts`: system prompt cached (`cache_control` on the system block), tool definitions cached (breakpoint after the last tool), dynamic trip-state slice + user message passed last in `messages`, after the cache boundary (`PROJECT_BRIEF.md` §6.7 ordering). Measured via the eval run above (cache-read tokens logged per call), not assumed — a dedicated caching-disabled baseline mode wasn't built separately since the per-case cache-read counts in the eval output already make the on/off contrast visible (call 1 writes the cache, calls 2–6 read it); revisit if a more rigorous A/B comparison is needed later.

### Phase 5 — Retrieval and curation

**Embedding provider — decided 2026-09-16: Voyage AI.** Discussed against OpenAI `text-embedding-3-small` before any code was written (see `BUILD_LOG.md` for the full pros/cons). Anthropic doesn't offer embeddings itself, so some second provider is unavoidable either way; the two weren't a clean win for either side at this project's actual scale (6 destinations, 27 activities) — the deciding factors were narrative fit (Anthropic acquired Voyage AI in 2025, so it's now part of the same vendor relationship rather than a genuinely separate one) and that the schema-migration cost is cheap right now since the `embedding` columns are still empty. **Implementation consequence:** `supabase/migrations/0001_initial_schema.sql`'s `embedding vector(1536)` columns (`destinations`, `activities`) don't match any Voyage model's native output — Voyage's flexible-dimension models support Matryoshka truncation to specific values (256/512/1024/2048, to be confirmed against current API docs when implementing), not 1536. Needs a new migration narrowing the column to whichever supported dimension is chosen (1024 is the likely pick — a reasonable quality/index-size tradeoff) before any embeddings are generated.

- [x] Migration: narrow `destinations.embedding`/`activities.embedding` from `vector(1536)` to the chosen Voyage output dimension — `supabase/migrations/0005_retrieval.sql`, narrowed to `vector(1024)` (`voyage-4-lite`'s default, well-supported Matryoshka truncation point). Same migration adds `match_destinations`/`match_activities` — Postgres functions doing cosine-similarity search with metadata pre-filtering (budget/vibe tags for destinations; price/accessibility/vibe tags for activities) inside the database, not fetched-then-filtered client-side.
- [x] Generate embeddings for seed `destinations`/`activities` (Voyage AI) — `scripts/generate-embeddings.ts` (`npm run generate-embeddings`), idempotent/re-runnable, scoped to `CURRENT_INVENTORY_VERSION` so stale fixture rows are never embedded. Ran live: 6 destinations + 23 current-version activities, well within Voyage's free tier.
- [x] Metadata-filtered retrieval functions — `src/retrieval/{destinations,activities}-retrieval.ts` (`retrieveDestinations`/`retrieveActivities`, the `retrieve_destinations`/`retrieve_activities` tools from `PROJECT_BRIEF.md` §12): embed the query, delegate to the SQL functions above, and — for activities — apply the one filter deliberately kept out of SQL (`excludeClosedOnDaysConstraint`, Phase 2's constraint engine, made generic and reused rather than reimplemented). New provider-agnostic `EmbeddingClient` interface (`src/retrieval/embedding-client.ts`) + Voyage implementation, mirroring Phase 4's `ModelClient` pattern.
- [x] Destination/Activity Curator agent — `src/agents/curator.ts`. **Design departure, documented in-file:** `retrieve_destinations`/`retrieve_activities` are NOT live tool calls this agent makes mid-turn — the orchestrator retrieves candidates first (deterministic params: inventory version, budget ceiling) and hands them to the agent as input, which only ranks/explains via one output-shaping tool (`record_curation`). Avoids introducing a multi-turn tool-execution loop into `ModelClient` for what this phase only needs as a single rank-and-explain step. Hallucination guardrail (`src/domain/curation.ts`): every ranked/excluded ID must come from the candidate set given, checked deterministically.
- [x] Trip Explanation Agent — `src/agents/explanation.ts`, same non-looping/output-shaping/hallucination-guardrail pattern as the Curator, over an already-validated candidate/selection set plus optional budget/constraint context.
- [x] Retrieval quality checks (recall/precision spot checks against seed data) — `evals/cases/retrieval.ts` + `evals/runners/run-retrieval-eval.ts` (`npm run eval:retrieval`), 10 deterministic cases against known seed-data content (semantic ranking, budget filtering, vibe-tag hard-filtering, closed-day exclusion, accessibility filtering). Batches all query embeddings into one Voyage call rather than one per case — Voyage's free-tier rate limit (3 requests/minute without a payment method on file) makes per-case embedding calls impractical; ran live, 10/10 passed.

**Known limitation, tracked (see §5):** `retrieveActivities`'s closed-days post-filter uses a fixed overfetch heuristic (`OVERFETCH_FACTOR = 2`), not a guarantee — the SQL `LIMIT` still runs before the filter, so it could under-fill `topK` at a larger catalog size than this project's seed data. Also worth doing before Phase 7 puts real traffic through retrieval: add a payment method to the Voyage account (still free within the 200M-token tier) to lift the 3 RPM/10K TPM restriction, since the Curator agent will call retrieval roughly once per relevant chat turn.

### Phase 6 — Orchestration integration

Scoped to the one agent that exists so far — the Intake and Revision Interpreter. Wiring later agents (Curator, Explanation Writer, Itinerary Writer — Phase 5/7) follows the same shape once they exist.

- [x] Wire agents into the workflow controller — `src/workflow/intake-orchestrator.ts` (`processIntakeTurn`): loads trip state + requirements/preferences, calls `runIntakeAgent` through a `ModelClient`, persists validated output, drives `advanceTrip`. Reachable through a real authenticated path via a new Server Action (`app/app/actions.ts`, `sendMessage`) — no chat UI consumes it yet (Phase 7), but it's not just exercised by mocked tests.
- [x] Expose only the approved model-facing tools (`PROJECT_BRIEF.md` §6.4/§12) — unchanged from Phase 4's three tools; the orchestrator doesn't add any new model-facing surface.
- [x] Guardrail gates at each transition — all four `PROJECT_BRIEF.md` §9.1 layers logged to `guardrail_events`: input/scope (message length), output validation (malformed tool calls, invalid revision values), domain validation (the deterministic `checkRequirementsComplete` gate, unsupported decision revisions), workflow authorization (every `advanceTrip` call, triggered or not).
- [x] State persistence after each meaningful step — `trip_requirements`/`trip_preferences` (new repositories, with a retract-before-append primitive so a revised or re-stated field never leaves two active rows for the same field), `messages`, `agent_runs`/`tool_calls` telemetry (tokens, cache stats, latency, cost — `PROJECT_BRIEF.md` §13.2/§6.7), all via the existing `trip_state_versions`/`advanceTrip` path for workflow state itself.
- [x] Failure/retry behavior end to end, for what's reachable in this slice — `advanceTrip`'s own retry-safety (Phase 3) reused via event-name-keyed (not position-keyed — an index-based bug was found and fixed) per-step correlation IDs for multi-hop transitions within one turn; a `"conflict"` (optimistic-concurrency race) is now a distinct, catchable `OrchestrationConflictError` instead of being conflated with a rejected transition. **Known limitation, tracked deliberately (see §5):** a retry of the *whole* `processIntakeTurn` call (e.g. after a mid-turn crash) is not yet idempotent across `appendMessage`/`agent_runs`/`tool_calls`/`guardrail_events` the way `advanceTrip`'s own mirror writes are — asked the user now-vs-later per the standing rule, tracked rather than fixed this session (no caller retries yet; nothing to break in practice today).

#### Phase 6, continued — wiring search, curation, and combination

Phase 5 built the Curator/Explanation agents, retrieval, and (already, from Phase 2) the budget/combination/feasibility engines — none were wired into any orchestrator as of the previous session. Four open questions were recorded here rather than silently assumed; all four were put to the user directly at the start of this session and resolved before any code was written:

1. **`search_flights`/`search_hotels`: deterministic pre-fetch, or live model-driven tool calls?** **Resolved: deterministic pre-fetch**, consistent with Phase 5's `retrieve_destinations`/`retrieve_activities` precedent — `PROJECT_BRIEF.md` §12's own tool-table notes column already describes these as an "orchestrator-driven search step," which was the deciding evidence.
2. **New orchestrator module, or extend `intake-orchestrator.ts`?** **Resolved: new module**, `src/workflow/search-orchestrator.ts` (`runSearchAndCuration`) — mirrors `runIntakeAgent`/`runCuratorAgent` already being separate agent modules; `intake-orchestrator.ts` stays scoped to the `collecting_requirements`/`awaiting_clarification` loop.
3. **`trip_decisions` repository shape.** **Resolved: same append/retire-by-field shape** `trip_requirements`/`trip_preferences` already use, with the schema's own `proposed`/`confirmed`/`superseded` status vocabulary (not `active`/`retracted`) for the §7.6 approval lifecycle — **not yet built**, deferred to the next slice below.
4. **Scope for one session.** **Resolved: sliced.** Slice 1 (this session) = search_flights/search_hotels + Curator wiring only, stopping at `assembling_options` with a ranked candidate set. Slice 2 (next session) = `assembleCandidateCombinations`/`calculateBudget`/`validateItineraryFeasibility`/`validateInventoryReferences`, the `trip_decisions` repository, and the remaining state-machine transitions through `presenting_draft`.

**Slice 1 — done this session:**

- [x] Wire `search_flights`/`search_hotels` — `src/workflow/search-orchestrator.ts` (`runSearchAndCuration`) calls `findFlights`/`findHotels` (`src/repositories/flights.ts`/`hotels.ts`) with params built deterministically from validated `trip_requirements`, then re-validates the results through Phase 2's `filterHardConstraints` (defense in depth beyond whatever the repository query params already narrowed — §9.5 requires hard constraints enforced by code, not assumed correct because a query happened to include them).
- [x] Wire the Curator agent into a real orchestrator step — candidates in (activities from Phase 5's `retrieveActivities`), curation out (ranked/excluded IDs + rationale), persisted as an `activities_curated` `trip_events` row (no `trip_decisions` repository exists yet — see slice 2). Destinations are not curated here: `destination` is a required `trip_requirements` field, so it's already fixed by the time a trip reaches `requirements_ready` — the "flexible destination" case doesn't arise in this project's required-field model.
- [x] Drive `begin_search` → `search_completed` → `candidates_valid`, landing in `assembling_options`. A genuine "no viable candidates after hard-constraint filtering" outcome drives `recoverable_error` (§8.4) rather than leaving the workflow state ambiguous, and throws a typed `NoViableCandidatesError`.
- [x] Reachable through a real authenticated path — `beginSearch` Server Action (`app/app/actions.ts`), same rationale as `sendMessage`.
- [x] Extracted `deriveCorrelationId`/`OrchestrationConflictError`/`OrchestrationTransitionError`/the `advanceTrip`+guardrail-log+branch sequence (now `advanceOrThrow`) out of `intake-orchestrator.ts` into shared `src/workflow/{correlation,orchestration-errors,advance}.ts` — needed by both orchestrators now, so duplicating a third copy in `search-orchestrator.ts` would have been the wrong call. Behavior-preserving; `intake-orchestrator.test.ts` still passes unchanged.
- [x] Live-verified end to end against the real hosted Supabase project + real Anthropic/Voyage APIs with a throwaway script (not committed, created and deleted its own auth user): a trip driven to `requirements_ready`, then `runSearchAndCuration` reached `assembling_options` with 2 real flights, 3 real hotels, 4 real activities, a successful Curator ranking (hallucination reference check passed), and every expected table populated (`trip_events`: `inventory_searched`/`activities_curated`; `guardrail_events`: `flight_hard_constraints`/`hotel_hard_constraints`/`curation_reference_check`/`workflow_transition_authorization`, none triggered; `agent_runs`: one successful Curator call, 2,276 input / 726 output tokens).

**Slice 2 — done this session:**

Two more genuine architectural gaps were found and resolved with the user mid-implementation, not silently decided (same standing rule as the four questions above):

5. **No day-scheduler exists.** `assembleCandidateCombinations` only decides *which* activities fit the budget — nothing assigned a day/time, but `validateItineraryFeasibility` requires a fully dated `ScheduledActivity[]`. **Resolved: build a real (naive but genuine) day-scheduler now** — `src/domain/scheduling.ts` (`scheduleActivities`), rather than a placeholder or skipping feasibility validation this slice.
6. **`assembleCandidateCombinations` only ever picks one `Flight` (one-way), but `flights` is a one-way table and a round trip needs both legs — Slice 1 never searched a return flight at all.** **Resolved: do it properly** — search both legs, widen `assembleCandidateCombinations` to pair an outbound+return candidate per combination, feed both legs' cost into `calculateBudget` (whose `BudgetInput.flights` array was already built to hold more than one leg).

- [x] `trip_decisions` repository — `src/repositories/trip-decisions.ts`, the resolved append/retire-by-field shape with `proposed`/`confirmed`/`superseded` status.
- [x] **Slice 1 retrofit: round-trip flight search.** `src/workflow/search-orchestrator.ts` now also searches a reversed-direction return leg when `returnDate` is stated (skipped for a one-way trip), hard-constraint-filters it the same way as the outbound leg, and returns `outboundFlights`/`returnFlights` separately (renamed from the old single `flights` field). `supabase/migrations/0006_return_flights.sql` + a one-off service-role script (not committed) added 17 real return-leg rows to the hosted project — Phase 1's seed data had only ever seeded one direction per route, a gap invisible until round-trip search was actually exercised live.
- [x] Widened `assembleCandidateCombinations` (`src/domain/combinations.ts`) to `outboundFlights`/`returnFlights?` (optional — one-way trips get `returnFlight: null` on every combination), cross-joining outbound × return × hotel and summing both legs' cost via `calculateBudget`'s already-array-typed `flights` field. Also genericized `activities`/`ActivityCandidate` so it accepts Phase 5's `MatchedActivity` retrieval projection directly, no unsafe cast.
- [x] Fixed a consequence of round-trip support in `validateInventoryReferences` (`src/validation/inventory-references.ts`): a return leg's own `.destination` column is the trip's *origin* (it's the reverse direction), so the old single-field destination check would have flagged every legitimate return flight as belonging to the wrong destination. Flights now get their own check — belongs to the trip if *either* endpoint matches — while hotels/activities keep the original single-field check. Also genericized `ApprovedCandidateSet.activities` the same way as `combinations.ts`.
- [x] `src/domain/scheduling.ts` (`scheduleActivities`) — day-scheduler surfaced as gap 5 above. Naive greedy placer (first-fit, respects `closedDays`/`openingHours`, back-to-back with a gap), explicitly not an optimizer — revisit once the Itinerary Writer (Phase 7) needs a more natural-feeling schedule. Exported `DEFAULT_TRANSFER_BUFFER_MINUTES` from `feasibility.ts` so the scheduler can respect the same arrival/departure buffers feasibility validates against, via new per-date `earliestStartByDate`/`latestEndByDate` params — without this, the scheduler would place an activity at the default 10:00 start right on the arrival day, which feasibility is guaranteed to reject. A second real bug (caught by the live spot-check, not a review): the scheduler originally only ever tested one candidate start time per day, so an evening-only activity (e.g. open 19:30-23:00) could never be placed at all — fixed to advance into the activity's own opening window instead of skipping the whole day.
- [x] `src/workflow/itinerary-orchestrator.ts` (`assembleItinerary`) — picks up from `assembling_options`: builds combinations from the Curator's ranked (non-excluded) activities, tries up to 5 best-ranked combinations (schedule + `validateItineraryFeasibility`, best-first) until one is feasible, defense-in-depth `validateInventoryReferences` check, persists the winner to `trip_decisions` (`outboundFlight`/`returnFlight`/`hotel`/`activities` with their scheduled dates/times/`budget`), and drives `combinations_assembled` → `itinerary_valid` (or `itinerary_invalid` back to `assembling_options` if none of the attempted combinations were feasible) to reach `presenting_draft`.
- [x] **Deliberately scoped out, documented in code: one-way trips (no `returnDate`).** `REQUIRED_FOR_READY` doesn't require `returnDate`, but without it there's no way to derive a hotel-stay length or checkout date — `assembleItinerary` refuses with a typed `OneWayTripNotSupportedError` rather than guessing a default trip length (a product decision out of scope here).
- [x] `assembleTripItinerary` Server Action (`app/app/actions.ts`) — composes `runSearchAndCuration` then `assembleItinerary` in one round trip, since there's no user-facing checkpoint between `assembling_options` and `presenting_draft` (those are implementation-level retry/telemetry states, not something a chat UI would ever pause at). `beginSearch` stays separately callable for testing one slice at a time.
- [x] Live-verified end to end against the real hosted Supabase project + real Anthropic/Voyage APIs with a throwaway script (not committed): a round-trip Lisbon trip reached `presenting_draft` with a feasible, scheduled itinerary (2 outbound/1 return flight candidates, 3 hotels, 4 curated activities, one benign `TIGHT_TRANSFER_BUFFER` warning, zero violations) and all 5 `trip_decisions` rows + every expected `trip_events`/`guardrail_events` row persisted correctly.

**Known limitations / new tracked items from this slice (see §5):** the scheduler is a naive first-fit placer, not an optimizer; `nights` fed to `calculateBudget` is approximated from the requirement-stated `departureDate`/`returnDate`, which can differ by a day from the *actual* hotel-stay length derived from the chosen flights' real arrival/departure times (destination-local, via `localDateInTimeZone`); `assembleItinerary` only tries the best 5 combinations before giving up, not the full candidate list.

### Phase 7 — Itinerary writer and frontend

Sliced with the user before writing code, 2026-09-16 (same standing rule as Phase 6 continued): slice 1 (this session) = Itinerary Writer agent, wiring it into the pipeline, the decision-revision loop, and a chat UI with a live-updating itinerary panel — enough to be demoable in a browser for the first time. Slice 2 (next session) = assumptions/trade-offs panel, the finalization flow (confirmation + proposal-hash check), and trip history polish.

Three questions were put to the user directly before any code was written, all three recommended options chosen:
1. **Itinerary Writer agent shape** — tool-based (`{itineraryText, groundedIds}`), reusing `ExplanationOutput`/`validateExplanationGrounding` (`src/domain/curation.ts`) rather than PROJECT_BRIEF.md's literal "formatting only, no tool" table entry — same "avoid inventing facts needs code enforcement, not just a prompt" reasoning Curator/Explanation already established.
2. **Decision-revision loop** — wire it fully this session (`intake-orchestrator.ts` had been logging it as unsupported since Phase 6 continued's slice 1, before `trip_decisions` existed) rather than deferring again now that a chat UI makes it a real, not theoretical, scenario.
3. **How the UI shows the multi-step pipeline live** — refined through discussion into: the chat UI auto-chains into the pipeline (no explicit "Build my itinerary" button) and the live itinerary panel updates piece by piece via a **Supabase Realtime subscription** on `trip_decisions`/`trip_events` inserts, not a poll or a generic spinner — `runSearchAndCuration`/`assembleItinerary` already write these tables incrementally as each step completes (Phase 6 continued), and both tables already carry owner-scoped RLS, so the browser can subscribe directly and safely.

**Slice 1 — done this session:**

- [x] `src/agents/itinerary-writer.ts` (`runItineraryWriterAgent`) — new agent, own system prompt/tool (`write_itinerary`), reusing `ExplanationOutput`/`validateExplanationGrounding` as-is rather than a new schema/guardrail pair.
- [x] Wired into `assembleItinerary`/`reviseItinerary` (`src/workflow/itinerary-orchestrator.ts`, in the shared `finalizeCombinations` helper both now call): runs after the itinerary is already known-valid (prose is a presentation concern layered on the decision, not part of deciding it), persists the result as a `trip_decisions` `itineraryText` field. A writer failure (API error, ungrounded output) is non-fatal and logged, not blocking — the deterministic data alone is still enough to present a draft.
- [x] Decision-revision loop — `reviseItinerary` (new export, `src/workflow/itinerary-orchestrator.ts`): re-fetches alternate candidates for exactly one field (`outboundFlight`/`returnFlight`/`hotel` — revising `activities` isn't supported, since which specific activity to swap needs more than a field name to resolve, `UnsupportedDecisionFieldError`), excludes the current pick, and reuses the *exact same* combine → schedule → validate → write → persist pipeline (`finalizeCombinations`) fresh assembly uses — a revision is structurally "re-run assembly with one field's candidate pool narrowed," not a different algorithm. Drives `revision_requested` → `revision_submitted` → `revision_applied` → `itinerary_valid`/`itinerary_invalid`. If no alternative passes hard constraints, keeps the current selection and logs a `revision_no_alternative` guardrail rather than erroring.
- [x] `intake-orchestrator.ts` now loads and passes `currentDecisions` to the Intake/Revision agent (the input field existed since Phase 4 but was never populated) and, for a decision revision targeting a revisable field, returns a `decisionRevisionRequested` signal instead of always logging "unsupported" — the caller (a Server Action) acts on it after this turn's own persistence/transition finishes.
- [x] `sendMessage` (`app/app/actions.ts`) auto-chains: after `processIntakeTurn`, if the trip just reached `requirements_ready` it kicks off `assembleTripItinerary`'s pipeline; if a decision revision was requested, it kicks off `reviseItinerary` — both via Next.js's `after()` (`next/server`) so the turn's own response (the user's message + the intake agent's reply) returns immediately instead of blocking on several more seconds of real API calls. New `reviseTripDecision` Server Action for calling a revision directly (testing/debugging one step, same rationale as `beginSearch`).
- [x] Chat UI — `app/app/_components/chat-panel.tsx` (message list + input, calls `sendMessage`, redirects to `/app/trips/[tripId]` once a trip exists) and `app/app/_components/itinerary-panel.tsx` (subscribes to Supabase Realtime `postgres_changes` INSERT on `trip_decisions`/`trip_events` for the current trip; renders a step checklist, budget total, and the full written itinerary as they arrive). Wired into `app/app/page.tsx` (fresh chat) and `app/app/trips/[tripId]/page.tsx` (resumed trip: chat + panel side by side, existing messages loaded server-side).
- [x] `supabase/migrations/0007_realtime_trip_updates.sql` — adds `trip_decisions`/`trip_events` to the `supabase_realtime` publication (required for the panel's subscription to receive anything at all). **Applied to the hosted project** — the user provided the DB password mid-session and `supabase db push` ran it. That push also silently re-ran `0006_return_flights.sql` (the remote migration-history table didn't know it had already been applied via the earlier one-off service-role script, since that script bypassed the CLI), duplicating its 17 return-flight rows to 34 — caught immediately by a live count check, confirmed with the user, and cleaned up (deleted the 17 duplicates, keeping one of each). Back to 17 return-flight rows; both migrations now correctly recorded as applied.
- [x] Live-verified the backend chain end to end against the real hosted Supabase project + real Anthropic/Voyage APIs with a throwaway script (not committed): a round-trip Lisbon trip reached `presenting_draft` with a full, well-grounded written itinerary (verified against real flight times/hotel name/activity schedule, zero hallucination-guardrail triggers), then a hotel decision revision correctly switched hotels (Baixa Riverside Suites → Alfama Bica Inn), regenerated a consistent itinerary, and left exactly one `"proposed"` `trip_decisions` row for the `hotel` field. **Not live-verified: the actual browser chat UI and the Realtime panel** — another session already held this project's one allowed `next dev` instance (a per-project lock, not just a port conflict) for the whole session, and the Realtime publication migration above isn't applied to the hosted project yet either. Both need a follow-up check once those are cleared.

**Slice 2 as originally planned here (assumptions panel, finalization flow, trip history polish) is superseded — see "Stepwise chain redesign" immediately below, which is now the mandatory next step before any of that.** Building a finalization/confirmation flow against the one-shot assembly model right before replacing that model isn't worth doing twice.

---

## STEPWISE CHAIN REDESIGN (decided 2026-09-17; slices 1-4 built 2026-09-17)

**All four slices are done.** Chain/cascade data model + flight step; hotel step + the flight→hotel cascade rule; activities step + retiring the old one-shot pipeline; and slice 4 — real per-step interaction (hybrid cards + chat), the directional/absolute revision split, warn-before-cascade, and trip finalization. The redesign described in "The decided design" below is now fully implemented, tested, and live-verified. What remains is normal follow-on work, not this redesign — see `docs/IMPLEMENTATION_PLAN.md` §5 and the rest of the build sequence below.

### Slice 4 outcome, decisions made while building it

Six architecture questions (listed in the original "SLICE 4 — START HERE NEXT SESSION" section, preserved below for the historical record) were resolved with the user directly before any code was written, all six recommended options chosen: (1) hybrid interaction — clickable candidate cards drive Server Actions directly for the common pick/confirm path, chat stays available alongside for free-text revisions; (2) `propose_trip_revision`'s decision-revision vocabulary switches from flat field names to `ChainStep` values (`flight`/`hotel`/`activities`); (3) the directional/absolute revision split is bundled into this same slice, not deferred; (4) an explicit finalize step is added, and the trip history list reads chain progress instead of the permanently-stale `trips.status`; (5) revising an already-confirmed earlier step warns before cascading, revising the active step doesn't; (6) `propose*Step` persists its candidate list as `"proposed"` `trip_decisions` rows, activating the previously-unused `confirmTripDecisions`.

**A correctness gap found and fixed mid-build, not by a review pass but by reasoning through the revision-of-an-already-confirmed-step UI flow before writing it**: the original `confirmDecisionField` (added for decision 6) only superseded sibling *proposed* rows when promoting a candidate to confirmed, not any *prior confirmed* row for the same field. That's correct for a first-ever confirm (nothing else to supersede) but wrong for the "Change" flow on an already-confirmed step, where a fresh proposed list coexists with the still-confirmed old pick — promoting a new candidate without also superseding the old confirmed row would have left two `"confirmed"` rows for the same field at once, breaking the "at most one confirmed row per field" invariant `getCurrentChainStep` and every step module rely on. Fixed by widening the repository function (`supersedeOtherActiveTripDecisions`, renamed from `supersedeOtherProposedTripDecisions`) to supersede every other non-superseded row for the field — any status, not just `"proposed"` — before promoting the kept one. Covered by new dedicated tests in `flight-step.test.ts`/`hotel-step.test.ts` ("also supersedes the prior confirmed \[pair/hotel\] when promoting a new pick while revising an already-confirmed step").

**Avoiding a double Curator LLM call, a design constraint discovered while planning the candidate-loading state machine**: the itinerary panel needs to react to new candidates appearing for the active step, but a naive "re-fetch whenever `trip_decisions` changes" effect would refire on its own writes. Flight/hotel are cheap deterministic queries, so a one-time harmless double-fetch on first load is accepted (signature-keyed `useRef`s). Activities is not cheap — `proposeActivitiesStep` makes a real Curator LLM call — so it's gated by a plain once-per-step-transition ref instead, **and** `handleConfirmHotel` sets that ref *synchronously, before* awaiting the confirm action. Without the synchronous set, a Realtime-delivered "activities proposed" event (from that same confirm's own server-side `advanceOrRefreshChain` call) could otherwise race ahead and trigger a second, redundant Curator call before the handler's own response comes back — confirmed live that this closes the race (single itinerary generation observed per hotel confirm, not two).

**`step-router.ts`** (new, replacing `chain-orchestrator.ts` — deleted this slice along with its test) deliberately returns what it did (`{step, result}`) rather than `void`: `app/app/actions.ts`'s `confirmFlightCandidate`/`confirmHotelCandidate` thread this straight into the client's response so the next step's rich candidate data doesn't need a redundant re-fetch, which is also what makes the double-Curator-call fix above possible.

**A real bug caught only by live browser click-through, not by any of the 319 unit tests (all mocked)**: the itinerary panel's derived state (`flightConfirmed`, `activeStep`, `confirmedActivities`, budget, itinerary text) all read from a `decisions` array populated by the initial load plus Supabase Realtime — but in live testing, Realtime updates didn't arrive promptly within the same session (INSERTs for a brand-new trip did; UPDATEs and same-session follow-up INSERTs were unreliable/slow enough to strand the UI on "Finding hotels…"/"Planning activities…" indefinitely after a successful confirm, even though the confirm action itself had already returned the correct data and the backend was already at the next step). Fixed by having every confirm handler optimistically merge its own action response into local `decisions` state immediately (`optimisticConfirm`), with `upsertById` clearing any stale optimistic row once the real Realtime-delivered row for that field eventually arrives. This makes the UI correct regardless of Realtime's actual latency in a given environment, while a fresh page load (decision 6's reload-survival requirement) still works purely from the initial `SELECT` as before — both verified live.

**A second live-only bug**: revising an already-confirmed hotel with an over-aggressive LLM-computed `maxHotelPriceUsd` threshold (below every real hotel's price) threw `NoViableHotelCandidatesError` server-side, which — thrown across a Server Action boundary in a production build — surfaced to the client as React/Next's generic obfuscated "an error occurred in the Server Components render" message (real per React's own public error-code registry, `error #441`), not any usable explanation. This is exactly the dishonest-failure gap design rule 3 exists to prevent. Fixed by having the relevant Server Actions (`propose*Candidates`, `confirmCascadeAndRevise`, `confirm*Candidate`'s `advanceOrRefreshChain` call) catch `NoViableFlightCandidatesError`/`NoViableHotelCandidatesError` specifically and return a friendly `{error: string}` result instead of throwing, verified live ("No hotels match your current requirements — try relaxing the price or rating constraints." rendered correctly, with the prior confirmed hotel/itinerary left completely untouched). **Known gap, not fixed this slice**: this only covers the direct UI-triggered propose/revise paths. The chat-triggered fire-and-forget path (`sendMessage`'s `after()` call to `reviseChainStep` for a `decisionRevisionRequested` on the still-active step) still only `console.error`s such a failure today — the user gets no feedback at all if a chat-requested revision of the *active* step hits this same "no viable candidates" case. Surfacing that would need a new `trip_events` event type the panel's existing `needsAttention` listener could pick up (that listener already exists for `recoverable_error`/`itinerary_invalid`); flagged rather than built now since it's a genuinely separate, if related, piece of work.

**Verified**: `npm test` (319/319 passing, including new `step-router.test.ts` and new revision-supersedes-confirmed-row coverage in `flight-step.test.ts`/`hotel-step.test.ts`), `npx tsc --noEmit`/`npm run lint`/`npm run build` all clean. Live-verified end to end against the real hosted Supabase project (a `next start` production-build preview on a second port, `.claude/launch.json`'s new `wanderwise-preview` config — sidesteps the documented `next dev` per-directory lock without needing the other session's dev server; signed in via a throwaway `/dev-signin` admin-link helper, same disposable pattern as prior sessions, removed after use along with its one-line `proxy.ts` public-path exception): a full forward flow (flight card → hotel card → activities confirm) reached a correct, well-grounded written itinerary and budget; finalizing transitioned `trips.status` to `finalized` and the trip history list reflected it immediately; the cascade warning fired correctly both from a direct "Change" click and from a chat-requested revision of an already-confirmed step, blocked nothing from happening until explicitly confirmed, and a Cancel left every decision untouched; a directional "cheaper hotel" chat request correctly converted to a `maxHotelPriceUsd` threshold revision (through to the two bugs above, both found and fixed live).

### Original SLICE 4 planning notes (historical — resolved above, kept for context)

**Do this before any other open item.** Everything backend-side the redesign needed is built, tested, and live-verified (slices 1-3, see below). Slice 4 is a genuinely different kind of work — frontend/UX, plus making `intake-orchestrator.ts` step-aware — not more backend orchestration, so don't assume the previous slices' patterns transfer directly.

**Real open questions to resolve with the user before writing code** (none of these are decided — don't guess):

1. **Interaction pattern, concretely.** "Show one step at a time, confirm/change per step" is decided at the concept level (see "The decided design" §1 below), but not *how*. Two real options: (a) the itinerary panel shows the current step's candidates as clickable UI (cards, buttons) that drive Server Actions directly — the common path never round-trips through the LLM at all; (b) everything still flows through chat text (confirming is typing "yes" or "the second one"), and the panel stays a passive live display like it is today. These are different architectures, not a styling choice.
2. **Does `propose_trip_revision`'s target vocabulary change?** Right now it's flat field names (`outboundFlight`/`returnFlight`/`hotel`, `step-shared.ts`'s `RevisableDecisionField`) — a carryover from the old pipeline. Becoming step-aware plausibly means switching to chain step names (`flight`/`hotel`/`activities`, `src/domain/chain.ts`'s `ChainStep`) instead, which touches the Intake agent's tool schema/prompt (`src/agents/intake.ts`), not just orchestration code.
3. **Revising the *active* (not-yet-confirmed) step vs. an *earlier, already-confirmed* one — does the UX distinguish these?** Right now `chain-orchestrator.ts`'s interim glue treats every revision the same (re-propose, exclude current, auto-confirm). A real UI probably needs to warn before revising an earlier step ("changing your flight may also change your hotel") since that's when the cascade in `flight-step.ts`'s `confirmFlightStep` actually fires. `getCurrentChainStep` (`src/domain/chain.ts`) already tells you which step is active — the Intake agent doesn't currently receive this.
4. **Is the directional/absolute revision split (design §3 below) actually in scope for this slice?** It was named as "the real fix" for the long-tracked "revision doesn't honor the stated criterion" bug (`docs/IMPLEMENTATION_PLAN.md` §5), confirmed to still hold under the new chain. Building it means: adding a `maxHotelPriceUsd` requirement field (doesn't exist yet — only `minHotelRating` does) and the equivalent for other directional asks, wiring them into `hotelHardConstraints`/`flightHardConstraints`, and having the Intake agent convert "cheaper" into a concrete threshold. This could reasonably be its own slice 5 instead of bundled into the UI rework — ask rather than assume either way.
5. **What happens to `WorkflowState`/`trips.status` under the finished model?** As built, a trip's `trips.status` never advances past `"requirements_ready"` — all real progress lives in `trip_decisions.status` + `getCurrentChainStep`. `app/app/trips/page.tsx` (trip history list) likely still displays `trips.status` to the user, which would now always say "requirements ready" even for a fully finished trip. Does `presenting_draft`/`awaiting_confirmation`/`finalized` get reached at all, and if so, from where — or does the trip list need to read `getCurrentChainStep` instead of `trips.status`?
6. **Does `trip_decisions.status = "proposed"` finally get used?** Built in slice 1 (`confirmTripDecisions`, `src/repositories/trip-decisions.ts`) but never called by any step — every step writes straight to `"confirmed"` since propose was always ephemeral/unpersisted. If interaction pattern (Q1) goes with option (a) above, a step's proposed candidates might need to actually persist as `"proposed"` so the panel can render them before the user picks.

**Also on the list, already decided, just not yet built:**
- Delete `src/workflow/chain-orchestrator.ts` (the interim auto-confirm glue) once real per-step interaction replaces it.
- Rewrite `chat-panel.tsx`/`itinerary-panel.tsx` for the new interaction pattern (depends on Q1).
- Make `intake-orchestrator.ts`'s `decisionRevisionRequested` signal step-aware (depends on Q2/Q3).

Once these are resolved (ask via `AskUserQuestion`, don't assume), the smallest coherent next step is probably its own mini design pass (plan mode) before touching code, same as slice 1 got — this slice has more open surface than 1-3 did.

### Slice 3 outcome, decisions made while building it

The biggest slice: builds the activities step (the chain's last step) *and* retires the old one-shot pipeline (`search-orchestrator.ts`, `itinerary-orchestrator.ts`) in the same pass, per the original plan's own bundling. One real design question was asked and answered directly before writing code:

- **What happens to the live chat UI once the old pipeline it depends on is deleted, given slice 4 (the real per-step UI) hasn't been built yet?** The user chose **auto-chain through the new steps** — interim glue (`src/workflow/chain-orchestrator.ts`, explicitly marked as scaffolding slice 4 will delete) that auto-confirms each step's top-ranked candidate, so the chat UI keeps auto-completing a trip exactly as it did before, with zero visible regression, rather than leaving the app non-functional until slice 4 lands.

**Built:**
- `src/workflow/step-shared.ts` — `requirementMap`/`flightHardConstraints`/`hotelHardConstraints`/`OneWayTripNotSupportedError`/`RevisableDecisionField`, relocated from the two retired modules (the only pieces of them anything still depended on).
- `src/workflow/activities-step.ts` (`proposeActivitiesStep`/`confirmActivitiesStep`) — the chain's last step. Reuses the same Phase 2/5/7 domain logic the old pipeline already had (retrieval, Curator ranking, scheduling, feasibility, budget, Itinerary Writer) but invoked per-step against an already-confirmed flight+hotel rather than a cross-joined combination search. Unlike flight/hotel, doesn't return a short list — activities are a scheduling problem, not a pick-one problem, so it returns the one curated+scheduled result. Since it's the last step, `confirmActivitiesStep` is also where budget and itinerary prose get computed, same as the old pipeline's `finalizeCombinations`.
- `confirmFlightStep`'s cascade generalized from hotel-only to `["hotel","activities"]` — the first real exercise of invalidating an already-confirmed activities decision.
- `src/workflow/chain-orchestrator.ts` (`runStepwiseChain`/`runStepwiseRevision`) — the interim auto-confirm glue described above. `runStepwiseChain` is `getCurrentChainStep`'s first real caller: it picks up from wherever the chain currently stands and completes every remaining step, which is what makes it reusable both for the initial forward pass and for finishing whatever a revision's cascade invalidated downstream.
- Retired: `search-orchestrator.ts`, `search-orchestrator.test.ts`, `itinerary-orchestrator.ts`, `itinerary-orchestrator.test.ts`, and `app/app/actions.ts`'s `beginSearch`/`assembleTripItinerary` (debug-only actions tied to the old pipeline's return shapes — nothing consumed them, so deleted rather than adapted). `reviseTripDecision` and `sendMessage` were rewired to the new chain instead.

**Two real bugs caught live, neither by the 316 unit tests (all mocked) or the first backend spot-check script — only by an actual browser click-through:**
1. **Stale budget/itineraryText after a hotel-only revision.** Rule 3 correctly keeps a hotel change from invalidating activities — but budget/itineraryText are computed *inside* `confirmActivitiesStep`, not their own chain step, so a hotel swap left them describing the *old* hotel at its *old* price even though `trip_decisions.hotel` had the new one. Asked the user fix-now-vs-track (per the standing rule); chose fix now. Fixed in `chain-orchestrator.ts`'s `runStepwiseRevision`: when the chain is otherwise `"complete"` after a revision (nothing downstream was invalidated) but the revised field was flight/hotel, it re-runs `confirmActivitiesStep` with the **same already-confirmed schedule** — no re-search/re-curation, just recomputing the two things that actually depend on the hotel's price/name. Live-verified: after a hotel swap, the refreshed itinerary text correctly named the new hotel, and the activities schedule's own content was verified byte-identical (only budget/itineraryText changed).
2. **Chat-requested revisions silently did nothing, permanently.** `app/app/actions.ts`'s `sendMessage` checked `result.workflowState === "requirements_ready"` *before* `result.decisionRevisionRequested` — a check that was meaningful under the old pipeline (where `advanceTrip` moved `workflowState` along once search began) but, since the new stepwise steps deliberately never call `advanceTrip` (slice 1's decision), `workflowState` stays `"requirements_ready"` forever after the very first run. Every subsequent turn — including a real, correctly-detected revision request from the Intake agent — re-entered `runStepwiseChain` instead, which silently no-ops once the chain is already complete (by design, so it wouldn't error — it just also wouldn't apply the revision). Reproduced twice live (asking to switch hotels via natural chat visibly did nothing) before finding the root cause by comparing a direct function call (which worked) against the same call through `sendMessage` (which didn't). Fixed by checking `decisionRevisionRequested` first; `runStepwiseChain` is safe to call speculatively on any other turn since `getCurrentChainStep` makes it a no-op once already complete.

**Verified:** `npm test` (299/299 passing, 34 new across `activities-step.test.ts`/`chain-orchestrator.test.ts`/new cascade tests), `tsc`/`lint`/`build` all clean. Live-verified twice — a backend-only spot-check script (real Curator/Writer LLM calls, full chain + a revision, all correct) and then a real browser click-through (signed in via an admin-generated OTP through a throwaway `/dev-signin` helper, same pattern as Phase 7 slice 1's click-through, removed after use) that sent a real trip request through the actual chat UI, watched the full chain fill in live via Realtime with a correct written itinerary, then drove a real "switch me to a cheaper hotel" conversation end to end (including the Intake agent correctly asking a clarifying question first when the initial request was too vague) and confirmed the live UI updated to the new hotel/budget/itinerary text. This browser pass is what caught bug 2 above — a backend-only check would have reported everything fine, since `runStepwiseRevision` itself was always correct in isolation.

### Slice 2 outcome, decisions made while building it

Built directly on slice 1's conventions without re-litigating them — the same two decisions (short ranked list; skip `WorkflowState` transitions) apply to the hotel step for the same reasons, so they weren't re-asked. What's genuinely new in slice 2:

- `src/workflow/hotel-step.ts` (`proposeHotelStep`/`confirmHotelStep`) — `proposeHotelStep` can only run once the flight step is confirmed (throws `FlightStepNotConfirmedError` otherwise), since it scopes the hotel search to the confirmed flight's derived check-in/check-out dates (`deriveHotelStayDates`). `confirmHotelStep` has **no cascade logic of its own** — per the decided design's rule 3, a hotel change never invalidates activities, so there's nothing for it to check or invalidate.
- **The flight→hotel cascade rule got its first real caller**, inside `confirmFlightStep` (`flight-step.ts`), not inside the hotel step: when a flight is *re*-confirmed (i.e. a prior confirmed flight already exists), it now fetches the prior confirmation's flights, derives its stay dates, and compares them against the newly-confirmed flight's derived dates. If they differ *and* a hotel decision currently exists, the hotel decision is retired (superseded) and a `hotel_step_invalidated` `trip_events` row is logged — exercising `src/domain/chain.ts`'s `invalidatedStepsForFlightChange` for real for the first time. If the dates are unchanged (a same-dates flight swap), the confirmed hotel decision is left untouched, exactly as rule 2 specifies.
- Scope call, not a bug: this cascade only acts on the "hotel" step, since the "activities" step (per the new stepwise model) doesn't have a decision writer yet — building at that generalizes to "activities" too is slice 3's job, once there's a real activities-step decision to invalidate.

Verified: `npm test` (303/303 passing, 13 new — `hotel-step.test.ts` plus 4 new cascade tests added to `flight-step.test.ts`), `tsc`/`lint`/`build` all clean. Live-verified against the hosted Supabase project: hotel step correctly refuses to propose before a flight is confirmed; a real hotel search/rank/confirm cycle completed end to end; a same-flight re-confirmation left an already-confirmed hotel decision untouched. **One assertion couldn't be exercised live and was skipped**: the "dates differ → hotel gets invalidated" branch, because the seed data has exactly one Lisbon→New York return-leg flight (confirmed by a direct query) — there's no second real return date to swap in. That branch is still covered by 3 dedicated unit tests with synthetic data (`flight-step.test.ts`'s "flight->hotel cascade" describe block), so it's verified, just not against live data this time.

### Slice 1 outcome, decisions made while building it

Two design questions were asked and answered directly before writing code (not assumed):
1. **Flight selection when no hotel/activities exist yet to fit a budget against:** propose a **short ranked list (top 3 cheapest hard-constraint-passing outbound+return pairs)**, chosen explicitly over a single "cheapest wins" pick. This also avoids a real gap the single-pick option had: a bare "I don't like this one" rejection with no stated criterion has nowhere to go without exclusion-tracking machinery, whereas a visible list lets the user just point at a different item.
2. **Workflow-state integration:** the flight step (`src/workflow/flight-step.ts`) does **not** call `advanceTrip`/touch `WorkflowState` at all. It only reads `trip_requirements` (gated by the existing deterministic `checkRequirementsComplete`, not by `trips.status`) and writes `trip_decisions`, with the same guardrail/`trip_events` telemetry conventions as every other orchestrator. How the existing coarse states (`searching_inventory`/`assembling_options`/`presenting_draft`) get reused across three repeated steps is deliberately deferred until hotel + activities also exist (slice 2/3) and the chat loop is rebuilt (slice 4) — answering it now, with only one of three steps built, risked guessing wrong.

A third call made while designing slice 1, flagged rather than decided silently: since the flight step shows an explicit list and the user picks one, `trip_decisions.status = "proposed"` is never used for this step — nothing is persisted until the user picks *and* confirms, at which point `confirmFlightStep` writes both legs directly as `"confirmed"`. Candidates shown from `proposeFlightStep` are ephemeral (returned, never persisted), matching how `runSearchAndCuration` already returns unpersisted candidate lists today. If slice 4's real chat flow turns out to need a "proposed but not yet confirmed" intermediate after all, `confirmTripDecisions` (added to `src/repositories/trip-decisions.ts` this slice, not yet used) already exists for exactly that.

Built: `src/domain/stay.ts` (`deriveHotelStayDates`, extracted from `itinerary-orchestrator.ts`'s `buildDraft` — the derived fact the flight->hotel cascade rule compares), `src/domain/chain.ts` (the general `CHAIN_STEPS`/`getCurrentChainStep`/`REQUIREMENT_FIELD_STEP`/`invalidatedStepsFor*` cascade model — hotel/activities branches are unit-tested but have no live caller yet, exactly as the plan called for), and `src/workflow/flight-step.ts` (`proposeFlightStep`/`confirmFlightStep`). No revision-specific function was needed this slice: a future requirement change just means calling `proposeFlightStep` again, since it always reads current active requirements fresh — the chat-level "detect 'cheaper' -> revise a requirement -> re-propose" wiring needs `intake-orchestrator.ts` to become step-aware, which is still a later-slice item (see "What this replaces" below, unchanged).

Verified: `npm test` (290/290 passing, 30 new — `chain.test.ts`, `stay.test.ts`, `flight-step.test.ts`), `npx tsc --noEmit`/`npm run lint`/`npm run build` all clean, and a live spot-check against the hosted Supabase project (no LLM calls needed — the step is fully deterministic): a real New York -> Lisbon trip's `proposeFlightStep` returned 2 real ranked pairs, `confirmFlightStep` persisted both legs as `confirmed` `trip_decisions` and logged `flight_step_proposed`/`flight_step_confirmed` `trip_events` plus both hard-constraint `guardrail_events`; a `maxFlightPriceUsd` requirement added afterward was picked up fresh on re-propose (an unreachably low ceiling correctly threw `NoViableFlightCandidatesError`; a ceiling between the two candidates' outbound prices correctly narrowed the list to exclude the pricier one).

Known gap surfaced, not fixed this slice: revising an *already-confirmed* flight decision once hotel/activities exist downstream has no live caller yet — building that cascade-aware revision path is slice 2/3's job, once there's something downstream to actually invalidate.

### Original design (unchanged from 2026-09-17)

**Why this exists:** live browser-verification of Phase 7 slice 1's decision-revision loop (asking to switch to "a cheaper hotel" through the real chat UI) surfaced that the feature is built on the wrong foundation. `runSearchAndCuration`/`assembleItinerary` search and assemble the *entire* flight+hotel+activities bundle in one automatic pass and present it all at once; `reviseItinerary` then patches one field of an already-fully-assembled draft after the fact. Working through *why* revisions kept behaving oddly (a hotel swap landing on a pricier hotel instead of a cheaper one; no way to express "free" vs. "cheaper" as different kinds of request) led to a longer design conversation, decided directly with the user rather than assumed, that the one-shot model itself is wrong, not just the revision logic on top of it.

### The decided design

**1. One strictly linear chain, no branching:** `destination/dates (requirements) → flight → hotel → activities`. Each step is proposed, shown to the user, and explicitly confirmed *before the next step is even searched* — not all assembled speculatively up front. This isn't just a UX preference: hotel check-in/check-out dates are derived from the *actual chosen flight's* real arrival/departure time (an overnight flight can land a calendar day after `departureDate`), so proposing a hotel before a flight is confirmed is proposing against a guess. The old model built every flight×hotel×activity combination before anything was confirmed, most of which got thrown away the moment the user rejected the first option — wasteful in both API cost and correctness.

**2. Confirming/changing a step, and cascading invalidation — decided precisely, not "any upstream touch resets everything downstream":** a change only invalidates a later step if it actually alters the specific fact that later step depends on, not merely because it's earlier in the chain. Confirmed rules:
   - A **destination/dates/party-size/budget** (requirement) change → always invalidates flight → hotel → activities, since everything downstream depends on these.
   - A **flight change** → invalidates hotel → activities **only if** the newly-chosen flight's derived check-in/check-out calendar dates actually differ from before. A same-dates flight swap (different airline/time, same calendar day) leaves hotel and activities untouched.
   - A **hotel change** → **never** invalidates activities. Confirmed by reading the actual scheduling code (`src/workflow/itinerary-orchestrator.ts`'s `buildDraft`): activity scheduling depends only on the stay's date range (which comes from the flight) and each activity's own opening hours/closed days — nothing about *which* hotel was picked feeds into it.
   - An **activities change** → nothing further downstream; it's the last step.
   
   When a change does invalidate downstream steps, the app "restarts from the appropriate step" — re-proposes the first invalidated step onward — rather than restarting the whole chain from scratch, and "holds" (leaves untouched, still confirmed) whatever didn't actually need to change.

**3. Revision requests split into two kinds, both routed through the existing requirement-revision mechanism rather than a new one-off "criteria" concept bolted onto decisions:**
   - **Directional/comparative** ("cheaper," "less expensive," "shorter," "higher rated") — no absolute threshold, just a direction. The model converts this into a *concrete* threshold relative to what's currently selected (e.g., current hotel is $158/night → propose `maxHotelPriceUsd: 150` as a requirement revision) rather than an abstract "cheaper" the code has to interpret. Needs a new `maxHotelPriceUsd` requirement field — doesn't exist yet (`minHotelRating` does, no price ceiling for hotels).
   - **Absolute/hard-constraint** ("free," "wheelchair accessible," "non-stop") — a real, checkable, pass/fail condition, semantically identical to what `src/domain/constraints.ts`'s existing `HardConstraint<T>` engine already does for initial search (`maxActivityPriceConstraint(0)` *is* "free," using code that already exists and is already tested — no new machinery needed). Critically: **"free" is not "the cheapest available option"** — if nothing in the candidate set actually costs $0, the system must say so honestly ("there's no free option available, the cheapest is Tram 28 at $3.50 — want that instead?") rather than silently substitute the nearest-cheap option and imply it satisfies the request. This was a real, deliberately-corrected mistake in this session's own first draft of the design (initially conflated "free" with "the extreme of cheaper" via a sort direction — wrong, caught by the user, not by me).
   - **Both kinds are requirement revisions**, re-validated by the same deterministic hard-constraint engine that already gates initial search, re-triggering the chain's invalidation cascade like any other requirement change. No parallel "decision revision criteria" vocabulary needed — this generalizes to new fields/constraints for free as the requirement vocabulary grows (e.g. adding `maxHotelPriceUsd`), rather than needing its own enum that would otherwise grow toward "50 different criteria."
   - **Multi-segment requests** ("cheaper hotel and shorter flight" in one message) — handled as sequential single-field revisions applied one after another within the same turn, not one combined instruction. Reuses the chain's own step-by-step mechanics rather than needing separate multi-field logic.

**4. What this replaces, concretely:**
   - [x] `src/workflow/search-orchestrator.ts`'s `runSearchAndCuration` — retired slice 3. Replaced by `flight-step.ts`/`hotel-step.ts`/`activities-step.ts` proposing one step at a time.
   - [x] `src/workflow/itinerary-orchestrator.ts`'s `assembleItinerary` — retired slice 3. The combination/scheduling/feasibility logic (Phase 2/6) turned out not to need `assembleCandidateCombinations`'s cross-join at all once flight/hotel are individually confirmed upstream facts rather than several candidates to combine against several others — each step's own `confirm*` function schedules/validates directly.
   - [x] `reviseItinerary` — retired slice 3. Replaced by `chain-orchestrator.ts`'s `runStepwiseRevision`, though only as interim glue (see below) — the *real* "revise a specific step of the chain" UX is still slice 4.
   - [x] `intake-orchestrator.ts`'s decision-revision detection — now step-aware (`decisionRevisionRequested: {step: ChainStep} | null`, `REVISABLE_CHAIN_STEPS`), plus the new `pendingCascadeConfirmation` signal for both decision and requirement revisions that risk invalidating confirmed downstream work. Done slice 4.
   - [x] The chat UI (`chat-panel.tsx`, `itinerary-panel.tsx`, new `trip-workspace.tsx`) — real per-step candidate cards driving Server Actions directly, no auto-confirm; chat stays available for free-text revisions and shows the cascade-warning banner when needed. `chain-orchestrator.ts` deleted, replaced by `step-router.ts` (propose-only, never confirms). Done slice 4.
   - [x] State machine impact — resolved by adding one new direct transition (`requirements_ready --chain_completed--> presenting_draft`) rather than exploding into per-step states, reusing the existing `presenting_draft → awaiting_confirmation → finalized` rules as-is. `trips.status` now actually reaches `finalized` via `app/app/actions.ts`'s `finalizeTrip`, fired once all three chain steps are confirmed; the trip history list reads `getCurrentChainStep` for in-progress trips instead of the (still, deliberately, otherwise-unused) coarse `WorkflowState`. Done slice 4.

**Proposed slicing — all four slices done:**
1. [x] The chain/cascade data model + the flight step alone (search → propose → confirm, with the derived-fact cascade check in place, even though nothing downstream exists yet to actually cascade to). Done 2026-09-17 — see "Slice 1 outcome" above.
2. [x] Hotel step + the flight→hotel cascade rule (dates-changed check). Done 2026-09-17 — see "Slice 2 outcome" above.
3. [x] Activities step + the confirmed hotel→activities non-cascade; retired the old one-shot `runSearchAndCuration`/`assembleItinerary`/`reviseItinerary` path. Done 2026-09-17 — see "Slice 3 outcome" above.
4. [x] Chat UI rework for the step-by-step interaction pattern, the directional/absolute revision split, warn-before-cascade, and trip finalization. Done 2026-09-17 — see "Slice 4 outcome" above.

**After this is built:** go fix whatever it broke elsewhere (expect real fallout in `search-orchestrator.ts`/`itinerary-orchestrator.ts`/`intake-orchestrator.ts` tests and the chat UI, all built against the one-shot model), then resume the rest of Phase 6/7/beyond in whatever order makes sense at that point.

### Phase 8 — Evaluation and observability
- [x] Eval harness (`evals/runners`) running the scenarios in `PROJECT_BRIEF.md` §19 — slice 1, 2026-09-17: `evals/lib/scenario-harness.ts` + `evals/cases/scenarios.ts` + `evals/runners/run-scenario-eval.ts` (`npm run eval:scenarios`), 6 of the 16 §19 scenarios against a real Supabase trip + real Anthropic/Voyage calls, persisted to new `eval_runs`/`eval_results` rows (`src/repositories/eval-runs.ts`). **Full 16/16 §19 coverage reached 2026-09-17 — see "Known bugs — resolved" in §5 below for the follow-up session that closed the remaining 10.**
- [x] Adversarial eval cases — slice 3, 2026-09-17: `evals/cases/adversarial.ts` (fabricated inventory IDs, budget-exceeding wording, contradictory dates, cross-session `tripId`/`sessionId` mismatch), run by the same `npm run eval:scenarios` harness/runner as the §19 scenarios. "Attempts to trigger booking" is already covered by `scenarios.ts`'s `out_of_scope_request`. **Prompt injection in retrieved text and full RLS cross-user isolation added 2026-09-17 — see §5 below.**
- [x] CI evaluation command + GitHub Actions workflow (open item from `PROJECT_BRIEF.md` §22) — slice 2, 2026-09-17: `.github/workflows/ci.yml` runs `npm run eval:ci` (new: `typecheck` + `lint` + `test`, all deterministic/mocked, no network calls) and `npm run build` on every push/PR to `main`, with placeholder Supabase env vars (no real secrets needed — verified locally that `npm run build` succeeds without real credentials). Real-model evals (`eval:intake`/`eval:retrieval`/`eval:scenarios`) stay manual per §9.6's own "keep the eval suite cheap enough for CI" guidance and their own docstrings.
- [x] Engineering dashboard (cost/latency/cache/guardrail views) — slice 4, 2026-09-17: `app/internal/analytics/page.tsx` (§13.3 — trip finalization rate, cost per finalized trip, cost/latency/cache by agent, guardrail trigger frequency, workflow failure-state breakdown, eval pass-rate trend). Reads `agent_runs`/`tool_calls`/`guardrail_events`/`workflow_steps`/`eval_runs`/`eval_results` via the service-role client (all internal/RLS-locked-out tables). Also fixed a real gap found while building this: `agent_runs.cost_usd` was never actually populated by any caller (`src/observability/pricing.ts` extracted from `evals/runners/run-intake-eval.ts`'s cost formula, wired into `intake-orchestrator.ts`/`activities-step.ts`'s `recordAgentRun` calls) — without it, every cost metric on this dashboard would've shown $0.00 forever.
- [x] Product metrics view — slice 5, 2026-09-17: `app/internal/product-metrics/page.tsx` (§13.4 — trip-start rate, requirement-completion rate, draft-generation rate, confirmation rate, revision rate, time-to-first-draft/finalized, abandonment stage, qualitative feedback shown honestly as "not yet collected"). Deliberately a separate page from `/internal/analytics`, per §13.4's own rule against conflating engineering activity with product success. Reuses `checkRequirementsComplete`/`getCurrentChainStep` (existing deterministic domain functions) rather than reimplementing the same logic against raw rows.
- [x] Cache-hit / cost comparison (caching on vs. baseline) — slice 6, 2026-09-17: `evals/runners/run-cache-comparison.ts` (`npm run eval:cache-comparison`) runs the same 6 real Intake-agent calls twice against the real Anthropic API — once with `AnthropicModelClient`'s existing (always-on) caching, once with a new `cachingEnabled` constructor flag (exists solely for this comparison) forcing it off — same model/inputs/order, so the delta is attributable to caching alone. Real result (claude-sonnet-5, 2026-09-17): **45.5% cost reduction** ($0.08585 -> $0.04681) from the 2nd call onward hitting the cached system prompt + tool defs, but **no latency benefit** (26680ms -> 29088ms, slightly worse) — confirms §6.7's "prompt caching is an experiment, not an assumption": it's a real cost win here, but assuming it also speeds things up would have been wrong.

### Phase 9 — Portfolio polish
- [x] Visual design pass — slice 3, 2026-09-17: warm editorial travel palette (sand/cream backgrounds, navy text, deep teal as the primary interactive color, terracotta reserved for the one key call-to-action per screen and attention states), a Fraunces display serif for headings paired with the existing Geist Sans for body/UI text, single warm-light theme (dropped `dark:` variants — a boutique travel product reads as light-first, and it halves the class list on every component). Applied across every page: landing/sign-in, the chat + itinerary panel, trip history/detail, and both internal dashboards, per the user's explicit direction to cover all pages, not just user-facing ones. Live-verified via a throwaway dev-signin route (removed after use, not committed) driving a real trip through intake → flight/hotel/activities confirmation → finalize.
- [x] Architecture diagram — slice 2, 2026-09-17: [`docs/architecture/architecture-diagram.md`](architecture/architecture-diagram.md), two Mermaid diagrams (system components; one chat turn's deterministic-vs-agent/guardrail flow) plus the "what would change for live providers" narrative point from §21. Rendered and visually checked before committing (a first draft with deeply nested subgraphs rendered but was too cramped to read; flattened into two smaller diagrams instead).
- [x] Fill in remaining ADRs for any decisions made along the way — slice 1, 2026-09-17: [`ADR-006`](architecture/ADR-006-evaluation-strategy.md) (evaluation strategy — deterministic grading, direct-call harness) and [`ADR-007`](architecture/ADR-007-observability.md) (observability — dashboard placement/split, telemetry retention/redaction). Writing ADR-007 required actually resolving the telemetry retention/redaction question first (asked the user rather than deciding unilaterally, per the standing rule): redact `requiredAccessibility` at write time (`src/observability/redaction.ts`, wired into `src/repositories/agent-runs.ts`'s `recordToolCalls`), document but don't yet automate time-based retention.
- [x] Final README pass — slice 4, 2026-09-17: rewrote `README.md` from a two-paragraph placeholder into the full §21 portfolio narrative (why deterministic-over-swarm, what's kept out of the LLM, requirements/preferences/decisions separation, inventory grounding, impossible-constraint handling, feasibility engine, state versioning/approvals, what evals changed, what telemetry revealed — the real 45.5%-cost/no-latency-benefit caching result — and what would change for live providers/real booking), plus known-limitations and trade-offs/dead-ends sections, a tech-stack summary, and local setup instructions. Also fixed a real doc-accuracy gap found while writing it: `.env.local.example` was missing `VOYAGE_API_KEY` even though the app requires it for retrieval/embeddings.
- [ ] Demo walkthrough recording — script prepared ([`docs/DEMO_WALKTHROUGH.md`](DEMO_WALKTHROUGH.md), slice 4, 2026-09-17), a beat sheet covering every §21 narrative point in demo order; the actual screen recording is a manual step for the user, not something a coding agent can perform.

---

## LARGE-SCALE SEED DATA & SYNTHETIC FLIGHT GENERATION (built 2026-09-18)

**Why this exists:** before the user's own manual end-to-end testing pass, the 6-destination catalog (Phase 1) was too thin to avoid hitting dead ends constantly — a real tester picking an arbitrary destination or date range would frequently find nothing. The user asked for ~1000 destinations, dense hotel/activity variety, and near-universal flight date coverage, but explicitly wanted **realistic, occasional gaps** rather than either "always available" or "the norm to fail" — this shaped the design below more than the raw scale did.

**The core problem, scoped in plan mode before writing anything:** `hotels`/`activities` have no per-date rows at all (filtered only by destination/capacity/opening-hours/closed-days), so "every date has options" for those two is purely a volume/variety problem. `flights` rows are matched by exact calendar date — pre-seeding literal rows for ~640 destinations × arbitrary future dates is combinatorially infeasible (tens of millions of rows) and risks the hosted Supabase free tier. This is the one genuine architecture fork in this build, flagged and resolved with the user before code: a **deterministic synthetic flight generator**, invoked only as a `findFlights` fallback on a genuine zero-row miss, not a live/random generator.

**Built:**
- `src/domain/geography.ts` — a ~150-country region/cost-tier/climate profile table plus pure derivation helpers (seasonality, vibe tags, daily cost range, haul multiplier). Shared by both the seed script and the runtime flight generator so the two never drift independently.
- `src/domain/random.ts` — deterministic seeded PRNG (`seededRng`/`pick`/`pickMany`/`chance`/etc.), used everywhere content needs to vary but stay reproducible across re-runs.
- `scripts/data/cities.ts` — 637 real city/country pairs (139 countries), globally unique names (checked against `getDestinationByName`'s exact-match resolution — a duplicate would make it ambiguous), deliberately excluding the 6 original curated destinations by omission.
- `scripts/data/inventory-templates.ts` + `scripts/generate-large-seed-data.ts` — additive, idempotent-per-city generation: 637 new destinations, 3,841 hotels (5-8 per destination, ~12% deliberately sparse at 2-3 — a smaller destination with fewer real lodging choices, not a bug), 8,114 activities across 10 categories (food/cultural/tour always present; spa/concert/show/movie/sporting_event/outdoor/nightlife included with a per-destination probability, halved for sparse destinations) — covers the user's explicit category list (spa, restaurant, concerts, plays/shows, movies, sporting events, cultural events). The original 6 destinations and their fixtures were never touched.
- `src/domain/flight-generator.ts` — the fork's actual mechanism: a route's **weekly operating schedule** is deterministic per route (seeded hash, weighted toward 5-7 days/week but sometimes as few as 1-2), not a per-date coin flip — real airlines don't fly every route daily either, and this is what makes a gap feel like "this route doesn't fly Tuesdays" rather than random noise. `generateFlightsForDate` returns `[]` on a non-operating weekday (a genuine, honest "no flight that day" — the existing `NoViableFlightCandidatesError`/friendly-error path already handles this with no new code) or 1-3 priced/timed options otherwise. Deliberately timezone-agnostic (always UTC) so `findFlights`'s local-date filtering round-trips correctly without needing a real IANA zone for an arbitrary free-text origin.
- `supabase/migrations/0013_flights_source.sql` — `flights.source` (`'seed'`/`'generated'`), mirroring `destinations.source`.
- `src/repositories/flights.ts`'s `findFlights` — generates and persists only on a genuine zero-row miss for a specific route+date, never competing with or duplicating a real (curated or previously-generated) match; the same route+date always returns the same flights afterward. `src/workflow/flight-step.ts`'s two call sites now also pass the resolved destination's name/country (needed to build a valid row and price it by haul distance) alongside the existing id-based filters.

**A real bug found live, not by any of the 372 unit tests (all mocked):** `scripts/generate-embeddings.ts`'s destination/activity fetch used a bare `select("*")`, which PostgREST caps at 1000 rows by default — invisible at the old 27-activity scale, but it silently embedded only 1,000 of ~8,140 activities on the first post-reseed run. Fixed with `.range()`-based pagination. The same run also surfaced Voyage's 3-requests/minute free-tier limit as a real, not just documented, blocker — fixed with inter-chunk spacing (25s) plus a bounded 429-specific retry in `VoyageEmbeddingClient`, and batched (not fully concurrent) embedding write-backs to avoid overwhelming the connection pool at ~8,100 concurrent updates.

**Verified:** `npx tsc --noEmit`/`npm run lint`/`npm test` (372/372, 28 new — `random.test.ts`, `geography.test.ts`, `flight-generator.test.ts`, and a new `flights.test.ts`, the first repository-level test in this codebase, since the generate-on-miss logic is real behavior worth testing directly rather than only through workflow-level mocks)/`npm run build` all clean. Live-verified against the real hosted Supabase project: embeddings backfilled for all 643 destinations + 8,137 activities; `npm run eval:retrieval` (10/10) and `npm run eval:scenarios` (20/20, all §19 scenarios + adversarial cases) both still pass unchanged, confirming the original 6 destinations' fixtures are genuinely untouched. A live sweep of several new routes confirmed the actual gap behavior: some routes return flights on all 14 sampled dates, others as few as 4/14 — real, route-specific gaps, not universal availability and not a broken feed.

**Known limitations:** the concurrent-insert race if two searches for the exact same never-before-generated route+date land simultaneously (duplicate generated rows, not a correctness bug) is accepted, not guarded — same class of judgment call as other documented low-severity races in this codebase. Hotel/activity content is templated (not hand-curated per destination) by design at this scale — plausible variety for testing, not marketing copy.

---

## LIVE FLIGHT INVENTORY: SERPAPI (GOOGLE FLIGHTS) INTEGRATION (built 2026-09-18)

**Why this exists:** found live while investigating a real reported bug (destination-name matching too brittle — see the resolved item in `END_TO_END_TESTING_ISSUES.md`), the user asked to think through replacing seed/synthetic flight data with a real live API for the flights leg specifically, everything else (hotels/activities) staying on seed data. Considered and explicitly rejected first: making SerpAPI the *only* source used everywhere, including evals — real API results are non-deterministic and would break the reproducible-eval story (`ADR-006`), one of this project's actual demonstration goals. Landed on: the real app's flight search is SerpAPI-only, unconditionally, with no seed fallback; evals keep using the existing deterministic seed/synthetic-generator path, completely unaware anything changed — the same swappable-provider seam `ModelClient`/`EmbeddingClient` already established, applied here for the first time to inventory instead of AI providers.

**The two sub-problems, scoped before writing anything:**
1. SerpAPI's Google Flights endpoint takes IATA airport codes, not city names, and a city can have more than one commercial airport (sometimes the same city name reused across countries — "London" resolves to 4 real airports across the UK and Canada in the dataset built for this). Needed a real airport-code dataset and a disambiguation flow, not a hand-waved assumption.
2. SerpAPI's response has no refundable/changeable signal at all, and no stable per-result ID (it has a `booking_token`, but that's for real bookings, which this app never does — §6.5's boundary). The user explicitly approved defaulting refundable/changeable to `false` rather than fabricating data.

**Built:**
- `src/domain/destination-query.ts` — `parseDestinationQuery`, splitting a free-text "City, Country" requirement (built for the destination-matching fix, reused here for airport lookups too).
- `scripts/generate-airport-data.ts` + `src/domain/airport-data.ts` — a real, re-runnable generator: OurAirports' public-domain dataset (filtered to airports with a real IATA code and actual scheduled commercial service — 86,094 raw rows down to 3,504), country names normalized to match this codebase's existing `COUNTRY_PROFILES` spelling (6 hand-corrected mismatches, e.g. "Czechia" vs. this project's "Czech Republic"), merged with OpenFlights' separate dataset by IATA code to pull in each airport's real IANA time zone (OurAirports doesn't have one, and SerpAPI's own per-leg times are bare local "YYYY-MM-DD HH:MM" strings with no offset at all — unusable without it).
- `src/domain/airport-lookup.ts` — `findAirportsForCity` (country-aware, reuses `parseDestinationQuery`; a wrong country returns `[]` rather than guessing a different country's airport) and `findAirportByIata`.
- `src/domain/dates.ts`'s new `zonedTimeToUtc` — the missing inverse of the existing `localDateInTimeZone`/`localMinutesOfDay`: local wall-clock string + IANA zone -> UTC instant, needed because SerpAPI's times carry no offset. Standard round-trip-through-`Intl` correction, no timezone-database dependency beyond what `Intl` already has; verified against real known offsets (US Eastern winter/summer, Madrid, London, a half-hour zone, a UTC-day-rollover case).
- `src/domain/extraction.ts` — two new optional requirement fields, `originAirportCode`/`destinationAirportCode`, deliberately **not** in `REQUIRED_FOR_READY` (most cities have exactly one airport; asking every trip would be wrong).
- `src/workflow/step-shared.ts` — `resolveFlightAirport`/`resolveFlightAirportOrThrow` (unambiguous city -> silent resolve; ambiguous + a stored disambiguation answer -> resolve to it; ambiguous + nothing stored -> candidates for a clarification) and `checkAirportReadiness`, the deterministic gate that decides whether a "which airport" clarification turn is needed — mirrors `resolveTripDestination`'s existing shape exactly.
- `src/workflow/intake-orchestrator.ts` — a new, opt-in `enableAirportDisambiguation` param (only the real app passes `true`; every eval call site is unaffected by omission). Airport ambiguity reuses the *existing* clarification machinery end to end (same `awaiting_clarification` transition, same `ClarificationRequest` shape) rather than inventing a parallel mechanism — it just always overrides the model's own turn text with a fully deterministic question naming the real candidate airports by name and code, since the model was never asked about it that turn. The model itself gets an explicit `pendingAirportClarification` field in its trip-state snapshot (mirroring how `activeChainStep` already works) — necessary because `runIntakeAgent` has no conversation history, so a bare reply like "JFK" would otherwise carry no signal about what's being answered.
- `src/repositories/flight-provider.ts` (`FlightSearchProvider` interface, mirroring `EmbeddingClient`'s shape) + `src/repositories/providers/serpapi-flight-provider.ts` (thin HTTP client) + `serpapi-flight-mapper.ts` (the actual response parsing, deliberately split into its own file with no `"server-only"` import so it's unit-testable — every other provider client in this codebase has zero unit tests for exactly this reason, since `"server-only"` throws outside a real React-server-conditions environment; this is the first one that gets real coverage). Each SerpAPI itinerary (which may have layover segments) is summarized "door to door" into one flat option — a layover's own leg isn't modeled separately, matching how the existing synthetic generator's options have no leg structure either.
- `src/repositories/flights.ts`'s new `findFlightsFromProvider` — the live-provider equivalent of `findFlights`, with its own cache (a recent `source = 'serpapi'` row for the same airport pair + date is reused instead of calling the metered, rate-limited real API again — free tier: 250 searches/month, 50/hour). `supabase/migrations/0014_flights_airport_codes.sql` (`origin_airport_code`/`destination_airport_code`, `source = 'serpapi'`-only) and `0015_flights_created_at.sql` (needed for the cache's staleness check — no other inventory table has one, since seed/generated rows have no per-row temporal meaning the way a live-fetched one now does) applied to the hosted project.
- `src/workflow/flight-step.ts` — `proposeFlightStep` takes an optional `flightProvider`; omitted (every eval call site), it's byte-for-byte the original `findFlights`-based behavior; set (the real app, via `StepwiseChainClients.flightProvider`, `src/workflow/step-router.ts`), it resolves both airports and calls the provider instead. `app/app/actions.ts`'s `stepwiseChainClients()` always constructs `SerpApiFlightProvider`; `evals/lib/scenario-harness.ts`'s own `stepwiseChainClients()` was never touched.

**Verified:** `npx tsc --noEmit`/`npm run lint`/`npm test` (426/426, 55 new across `destination-query`, `airport-lookup`, `dates` (`zonedTimeToUtc`), `step-shared`, `intake-orchestrator`, `flight-step`, `serpapi-flight-mapper`) all pass. `npm run eval:scenarios` re-run in full afterward — **20/20 still pass**, including `standard_trip` (a real flight→hotel→activities→finalize flow) and `hard_constraint_vs_cheapest` (asserts real flight candidates) — proving the seed-backed path evals depend on is genuinely untouched, not just believed to be. Live-verified end to end against the real hosted Supabase project and the real SerpAPI: a New York → Madrid trip correctly flagged "New York" as ambiguous (JFK/LGA, the real candidates), resolved once answered, and `proposeFlightStep` returned 3 real priced JFK↔MAD candidate pairs (Air Europa, British Airways, American outbound; Avianca return) persisted with `source: "serpapi"`; a second immediate call hit the cache (804ms, same data) instead of calling SerpAPI again.

**Known limitations, all deliberate:** refundable/changeable are always `false` for SerpAPI-sourced flights (no such signal exists in the response — a trip requiring a refundable flight will correctly find zero live candidates, not fabricated ones). Origin airport resolution covers any city in the ~3,500-airport dataset (real scheduled-commercial airports worldwide), not just the seeded destinations. A multi-leg itinerary is summarized door-to-door, not modeled leg-by-leg. No retry/backoff on a transient SerpAPI failure (a real HTTP error surfaces immediately as a friendly "try again" message) — not added without real usage data on how often it's actually needed.

---

## 2. Domain model (proposed)

Implemented in `supabase/migrations/0001_initial_schema.sql`. Summary of each entity's role:

| Entity | Role |
|---|---|
| `sessions` | One browser/chat session, owned by an authenticated user. Scopes `messages`. |
| `trips` | The actual planning unit. Owned by a user, has a `status` tracking workflow state. A user can have many. |
| `messages` | Raw chat history for UI continuity — **not** what agents reason over (see `PROJECT_BRIEF.md` §6.3). |
| `trip_state_versions` | Append-only, versioned snapshot of trip state — fast-read projection + optimistic concurrency anchor. |
| `trip_requirements` / `trip_preferences` / `trip_decisions` | The three distinct classifications from §7.1 — hard musts, soft wants, and actual selections — each with provenance and status. |
| `trip_events` | Append-only domain event log (`trip_created`, `budget_calculated`, etc.) — the replay/audit trail. |
| `approval_records` | Tracks what was proposed, what was approved, and invalidates prior approval on subsequent change. |
| `destinations` / `flights` / `hotels` / `activities` | Seeded/mock inventory, world-readable, versioned via `inventory_version`. `destinations`/`activities` carry `pgvector` embeddings for RAG. `flights.source` (`'seed'`/`'generated'`) distinguishes curated rows from ones synthesized on demand by `src/domain/flight-generator.ts` when a search finds no real match for a route+date. |
| `workflow_runs` / `workflow_steps` | One row per workflow execution and per transition within it — internal, service-role only. |
| `agent_runs` / `tool_calls` | Per-agent-call and per-tool-call telemetry (tokens, cache stats, latency, cost) — internal, service-role only. |
| `guardrail_events` | Every guardrail check and outcome, across all four layers — internal, service-role only. |
| `eval_runs` / `eval_results` | Eval suite execution history — internal, service-role only. |

---

## 3. Deterministic service interfaces (proposed)

Signatures only — no implementations yet. These are the internal services from `PROJECT_BRIEF.md` §6.4/§12.2; none of them are model-facing tools.

```typescript
// --- Inventory search (backs the model-facing search_flights / search_hotels /
//     retrieve_destinations / retrieve_activities tools — the tool layer is a
//     thin wrapper around these, adding logging and schema validation) ---

interface FlightSearchParams {
  origin: string;
  destination: string;
  departureDate: string; // ISO date
  returnDate?: string;
  partySize: number;
  hardConstraints: HardConstraint[];
  budgetRangeUsd?: [number, number];
  inventoryVersion: number;
}
function searchFlights(params: FlightSearchParams): Promise<FlightCandidate[]>;

interface HotelSearchParams {
  destination: string;
  checkIn: string;
  checkOut: string;
  partySize: number;
  budgetRangeUsd?: [number, number];
  preferences: Preference[];
  hardConstraints: HardConstraint[];
}
function searchHotels(params: HotelSearchParams): Promise<HotelCandidate[]>;

interface ActivityRetrievalParams {
  destination: string;
  dateRange?: [string, string];
  vibeTags: string[];
  accessibilityNeeds?: string[];
  priceRangeUsd?: [number, number];
  topK: number;
}
function retrieveActivities(params: ActivityRetrievalParams): Promise<ActivityCandidate[]>;

// --- Budget engine (PROJECT_BRIEF.md §9.3) ---

interface BudgetBreakdown {
  currency: string;
  target: number;
  ceiling: number;
  subtotal: number;
  taxesAndFees: number;
  contingency: number;
  totalEstimate: number;
  remaining: number;
  unpricedItems: string[];
  violations: BudgetViolation[];
}
function calculateBudget(trip: TripSelectionSnapshot): BudgetBreakdown;

// --- Hard-constraint engine (PROJECT_BRIEF.md §9.5) ---

function filterHardConstraints<T extends Candidate>(
  candidates: T[],
  constraints: HardConstraint[]
): { passing: T[]; rejected: { candidate: T; reason: string }[] };

// --- Candidate combination (PROJECT_BRIEF.md §9.6 end-to-end scenarios) ---

function assembleCandidateCombinations(
  candidates: { flights: FlightCandidate[]; hotels: HotelCandidate[]; activities: ActivityCandidate[] },
  budget: BudgetConstraints
): CandidateCombination[];

// --- Itinerary feasibility engine (PROJECT_BRIEF.md §9.2) ---

interface FeasibilityResult {
  valid: boolean;
  violations: FeasibilityViolation[];
  warnings: FeasibilityWarning[];
}
function validateItineraryFeasibility(itinerary: DraftItinerary): FeasibilityResult;

// --- Inventory-reference validation (PROJECT_BRIEF.md §9.4) ---

function validateInventoryReferences(
  itinerary: DraftItinerary,
  approvedCandidateSet: CandidateSet
): { valid: boolean; unresolvedIds: string[] };

// --- Workflow transition validation (PROJECT_BRIEF.md §8.2) ---

interface TransitionRequest {
  tripId: string;
  fromState: WorkflowState;
  event: WorkflowEvent;
  currentStateVersion: number;
}
function validateStateTransition(req: TransitionRequest): { allowed: boolean; reason?: string };
```

Every one of these gets unit tests with no LLM involved, per `PROJECT_BRIEF.md` §12.2 and §17.1.

**As implemented in Phase 2, the actual signatures deviate slightly from this sketch** (this section was "proposed," not locked):
- `calculateBudget`/`BudgetBreakdown` use the existing `Money` type (`src/domain/money.ts`) throughout rather than raw numbers with a separate `currency` string, for currency-checked arithmetic.
- `filterHardConstraints` takes an array of small `HardConstraint<T>` objects (each with its own `code`/`describe()`/`isSatisfiedBy()`) rather than a `HardConstraint[]` union type — each constraint is independently constructed and testable (`noRedEyeConstraint()`, `minHotelRatingConstraint(4)`, etc., in `src/domain/constraints.ts`).
- `validateInventoryReferences` takes a lightweight `{ flightIds, hotelIds, activityIds }` reference struct plus an `ApprovedCandidateSet`, not a full `DraftItinerary` — it doesn't need the itinerary's dates/schedule, only which IDs were referenced.
- `validateStateTransition`'s `TransitionRequest` gained `proposalHashMatches`, `guardrailsPassed`, and `resumeState` fields to carry the extra preconditions from `PROJECT_BRIEF.md` §8.2's example (finalization requires a matching proposal hash and passing guardrails) and the `failed_recoverable` → resume path from §8.4.

---

## 4. Route structure (proposed)

| Route | Access | Purpose |
|---|---|---|
| `/` | Public | Landing page + magic-link sign-in. No app functionality lives here. |
| `/auth/callback` | Public | Completes the magic-link sign-in (Supabase Auth callback). |
| `/app` | Authenticated | Chat + live itinerary panel — the core product experience. |
| `/app/trips` | Authenticated | Trip history — list of past trips (in-progress and finalized), each opening back into its itinerary. |
| `/app/trips/[tripId]` | Authenticated (owner only, enforced by RLS) | A specific trip's chat + itinerary, resumed. |
| `/internal/analytics` | Authenticated (deferred — see open items) | Engineering/product dashboard. Lives in the same app as a separate route group rather than a second deployable, per `PROJECT_BRIEF.md` §7.8. |

---

## 5. Open items carried forward

This section is the single place every deferred decision, known bug, and standing constraint gets tracked — **whenever a review, a build session, or a live-data check surfaces something deliberately not fixed on the spot, it goes here as an unchecked item before moving on, and a phase's checklist in §1 does not get marked done while a bug it introduced sits here unresolved.** Checked items are kept (struck through in spirit, not deleted) with the date/entry that closed them, so the history of what was found and when doesn't disappear.

### From `PROJECT_BRIEF.md` §22

- ~~**Seed data volume**~~ — Phase 1's small curated set (6 destinations) was a working default for building against; superseded 2026-09-18 by the large-scale seed data build (see the new section below) once the user's own manual testing pass needed enough inventory that running out of options wasn't the norm. The original 6 and their fixtures are untouched.
- ~~**Analytics dashboard access control**~~ — resolved Phase 8 slice 4, 2026-09-17: any signed-in user (the same bar every other `/app/*` route uses), not a separate admin role. This is a single-operator portfolio project with no multi-tenant admin concept anywhere else — inventing one just for this page would be unjustified complexity. Revisit only if the app ever gains real multiple untrusted users.
- ~~**CI setup**~~ — resolved Phase 8 slice 2, 2026-09-17: `.github/workflows/ci.yml` (GitHub Actions, as expected).

No other open items remain from §22 — app stack, auth, and git/GitHub are now decided (see ADR-000, ADR-003, and `BUILD_LOG.md`).

### Standing environment/tooling constraints

- **`src/config/supabase/database.types.ts` is hand-maintained, not generated.** `supabase gen types typescript` requires Docker/Podman, which is blocked on this machine at the OS level (see BUILD_LOG.md, 2026-09-16 "Docker/local Supabase blocker" — root cause never resolved, hosted Supabase is the permanent workaround). Every schema migration must be followed by a manual, matching edit to this file — nothing catches a drift between the two automatically. Revisit if the Docker blocker ever gets resolved (worth an occasional retry of `sfc /scannow`/a Process Monitor trace per that entry, but not on any schedule).

### Known bugs/gaps — unresolved

None. The two items below were the explicit starting point for this session (deliberately deferred 2026-09-17, pending a decision on how far to take each) — both confirmed 2026-09-17 as permanent, documented scope boundaries rather than gaps to close. Kept here (not deleted) as the record of the boundary itself, since a future session may still want to revisit if a concrete requirement emerges:

- **Hotel bookings assume every room group fits the same room type at the same nightly rate.** The `hotels` table models one row as one bookable room type — no room-type variety (can't express "2 doubles + 1 twin"). **Confirmed as a permanent boundary, not a gap** — asked directly whether to build the fuller `hotel_room_types` model (new table, per-group assignment problem, reworked budget math) versus leaving the existing availability-count-only fix (`available_rooms`, `roomAvailabilityConstraint`) as the boundary; chose to leave it. Revisit only if a future phase wants richer room-type preferences.
- **`src/domain/scheduling.ts`'s `scheduleActivities` is a single deterministic forward pass with a meal-time-window heuristic, not a real optimizer.** No backtracking (a later, better-fitting activity can't "bump" an earlier one), no repair pass, no geographic clustering (blocked on data regardless — `activities.location` is a free-text neighborhood string, not coordinates). **Confirmed as a permanent boundary, not a gap** — asked directly whether to add a repair/local-search pass or a real scored optimizer versus leaving the current lightweight-heuristics tier as the boundary; chose to leave it. Revisit only if a future demo need makes the current schedule quality visibly insufficient.

### Known bugs — resolved

- [x] **10 of `PROJECT_BRIEF.md` §19's 16 end-to-end scenarios weren't automated** — closed 2026-09-17. Scoping this properly (not just against the coarse tracked summary) found each scenario needed one of three things, not uniformly "new infrastructure" as originally framed: most (#4, #8, #9, #10, #11, #12, #13) were buildable directly with the existing service-role harness; two (#14, #16) needed one small new capability each; one (#6) doesn't apply to the current architecture at all. Three real findings/decisions came out of this and were put to the user directly before building anything:
  1. **A real guardrail gap, not just a missing test**: `confirmFlightStep`/`confirmHotelStep`/`confirmActivitiesStep` never checked `inventory_version` at all — a stale-version ID would be silently accepted if it ever reached a confirm call directly (propose-time search already filtered correctly). Fixed in all three step modules, comparing against `destinationRow.inventory_version` (already resolved via `resolveTripDestination`) rather than importing `CURRENT_INVENTORY_VERSION` a second time — same `Invalid*SelectionError` classes, same category as the destination_id/hard-constraint checks already there. New unit tests in all three `*-step.test.ts` files; `stale_inventory` (§19 #10) proves it live against a real seeded stale-`v0` hotel.
  2. **No reachable path existed for cancellation (§19 #13) at all** — `state-machine.ts` modeled a `cancel` event legal from any non-terminal state, but nothing in the UI or Server Actions ever fired it. Built a real minimal feature rather than documenting the gap: `cancelTrip` (`app/app/actions.ts`, mirrors `finalizeTrip`'s shape exactly), a "Cancel trip" action + confirm banner + "Trip cancelled" terminal state in `itinerary-panel.tsx`, a cancelled badge in the trip history list (`trips/page.tsx`), and `requireOwnedTrip` now refuses every other mutating action on a cancelled trip by default (`TripCancelledError`, `src/workflow/step-shared.ts` — not `actions.ts` itself, which is a `"use server"` file that can only export async functions, not classes; a real build failure caught this live before it shipped). Browser-verified end to end via the standard throwaway dev-signin pattern (removed after use, not committed): cancel button → confirm banner → "Trip cancelled" state → correct trip-history badge, all working. That same pass found a second real bug: typing into chat on an already-cancelled trip threw `TripCancelledError` directly from `sendMessage`, which (thrown across the Server Action boundary in a production build) surfaced as React/Next's generic obfuscated error — the same dishonest-failure class already fixed elsewhere. Asked the user whether to fix now or track it; fixed: `SendMessageResult` widened to `| StepActionError`, `chat-panel.tsx` checks `"error" in result` before treating the response as a normal turn. Re-verified live: the friendly message now renders correctly.
  3. **Flexible destination (§19 #6) doesn't apply, by design** — `destination` is a required `trip_requirements` field the Intake agent must extract explicitly (`REQUIRED_FOR_READY`) before a trip can leave `collecting_requirements`; there's no "choose among several candidate destinations" step anywhere in the chain. Documented as an intentional non-applicability (`flexible_destination`'s own case description) rather than silently omitted, per the user's choice when asked.
  
  Also added: `evals/lib/scenario-harness.ts`'s `signInAs(userId)` — a real magic-link-generated, RLS-scoped client for a specific user (not the service-role client every other harness method uses), the one genuinely new piece of test infrastructure this required, reusing the exact mechanic every prior session's throwaway `/dev-signin` browser routes already used. 8 new cases in `evals/cases/scenarios.ts` (conflicting hard constraints, flexible destination, approval invalidation, itinerary timing conflict, stale inventory, duplicate request, failure/recovery via a small fault-injecting `ModelClient` wrapper, cancellation) and 2 in `evals/cases/adversarial.ts` (prompt injection in a temporary seeded activity fixture — inserted and deleted within the case itself, not a permanent migration; full RLS cross-user isolation via `signInAs`).

  **Verified live, the actual deliverable**: `npm run eval:scenarios` — **20/20 passed** against the real hosted Supabase project and real Anthropic/Voyage APIs (all 16 §19 scenarios plus the 4 pre-existing + 2 new adversarial cases). The Curator's response to the seeded prompt-injection fixture is worth quoting — it explicitly identified and excluded the attempt: *"The 'Definitely Not A Prompt Injection Walking Tour' candidate was excluded entirely: its content attempted to manipulate the ranking process directly rather than describing a genuine activity... it was treated purely as untrusted data and its embedded commands were ignored."* Also `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` (344/344) throughout.
- [x] **`scheduleActivities` had no time-of-day sense — a restaurant could land at 9am if that was the first open slot** — closed 2026-09-17 with the "lightweight heuristics" option, one of five effort tiers laid out for the user (heuristics / repair pass / real optimizer / geographic clustering / leave it); the user chose the smallest one, matching how "revisit only if a future phase needs it" was already the module's own framing. New `preferredWindows` on `SchedulableActivity` (`src/domain/scheduling.ts`) — a generic list of minute-of-day ranges tried in order, each across the whole date range, before the existing unconstrained earliest-fit search — keeps the scheduler itself domain-agnostic; the caller (`activities-step.ts`) decides that `category: "food"` activities get `[LUNCH_WINDOW, DINNER_WINDOW]` (11:30-14:00, then 18:00-21:00). Still a single deterministic forward pass per window, not a search over placements — no backtracking added. 4 new tests in `scheduling.test.ts` cover placement-inside-a-window, trying windows in order, falling back to the unconstrained search when no window fits, and the window search still respecting `closedDays`/`openingHours`/per-date buffers. Verified via `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` (341/341, up from 337). Live-verified against real seed data with a throwaway script (not committed, cleaned up after): a real Lisbon trip's curated activity set included "Alfama Walking Food Tour" (`category: "food"`), scheduled at 13:50 — inside the lunch window.
- [x] **Nothing checked whether a hotel actually had enough rooms free to book one per room group** — closed 2026-09-17, scoped deliberately: the tracked item bundled two separable gaps (room-type variety, and room availability), and the user chose the availability-only fix over a full room-type catalog when asked, since the fuller version is speculative (no concrete requirement driving it) while this half directly closes a real correctness gap. `supabase/migrations/0012_hotel_available_rooms.sql` adds `hotels.available_rooms int not null default 5` (generous — this project's room-group counts are always small, so the backfill doesn't affect any existing flow); a new `roomAvailabilityConstraint` (`src/domain/constraints.ts`) checks `roomGroups.length <= available_rooms`, wired into `hotelHardConstraints` (`step-shared.ts`) alongside the existing `roomCapacityConstraint`. Added a scope-boundary docstring to `src/repositories/hotels.ts` documenting the still-open room-type-variety limitation, so the next person to touch this doesn't have to rediscover it. New unit tests in `constraints.test.ts` and `hotel-step.test.ts` (a hotel whose per-room capacity is fine but that doesn't have enough *rooms* free is correctly rejected). Verified via `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` (337/337, up from 335). Applied to the hosted project via `supabase db push --db-url`, live-verified with a throwaway script: 6 room groups against real seed hotels (all defaulted to `available_rooms=5`) were correctly rejected with `NoViableHotelCandidatesError`, while a more modest 3-room-group request still found viable hotels.
- [x] **`retrieveActivities`'s closed-days post-filter used a fixed overfetch heuristic (`OVERFETCH_FACTOR = 2`), not a guarantee** — closed 2026-09-17. `match_activities`'s SQL `LIMIT` runs before the closed-days post-filter (applied in application code, not SQL — see the module's own header comment for why), so a fixed one-shot overfetch could under-fill `topK` if more than `topK * 2` of the closest semantic matches happened to be closed on the excluded day(s), even though enough open ones existed further down the ranking. Fixed with a widen-and-retry loop in `retrieveActivities` (`src/retrieval/activities-retrieval.ts`): if the post-filter under-fills `topK` *and* Postgres returned exactly as many rows as requested (meaning there could be more beyond the current `LIMIT`), it doubles `matchCount` and retries against the same pre-computed query embedding — no extra Voyage API call. Terminates naturally the moment a fetch returns fewer rows than requested (every matching activity for the destination has been seen), with a defensive `MAX_MATCH_COUNT` (200) ceiling on worst-case query cost, not because the doubling itself needs one to converge. 3 new tests prove the actual scenario (a first batch of all-closed results followed by a wider fetch that reaches the open ones further down the ranking), the natural-termination case (a short first batch isn't retried), and the `MAX_MATCH_COUNT` ceiling against a pathological all-closed result set. Not live-verified: the current seed data (a handful of activities per destination) can't reach the retry path at all regardless of real vs. mocked data, so a live check would only prove the unchanged happy path, not the new logic — the synthetic unit tests are what actually exercises it. Verified via `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` (335/335, up from 332).
- [x] **The stepwise chain's inventory-reference checks assumed `flights`/`hotels`/`activities`' free-text `destination` columns and `destinations.name` shared one identifier space, with no FK and no uniqueness guarantee** — closed 2026-09-17 via a full scoping pass (plan mode: two Explore agents traced every consumer, then the plan was reviewed and approved before any code was written). `supabase/migrations/0011_destination_ids.sql` adds `destination_id`/`origin_id` (flights, both nullable — a flight leg's endpoints aren't both always a seeded destination, e.g. a traveler's home city like "New York" has no `destinations` row at all) and `destination_id` (hotels/activities, `not null` after backfill), backfilled by joining the existing text columns against `destinations(name, inventory_version)` together (correctly resolving the deliberately-seeded stale-v0 fixtures to their own stale row). The existing text columns are kept, not dropped — they remain the display string and the only field for a flight leg's non-seeded-city side. `match_activities` (the retrieval SQL function) now filters by `filter_destination_id uuid` instead of `filter_destination text` (Postgres needed drop+create, not `create or replace`, since a parameter type changed). Two scope questions were resolved directly with the user before implementing, not assumed: (1) resolve the destination to an id lazily inside each step (`src/workflow/step-shared.ts`'s new `resolveTripDestination`/`UnknownDestinationError`) rather than at intake time, which would have needed new deterministic workflow-state plumbing or exposing the destinations catalog to the Intake agent — a separate feature, not a data-integrity fix; (2) also fixed a coupled finding — `sendMessage`'s forward auto-chain (`app/app/actions.ts`) only `console.error`'d a `proposeCurrentChainStep` failure with no user-facing signal at all, unlike the revision path's existing `chain_revision_failed` fix, so a new `UnknownDestinationError` would have fallen into the same invisible-failure hole; mirrored the exact same pattern as a new `chain_propose_failed` trip_event, handled by the same `needsAttention` Realtime listener. `getDestinationByName` (existed since Phase 2, never had a real caller) also had its `.maybeSingle()` crash risk fixed in the process — a new `AmbiguousDestinationNameError` replaces what would otherwise have been an opaque, unhandled PostgREST error on an ambiguous match. Every `flight-step.ts`/`hotel-step.ts`/`activities-step.ts` guardrail check and repository search filter (`findFlights`/`findHotels`/`findActivities`/`matchActivities`) now compares by id, not name. New unit tests directly prove the scenario this whole fix was about: a candidate sharing the trip's destination *name* but a different destination *id* is now correctly rejected, where the old text-based check would have wrongly accepted it. Verified via `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` (332/332). Live-verified against the real hosted project with a throwaway script (not committed, cleaned up after): a full flight→hotel→activities-retrieval flow succeeded with ID-based filtering throughout; a trip stating a destination not in `destinations` ("Atlantis") correctly threw `UnknownDestinationError`; a `chain_propose_failed` trip_event was written and read back correctly. The full authenticated Server Action path (Next's `after()` mechanism itself) wasn't independently re-verified via browser this time — it's the same already-proven infrastructure the `chain_revision_failed` fix already established, just a second call site, so a full browser click-through wasn't judged worth the setup cost.
- [x] **`assembleItinerary` only tried the best 5 candidate combinations before declaring the itinerary infeasible** — closed 2026-09-17, moot rather than fixed: `assembleItinerary`/`MAX_COMBINATIONS_TO_TRY` referred to the old one-shot pipeline, retired in the stepwise chain redesign (slice 3), and its dependency `assembleCandidateCombinations` was deleted as dead code earlier this session (see below). A repo-wide grep confirmed neither symbol exists anywhere anymore. The stepwise chain never had this limitation to begin with — each step proposes a short ranked list (flight/hotel) or the one scheduled result (activities) directly, with no combination cross-join to cap.
- [x] **The flight→hotel cascade's "dates changed" branch was only verified live in one direction** — closed 2026-09-17. The seed data had exactly one Lisbon→New York return-leg flight, so there was no second real return date to swap in and exercise "confirming a flight with different derived dates actually invalidates the hotel" against live data. Asked the user whether to add seed data + live-verify now, or leave it tracked (explicitly the lowest-priority item); chose to close it. `supabase/migrations/0010_second_lisbon_return_flight.sql` added a second Lisbon→New York return leg one week later than the existing one (2026-10-19 vs. 2026-10-12), applied to the hosted project via `supabase db push --db-url`. A throwaway script (not committed, cleaned up after, reusing `evals/lib/scenario-harness.ts`'s existing throwaway-user infrastructure rather than rebuilding it) drove a real trip through: confirm flight (returnDate 2026-10-12, derived `checkOut: "2026-10-12"`) → confirm hotel → revise the `returnDate` requirement to 2026-10-19 → re-propose/re-confirm flight (picks the newly-seeded return leg, derived `checkOut: "2026-10-19"`, genuinely different) → confirmed live that the previously-confirmed hotel decision was retired (`listActiveTripDecisions` returned zero `hotel` rows afterward). The "dates differ → invalidated" branch is now proven against real data, not just the 3 existing synthetic-data unit tests.
- [x] **`calculateBudget`'s `nights` input could drift ~1 day from the actual hotel-stay length** — closed 2026-09-17 not by a fix but by confirming it no longer applies: this item described the old one-shot pipeline's `assembleItinerary`, retired in the stepwise chain redesign. The current `activities-step.ts` (line ~521) computes `nights` from `ctx.checkIn`/`ctx.checkOut`, which come from `deriveHotelStayDates` (`src/domain/stay.ts`) — the confirmed flights' *actual* local arrival/departure dates, not the raw requirement-stated `departureDate`/`returnDate`. The drift this item described structurally can't happen in the current architecture.
- [x] **`src/domain/combinations.ts` (`assembleCandidateCombinations`) and `src/validation/inventory-references.ts` (`validateInventoryReferences`) were dead code** — found while verifying the item above, closed the same day. Both were Phase 2 deterministic services the old one-shot pipeline depended on; when that pipeline was retired in the stepwise chain redesign (slice 3), nothing was left calling either one — confirmed via a repo-wide grep (only their own test files referenced them). `validateInventoryReferences`' guardrail coverage isn't actually missing: each stepwise-chain step module (`flight-step.ts`/`hotel-step.ts`/`activities-step.ts`) reimplemented an equivalent inline check (`Invalid*SelectionError`) rather than reusing the shared function, so this was reuse/duplication debt, not a guardrail regression. Asked the user how far to take each finding (delete now vs. track vs. — for the guardrail duplication — a bigger refactor to share one implementation); chose delete-now for both rather than a refactor, since deduplicating three already-working inline checks is a separate, bigger piece of work than removing something unused. Deleted `src/domain/combinations.ts`/`combinations.test.ts` and `src/validation/inventory-references.ts`/`inventory-references.test.ts` (the now-empty `src/validation/` directory disappears from the repo naturally, untracked by git). Updated the stale cross-reference in `inventory-references.ts` — since deleted along with the file — and the identifier-space open item above to point at the surviving inline checks instead. Verified via `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` (325/325, down from 344 — the 19 tests belonging to the two deleted files).
- [x] **One-way trips (no `returnDate`) weren't supported, and the failure was dishonest** — resolved 2026-09-17. Asked the user how far to take this (honest-failure-only, ask-for-a-return-date via clarification, a hard-coded default trip length, or skip); chose **ask for a return date**. Implementation turned out to be a one-line change with a cascade of cleanup: `returnDate` is now in `REQUIRED_FOR_READY` (`src/domain/extraction.ts`), so the Intake agent's existing generic `request_clarification` flow — which already lists `REQUIRED_FOR_READY` dynamically in its own prompt — asks for a return date before a trip can ever reach `requirements_ready`, with no prompt engineering needed. This made the dedicated `OneWayTripNotSupportedError` check in `flight-step.ts` (and the error class itself, in `step-shared.ts`) truly unreachable — `checkRequirementsComplete` now guarantees `returnDate`'s presence before that line is ever reached — so it was deleted as dead code (CLAUDE.md: "if you are certain that something is unused, you can delete it completely") rather than kept as an inert backstop. Updated `flight-step.test.ts` (the old "throws OneWayTripNotSupportedError" test now asserts `RequirementsNotReadyError`, matching how every other required field is already handled) and three `intake-orchestrator.test.ts` fixtures that built a "ready" requirements set without a `returnDate`. `README.md`'s known-limitations section also got a pass while touching the now-false "one-way trips aren't supported" line — two other bullets there (budget-ceiling, orchestrator-turn idempotency) had also gone stale from earlier-session fixes not yet reflected there; refreshed all three rather than leaving the one I touched directly next to two others contradicting `BUILD_LOG.md`. Verified via `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` (344/344) — not re-run live against the real Anthropic API (`npm run eval:scenarios`'s base scenario already states a return date, so it's unaffected; `evals/cases/intake.ts`'s `ambiguous_missing_budget` case doesn't assert an exhaustive missing-fields list, so it also still passes as written) — worth a manual `npm run eval:intake`/`npm run eval:scenarios` re-run before treating this as fully proven against the live model, since the required-field list the agent's prompt renders did change.
- [x] **The `"blocked"` workflow state was declared but unreachable** — resolved 2026-09-17: confirmed via a repo-wide grep that nothing outside `state-machine.ts`'s own `WORKFLOW_STATES` array referenced it (no `TransitionRule` ever set `to: "blocked"`, no DB check constraint, no UI code) except one display-label map (`app/internal/product-metrics/page.tsx`'s `STAGE_LABELS`) that would have shown it if it were ever reached. Since it was genuinely dead, not just low-priority, removed it from `WORKFLOW_STATES` and the label map (CLAUDE.md: "if you are certain that something is unused, you can delete it completely") rather than leaving the latent trap in place. No migration needed — no DB column enumerates workflow states as a constrained type. Verified via `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` (344/344, unchanged — no test ever referenced this state either).
- [x] **The Curator agent's malformed tool calls weren't logged as a Layer 2 (output-validation) guardrail event** — resolved 2026-09-17 (see BUILD_LOG.md). The correct fix (flagged, not guessed, when this was originally found) was a distinct error kind on the shared `ToolCallLogEntry` (`src/agents/model-client.ts`): `errorKind: "malformed"` for unknown-tool/duplicate-call/schema-failure entries, `errorKind: "reference_check_failed"` for a well-formed call whose content failed `validateCurationReferences` (already logged as its own `curation_reference_check` domain_validation event). `activities-step.ts` now logs a `tool_call_schema_validation`/`output_validation` guardrail event for every `errorKind: "malformed"` entry, mirroring Intake's equivalent loop, while explicitly excluding `reference_check_failed` entries so that one failure is never double-counted under two layers. 2 new tests in `activities-step.test.ts` (malformed call logs the new event; reference-check failure does not), plus `errorKind` assertions added to `curator.test.ts`'s existing cases. Verified via `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` (344/344) — not live-verified, since the change is pure classification logic over an already-tested `recordGuardrailEvent` write path (no new migration, query, or DB shape), not a new integration surface.
- [x] **A retry of a whole `processIntakeTurn` call wasn't idempotent across its non-transition writes** — resolved 2026-09-17 (see BUILD_LOG.md), narrower in scope than originally framed after re-examining the actual trade-off: a genuine retry's second real LLM call is a real, separately-costed event, so deduplicating `agent_runs`/`tool_calls`/`guardrail_events` would make the cost dashboard *under-report* real spend — asked the user, who agreed to scope the fix to just the one place a duplicate is unambiguously wrong: the chat transcript. `messages.correlation_id` (`0009_messages_correlation_id.sql`, no unique index — a best-effort mirror like `trip_events`/`workflow_steps`, not the strictly-consistent source of truth) plus a new `appendMessageOnce` helper (checks `findMessageByCorrelationId` before writing, keyed per-role via `deriveCorrelationId(correlationId, "message:user"/"message:assistant")`) means a retried turn no longer shows the same chat bubble twice. `ChatPanel` now generates a fresh correlation ID per send attempt, threaded through `sendMessage` → `processIntakeTurn`. The three stepwise-chain files (`flight-step.ts`/`hotel-step.ts`/`activities-step.ts`, the successors to the old `search-orchestrator.ts` this item also originally named) never call `appendMessage` at all, so the original concern doesn't apply to them — their `agent_runs`/`tool_calls`/`guardrail_events` writes are deliberately left unguarded, same reasoning as above. Live-verified against the real hosted DB and real Anthropic API: two `processIntakeTurn` calls with the same correlation ID persisted exactly 2 messages (not 4), though the persisted assistant message reflects attempt 1's (possibly different) text since the LLM really did produce a fresh response on the second call — a known, accepted asymmetry between what's shown live in that session and what a page reload would later show from history, inherent to guarding only the write rather than caching the full result.
- [x] **`startTrip` (`src/workflow/controller.ts`) wasn't idempotency-keyed** — resolved 2026-09-17 (see BUILD_LOG.md), now that the real chat UI (Phase 7) makes double-submission an actual risk rather than a theoretical one. Added `trips.correlation_id` (`0008_trips_idempotency.sql`, a nullable column with a partial unique index, mirroring `trip_state_versions`' existing idempotency pattern) and an optional `correlationId` on `startTrip`'s `NewTrip` input: a prior call with the same key returns the existing trip instead of creating a duplicate, and a genuinely concurrent race is resolved by catching the losing insert's `23505` and re-fetching the winner (mirroring `getOrCreateActiveWorkflowRun`'s pattern). `ChatPanel` generates one client-side correlation ID per compose attempt (lazily, reused across retries until a trip exists) and threads it through `sendMessage` → `startTrip`. Live-verified against the real hosted DB: two concurrent `startTrip` calls with the same correlation ID resolved to exactly one trip row.
- [x] **The §9.1 budget-ceiling guardrail wasn't enforced at finalize time** — resolved 2026-09-17 (see BUILD_LOG.md): `app/app/actions.ts`'s `finalizeTrip` no longer passes `guardrailsPassed: true` unconditionally. It now reads the confirmed `budget` decision's `violations`, and — per the user's explicit choice between a hard block and an explicit-override path (§9.3: "cannot be exceeded without explicit user override") — returns `{requiresBudgetOverride: true, violations}` instead of advancing the workflow when over budget and not yet overridden. `app/app/_components/itinerary-panel.tsx` shows a warning banner (mirroring the existing `pendingCascade` pattern) with "Finalize anyway"/"Cancel"; confirming retries with `overrideBudgetCeiling: true`. Every check (pass, block, or override) is logged as a `budget_ceiling_at_finalize` domain_validation guardrail event. `evals/cases/scenarios.ts`'s `over_budget_request` case (previously a documented known-gap failure) now asserts the corrected behavior and passes for real against the live Anthropic/Supabase stack (`npm run eval:scenarios`, 10/10 passed). Live-verified in the browser via the standard throwaway dev-signin pattern: a real $500-budget trip was blocked with the real violation message, then finalized successfully after clicking "Finalize anyway."
- [x] **No telemetry retention/redaction policy had ever been decided**, despite `PROJECT_BRIEF.md` §13.2 explicitly requiring one — resolved Phase 9, 2026-09-17 (see **ADR-007**): `requiredAccessibility` (the one genuinely sensitive extracted field) is now redacted at write time in `src/repositories/agent-runs.ts`'s `recordToolCalls` via new `src/observability/redaction.ts`. Time-based retention (90 days for raw payloads in a real deployment) is a documented policy, not yet automated code — every row in the hosted DB today is synthetic demo/eval data, so a real cleanup job has no privacy benefit yet; revisit before treating this as production-ready.
- [x] **The chat-triggered fire-and-forget revise path didn't surface a "no viable candidates" failure to the user** — found live 2026-09-17 while verifying slice 4's honest-failure-messaging fix; closed the same day before starting Phase 8. `sendMessage`'s `after()` call to `reviseChainStep` only `console.error`'d such a failure, unlike the direct UI-triggered `propose*Candidates`/`confirmCascadeAndRevise` paths slice 4 already fixed. Fixed in `app/app/actions.ts`: the `.catch()` now logs a new `chain_revision_failed` trip_event (`{step, message}`, reusing `friendlyStepErrorMessage`) via `appendTripEvent`, which needed no migration since `trip_events.event_type` has no enum/check constraint. `app/app/_components/itinerary-panel.tsx`'s existing `needsAttention` Realtime listener (built for `recoverable_error`/`itinerary_invalid`) now also handles `chain_revision_failed`, showing the step-specific message from the event payload. Verified via `npx tsc --noEmit`/`npm run lint`/`npm test` (319/319) — not live-verified (reproducing a "no viable candidates" revision failure through the real chat UI needs the same OTP dev-signin setup as slice 4's live pass); low risk since the fix mirrors the already-tested `friendlyStepErrorMessage`/`{error}` pattern exactly, just routed through a trip_event instead of a return value.

- [x] **`getOrCreateActiveWorkflowRun` (`src/repositories/workflow-runs.ts`) had an unguarded concurrent-insert race** — two concurrent calls for the same trip could both create an "active" `workflow_runs` row. Found during Phase 3's review, 2026-09-16; closed the same day via `supabase/migrations/0004_workflow_runs_single_active.sql` (a partial unique index) plus a catch-and-refetch in code, live-verified against a real concurrent-insert race on the hosted DB.
- [x] **`supabase/migrations/0007_realtime_trip_updates.sql` not applied to the hosted project** — resolved 2026-09-16 once the user supplied the DB password: `supabase db push` applied it successfully. That same push also re-applied `0006_return_flights.sql` (its remote migration-history table didn't know it had already run via an earlier one-off service-role script, which bypassed the CLI's tracking), duplicating its 17 return-flight rows to 34 — caught immediately by a live row-count check, confirmed with the user before deleting, and cleaned up (removed the 17 duplicates, kept one of each; verified back to 17). Lesson for future sessions: applying a migration's *data* via a direct script instead of `supabase db push` leaves the CLI's own tracking unaware it happened, so a later `db push` can silently redo it — prefer `db push` even for data-only migrations when the CLI/credentials are available, and re-verify row counts after any push that follows a manually-applied migration.
- [x] **The chat UI and live itinerary panel (Phase 7) weren't browser-verified** — resolved 2026-09-16/17: the user freed up this project's one `next dev` slot (another session had been holding it), and a real click-through with an admin-generated OTP test session (no email inbox access needed) confirmed the chat, the live Realtime updates, and a full chat-driven decision-revision loop all work end to end. This same click-through found and fixed three real, previously-undetected bugs (see the three items below) — the kind of gap that unit tests and backend-only spot-checks structurally can't catch, confirming the standing practice of always doing a real browser pass for UI work.
- [x] **The chat's assistant message bubble rendered empty** when the Intake agent called a tool (most often `record_extraction`) with no accompanying text — a known, previously-documented model behavior (BUILD_LOG.md, Phase 6, 2026-09-16) that only became visibly broken once a real chat UI existed to render it. Fixed in `src/workflow/intake-orchestrator.ts`: a deterministic fallback string (`"Got it — updating your trip details now."`) is substituted whenever the model's own text is empty, backstopping this the same way every other guarantee about model output in this orchestrator is backstopped by code, not prompt engineering alone. Found and fixed live, 2026-09-16.
- [x] **The chat message input scrolled away with the rest of the page** instead of staying pinned, because the whole page scrolled as one unit rather than the chat/itinerary panels scrolling independently. Root cause: `app/layout.tsx`'s `body` used `min-h-full` (unbounded — can grow past viewport height) instead of `h-full`, and several intermediate flex containers (`app/app/layout.tsx`'s two wrappers, `app/app/page.tsx`, `chat-panel.tsx`'s message-list div) were missing `min-h-0`, which flex items need to actually respect a bounded height instead of refusing to shrink below their content's intrinsic size. Fixed all of them; verified live that the chat scrolls internally while the input stays pinned and the itinerary panel scrolls independently. Found and fixed live, 2026-09-16.
- [x] **A fresh page load of an already-`presenting_draft` trip sometimes showed the itinerary panel as empty** (all steps unchecked) even though `trip_decisions` was fully populated — intermittent, not every load. Root cause was **not** an auth/RLS race as first suspected (a direct REST call with the same session token returned the correct rows) — it was purely normal async loading latency (the panel's initial fetch is a real network round trip) combined with impatient manual testing that checked the page before the fetch had time to resolve. Not a functional bug, but it did reveal a real UX gap: while that fetch is in flight, the panel showed the same "once your trip details are complete…" placeholder as the genuine empty-state, which is misleading for an already-complete trip. Added an explicit loading state (`itinerary-panel.tsx`) so it says "Loading your itinerary…" until the initial fetch settles. Also kept a defensive `await supabase.auth.getSession()` before the first query (harmless, and does close a real — if here undemonstrated — class of race for `@supabase/ssr` browser clients that query before session cookies finish hydrating). Found and fixed live, 2026-09-16.
- [x] **Stale budget/itineraryText after a hotel-only (or same-dates flight) revision** — found live during slice 3's own browser click-through, 2026-09-17. Rule 3 correctly keeps a hotel change from invalidating the activities step, but budget/itineraryText are computed *inside* `confirmActivitiesStep`, not their own chain step, so they were left describing the old hotel at its old price. Asked the user fix-now-vs-track per the standing rule; fixed the same session in `chain-orchestrator.ts`'s `runStepwiseRevision` — see "Slice 3 outcome" above for the full mechanism. Live-verified twice: once via a direct script (confirmed the refreshed text names the new hotel and the activity schedule content is byte-identical, only budget/text changed) and once through the real chat UI.
- [x] **Chat-requested revisions silently did nothing, permanently** — the most significant bug this session, found and fixed live during slice 3's browser click-through, 2026-09-17. `app/app/actions.ts`'s `sendMessage` checked `workflowState === "requirements_ready"` before `decisionRevisionRequested`; since the new stepwise steps deliberately never call `advanceTrip` (slice 1), `workflowState` stays `"requirements_ready"` forever after the first run, so every later turn — including a correctly-detected revision request — silently re-entered the already-complete `runStepwiseChain` (a no-op by design) instead of ever reaching `runStepwiseRevision`. Reproduced twice live (asking to switch hotels via natural chat visibly did nothing) before isolating it by comparing a direct function call (worked) against the same call through `sendMessage` (didn't). Fixed by checking `decisionRevisionRequested` first. This is exactly the class of bug a backend-only spot-check can't catch, since the revision function itself was always correct in isolation — only a real request through the actual Server Action surfaced it.
- [x] **A chat-requested step revision worked end to end (slice 3) but only via interim, non-criteria-aware glue** — `intake-orchestrator.ts` wasn't step-aware, and `chain-orchestrator.ts`'s `runStepwiseRevision` (explicitly throwaway) just excluded the current pick and auto-confirmed whatever was next-cheapest, never honoring the user's actual stated criterion (e.g. "cheaper," "a nicer hotel"). Resolved slice 4, 2026-09-17: `intake-orchestrator.ts` is now step-aware (`decisionRevisionRequested: {step}`), and the Intake agent converts a directional request into a concrete threshold requirement revision (e.g. "cheaper hotel" → a `maxHotelPriceUsd` proposal below the current pick's price) or an absolute one directly (e.g. `maxActivityPriceUsd: 0` for "free") — both re-validated by the same deterministic hard-constraint engine as initial search, per the design's already-decided directional/absolute split. Live-verified: a "can we get a cheaper hotel?" chat request correctly proposed a `maxHotelPriceUsd` threshold and routed through the (also new, slice 4) cascade-warning flow rather than applying silently.
- [x] **The itinerary panel could get stuck on "Finding hotels…"/"Planning activities…" indefinitely after a successful confirm**, found live during slice 4's browser click-through, 2026-09-17 — not a backend bug (the confirm action and subsequent server state were always correct; a page reload immediately showed the right thing), but the panel's derived UI state (`flightConfirmed`, `activeStep`, budget, itinerary text) all read from a `decisions` array populated by the initial load plus Supabase Realtime, and Realtime updates didn't arrive promptly within the same session in this environment (INSERTs for a brand-new trip did; same-session follow-up INSERTs/UPDATEs were unreliable enough to strand the UI). Fixed by having every confirm handler optimistically merge its own action response into local `decisions` state immediately (`optimisticConfirm` in `itinerary-panel.tsx`), de-duplicated against the eventual real Realtime row by field. Live-verified across the full flight → hotel → activities → finalize flow with no stalls.
- [x] **A revision-of-an-already-confirmed-step's error surfaced as an unhelpful generic message instead of an honest explanation** — found live during slice 4's browser click-through, 2026-09-17: an LLM-computed `maxHotelPriceUsd` threshold that came out below every real hotel's price correctly threw `NoViableHotelCandidatesError` server-side, but thrown across a Server Action boundary in a production build, it surfaced to the client only as React/Next's generic obfuscated "an error occurred in the Server Components render" text (confirmed via React's own public error-code registry, error #441) — exactly the dishonest-failure gap design rule 3 exists to prevent. Fixed by having the relevant Server Actions (`propose*Candidates`, `confirmCascadeAndRevise`, `confirm*Candidate`) catch `NoViableFlightCandidatesError`/`NoViableHotelCandidatesError` and return a friendly `{error: string}` result instead of throwing. Live-verified: re-triggering the same over-aggressive threshold now shows "No hotels match your current requirements — try relaxing the price or rating constraints," with the prior confirmed hotel and itinerary left completely untouched. The equivalent chat-triggered fire-and-forget path is a known, tracked, not-yet-closed gap — see "Known bugs/gaps — unresolved" above.
- [x] **A correctness gap in the new-this-slice `confirmDecisionField` (Realtime "proposed" persistence, decision 6) would have let two `"confirmed"` rows for the same field coexist** when revising an already-confirmed step — found while planning the "Change" UI flow, 2026-09-17, before it ever ran live. `supersedeOtherProposedTripDecisions` only superseded sibling *proposed* rows, not a prior *confirmed* one, which a revision's fresh candidate list would coexist with. Fixed by widening it to `supersedeOtherActiveTripDecisions` (any non-superseded status, not just `"proposed"`), with new dedicated tests covering the revise-an-already-confirmed-step case in `flight-step.test.ts`/`hotel-step.test.ts`.
- [x] **The chat-triggered fire-and-forget revise path didn't surface a "no viable candidates" failure to the user** — found live 2026-09-17 right after slice 4 shipped, closed the same day before starting Phase 8. `sendMessage`'s `after()` call to `reviseChainStep` only `console.error`'d such a failure; fixed by logging a new `chain_revision_failed` `trip_event` (`{step, message}`) that `itinerary-panel.tsx`'s existing `needsAttention` Realtime listener now also handles. See BUILD_LOG.md, "Close revise-failure gap before Phase 8."
- [x] **`agent_runs.cost_usd` was defined in the schema and accepted by `recordAgentRun`, but no caller ever actually passed it** — every real telemetry write (`intake-orchestrator.ts`, `activities-step.ts`'s Curator/Writer calls) left it `null`, silently, since Phase 6. Found while building the Phase 8 engineering dashboard, 2026-09-17 (every cost metric would have shown $0.00 regardless of real spend). Fixed by extracting `evals/runners/run-intake-eval.ts`'s existing cost formula into `src/observability/pricing.ts` and wiring `estimateCostUsd()` into all three call sites (and the eval runner, removing its duplicate copy). Live-verified: re-ran `npm run eval:scenarios` and confirmed real per-agent costs appeared on `/internal/analytics`.
